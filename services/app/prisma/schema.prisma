generator client {
  provider = "prisma-client-js"
  output   = "./generated/client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model account {
  id                String  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  userId            String  @db.Uuid
  type              String  @db.VarChar(255)
  provider          String  @db.VarChar(255)
  providerAccountId String  @db.VarChar(255)
  refresh_token     String?
  access_token      String?
  expires_at        BigInt?
  token_type        String? @db.VarChar(255)
  scope             String?
  id_token          String?
  session_state     String?
  users             users   @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([provider, providerAccountId], map: "account_provider_unique")
  @@index([userId], map: "idx_account_userid")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employees {
  id                                                                                                 String                                @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                                                                                          String                                @db.Uuid
  first_name                                                                                         String                                @db.VarChar(100)
  last_name                                                                                          String                                @db.VarChar(100)
  email                                                                                              String                                @db.VarChar(255)
  job_title                                                                                          String?                               @db.VarChar(255)
  department                                                                                         String?                               @db.VarChar(100)
  location                                                                                           String?                               @db.VarChar(255)
  manager_id                                                                                         String?                               @db.Uuid
  hire_date                                                                                          DateTime?                             @db.Date
  skills                                                                                             String[]                              @default([])
  performance_rating                                                                                 Decimal?                              @db.Decimal(3, 2)
  potential                                                                                          String?                               @db.VarChar(20)
  is_active                                                                                          Boolean?                              @default(true)
  created_at                                                                                         DateTime?                             @default(now()) @db.Timestamp(6)
  updated_at                                                                                         DateTime?                             @default(now()) @db.Timestamp(6)
  pernr                                                                                              String?                               @db.VarChar(8)
  birth_date                                                                                         DateTime?                             @db.Date
  gender                                                                                             String?                               @db.VarChar(10)
  nationality                                                                                        String?                               @db.VarChar(50)
  salary                                                                                             Decimal?                              @db.Decimal(15, 2)
  currency                                                                                           String?                               @default("EUR") @db.VarChar(3)
  cost_center                                                                                        String?                               @db.VarChar(50)
  employee_group                                                                                     String?                               @db.VarChar(50)
  employee_subgroup                                                                                  String?                               @db.VarChar(50)
  position_id                                                                                        String?                               @db.VarChar(50)
  legacy_org_unit_code                                                                               String?                               @db.VarChar(50)
  cost_center_id                                                                                     String?                               @db.Uuid
  org_unit_id                                                                                        String?                               @db.Uuid
  middle_name                                                                                        String?                               @db.VarChar(100)
  birth_place                                                                                        String?                               @db.VarChar(100)
  marital_status                                                                                     String?                               @db.VarChar(20)
  address_street                                                                                     String?                               @db.VarChar(255)
  address_city                                                                                       String?                               @db.VarChar(100)
  address_postal_code                                                                                String?                               @db.VarChar(20)
  address_country                                                                                    String?                               @db.VarChar(3)
  address_region                                                                                     String?                               @db.VarChar(50)
  temp_address_street                                                                                String?                               @db.VarChar(255)
  temp_address_city                                                                                  String?                               @db.VarChar(100)
  temp_address_postal_code                                                                           String?                               @db.VarChar(20)
  temp_address_country                                                                               String?                               @db.VarChar(3)
  pay_scale_area                                                                                     String?                               @db.VarChar(50)
  pay_scale_type                                                                                     String?                               @db.VarChar(50)
  pay_scale_group                                                                                    String?                               @db.VarChar(50)
  pay_scale_level                                                                                    String?                               @db.VarChar(20)
  pay_periods_per_year                                                                               Int?                                  @default(12)
  work_schedule_percentage                                                                           Decimal?                              @default(100.00) @db.Decimal(5, 2)
  iban                                                                                               String?                               @db.VarChar(34)
  swift_bic                                                                                          String?                               @db.VarChar(11)
  bank_name                                                                                          String?                               @db.VarChar(100)
  bank_account_number                                                                                String?                               @db.VarChar(50)
  phone_mobile                                                                                       String?                               @db.VarChar(30)
  phone_work                                                                                         String?                               @db.VarChar(30)
  phone_home                                                                                         String?                               @db.VarChar(30)
  personal_email                                                                                     String?                               @db.VarChar(255)
  tax_id                                                                                             String?                               @db.VarChar(20)
  national_id                                                                                        String?                               @db.VarChar(30)
  national_id_expiry                                                                                 DateTime?                             @db.Date
  passport_number                                                                                    String?                               @db.VarChar(20)
  passport_expiry                                                                                    DateTime?                             @db.Date
  driver_license                                                                                     String?                               @db.VarChar(20)
  driver_license_expiry                                                                              DateTime?                             @db.Date
  probation_end_date                                                                                 DateTime?                             @db.Date
  seniority_date                                                                                     DateTime?                             @db.Date
  contract_end_date                                                                                  DateTime?                             @db.Date
  emergency_contact_name                                                                             String?                               @db.VarChar(100)
  emergency_contact_phone                                                                            String?                               @db.VarChar(30)
  emergency_contact_relationship                                                                     String?                               @db.VarChar(50)
  family_members                                                                                     Json?
  education_history                                                                                  Json?
  highest_education_level                                                                            String?                               @db.VarChar(20)
  highest_education_institution                                                                      String?                               @db.VarChar(100)
  highest_education_field                                                                            String?                               @db.VarChar(100)
  highest_education_year                                                                             Int?
  company_code                                                                                       String?                               @db.VarChar(10)
  personnel_area                                                                                     String?                               @db.VarChar(10)
  personnel_subarea                                                                                  String?                               @db.VarChar(10)
  employment_status                                                                                  String?                               @default("active") @db.VarChar(20)
  termination_date                                                                                   DateTime?                             @db.Date
  termination_reason                                                                                 String?                               @db.VarChar(100)
  auth_username                                                                                      String?                               @db.VarChar(100)
  auth_password_hash                                                                                 String?                               @db.VarChar(255)
  auth_role                                                                                          String?                               @default("USER") @db.VarChar(50)
  auth_permissions                                                                                   String[]                              @default([])
  auth_last_login                                                                                    DateTime?                             @db.Timestamp(6)
  auth_username_candidate                                                                            String?                               @db.VarChar(200)
  location_id                                                                                        String?                               @db.Uuid
  profile_embedding                                                                                  Unsupported("vector")?
  embedding_text_hash                                                                                String?                               @db.VarChar(64)
  embedding_model                                                                                    String?                               @db.VarChar(100)
  embedding_generated_at                                                                             DateTime?                             @db.Timestamptz(6)
  deleted_at                                                                                         DateTime?                             @db.Timestamptz(6)
  enrichment_consent                                                                                 Boolean                               @default(false)
  enrichment_consent_at                                                                              DateTime?                             @db.Timestamptz(6)
  enrichment_consent_scope                                                                           String[]
  ai_escalation_queue_ai_escalation_queue_assigned_toToemployees                                     ai_escalation_queue[]                 @relation("ai_escalation_queue_assigned_toToemployees")
  ai_escalation_queue_ai_escalation_queue_employee_idToemployees                                     ai_escalation_queue[]                 @relation("ai_escalation_queue_employee_idToemployees")
  ai_escalation_queue_ai_escalation_queue_resolved_byToemployees                                     ai_escalation_queue[]                 @relation("ai_escalation_queue_resolved_byToemployees")
  ai_query_audit                                                                                     ai_query_audit[]
  analytics_events                                                                                   analytics_events[]
  audit_logs                                                                                         audit_logs[]
  benchmark_reports                                                                                  benchmark_reports[]
  bonus_allocations_bonus_allocations_approved_by_employee_idToemployees                             bonus_allocations[]                   @relation("bonus_allocations_approved_by_employee_idToemployees")
  bonus_allocations_bonus_allocations_employee_idToemployees                                         bonus_allocations[]                   @relation("bonus_allocations_employee_idToemployees")
  bonus_plans                                                                                        bonus_plans[]
  burnout_assessments                                                                                burnout_assessments[]
  calibration_adjustments_calibration_adjustments_applied_byToemployees                              calibration_adjustments[]             @relation("calibration_adjustments_applied_byToemployees")
  calibration_adjustments_calibration_adjustments_approved_byToemployees                             calibration_adjustments[]             @relation("calibration_adjustments_approved_byToemployees")
  calibration_adjustments_calibration_adjustments_employee_idToemployees                             calibration_adjustments[]             @relation("calibration_adjustments_employee_idToemployees")
  calibration_adjustments_calibration_adjustments_proposed_byToemployees                             calibration_adjustments[]             @relation("calibration_adjustments_proposed_byToemployees")
  calibration_audit_log                                                                              calibration_audit_log[]
  calibration_discussions                                                                            calibration_discussions[]
  calibration_participants                                                                           calibration_participants[]
  calibration_results_calibration_results_approved_byToemployees                                     calibration_results[]                 @relation("calibration_results_approved_byToemployees")
  calibration_results_calibration_results_employee_idToemployees                                     calibration_results[]                 @relation("calibration_results_employee_idToemployees")
  calibration_sessions_calibration_sessions_created_by_employee_idToemployees                        calibration_sessions[]                @relation("calibration_sessions_created_by_employee_idToemployees")
  calibration_sessions_calibration_sessions_facilitator_id_employee_idToemployees                    calibration_sessions[]                @relation("calibration_sessions_facilitator_id_employee_idToemployees")
  career_path_recommendations                                                                        career_path_recommendations[]
  career_paths                                                                                       career_paths[]
  career_profiles                                                                                    career_profiles?
  career_simulations_career_simulations_created_byToemployees                                        career_simulations[]                  @relation("career_simulations_created_byToemployees")
  career_simulations_career_simulations_employee_idToemployees                                       career_simulations[]                  @relation("career_simulations_employee_idToemployees")
  check_ins_check_ins_employee_idToemployees                                                         check_ins[]                           @relation("check_ins_employee_idToemployees")
  check_ins_check_ins_manager_idToemployees                                                          check_ins[]                           @relation("check_ins_manager_idToemployees")
  club_memberships                                                                                   club_memberships[]
  competency_review_ratings                                                                          competency_review_ratings[]
  continuous_feedback_continuous_feedback_from_employee_idToemployees                                continuous_feedback[]                 @relation("continuous_feedback_from_employee_idToemployees")
  continuous_feedback_continuous_feedback_to_employee_idToemployees                                  continuous_feedback[]                 @relation("continuous_feedback_to_employee_idToemployees")
  contracts                                                                                          contracts[]
  course_enrollments_course_enrollments_enrolled_by_employee_idToemployees                           course_enrollments[]                  @relation("course_enrollments_enrolled_by_employee_idToemployees")
  course_enrollments_course_enrollments_employee_idToemployees                                       course_enrollments[]                  @relation("course_enrollments_employee_idToemployees")
  course_enrollments_semantic                                                                        course_enrollments_semantic[]
  courses                                                                                            courses[]
  dashboards                                                                                         dashboards[]
  data_subject_requests                                                                              data_subject_requests[]
  document_acknowledgments                                                                           document_acknowledgments[]
  document_comments                                                                                  document_comments[]
  document_locks                                                                                     document_locks[]
  document_requests_document_requests_assigned_toToemployees                                         document_requests[]                   @relation("document_requests_assigned_toToemployees")
  document_requests_document_requests_employee_idToemployees                                         document_requests[]                   @relation("document_requests_employee_idToemployees")
  document_versions                                                                                  document_versions[]
  employee_addresses                                                                                 employee_addresses[]
  employee_attendance                                                                                employee_attendance[]
  employee_bank_details                                                                              employee_bank_details[]
  employee_benefit_enrollments                                                                       employee_benefit_enrollments[]
  employee_career_paths_employee_career_paths_assigned_by_employee_idToemployees                     employee_career_paths[]               @relation("employee_career_paths_assigned_by_employee_idToemployees")
  employee_career_paths_employee_career_paths_employee_idToemployees                                 employee_career_paths[]               @relation("employee_career_paths_employee_idToemployees")
  employee_career_progress                                                                           employee_career_progress[]
  employee_certifications_employee_certifications_verified_by_employee_idToemployees                 employee_certifications[]             @relation("employee_certifications_verified_by_employee_idToemployees")
  employee_certifications_employee_certifications_employee_idToemployees                             employee_certifications[]             @relation("employee_certifications_employee_idToemployees")
  employee_contracts                                                                                 employee_contracts[]
  employee_documents_employee_documents_employee_idToemployees                                       employee_documents[]                  @relation("employee_documents_employee_idToemployees")
  employee_documents_employee_documents_signed_byToemployees                                         employee_documents[]                  @relation("employee_documents_signed_byToemployees")
  employee_documents_employee_documents_uploaded_byToemployees                                       employee_documents[]                  @relation("employee_documents_uploaded_byToemployees")
  employee_documents_employee_documents_verified_byToemployees                                       employee_documents[]                  @relation("employee_documents_verified_byToemployees")
  employee_emergency_contacts                                                                        employee_emergency_contacts[]
  employee_job_assignments                                                                           employee_job_assignments[]
  employee_kpi_targets                                                                               employee_kpi_targets[]
  employee_occupations                                                                               employee_occupations[]
  employee_overtime                                                                                  employee_overtime[]
  employee_pay_stubs                                                                                 employee_pay_stubs[]
  employee_permission_overrides_employee_permission_overrides_employee_idToemployees                 employee_permission_overrides[]       @relation("employee_permission_overrides_employee_idToemployees")
  employee_permission_overrides_employee_permission_overrides_granted_byToemployees                  employee_permission_overrides[]       @relation("employee_permission_overrides_granted_byToemployees")
  employee_requests_employee_requests_approver_idToemployees                                         employee_requests[]                   @relation("employee_requests_approver_idToemployees")
  employee_requests_employee_requests_employee_idToemployees                                         employee_requests[]                   @relation("employee_requests_employee_idToemployees")
  employee_skill_assessments                                                                         employee_skill_assessments[]
  employee_skill_history                                                                             employee_skill_history[]
  employee_skill_mappings_employee_skill_mappings_employee_idToemployees                             employee_skill_mappings[]             @relation("employee_skill_mappings_employee_idToemployees")
  employee_skill_mappings_employee_skill_mappings_verified_by_employee_idToemployees                 employee_skill_mappings[]             @relation("employee_skill_mappings_verified_by_employee_idToemployees")
  employee_skill_profiles_employee_skill_profiles_employee_idToemployees                             employee_skill_profiles[]             @relation("employee_skill_profiles_employee_idToemployees")
  employee_skill_profiles_employee_skill_profiles_verified_byToemployees                             employee_skill_profiles[]             @relation("employee_skill_profiles_verified_byToemployees")
  employee_skills_employee_skills_employee_idToemployees                                             employee_skills[]                     @relation("employee_skills_employee_idToemployees")
  employee_skills_employee_skills_verified_by_employee_idToemployees                                 employee_skills[]                     @relation("employee_skills_verified_by_employee_idToemployees")
  employee_time_off_balances                                                                         employee_time_off_balances[]
  employee_time_off_requests_employee_time_off_requests_approver_idToemployees                       employee_time_off_requests[]          @relation("employee_time_off_requests_approver_idToemployees")
  employee_time_off_requests_employee_time_off_requests_employee_idToemployees                       employee_time_off_requests[]          @relation("employee_time_off_requests_employee_idToemployees")
  employee_timeline                                                                                  employee_timeline[]
  employee_training_records                                                                          employee_training_records[]
  tenants                                                                                            tenants                               @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  cost_centers                                                                                       cost_centers?                         @relation(fields: [cost_center_id], references: [id], onUpdate: NoAction, map: "fk_employees_cost_center")
  locations                                                                                          locations?                            @relation(fields: [location_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_employees_location")
  employees                                                                                          employees?                            @relation("employeesToemployees", fields: [manager_id], references: [id], onUpdate: NoAction, map: "fk_employees_manager")
  other_employees                                                                                    employees[]                           @relation("employeesToemployees")
  org_units_employees_org_unit_idToorg_units                                                         org_units?                            @relation("employees_org_unit_idToorg_units", fields: [org_unit_id], references: [id], onUpdate: NoAction, map: "fk_employees_org_unit")
  employees_staging_employees_staging_original_employee_idToemployees                                employees_staging[]                   @relation("employees_staging_original_employee_idToemployees")
  employees_staging_employees_staging_manager_idToemployees                                          employees_staging[]                   @relation("employees_staging_manager_idToemployees")
  engagement_survey_responses                                                                        engagement_survey_responses[]
  export_configurations                                                                              export_configurations[]
  export_jobs                                                                                        export_jobs[]
  feedback_360_feedback_360_reviewer_employee_idToemployees                                          feedback_360[]                        @relation("feedback_360_reviewer_employee_idToemployees")
  feedback_360_feedback_360_target_employee_idToemployees                                            feedback_360[]                        @relation("feedback_360_target_employee_idToemployees")
  feedback_360_peer_suggestions_feedback_360_peer_suggestions_suggested_peer_idToemployees           feedback_360_peer_suggestions[]       @relation("feedback_360_peer_suggestions_suggested_peer_idToemployees")
  feedback_360_peer_suggestions_feedback_360_peer_suggestions_target_employee_idToemployees          feedback_360_peer_suggestions[]       @relation("feedback_360_peer_suggestions_target_employee_idToemployees")
  feedback_360_questionnaires                                                                        feedback_360_questionnaires[]
  goal_check_ins                                                                                     goal_check_ins[]
  goal_comments                                                                                      goal_comments[]
  goal_review_ratings                                                                                goal_review_ratings[]
  goal_templates                                                                                     goal_templates[]
  goal_updates                                                                                       goal_updates[]
  goals_goals_employee_idToemployees                                                                 goals[]                               @relation("goals_employee_idToemployees")
  goals_goals_owner_idToemployees                                                                    goals[]                               @relation("goals_owner_idToemployees")
  internal_applications                                                                              internal_applications[]
  internal_job_alerts                                                                                internal_job_alerts[]
  internal_job_bookmarks                                                                             internal_job_bookmarks[]
  internal_job_postings                                                                              internal_job_postings[]
  internal_job_views                                                                                 internal_job_views[]
  internal_mobility_postings_internal_mobility_postings_created_byToemployees                        internal_mobility_postings[]          @relation("internal_mobility_postings_created_byToemployees")
  internal_mobility_postings_internal_mobility_postings_hiring_manager_idToemployees                 internal_mobility_postings[]          @relation("internal_mobility_postings_hiring_manager_idToemployees")
  internal_mobility_requests                                                                         internal_mobility_requests[]
  job_analysis                                                                                       job_analysis[]
  job_evaluations                                                                                    job_evaluations[]
  learning_bookmarks                                                                                 learning_bookmarks[]
  learning_path_enrollments_learning_path_enrollments_employee_idToemployees                         learning_path_enrollments[]           @relation("learning_path_enrollments_employee_idToemployees")
  learning_path_enrollments_learning_path_enrollments_enrolled_by_employee_idToemployees             learning_path_enrollments[]           @relation("learning_path_enrollments_enrolled_by_employee_idToemployees")
  learning_paths                                                                                     learning_paths[]
  learning_ratings                                                                                   learning_ratings[]
  learning_recommendations                                                                           learning_recommendations[]
  leave_approval_steps                                                                               leave_approval_steps[]
  medical_certificates                                                                               medical_certificates[]
  mentor_match_scores_mentor_match_scores_mentee_idToemployees                                       mentor_match_scores[]                 @relation("mentor_match_scores_mentee_idToemployees")
  mentor_match_scores_mentor_match_scores_mentor_idToemployees                                       mentor_match_scores[]                 @relation("mentor_match_scores_mentor_idToemployees")
  merit_cycles                                                                                       merit_cycles[]
  merit_recommendations_merit_recommendations_employee_idToemployees                                 merit_recommendations[]               @relation("merit_recommendations_employee_idToemployees")
  merit_recommendations_merit_recommendations_approved_by_employee_idToemployees                     merit_recommendations[]               @relation("merit_recommendations_approved_by_employee_idToemployees")
  merit_recommendations_merit_recommendations_submitted_by_employee_idToemployees                    merit_recommendations[]               @relation("merit_recommendations_submitted_by_employee_idToemployees")
  notification_preferences                                                                           notification_preferences[]
  notifications                                                                                      notifications[]
  okr_check_ins                                                                                      okr_check_ins[]
  okr_checkins                                                                                       okr_checkins[]
  okrs                                                                                               okrs[]
  onboarding_checklist                                                                               onboarding_checklist[]
  onboarding_documents                                                                               onboarding_documents[]
  onboarding_instances_onboarding_instances_employee_idToemployees                                   onboarding_instances[]                @relation("onboarding_instances_employee_idToemployees")
  onboarding_instances_onboarding_instances_created_by_employee_idToemployees                        onboarding_instances[]                @relation("onboarding_instances_created_by_employee_idToemployees")
  onboarding_instances_onboarding_instances_hr_contact_id_employee_idToemployees                     onboarding_instances[]                @relation("onboarding_instances_hr_contact_id_employee_idToemployees")
  onboarding_tasks                                                                                   onboarding_tasks[]
  onboarding_templates                                                                               onboarding_templates[]
  ontology_feedback                                                                                  ontology_feedback[]
  org_units_org_units_manager_idToemployees                                                          org_units[]                           @relation("org_units_manager_idToemployees")
  payroll_export_employees                                                                           payroll_export_employees[]
  performance_predictions                                                                            performance_predictions[]
  performance_reviews_performance_reviews_employee_idToemployees                                     performance_reviews[]                 @relation("performance_reviews_employee_idToemployees")
  performance_reviews_performance_reviews_reviewer_idToemployees                                     performance_reviews[]                 @relation("performance_reviews_reviewer_idToemployees")
  performance_reviews_performance_reviews_calibrated_byToemployees                                   performance_reviews[]                 @relation("performance_reviews_calibrated_byToemployees")
  performance_reviews_performance_reviews_finalized_byToemployees                                    performance_reviews[]                 @relation("performance_reviews_finalized_byToemployees")
  performance_skill_links                                                                            performance_skill_links[]
  performance_trends                                                                                 performance_trends[]
  permission_overrides                                                                               permission_overrides[]
  plugin_api_keys                                                                                    plugin_api_keys[]
  plugin_configurations                                                                              plugin_configurations[]
  plugin_installations                                                                               plugin_installations[]
  plugin_reviews                                                                                     plugin_reviews[]
  plugin_webhooks                                                                                    plugin_webhooks[]
  preboarding_equipment_preboarding_equipment_approved_by_employee_idToemployees                     preboarding_equipment[]               @relation("preboarding_equipment_approved_by_employee_idToemployees")
  preboarding_equipment_preboarding_equipment_requested_by_employee_idToemployees                    preboarding_equipment[]               @relation("preboarding_equipment_requested_by_employee_idToemployees")
  preboarding_sessions_preboarding_sessions_manager_idToemployees                                    preboarding_sessions[]                @relation("preboarding_sessions_manager_idToemployees")
  preboarding_sessions_preboarding_sessions_created_by_employee_idToemployees                        preboarding_sessions[]                @relation("preboarding_sessions_created_by_employee_idToemployees")
  preboarding_templates                                                                              preboarding_templates[]
  preboarding_welcome_content                                                                        preboarding_welcome_content[]
  pulse_checks                                                                                       pulse_checks[]
  rag_documents                                                                                      rag_documents[]
  rag_sessions                                                                                       rag_sessions[]
  rbp_team_leaders                                                                                   rbp_team_leaders[]
  rbp_team_members                                                                                   rbp_team_members[]
  recruiting_interview_participants_recruiting_interview_participants_employee_idToemployees         recruiting_interview_participants[]   @relation("recruiting_interview_participants_employee_idToemployees")
  recruiting_interview_participants_recruiting_interview_participants_user_id_employee_idToemployees recruiting_interview_participants[]   @relation("recruiting_interview_participants_user_id_employee_idToemployees")
  recruiting_interviewer_availability                                                                recruiting_interviewer_availability[]
  recruiting_interviews                                                                              recruiting_interviews[]
  report_definitions                                                                                 report_definitions[]
  report_executions                                                                                  report_executions[]
  report_subscriptions                                                                               report_subscriptions[]
  review_cycle_notifications                                                                         review_cycle_notifications[]
  review_cycle_participants_review_cycle_participants_employee_idToemployees                         review_cycle_participants[]           @relation("review_cycle_participants_employee_idToemployees")
  review_cycle_participants_review_cycle_participants_manager_idToemployees                          review_cycle_participants[]           @relation("review_cycle_participants_manager_idToemployees")
  role_skill_requirements_role_skill_requirements_created_byToemployees                              role_skill_requirements[]             @relation("role_skill_requirements_created_byToemployees")
  role_skill_requirements_role_skill_requirements_updated_byToemployees                              role_skill_requirements[]             @relation("role_skill_requirements_updated_byToemployees")
  salary_band_assignments_salary_band_assignments_employee_idToemployees                             salary_band_assignments[]             @relation("salary_band_assignments_employee_idToemployees")
  salary_band_assignments_salary_band_assignments_assigned_by_employee_idToemployees                 salary_band_assignments[]             @relation("salary_band_assignments_assigned_by_employee_idToemployees")
  salary_bands                                                                                       salary_bands[]
  salary_history_salary_history_approved_byToemployees                                               salary_history[]                      @relation("salary_history_approved_byToemployees")
  salary_history_salary_history_employee_idToemployees                                               salary_history[]                      @relation("salary_history_employee_idToemployees")
  sap_employee_mapping                                                                               sap_employee_mapping[]
  saved_jobs                                                                                         saved_jobs[]
  self_assessment_evidence_self_assessment_evidence_employee_idToemployees                           self_assessment_evidence[]            @relation("self_assessment_evidence_employee_idToemployees")
  self_assessment_evidence_self_assessment_evidence_verified_byToemployees                           self_assessment_evidence[]            @relation("self_assessment_evidence_verified_byToemployees")
  self_reviews                                                                                       self_reviews[]
  signature_recipients                                                                               signature_recipients[]
  signature_requests                                                                                 signature_requests[]
  skill_development_paths                                                                            skill_development_paths[]
  skill_gap_analyses                                                                                 skill_gap_analyses[]
  skill_gap_snapshots                                                                                skill_gap_snapshots[]
  skill_taxonomy_extensions                                                                          skill_taxonomy_extensions[]
  social_likes                                                                                       social_likes[]
  sso_configurations                                                                                 sso_configurations[]
  survey_responses                                                                                   survey_responses[]
  talent_pool_members                                                                                talent_pool_members[]
  tenants_books                                                                                      tenants_books[]
  turnover_risk_scores                                                                               turnover_risk_scores[]
  unknown_skills                                                                                     unknown_skills[]
  users                                                                                              users[]
  wellbeing_checkins                                                                                 wellbeing_checkins[]
  wellbeing_goals                                                                                    wellbeing_goals[]
  wellbeing_program_enrollments                                                                      wellbeing_program_enrollments[]
  whistleblowing_audit_log                                                                           whistleblowing_audit_log[]
  whistleblowing_handlers                                                                            whistleblowing_handlers[]
  whistleblowing_reports                                                                             whistleblowing_reports[]
  workforce_plan_scenarios                                                                           workforce_plan_scenarios[]

  @@unique([tenant_id, pernr], map: "employees_pernr_tenant_unique")
  @@index([auth_role], map: "idx_employees_auth_role")
  @@index([cost_center_id], map: "idx_employees_cost_center")
  @@index([department], map: "idx_employees_department")
  @@index([email], map: "idx_employees_email")
  @@index([employment_status], map: "idx_employees_employment_status")
  @@index([location_id], map: "idx_employees_location")
  @@index([manager_id], map: "idx_employees_manager_id")
  @@index([legacy_org_unit_code], map: "idx_employees_org_unit")
  @@index([org_unit_id], map: "idx_employees_org_unit_new")
  @@index([pernr], map: "idx_employees_pernr")
  @@index([profile_embedding], map: "idx_employees_profile_embedding")
  @@index([tenant_id], map: "idx_employees_tenant_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model session {
  id           String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  sessionToken String   @unique(map: "session_sessiontoken_unique") @db.VarChar(255)
  userId       String   @db.Uuid
  expires      DateTime @db.Timestamptz(6)
  users        users    @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([expires], map: "idx_session_expires")
  @@index([userId], map: "idx_session_userid")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model tenants {
  id                                  String                                @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  code                                String                                @unique @db.VarChar(20)
  name                                String                                @db.VarChar(255)
  description                         String?
  region                              String?                               @db.VarChar(100)
  status                              String?                               @default("configuring") @db.VarChar(50)
  employee_count                      Int?                                  @default(0)
  subscription_plan                   String?                               @default("starter") @db.VarChar(50)
  created_at                          DateTime?                             @default(now()) @db.Timestamp(6)
  updated_at                          DateTime?                             @default(now()) @db.Timestamp(6)
  sap_company_code                    String?                               @db.VarChar(4)
  industry_type                       String?                               @db.VarChar(100)
  profile_id                          String?                               @db.Uuid
  annual_revenue_eur                  BigInt?
  settings                            Json?                                 @default("{}")
  tax_id                              String?                               @db.VarChar(20)
  contact_email                       String?                               @db.VarChar(255)
  contact_phone                       String?                               @db.VarChar(50)
  address_street                      String?                               @db.VarChar(255)
  address_city                        String?                               @db.VarChar(100)
  address_postal_code                 String?                               @db.VarChar(20)
  address_country                     String?                               @default("ITA") @db.VarChar(3)
  setup_completed                     Boolean?                              @default(false)
  setup_completed_at                  DateTime?                             @db.Timestamp(6)
  setup_step                          Int?                                  @default(0)
  company_size                        String?                               @db.VarChar(10)
  employee_count_range_min            Int?
  employee_count_range_max            Int?
  onboarding_completed_at             DateTime?                             @db.Timestamptz(6)
  industry_profile_id                 String?                               @db.Uuid
  verified_website                    String?                               @db.VarChar(255)
  admin_component_registry            admin_component_registry[]
  ai_analytics_daily                  ai_analytics_daily[]
  ai_escalation_queue                 ai_escalation_queue[]
  ai_prompt_templates                 ai_prompt_templates[]
  ai_query_audit                      ai_query_audit[]
  ai_tenant_config                    ai_tenant_config?
  ai_usage_log                        ai_usage_log[]
  analysis_sessions                   analysis_sessions[]
  analytics_aggregations              analytics_aggregations[]
  analytics_events                    analytics_events[]
  api_keys                            api_keys[]
  audit_logs                          audit_logs[]
  benchmark_configs                   benchmark_configs[]
  benchmark_reports                   benchmark_reports[]
  blueprint_runs                      blueprint_runs[]
  bonus_plans                         bonus_plans[]
  burnout_assessments                 burnout_assessments[]
  calibration_adjustments             calibration_adjustments[]
  calibration_audit_log               calibration_audit_log[]
  calibration_participants            calibration_participants[]
  calibration_results                 calibration_results[]
  calibration_sessions                calibration_sessions[]
  candidates                          candidates[]
  career_conversations                career_conversations[]
  career_goal_milestones              career_goal_milestones[]
  career_goals                        career_goals[]
  career_path_level_skills            career_path_level_skills[]
  career_path_recommendations         career_path_recommendations[]
  career_path_templates               career_path_templates[]
  career_paths                        career_paths[]
  career_profiles                     career_profiles[]
  career_recommendations              career_recommendations[]
  career_simulations                  career_simulations[]
  career_skills                       career_skills[]
  certifications                      certifications[]
  check_ins                           check_ins[]
  competencies                        competencies[]
  competency_frameworks               competency_frameworks[]
  competency_review_ratings           competency_review_ratings[]
  compliance_training_requirements    compliance_training_requirements[]
  continuous_feedback                 continuous_feedback[]
  contract_amendments                 contract_amendments[]
  contracts                           contracts[]
  cost_centers                        cost_centers[]
  course_enrollments                  course_enrollments[]
  course_enrollments_semantic         course_enrollments_semantic[]
  courses                             courses[]
  crawl_runs                          crawl_runs[]
  crawler_configs                     crawler_configs[]
  critical_roles                      critical_roles[]
  cross_entity_relations              cross_entity_relations[]
  cross_entity_searches               cross_entity_searches[]
  dashboard_widgets                   dashboard_widgets[]
  dashboards                          dashboards[]
  data_subject_requests               data_subject_requests[]
  document_acknowledgments            document_acknowledgments[]
  document_requests                   document_requests[]
  embedding_queue                     embedding_queue[]
  employee_addresses                  employee_addresses[]
  employee_attendance                 employee_attendance[]
  employee_bank_details               employee_bank_details[]
  employee_benefits                   employee_benefits[]
  employee_career_progress            employee_career_progress[]
  employee_clubs                      employee_clubs[]
  employee_contract_amendments        employee_contract_amendments[]
  employee_contracts                  employee_contracts[]
  employee_documents                  employee_documents[]
  employee_emergency_contacts         employee_emergency_contacts[]
  employee_overtime                   employee_overtime[]
  employee_permission_overrides       employee_permission_overrides[]
  employee_skill_mappings             employee_skill_mappings[]
  employee_skill_profiles             employee_skill_profiles[]
  employee_skills                     employee_skills[]
  employee_time_off_balances          employee_time_off_balances[]
  employee_time_off_requests          employee_time_off_requests[]
  employee_timeline                   employee_timeline[]
  employee_training_records           employee_training_records[]
  employees                           employees[]
  employees_staging                   employees_staging[]
  engagement_action_plans             engagement_action_plans[]
  engagement_feedback                 engagement_feedback[]
  engagement_pulse_configs            engagement_pulse_configs[]
  engagement_survey_responses         engagement_survey_responses[]
  engagement_survey_templates         engagement_survey_templates[]
  engagement_surveys                  engagement_surveys[]
  enrichment_candidates               enrichment_candidates[]
  enrichment_entity_descriptors       enrichment_entity_descriptors[]
  enrichment_extraction_schemas       enrichment_extraction_schemas[]
  enrichment_job_events               enrichment_job_events[]
  enrichment_jobs                     enrichment_jobs[]
  enrichment_lineage                  enrichment_lineage[]
  enrichment_matches                  enrichment_matches[]
  enrichment_merge_policies           enrichment_merge_policies[]
  enrichment_merges                   enrichment_merges[]
  enrichment_observations             enrichment_observations[]
  enrichment_source_snapshots         enrichment_source_snapshots[]
  enrichment_sources                  enrichment_sources[]
  enrichment_trust_rules              enrichment_trust_rules[]
  enrichment_writes                   enrichment_writes[]
  error_analytics_hourly              error_analytics_hourly[]
  error_logs                          error_logs[]
  export_configurations               export_configurations[]
  export_jobs                         export_jobs[]
  ext_hrp1007                         ext_hrp1007[]
  ext_pa0002                          ext_pa0002[]                          @ignore
  ext_pb0002                          ext_pb0002[]                          @ignore
  extracted_skills                    extracted_skills[]
  feedback_360                        feedback_360[]
  feedback_360_peer_suggestions       feedback_360_peer_suggestions[]
  feedback_360_questionnaires         feedback_360_questionnaires[]
  feedback_360_questions              feedback_360_questions[]
  feedback_categories                 feedback_categories[]
  feedback_requests                   feedback_requests[]
  goal_alignments                     goal_alignments[]
  goal_check_ins                      goal_check_ins[]
  goal_comments                       goal_comments[]
  goal_milestones                     goal_milestones[]
  goal_review_ratings                 goal_review_ratings[]
  goal_templates                      goal_templates[]
  goal_updates                        goal_updates[]
  goals                               goals[]
  holidays                            holidays[]
  import_jobs                         import_jobs[]
  import_skill_links                  import_skill_links[]
  integration_sync_logs               integration_sync_logs[]
  integrations                        integrations[]
  internal_job_postings               internal_job_postings[]
  internal_mobility_postings          internal_mobility_postings[]
  internal_mobility_requests          internal_mobility_requests[]
  job_analysis                        job_analysis[]
  job_evaluations                     job_evaluations[]
  job_families                        job_families[]
  job_postings                        job_postings[]
  job_postings_raw                    job_postings_raw[]
  job_template_skills                 job_template_skills[]
  job_templates                       job_templates[]
  job_title_courses                   job_title_courses[]
  job_title_learning_paths            job_title_learning_paths[]
  key_results                         key_results[]
  learning_content_providers          learning_content_providers[]
  learning_path_enrollments           learning_path_enrollments[]
  learning_paths                      learning_paths[]
  leave_accrual_rules                 leave_accrual_rules[]
  leave_approval_steps                leave_approval_steps[]
  leave_balance_transactions          leave_balance_transactions[]
  locations                           locations[]
  market_benchmarks                   market_benchmarks[]
  market_salary_data                  market_salary_data[]
  medical_certificates                medical_certificates[]
  mentor_match_scores                 mentor_match_scores[]
  mentorship_programs                 mentorship_programs[]
  mentorships                         mentorships[]
  merit_cycles                        merit_cycles[]
  model_predictions                   model_predictions[]
  news_articles                       news_articles[]
  news_bookmarks                      news_bookmarks[]
  news_categories                     news_categories[]
  news_comments                       news_comments[]
  news_reactions                      news_reactions[]
  news_reads                          news_reads[]
  news_tags                           news_tags[]
  notifications                       notifications[]
  okr_check_ins                       okr_check_ins[]
  okr_checkins                        okr_checkins[]
  okrs                                okrs[]
  onboarding_instances                onboarding_instances[]
  onboarding_templates                onboarding_templates[]
  ontology_embedding_jobs             ontology_embedding_jobs[]
  ontology_feedback                   ontology_feedback[]
  ontology_inference_jobs             ontology_inference_jobs[]
  ontology_source_mappings            ontology_source_mappings[]
  org_chart_generation_sessions       org_chart_generation_sessions[]
  org_chart_snapshots                 org_chart_snapshots[]
  org_scenarios                       org_scenarios[]
  org_units                           org_units[]
  payroll_anomaly_patterns            payroll_anomaly_patterns[]
  payroll_export_employees            payroll_export_employees[]
  payroll_export_files                payroll_export_files[]
  payroll_export_jobs                 payroll_export_jobs[]
  payroll_field_mappings              payroll_field_mappings[]
  payroll_integrations                payroll_integrations[]
  payroll_transmission_log            payroll_transmission_log[]
  payroll_validation_rules            payroll_validation_rules[]
  performance_predictions             performance_predictions[]
  performance_review_templates        performance_review_templates[]
  performance_reviews                 performance_reviews[]
  performance_skill_links             performance_skill_links[]
  performance_trends                  performance_trends[]
  permission_overrides                permission_overrides[]
  plugin_api_keys                     plugin_api_keys[]
  plugin_configurations               plugin_configurations[]
  plugin_hook_executions              plugin_hook_executions[]
  plugin_installations                plugin_installations[]
  plugin_reviews                      plugin_reviews[]
  plugin_webhooks                     plugin_webhooks[]
  plugins                             plugins[]
  position_skill_requirements         position_skill_requirements[]
  preboarding_sessions                preboarding_sessions[]
  preboarding_templates               preboarding_templates[]
  preboarding_welcome_content         preboarding_welcome_content[]
  prediction_model_accuracy           prediction_model_accuracy[]
  predictive_models                   predictive_models[]
  pulse_checks                        pulse_checks[]
  rag_document_chunks                 rag_document_chunks[]
  rag_documents                       rag_documents[]
  rag_knowledge_bases                 rag_knowledge_bases[]
  rag_provider_keys                   rag_provider_keys[]
  rag_sessions                        rag_sessions[]
  rag_usage_stats                     rag_usage_stats[]
  rating_scales                       rating_scales[]
  rbp_field_classifications           rbp_field_classifications[]
  rbp_section_translations            rbp_section_translations[]
  rbp_sections                        rbp_sections[]
  rbp_teams                           rbp_teams[]
  recognition                         recognition[]
  recruiting_candidates               recruiting_candidates[]
  recruiting_interview_templates      recruiting_interview_templates[]
  recruiting_interviewer_availability recruiting_interviewer_availability[]
  recruiting_interviews               recruiting_interviews[]
  recruiting_offers                   recruiting_offers[]
  recruiting_requisitions             recruiting_requisitions[]
  report_definitions                  report_definitions[]
  report_delivery_log                 report_delivery_log[]
  report_subscriptions                report_subscriptions[]
  requisitions                        requisitions[]
  review_cycle_notifications          review_cycle_notifications[]
  review_cycle_participants           review_cycle_participants[]
  review_cycle_phases                 review_cycle_phases[]
  review_cycles                       review_cycles[]
  role_permissions                    role_permissions[]
  role_skill_requirements             role_skill_requirements[]
  salary_bands                        salary_bands[]
  salary_history                      salary_history[]
  sap_delta_sync_log                  sap_delta_sync_log[]
  sap_employee_mapping                sap_employee_mapping[]
  sap_infotype_mappings               sap_infotype_mappings[]
  sap_migration_jobs                  sap_migration_jobs[]
  sap_migration_rollback_log          sap_migration_rollback_log[]
  sap_staged_data                     sap_staged_data[]
  saved_jobs                          saved_jobs[]
  self_assessment_evidence            self_assessment_evidence[]
  self_reviews                        self_reviews[]
  semantic_entity_index               semantic_entity_index[]
  semantic_entity_relations           semantic_entity_relations[]
  semantic_search_log                 semantic_search_log[]
  signature_requests                  signature_requests[]
  skill_demand_metrics                skill_demand_metrics[]
  skill_extraction_jobs               skill_extraction_jobs[]
  skill_gap_analyses                  skill_gap_analyses[]
  skill_gap_snapshots                 skill_gap_snapshots[]
  skill_matrices                      skill_matrices[]
  skill_migration_jobs                skill_migration_jobs[]
  skill_pair_usage                    skill_pair_usage[]
  skill_requirements_templates        skill_requirements_templates[]
  skill_supply_metrics                skill_supply_metrics[]
  skill_taxonomy_extensions           skill_taxonomy_extensions[]
  social_posts                        social_posts[]
  sso_configurations                  sso_configurations[]
  succession_plans                    succession_plans[]
  survey_templates                    survey_templates[]
  surveys                             surveys[]
  sync_log                            sync_log[]
  sync_queue                          sync_queue[]
  talent_pool_members                 talent_pool_members[]
  talent_pools                        talent_pools[]
  tenant_custom_skills                tenant_custom_skills[]
  tenant_industry_classifications     tenant_industry_classifications[]
  tenant_jobs                         tenant_jobs[]
  tenant_onboarding_profiles          tenant_onboarding_profiles[]
  tenant_org_charts                   tenant_org_charts[]
  tenant_retirement_rules             tenant_retirement_rules[]
  industry_profiles                   industry_profiles?                    @relation(fields: [industry_profile_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants_books                       tenants_books?
  turnover_risk_scores                turnover_risk_scores[]
  unknown_skills                      unknown_skills[]
  user_workspaces                     user_workspaces[]
  webhook_deliveries                  webhook_deliveries[]
  webhooks                            webhooks[]
  wellbeing_checkins                  wellbeing_checkins[]
  wellbeing_goals                     wellbeing_goals[]
  wellbeing_program_enrollments       wellbeing_program_enrollments[]
  wellbeing_resources                 wellbeing_resources[]
  whistleblowing_handlers             whistleblowing_handlers[]
  whistleblowing_reports              whistleblowing_reports[]
  whistleblowing_settings             whistleblowing_settings?
  widget_templates                    widget_templates[]
  workforce_plan_actions              workforce_plan_actions[]
  workforce_plan_scenarios            workforce_plan_scenarios[]
  workforce_plans                     workforce_plans[]
  workspace_templates                 workspace_templates[]
  dashboard_elements                  dashboard_elements[]

  @@index([profile_id], map: "idx_tenants_profile")
  @@index([sap_company_code], map: "idx_tenants_sap_company_code")
  @@index([settings], map: "idx_tenants_settings", type: Gin)
  @@index([status], map: "idx_tenants_status")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model users {
  id                                                                String                                @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  username                                                          String                                @unique @db.VarChar(100)
  password_hash                                                     String?                               @db.VarChar(255)
  role                                                              String?                               @default("USER") @db.VarChar(50)
  permissions                                                       String[]                              @default([])
  is_active                                                         Boolean?                              @default(true)
  last_login                                                        DateTime?                             @db.Timestamp(6)
  created_at                                                        DateTime?                             @default(now()) @db.Timestamp(6)
  updated_at                                                        DateTime?                             @default(now()) @db.Timestamp(6)
  employee_id                                                       String?                               @db.Uuid
  deleted_at                                                        DateTime?                             @db.Timestamptz(6)
  totp_secret                                                       String?                               @db.VarChar(64)
  totp_enabled                                                      Boolean?                              @default(false)
  totp_recovery_codes                                               String[]
  totp_failed_attempts                                              Int?                                  @default(0)
  totp_lockout_until                                                DateTime?                             @db.Timestamptz(6)
  totp_secret_encrypted                                             String?
  palette_preference_id                                             String?                               @db.VarChar(32)
  theme_preference                                                  String?                               @db.VarChar(8)
  account                                                           account[]
  ai_usage_log                                                      ai_usage_log[]
  analysis_sessions                                                 analysis_sessions[]
  analytics_events                                                  analytics_events[]
  audit_logs                                                        audit_logs[]
  blueprint_results                                                 blueprint_results[]
  blueprint_runs                                                    blueprint_runs[]
  blueprint_templates                                               blueprint_templates[]
  career_skills                                                     career_skills[]
  cross_entity_searches                                             cross_entity_searches[]
  document_comments                                                 document_comments[]
  engagement_action_plans_engagement_action_plans_created_byTousers engagement_action_plans[]             @relation("engagement_action_plans_created_byTousers")
  engagement_action_plans_engagement_action_plans_owner_idTousers   engagement_action_plans[]             @relation("engagement_action_plans_owner_idTousers")
  engagement_feedback                                               engagement_feedback[]
  engagement_pulse_configs                                          engagement_pulse_configs[]
  engagement_survey_templates                                       engagement_survey_templates[]
  engagement_surveys                                                engagement_surveys[]
  enrichment_jobs                                                   enrichment_jobs[]
  enrichment_matches                                                enrichment_matches[]
  enrichment_merges_enrichment_merges_committed_by_user_idTousers   enrichment_merges[]                   @relation("enrichment_merges_committed_by_user_idTousers")
  enrichment_merges_enrichment_merges_rolled_back_by_user_idTousers enrichment_merges[]                   @relation("enrichment_merges_rolled_back_by_user_idTousers")
  error_logs                                                        error_logs[]
  error_patterns                                                    error_patterns[]
  news_articles                                                     news_articles[]
  news_bookmarks                                                    news_bookmarks[]
  news_comments                                                     news_comments[]
  news_reactions                                                    news_reactions[]
  news_reads                                                        news_reads[]
  notification_preferences                                          notification_preferences?
  notifications                                                     notifications[]
  org_scenarios                                                     org_scenarios[]
  plugin_reviews                                                    plugin_reviews[]
  rag_sessions                                                      rag_sessions[]
  rbp_teams                                                         rbp_teams[]
  recruiting_interview_participants                                 recruiting_interview_participants[]
  recruiting_interviewer_availability                               recruiting_interviewer_availability[]
  semantic_search_log                                               semantic_search_log[]
  session                                                           session[]
  skill_classifications                                             skill_classifications[]
  skill_relationships                                               skill_relationships[]
  user_workspaces                                                   user_workspaces[]
  employees                                                         employees?                            @relation(fields: [employee_id], references: [id], onUpdate: NoAction, map: "fk_users_employee")
  whistleblowing_audit_log                                          whistleblowing_audit_log[]
  whistleblowing_handlers                                           whistleblowing_handlers[]
  workforce_plans_workforce_plans_created_byTousers                 workforce_plans[]                     @relation("workforce_plans_created_byTousers")
  workforce_plans_workforce_plans_updated_byTousers                 workforce_plans[]                     @relation("workforce_plans_updated_byTousers")

  @@index([employee_id], map: "idx_users_employee_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model verification_token {
  identifier String
  token      String
  expires    DateTime @db.Timestamptz(6)

  @@id([identifier, token])
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admin_component_registry {
  id                   String               @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id            String?              @db.Uuid
  code                 String               @db.VarChar(100)
  name                 String               @db.VarChar(200)
  description          String?
  frontend_path        String
  export_name          String               @db.VarChar(100)
  export_kind          String               @default("named") @db.VarChar(20)
  prop_shape           Json                 @default("{}")
  functional_area_code String               @db.VarChar(50)
  scope_level          String               @default("employee") @db.VarChar(20)
  read_only            Boolean              @default(true)
  reuse_contexts       String[]             @default(["admin", "portal"])
  api_endpoints        String[]
  verified_with_data   Boolean              @default(false)
  verified_at          DateTime?            @db.Timestamptz(6)
  created_at           DateTime?            @default(now()) @db.Timestamptz(6)
  updated_at           DateTime?            @default(now()) @db.Timestamptz(6)
  name_it              String?              @db.VarChar(200)
  name_en              String?              @db.VarChar(200)
  description_it       String?
  description_en       String?
  rbp_functional_areas rbp_functional_areas @relation(fields: [functional_area_code], references: [code], onDelete: NoAction, onUpdate: NoAction)
  tenants              tenants?             @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, code])
  @@index([functional_area_code], map: "idx_admin_components_area")
  @@index([tenant_id], map: "idx_admin_components_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ai_analytics_daily {
  id                        String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                 String    @db.Uuid
  date                      DateTime  @db.Date
  total_queries             Int?      @default(0)
  unique_users              Int?      @default(0)
  total_sessions            Int?      @default(0)
  chat_queries              Int?      @default(0)
  search_queries            Int?      @default(0)
  sql_queries               Int?      @default(0)
  document_qa_queries       Int?      @default(0)
  avg_response_time_ms      Int?
  p95_response_time_ms      Int?
  error_count               Int?      @default(0)
  total_tokens_input        Int?      @default(0)
  total_tokens_output       Int?      @default(0)
  avg_confidence_score      Decimal?  @db.Decimal(5, 4)
  escalation_count          Int?      @default(0)
  escalation_resolved_count Int?      @default(0)
  feedback_count            Int?      @default(0)
  avg_feedback_rating       Decimal?  @db.Decimal(3, 2)
  top_query_categories      Json?
  created_at                DateTime? @default(now()) @db.Timestamp(6)
  tenants                   tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, date], map: "idx_ai_analytics_unique")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ai_escalation_queue {
  id                                                   String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                            String       @db.Uuid
  message_id                                           String       @db.Uuid
  session_id                                           String       @db.Uuid
  employee_id                                          String?      @db.Uuid
  original_query                                       String
  ai_response                                          String?
  confidence_score                                     Decimal?     @db.Decimal(5, 4)
  escalation_reason                                    String       @db.VarChar(255)
  category                                             String?      @db.VarChar(50)
  priority                                             String?      @default("normal") @db.VarChar(20)
  assigned_to                                          String?      @db.Uuid
  assigned_at                                          DateTime?    @db.Timestamp(6)
  status                                               String?      @default("pending") @db.VarChar(20)
  human_response                                       String?
  resolved_at                                          DateTime?    @db.Timestamp(6)
  resolved_by                                          String?      @db.Uuid
  resolution_notes                                     String?
  should_train                                         Boolean?     @default(false)
  created_at                                           DateTime?    @default(now()) @db.Timestamp(6)
  updated_at                                           DateTime?    @default(now()) @db.Timestamp(6)
  employees_ai_escalation_queue_assigned_toToemployees employees?   @relation("ai_escalation_queue_assigned_toToemployees", fields: [assigned_to], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees_ai_escalation_queue_employee_idToemployees employees?   @relation("ai_escalation_queue_employee_idToemployees", fields: [employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  rag_messages                                         rag_messages @relation(fields: [message_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees_ai_escalation_queue_resolved_byToemployees employees?   @relation("ai_escalation_queue_resolved_byToemployees", fields: [resolved_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  rag_sessions                                         rag_sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                                              tenants      @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([employee_id], map: "idx_ai_escalation_employee")
  @@index([message_id], map: "idx_ai_escalation_message")
  @@index([resolved_by], map: "idx_ai_escalation_queue_resolved_by")
  @@index([session_id], map: "idx_ai_escalation_session")
  @@index([assigned_to], map: "idx_escalation_assigned")
  @@index([priority, created_at], map: "idx_escalation_priority")
  @@index([status], map: "idx_escalation_status")
  @@index([tenant_id], map: "idx_escalation_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ai_prompt_templates {
  id                   String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id            String?  @db.Uuid
  code                 String   @db.VarChar(50)
  language             String   @default("it") @db.VarChar(5)
  name                 String   @db.VarChar(255)
  description          String?
  prompt_template      String
  context_requirements Json?
  llm_provider         String?  @default("openai") @db.VarChar(50)
  model_name           String?  @default("gpt-4o") @db.VarChar(50)
  temperature          Decimal? @default(0.3) @db.Decimal(3, 2)
  max_tokens           Int?     @default(2000)
  is_active            Boolean  @default(true)
  created_at           DateTime @default(now()) @db.Timestamptz(6)
  updated_at           DateTime @default(now()) @db.Timestamptz(6)
  tenants              tenants? @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([code, language], map: "uq_ai_prompt_code_lang")
  @@index([is_active, code], map: "idx_ai_prompts_active")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ai_provider_config {
  id                    String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  provider              String    @unique @db.VarChar(50)
  model                 String    @db.VarChar(100)
  is_enabled            Boolean   @default(true)
  priority              Int       @default(10)
  rate_limit_per_minute Int       @default(1000)
  cost_per_1k_tokens    Decimal   @default(0.0001) @db.Decimal(10, 6)
  max_batch_size        Int       @default(100)
  api_key_env_var       String?   @db.VarChar(100)
  created_at            DateTime? @default(now()) @db.Timestamptz(6)
  updated_at            DateTime? @default(now()) @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ai_provider_metrics {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  provider       String    @db.VarChar(50)
  request_count  Int       @default(0)
  token_count    BigInt    @default(0)
  total_cost     Decimal   @default(0) @db.Decimal(10, 6)
  avg_latency_ms Decimal   @default(0) @db.Decimal(10, 2)
  error_count    Int       @default(0)
  status         String    @default("available") @db.VarChar(50)
  recorded_at    DateTime  @default(now()) @db.Timestamptz(6)
  created_at     DateTime? @default(now()) @db.Timestamptz(6)

  @@index([provider, recorded_at(sort: Desc)], map: "idx_ai_metrics_provider_date")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ai_query_audit {
  id               String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id        String        @db.Uuid
  session_id       String?       @db.Uuid
  message_id       String?       @db.Uuid
  employee_id      String?       @db.Uuid
  query_text       String
  query_type       String?       @db.VarChar(50)
  response_text    String?
  response_time_ms Int?
  tokens_input     Int?
  tokens_output    Int?
  provider         String?       @db.VarChar(50)
  model            String?       @db.VarChar(100)
  sources_used     Json?
  chunks_retrieved Int?
  retrieval_score  Decimal?      @db.Decimal(5, 4)
  confidence_score Decimal?      @db.Decimal(5, 4)
  was_escalated    Boolean?      @default(false)
  had_error        Boolean?      @default(false)
  error_type       String?       @db.VarChar(100)
  error_message    String?
  created_at       DateTime?     @default(now()) @db.Timestamp(6)
  employees        employees?    @relation(fields: [employee_id], references: [id], onUpdate: NoAction)
  rag_messages     rag_messages? @relation(fields: [message_id], references: [id], onUpdate: NoAction)
  rag_sessions     rag_sessions? @relation(fields: [session_id], references: [id], onUpdate: NoAction)
  tenants          tenants       @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_at], map: "idx_ai_audit_date")
  @@index([employee_id], map: "idx_ai_audit_employee")
  @@index([provider], map: "idx_ai_audit_provider")
  @@index([tenant_id], map: "idx_ai_audit_tenant")
  @@index([query_type], map: "idx_ai_audit_type")
  @@index([message_id], map: "idx_ai_query_audit_message_id")
  @@index([session_id], map: "idx_ai_query_audit_session_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ai_tenant_config {
  id                              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                       String    @unique @db.Uuid
  default_provider                String?   @default("openai") @db.VarChar(50)
  default_chat_model              String?   @default("gpt-4o-mini") @db.VarChar(100)
  default_embedding_model         String?   @default("text-embedding-3-small") @db.VarChar(100)
  enable_sql_generation           Boolean?  @default(false)
  enable_document_qa              Boolean?  @default(true)
  enable_escalation               Boolean?  @default(true)
  confidence_threshold_low        Decimal?  @default(0.5000) @db.Decimal(5, 4)
  confidence_threshold_escalation Decimal?  @default(0.3000) @db.Decimal(5, 4)
  max_queries_per_user_day        Int?      @default(100)
  max_tokens_per_user_day         Int?      @default(50000)
  system_prompt_base              String?
  system_prompt_hr_context        String?
  escalation_email                String?   @db.VarChar(255)
  escalation_notify_slack         Boolean?  @default(false)
  escalation_slack_webhook        String?
  created_at                      DateTime? @default(now()) @db.Timestamp(6)
  updated_at                      DateTime? @default(now()) @db.Timestamp(6)
  tenants                         tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ai_usage_log {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  provider         String    @db.VarChar(50)
  model            String    @db.VarChar(100)
  operation        String    @db.VarChar(50)
  input_tokens     Int?
  output_tokens    Int?
  total_tokens     Int
  cost             Decimal   @db.Decimal(10, 8)
  latency_ms       Int
  success          Boolean
  error_message    String?
  tenant_id        String    @db.Uuid
  user_id          String?   @db.Uuid
  request_metadata Json?
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  tenants          tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users            users?    @relation(fields: [user_id], references: [id], onUpdate: NoAction, map: "fk_aul_user")

  @@index([created_at(sort: Desc)], map: "idx_ai_usage_created")
  @@index([user_id], map: "idx_ai_usage_log_user_id")
  @@index([operation], map: "idx_ai_usage_operation")
  @@index([provider], map: "idx_ai_usage_provider")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model analysis_sessions {
  id                 String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id          String    @db.Uuid
  name               String    @db.VarChar(255)
  description        String?
  session_type       String    @default("performance") @db.VarChar(50)
  status             String    @default("draft") @db.VarChar(50)
  parameters         Json?     @default("{}")
  results            Json?     @default("{}")
  findings_count     Int?      @default(0)
  participants_count Int?      @default(0)
  created_by         String?   @db.Uuid
  created_at         DateTime? @default(now()) @db.Timestamp(6)
  updated_at         DateTime? @default(now()) @db.Timestamp(6)
  deleted_at         DateTime? @db.Timestamp(6)
  users              users?    @relation(fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants            tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([created_by], map: "idx_analysis_sessions_created_by")
  @@index([tenant_id, status], map: "idx_analysis_sessions_status")
  @@index([tenant_id], map: "idx_analysis_sessions_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model analytics_aggregations {
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id       String    @db.Uuid
  metric_name     String    @db.VarChar(100)
  dimension       String?   @db.VarChar(100)
  dimension_value String?   @db.VarChar(255)
  period_type     String    @db.VarChar(20)
  period_start    DateTime  @db.Timestamptz(6)
  period_end      DateTime  @db.Timestamptz(6)
  value_sum       Decimal?  @db.Decimal(20, 4)
  value_count     Int?
  value_avg       Decimal?  @db.Decimal(20, 4)
  value_min       Decimal?  @db.Decimal(20, 4)
  value_max       Decimal?  @db.Decimal(20, 4)
  metadata        Json?     @default("{}")
  computed_at     DateTime? @default(now()) @db.Timestamptz(6)
  created_at      DateTime? @default(now()) @db.Timestamptz(6)
  tenants         tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([tenant_id, metric_name, dimension, dimension_value, period_type, period_start], map: "idx_aggregations_unique")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model analytics_events {
  id          String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id   String     @db.Uuid
  event_type  String     @db.VarChar(100)
  entity_type String?    @db.VarChar(50)
  entity_id   String?    @db.Uuid
  actor_id    String?    @db.Uuid
  event_data  Json?      @default("{}")
  occurred_at DateTime?  @default(now()) @db.Timestamptz(6)
  created_at  DateTime?  @default(now()) @db.Timestamptz(6)
  category    String?    @db.VarChar(100)
  user_id     String?    @db.Uuid
  session_id  String?    @db.Uuid
  data        Json?
  metrics     Json?
  employees   employees? @relation(fields: [actor_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants     tenants    @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users       users?     @relation(fields: [user_id], references: [id], onUpdate: NoAction, map: "fk_ae_user")

  @@index([actor_id], map: "idx_analytics_events_actor_id")
  @@index([entity_type, entity_id], map: "idx_analytics_events_entity")
  @@index([occurred_at], map: "idx_analytics_events_occurred")
  @@index([tenant_id], map: "idx_analytics_events_tenant")
  @@index([event_type], map: "idx_analytics_events_type")
  @@index([user_id], map: "idx_analytics_events_user_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model api_keys {
  id                  String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String    @db.Uuid
  key_hash            String    @unique(map: "idx_api_keys_hash") @db.VarChar(128)
  name                String    @db.VarChar(200)
  is_active           Boolean   @default(true)
  rate_limit_per_hour Int       @default(1000)
  last_used_at        DateTime? @db.Timestamptz(6)
  created_at          DateTime  @default(now()) @db.Timestamptz(6)
  expires_at          DateTime? @db.Timestamptz(6)
  tenants             tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_api_keys_tenant_id")
}

model applications {
  id               String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  requisition_id   String       @db.Uuid
  candidate_id     String       @db.Uuid
  stage            String?      @default("applied") @db.VarChar(50)
  status           String?      @default("active") @db.VarChar(50)
  rating           Decimal?     @db.Decimal(3, 2)
  fit_score        Decimal?     @db.Decimal(5, 2)
  rejection_reason String?      @db.VarChar(255)
  offer_salary     Decimal?     @db.Decimal(15, 2)
  offer_date       DateTime?    @db.Date
  applied_at       DateTime?    @default(now()) @db.Timestamptz(6)
  last_activity_at DateTime?    @default(now()) @db.Timestamptz(6)
  hired_at         DateTime?    @db.Timestamptz(6)
  recruiter_id     String?      @db.Uuid
  created_at       DateTime?    @default(now()) @db.Timestamptz(6)
  requisitions     requisitions @relation(fields: [requisition_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([candidate_id], map: "idx_applications_candidate")
  @@index([requisition_id], map: "idx_applications_requisition")
  @@index([stage], map: "idx_applications_stage")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model audit_logs {
  id                  String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  timestamp           DateTime?  @default(now()) @db.Timestamp(6)
  user_id             String?    @db.Uuid
  user_email          String?    @db.VarChar(255)
  user_role           String?    @db.VarChar(50)
  action              String     @db.VarChar(50)
  category            String     @db.VarChar(50)
  resource_type       String?    @db.VarChar(100)
  resource_id         String?    @db.VarChar(255)
  resource_name       String?    @db.VarChar(500)
  description         String
  old_value           Json?
  new_value           Json?
  ip_address          String?    @db.Inet
  user_agent          String?
  tenant_id           String     @db.Uuid
  metadata            Json?      @default("{}")
  success             Boolean?   @default(true)
  error_message       String?
  user_id_employee_id String?    @db.Uuid
  created_at          DateTime?  @default(now()) @db.Timestamptz(6)
  tenants             tenants    @relation(fields: [tenant_id], references: [id], onDelete: SetNull, onUpdate: NoAction)
  employees           employees? @relation(fields: [user_id_employee_id], references: [id], onUpdate: NoAction, map: "audit_logs_user_id_fkey_emp")
  users               users?     @relation(fields: [user_id], references: [id], onUpdate: NoAction, map: "fk_al_user")

  @@index([action], map: "idx_audit_logs_action")
  @@index([category], map: "idx_audit_logs_category")
  @@index([resource_type, resource_id], map: "idx_audit_logs_resource")
  @@index([success], map: "idx_audit_logs_success")
  @@index([tenant_id], map: "idx_audit_logs_tenant")
  @@index([timestamp(sort: Desc)], map: "idx_audit_logs_timestamp")
  @@index([user_id], map: "idx_audit_logs_user")
  @@index([user_email], map: "idx_audit_logs_user_email")
  @@index([user_id_employee_id], map: "idx_audit_logs_user_employee")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model benchmark_configs {
  id                String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String              @db.Uuid
  config_name       String              @db.VarChar(255)
  benchmark_type    String              @db.VarChar(50)
  industries        Json?               @default("[]")
  countries         Json?               @default("[]")
  regions           Json?               @default("[]")
  company_sizes     Json?               @default("[]")
  experience_levels Json?               @default("[]")
  isco_codes        Json?               @default("[]")
  is_default        Boolean?            @default(false)
  created_at        DateTime?           @default(now()) @db.Timestamptz(6)
  updated_at        DateTime?           @default(now()) @db.Timestamptz(6)
  tenants           tenants             @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  benchmark_reports benchmark_reports[]

  @@index([tenant_id], map: "idx_benchmark_config_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model benchmark_reports {
  id                     String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String             @db.Uuid
  config_id              String?            @db.Uuid
  report_name            String             @db.VarChar(255)
  report_type            String             @db.VarChar(50)
  report_date            DateTime?          @default(dbgenerated("CURRENT_DATE")) @db.Date
  salary_benchmarks      Json?              @default("{}")
  salary_percentiles     Json?              @default("{}")
  salary_trends          Json?              @default("[]")
  trending_skills        Json?              @default("[]")
  declining_skills       Json?              @default("[]")
  emerging_skills        Json?              @default("[]")
  skill_demand_scores    Json?              @default("{}")
  talent_supply          Json?              @default("{}")
  competition_index      Json?              @default("{}")
  time_to_hire_estimates Json?              @default("{}")
  internal_vs_market     Json?              @default("{}")
  recommendations        Json?              @default("[]")
  created_by             String?            @db.Uuid
  created_at             DateTime?          @default(now()) @db.Timestamptz(6)
  created_by_employee_id String?            @db.Uuid
  benchmark_configs      benchmark_configs? @relation(fields: [config_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees              employees?         @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                tenants            @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([report_date], map: "idx_benchmark_report_date")
  @@index([tenant_id], map: "idx_benchmark_report_tenant")
  @@index([report_type], map: "idx_benchmark_report_type")
  @@index([config_id], map: "idx_benchmark_reports_config_id")
  @@index([created_by_employee_id], map: "idx_benchmark_reports_created_by_employee_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model blueprint_results {
  id               String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  run_id           String         @db.Uuid
  result_type      String         @db.VarChar(50)
  entity_type      String?        @db.VarChar(100)
  entity_id        String?        @db.Uuid
  severity         String?        @db.VarChar(20)
  title            String         @db.VarChar(500)
  description      String?
  suggested_action Json?
  is_applied       Boolean?       @default(false)
  applied_at       DateTime?      @db.Timestamptz(6)
  applied_by       String?        @db.Uuid
  created_at       DateTime?      @default(now()) @db.Timestamptz(6)
  users            users?         @relation(fields: [applied_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  blueprint_runs   blueprint_runs @relation(fields: [run_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([applied_by], map: "idx_blueprint_results_applied_by")
  @@index([run_id], map: "idx_blueprint_results_run_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model blueprint_runs {
  id                  String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String              @db.Uuid
  template_id         String              @db.Uuid
  run_mode            String              @db.VarChar(20)
  status              String              @default("pending") @db.VarChar(30)
  input_config        Json?               @default("{}")
  started_at          DateTime?           @db.Timestamptz(6)
  completed_at        DateTime?           @db.Timestamptz(6)
  created_by          String?             @db.Uuid
  error_message       String?
  created_at          DateTime?           @default(now()) @db.Timestamptz(6)
  updated_at          DateTime?           @default(now()) @db.Timestamptz(6)
  blueprint_results   blueprint_results[]
  users               users?              @relation(fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  blueprint_templates blueprint_templates @relation(fields: [template_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants             tenants             @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([created_by], map: "idx_blueprint_runs_created_by")
  @@index([template_id], map: "idx_blueprint_runs_template_id")
  @@index([tenant_id], map: "idx_blueprint_runs_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model blueprint_templates {
  id                String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  profile_id        String            @db.Uuid
  template_name     String            @db.VarChar(255)
  template_version  String            @default("1.0") @db.VarChar(20)
  description       String?
  template_config   Json              @default("{}")
  is_active         Boolean?          @default(true)
  created_by        String?           @db.Uuid
  created_at        DateTime?         @default(now()) @db.Timestamptz(6)
  updated_at        DateTime?         @default(now()) @db.Timestamptz(6)
  blueprint_runs    blueprint_runs[]
  users             users?            @relation(fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  industry_profiles industry_profiles @relation(fields: [profile_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([profile_id, template_name, template_version])
  @@index([created_by], map: "idx_blueprint_templates_created_by")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model bonus_allocations {
  id                                                             String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  plan_id                                                        String?    @db.Uuid
  employee_id                                                    String?    @db.Uuid
  target_amount                                                  Decimal?   @db.Decimal(12, 2)
  actual_amount                                                  Decimal?   @db.Decimal(12, 2)
  performance_rating                                             String?    @db.VarChar(20)
  performance_multiplier                                         Decimal?   @default(1.0) @db.Decimal(5, 2)
  status                                                         String?    @default("pending") @db.VarChar(30)
  approved_by                                                    String?    @db.Uuid
  approved_at                                                    DateTime?  @db.Timestamp(6)
  paid_at                                                        DateTime?  @db.Timestamp(6)
  notes                                                          String?
  created_at                                                     DateTime?  @default(now()) @db.Timestamp(6)
  updated_at                                                     DateTime?  @default(now()) @db.Timestamp(6)
  approved_by_employee_id                                        String?    @db.Uuid
  employees_bonus_allocations_approved_by_employee_idToemployees employees? @relation("bonus_allocations_approved_by_employee_idToemployees", fields: [approved_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "bonus_allocations_approved_by_fkey_emp")
  employees_bonus_allocations_employee_idToemployees             employees? @relation("bonus_allocations_employee_idToemployees", fields: [employee_id], references: [id], onUpdate: NoAction, map: "fk_ba_employee")

  @@index([approved_by_employee_id], map: "idx_bonus_allocations_approved_by_employee_id")
  @@index([employee_id], map: "idx_bonus_allocations_emp")
  @@index([plan_id], map: "idx_bonus_allocations_plan")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model bonus_plans {
  id                      String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id               String     @db.Uuid
  name                    String     @db.VarChar(200)
  description             String?
  bonus_type              String     @db.VarChar(50)
  period_start            DateTime?  @db.Date
  period_end              DateTime?  @db.Date
  payout_date             DateTime?  @db.Date
  total_budget            Decimal?   @db.Decimal(15, 2)
  allocated_amount        Decimal?   @default(0) @db.Decimal(15, 2)
  calculation_method      String?    @default("percentage") @db.VarChar(50)
  eligibility_rules       Json?
  performance_multipliers Json?
  status                  String?    @default("draft") @db.VarChar(30)
  created_by              String?    @db.Uuid
  created_at              DateTime?  @default(now()) @db.Timestamp(6)
  updated_at              DateTime?  @default(now()) @db.Timestamp(6)
  created_by_employee_id  String?    @db.Uuid
  employees               employees? @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "bonus_plans_created_by_fkey_emp")
  tenants                 tenants    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_by_employee_id], map: "idx_bonus_plans_created_by_employee_id")
  @@index([tenant_id], map: "idx_bonus_plans_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model burnout_assessments {
  id                 String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String    @db.Uuid
  employee_id        String    @db.Uuid
  assessment_date    DateTime  @default(dbgenerated("CURRENT_DATE")) @db.Date
  exhaustion_score   Int?
  cynicism_score     Int?
  inefficacy_score   Int?
  overall_risk       String?   @db.VarChar(20)
  workload_factor    Int?
  autonomy_factor    Int?
  recognition_factor Int?
  recommendations    Json?
  created_at         DateTime? @default(now()) @db.Timestamptz(6)
  tenants            tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees          employees @relation(fields: [employee_id], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_bua_employee")

  @@index([employee_id], map: "idx_burnout_assessments_employee")
  @@index([tenant_id], map: "idx_burnout_assessments_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model business_processes {
  id                         String                       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  process_code               String                       @db.VarChar(20)
  process_name               String                       @db.VarChar(100)
  process_category           String                       @db.VarChar(50)
  value_chain_position       Int
  description                String?
  typical_inputs             String[]                     @default([])
  typical_outputs            String[]                     @default([])
  created_at                 DateTime?                    @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime?                    @default(now()) @db.Timestamptz(6)
  profile_id                 String?                      @db.Uuid
  industry_profiles          industry_profiles?           @relation(fields: [profile_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  org_unit_process_mapping   org_unit_process_mapping[]
  process_cost_centers       process_cost_centers[]
  process_kpis               process_kpis[]
  process_phases             process_phases[]
  process_roles              process_roles[]
  process_skill_requirements process_skill_requirements[]

  @@index([process_category], map: "idx_business_processes_category")
  @@index([value_chain_position], map: "idx_business_processes_position")
  @@index([profile_id], map: "idx_business_processes_profile_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model calibration_adjustments {
  id                                                       String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                                String                  @db.Uuid
  calibration_session_id                                   String                  @db.Uuid
  employee_id                                              String                  @db.Uuid
  original_rating                                          Decimal                 @db.Decimal(3, 1)
  adjusted_rating                                          Decimal                 @db.Decimal(3, 1)
  rating_category                                          String?                 @db.VarChar(50)
  adjustment_reason                                        String
  supporting_evidence                                      String?
  proposed_by                                              String?                 @db.Uuid
  approved_by                                              String?                 @db.Uuid
  approved_at                                              DateTime?               @db.Timestamptz(6)
  created_at                                               DateTime?               @default(now()) @db.Timestamptz(6)
  discussion_notes                                         String?
  outlier_flag                                             Boolean?                @default(false)
  outlier_reason                                           String?                 @db.VarChar(100)
  applied_at                                               DateTime?               @db.Timestamptz(6)
  applied_by                                               String?                 @db.Uuid
  employees_calibration_adjustments_applied_byToemployees  employees?              @relation("calibration_adjustments_applied_byToemployees", fields: [applied_by], references: [id], onUpdate: NoAction)
  employees_calibration_adjustments_approved_byToemployees employees?              @relation("calibration_adjustments_approved_byToemployees", fields: [approved_by], references: [id], onUpdate: NoAction)
  calibration_sessions                                     calibration_sessions    @relation(fields: [calibration_session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees_calibration_adjustments_employee_idToemployees employees               @relation("calibration_adjustments_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees_calibration_adjustments_proposed_byToemployees employees?              @relation("calibration_adjustments_proposed_byToemployees", fields: [proposed_by], references: [id], onUpdate: NoAction)
  tenants                                                  tenants                 @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  calibration_audit_log                                    calibration_audit_log[]

  @@index([applied_by], map: "idx_calibration_adj_applied")
  @@index([approved_by], map: "idx_calibration_adj_approved")
  @@index([proposed_by], map: "idx_calibration_adj_proposed")
  @@index([employee_id], map: "idx_calibration_adjustments_employee")
  @@index([calibration_session_id], map: "idx_calibration_adjustments_session")
  @@index([tenant_id], map: "idx_calibration_adjustments_tenant_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model calibration_audit_log {
  id                      String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id               String                   @db.Uuid
  calibration_session_id  String                   @db.Uuid
  adjustment_id           String?                  @db.Uuid
  action_type             String                   @db.VarChar(50)
  action_by               String?                  @db.Uuid
  action_at               DateTime?                @default(now()) @db.Timestamptz(6)
  old_value               Json?
  new_value               Json?
  description             String?
  created_at              DateTime?                @default(now()) @db.Timestamptz(6)
  employees               employees?               @relation(fields: [action_by], references: [id], onUpdate: NoAction)
  calibration_adjustments calibration_adjustments? @relation(fields: [adjustment_id], references: [id], onUpdate: NoAction)
  calibration_sessions    calibration_sessions     @relation(fields: [calibration_session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                 tenants                  @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([action_type], map: "idx_calibration_audit_action")
  @@index([adjustment_id], map: "idx_calibration_audit_adjustment")
  @@index([action_by], map: "idx_calibration_audit_log_action_by")
  @@index([calibration_session_id], map: "idx_calibration_audit_session")
  @@index([tenant_id], map: "idx_calibration_audit_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model calibration_discussions {
  id                   String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  session_id           String?    @db.Uuid
  employee_id          String?    @db.Uuid
  original_rating      String?    @db.VarChar(20)
  original_potential   String?    @db.VarChar(20)
  calibrated_rating    String?    @db.VarChar(20)
  calibrated_potential String?    @db.VarChar(20)
  discussion_notes     String?
  adjustment_reason    String?
  was_adjusted         Boolean?   @default(false)
  discussed_at         DateTime?  @default(now()) @db.Timestamp(6)
  created_at           DateTime?  @default(now()) @db.Timestamptz(6)
  updated_at           DateTime?  @default(now()) @db.Timestamptz(6)
  employees            employees? @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_calibration_discussions_employee")

  @@index([employee_id], map: "idx_calibration_discussions_employee_id")
  @@index([session_id], map: "idx_calibration_discussions_session")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model calibration_participants {
  id         String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  session_id String?    @db.Uuid
  manager_id String?    @db.Uuid
  role       String?    @default("participant") @db.VarChar(30)
  attended   Boolean?   @default(false)
  joined_at  DateTime?  @db.Timestamp(6)
  tenant_id  String     @db.Uuid
  created_at DateTime?  @default(now()) @db.Timestamptz(6)
  updated_at DateTime?  @default(now()) @db.Timestamptz(6)
  tenants    tenants    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees  employees? @relation(fields: [manager_id], references: [id], onUpdate: NoAction, map: "fk_cp_manager")

  @@index([manager_id], map: "idx_calibration_participants_manager_id")
  @@index([session_id], map: "idx_calibration_participants_session")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model calibration_results {
  id                                                   String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                            String               @db.Uuid
  calibration_session_id                               String               @db.Uuid
  employee_id                                          String               @db.Uuid
  performance_review_id                                String?              @db.Uuid
  pre_calibration_rating                               Decimal?             @db.Decimal(3, 1)
  post_calibration_rating                              Decimal?             @db.Decimal(3, 1)
  final_rating                                         Decimal?             @db.Decimal(3, 1)
  rating_change                                        Decimal?             @default(dbgenerated("(post_calibration_rating - pre_calibration_rating)")) @db.Decimal(3, 1)
  justification                                        String?
  is_outlier                                           Boolean?             @default(false)
  approved_by                                          String?              @db.Uuid
  approved_at                                          DateTime?            @db.Timestamptz(6)
  calibration_notes                                    String?
  created_at                                           DateTime?            @default(now()) @db.Timestamptz(6)
  updated_at                                           DateTime?            @default(now()) @db.Timestamptz(6)
  employees_calibration_results_approved_byToemployees employees?           @relation("calibration_results_approved_byToemployees", fields: [approved_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  calibration_sessions                                 calibration_sessions @relation(fields: [calibration_session_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees_calibration_results_employee_idToemployees employees            @relation("calibration_results_employee_idToemployees", fields: [employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  performance_reviews                                  performance_reviews? @relation(fields: [performance_review_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                                              tenants              @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([approved_by], map: "idx_calibration_results_approved_by")
  @@index([employee_id], map: "idx_calibration_results_employee")
  @@index([performance_review_id], map: "idx_calibration_results_performance_review_id")
  @@index([calibration_session_id], map: "idx_calibration_results_session")
  @@index([tenant_id], map: "idx_calibration_results_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model calibration_sessions {
  id                                                                   String                    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                                            String                    @db.Uuid
  name                                                                 String                    @db.VarChar(200)
  description                                                          String?
  review_cycle_id                                                      String?                   @db.Uuid
  department                                                           String?                   @db.VarChar(100)
  scheduled_date                                                       DateTime?                 @db.Timestamp(6)
  duration_minutes                                                     Int?                      @default(120)
  location                                                             String?                   @db.VarChar(200)
  facilitator_id                                                       String?                   @db.Uuid
  status                                                               String?                   @default("scheduled") @db.VarChar(30)
  summary_notes                                                        String?
  adjustments_count                                                    Int?                      @default(0)
  created_by                                                           String?                   @db.Uuid
  created_at                                                           DateTime?                 @default(now()) @db.Timestamp(6)
  updated_at                                                           DateTime?                 @default(now()) @db.Timestamp(6)
  created_by_employee_id                                               String?                   @db.Uuid
  facilitator_id_employee_id                                           String?                   @db.Uuid
  org_unit_id                                                          String?                   @db.Uuid
  calibration_adjustments                                              calibration_adjustments[]
  calibration_audit_log                                                calibration_audit_log[]
  calibration_results                                                  calibration_results[]
  employees_calibration_sessions_created_by_employee_idToemployees     employees?                @relation("calibration_sessions_created_by_employee_idToemployees", fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "calibration_sessions_created_by_fkey_emp")
  employees_calibration_sessions_facilitator_id_employee_idToemployees employees?                @relation("calibration_sessions_facilitator_id_employee_idToemployees", fields: [facilitator_id_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "calibration_sessions_facilitator_id_fkey_emp")
  org_units                                                            org_units?                @relation(fields: [org_unit_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                                                              tenants                   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_by_employee_id], map: "idx_calibration_sessions_created_by_employee_id")
  @@index([review_cycle_id], map: "idx_calibration_sessions_cycle")
  @@index([scheduled_date], map: "idx_calibration_sessions_date")
  @@index([facilitator_id_employee_id], map: "idx_calibration_sessions_facilitator_id_employee_id")
  @@index([status], map: "idx_calibration_sessions_status")
  @@index([tenant_id], map: "idx_calibration_sessions_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model candidates {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id        String    @db.Uuid
  first_name       String    @db.VarChar(100)
  last_name        String    @db.VarChar(100)
  email            String    @db.VarChar(255)
  phone            String?   @db.VarChar(50)
  current_company  String?   @db.VarChar(255)
  current_title    String?   @db.VarChar(255)
  linkedin_url     String?   @db.VarChar(500)
  years_experience Int?
  resume_url       String?   @db.VarChar(500)
  source           String?   @db.VarChar(100)
  source_detail    String?   @db.VarChar(255)
  referred_by_id   String?   @db.Uuid
  skills           Json?
  consent_given    Boolean?  @default(false)
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  updated_at       DateTime? @default(now()) @db.Timestamptz(6)
  tenants          tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([email], map: "idx_candidates_email")
  @@index([tenant_id], map: "idx_candidates_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model canonical_demo_users {
  role       String   @id @db.VarChar(50)
  username   String   @unique @db.VarChar(100)
  updated_at DateTime @default(now()) @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model career_conversations {
  id              String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id       String          @db.Uuid
  profile_id      String          @db.Uuid
  messages        Json            @default("[]")
  summary         String?
  last_message_at DateTime        @default(now()) @db.Timestamptz(6)
  created_at      DateTime        @default(now()) @db.Timestamptz(6)
  career_profiles career_profiles @relation(fields: [profile_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants         tenants         @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([profile_id], map: "idx_career_conversations_profile")
  @@index([tenant_id], map: "idx_career_conversations_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model career_goal_milestones {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id      String       @db.Uuid
  goal_id        String       @db.Uuid
  title          String       @db.VarChar(255)
  description    String?
  type           String       @default("action") @db.VarChar(50)
  due_date       DateTime?    @db.Date
  completed_at   DateTime?    @db.Timestamptz(6)
  reference_type String?      @db.VarChar(50)
  reference_id   String?      @db.Uuid
  sequence_order Int          @default(0)
  created_at     DateTime     @default(now()) @db.Timestamptz(6)
  career_goals   career_goals @relation(fields: [goal_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants        tenants      @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_career_goal_milestones_tenant_id")
  @@index([goal_id], map: "idx_career_milestones_goal")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model career_goals {
  id                     String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String                   @db.Uuid
  profile_id             String                   @db.Uuid
  target_role            String                   @db.VarChar(255)
  target_job_family_id   String?                  @db.Uuid
  target_date            DateTime?                @db.Date
  status                 String                   @default("active") @db.VarChar(50)
  progress               Int                      @default(0)
  motivation             String?
  created_at             DateTime                 @default(now()) @db.Timestamptz(6)
  updated_at             DateTime                 @default(now()) @db.Timestamptz(6)
  completed_at           DateTime?                @db.Timestamptz(6)
  career_goal_milestones career_goal_milestones[]
  career_profiles        career_profiles          @relation(fields: [profile_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                tenants                  @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([profile_id, status], map: "idx_career_goals_status")
  @@index([tenant_id], map: "idx_career_goals_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model career_path_level_skills {
  id                       String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                String                   @db.Uuid
  level_id                 String                   @db.Uuid
  skill_id                 String                   @db.Uuid
  required_knowledge_level Int?                     @db.SmallInt
  required_skill_level     Int?                     @db.SmallInt
  required_ability_level   Int?                     @db.SmallInt
  required_behavior_level  Int?                     @db.SmallInt
  required_attitude_level  Int?                     @db.SmallInt
  min_composite_score      Decimal?                 @db.Decimal(3, 2)
  importance               career_skill_importance? @default(important)
  weight                   Decimal?                 @default(1.0) @db.Decimal(3, 2)
  is_mandatory             Boolean?                 @default(false)
  notes                    String?
  created_at               DateTime?                @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?                @default(now()) @db.Timestamptz(6)
  career_path_levels       career_path_levels       @relation(fields: [level_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  esco_skills              esco_skills              @relation(fields: [skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                  tenants                  @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([level_id, skill_id])
  @@index([tenant_id], map: "idx_career_level_skills_tenant")
}

model career_path_levels {
  id                                                                                             String                        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  path_id                                                                                        String?                       @db.Uuid
  title                                                                                          String                        @db.VarChar(200)
  description                                                                                    String?
  level_order                                                                                    Int
  min_years_experience                                                                           Int?                          @default(0)
  required_skills                                                                                String[]
  required_certifications                                                                        String[]
  salary_band_min                                                                                Decimal?                      @db.Decimal(12, 2)
  salary_band_max                                                                                Decimal?                      @db.Decimal(12, 2)
  created_at                                                                                     DateTime?                     @default(now()) @db.Timestamp(6)
  target_job_id                                                                                  String?                       @db.Uuid
  typical_duration_months                                                                        Int?                          @default(24)
  skill_gap_threshold                                                                            Decimal?                      @default(0.5) @db.Decimal(3, 2)
  career_path_level_skills                                                                       career_path_level_skills[]
  tenant_jobs                                                                                    tenant_jobs?                  @relation(fields: [target_job_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  career_path_recommendations_career_path_recommendations_current_level_idTocareer_path_levels   career_path_recommendations[] @relation("career_path_recommendations_current_level_idTocareer_path_levels")
  career_path_recommendations_career_path_recommendations_reachable_level_idTocareer_path_levels career_path_recommendations[] @relation("career_path_recommendations_reachable_level_idTocareer_path_levels")
  career_path_recommendations_career_path_recommendations_target_level_idTocareer_path_levels    career_path_recommendations[] @relation("career_path_recommendations_target_level_idTocareer_path_levels")
  career_simulations                                                                             career_simulations[]
  employee_career_progress                                                                       employee_career_progress[]

  @@index([path_id], map: "idx_career_path_levels_path")
  @@index([target_job_id], map: "idx_career_path_levels_target_job_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model career_path_recommendations {
  id                                                                                    String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                                                             String              @db.Uuid
  employee_id                                                                           String              @db.Uuid
  path_id                                                                               String              @db.Uuid
  fit_score                                                                             Decimal?            @db.Decimal(3, 2)
  skill_match_score                                                                     Decimal?            @db.Decimal(3, 2)
  interest_match_score                                                                  Decimal?            @db.Decimal(3, 2)
  market_demand_score                                                                   Decimal?            @db.Decimal(3, 2)
  composite_score                                                                       Decimal?            @db.Decimal(3, 2)
  current_level_id                                                                      String?             @db.Uuid
  reachable_level_id                                                                    String?             @db.Uuid
  target_level_id                                                                       String?             @db.Uuid
  total_skill_gaps                                                                      Int?                @default(0)
  critical_skill_gaps                                                                   Int?                @default(0)
  estimated_months_to_next                                                              Int?
  estimated_months_to_target                                                            Int?
  development_summary                                                                   Json?
  is_primary_recommendation                                                             Boolean?            @default(false)
  recommendation_reason                                                                 String?
  generated_at                                                                          DateTime?           @default(now()) @db.Timestamptz(6)
  expires_at                                                                            DateTime?           @db.Timestamptz(6)
  version                                                                               Int?                @default(1)
  created_at                                                                            DateTime?           @default(now()) @db.Timestamptz(6)
  career_path_levels_career_path_recommendations_current_level_idTocareer_path_levels   career_path_levels? @relation("career_path_recommendations_current_level_idTocareer_path_levels", fields: [current_level_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees                                                                             employees           @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  career_paths                                                                          career_paths        @relation(fields: [path_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  career_path_levels_career_path_recommendations_reachable_level_idTocareer_path_levels career_path_levels? @relation("career_path_recommendations_reachable_level_idTocareer_path_levels", fields: [reachable_level_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  career_path_levels_career_path_recommendations_target_level_idTocareer_path_levels    career_path_levels? @relation("career_path_recommendations_target_level_idTocareer_path_levels", fields: [target_level_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                                                                               tenants             @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([employee_id, path_id])
  @@index([current_level_id], map: "idx_career_path_rec_current")
  @@index([target_level_id], map: "idx_career_path_rec_target")
  @@index([reachable_level_id], map: "idx_career_path_recommendations_reachable_level_id")
  @@index([tenant_id], map: "idx_career_path_recommendations_tenant_id")
  @@index([composite_score(sort: Desc)], map: "idx_career_recommendations_score")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model career_path_templates {
  id                  String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String    @db.Uuid
  name                String    @db.VarChar(255)
  description         String?
  from_role           String    @db.VarChar(255)
  to_role             String    @db.VarChar(255)
  from_job_family_id  String?   @db.Uuid
  to_job_family_id    String?   @db.Uuid
  estimated_months    Int?
  required_skills     Json?     @default("[]")
  recommended_courses Json?     @default("[]")
  success_rate        Decimal?  @db.Decimal(3, 2)
  is_active           Boolean   @default(true)
  created_at          DateTime  @default(now()) @db.Timestamptz(6)
  updated_at          DateTime  @default(now()) @db.Timestamptz(6)
  deleted_at          DateTime? @db.Timestamptz(6)
  tenants             tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_career_path_templates_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model career_paths {
  id                          String                        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                   String                        @db.Uuid
  name                        String                        @db.VarChar(200)
  description                 String?
  department                  String?                       @db.VarChar(100)
  path_type                   String?                       @default("linear") @db.VarChar(50)
  is_active                   Boolean?                      @default(true)
  created_by                  String?                       @db.Uuid
  created_at                  DateTime?                     @default(now()) @db.Timestamp(6)
  updated_at                  DateTime?                     @default(now()) @db.Timestamp(6)
  created_by_employee_id      String?                       @db.Uuid
  embedding                   Unsupported("vector")?
  embedding_text_hash         String?                       @db.VarChar(64)
  embedding_model             String?                       @db.VarChar(100)
  embedding_generated_at      DateTime?                     @db.Timestamptz(6)
  deleted_at                  DateTime?                     @db.Timestamptz(6)
  career_path_recommendations career_path_recommendations[]
  employees                   employees?                    @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "career_paths_created_by_fkey_emp")
  tenants                     tenants                       @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  career_simulations          career_simulations[]
  employee_career_progress    employee_career_progress[]

  @@index([created_by_employee_id], map: "idx_career_paths_created_by_employee_id")
  @@index([embedding], map: "idx_career_paths_embedding")
  @@index([tenant_id], map: "idx_career_paths_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model career_profiles {
  id                     String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String                   @db.Uuid
  employee_id            String                   @unique(map: "unique_career_profile") @db.Uuid
  current_job_family_id  String?                  @db.Uuid
  career_aspiration      String?
  preferred_work_style   String?                  @db.VarChar(50)
  mobility_preference    String?                  @default("same_location") @db.VarChar(50)
  last_assessment_date   DateTime?                @db.Timestamptz(6)
  created_at             DateTime                 @default(now()) @db.Timestamptz(6)
  updated_at             DateTime                 @default(now()) @db.Timestamptz(6)
  career_conversations   career_conversations[]
  career_goals           career_goals[]
  employees              employees                @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                tenants                  @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  career_recommendations career_recommendations[]
  career_skills          career_skills[]

  @@index([tenant_id], map: "idx_career_profiles_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model career_recommendations {
  id              String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id       String          @db.Uuid
  profile_id      String          @db.Uuid
  type            String          @db.VarChar(50)
  reference_type  String          @db.VarChar(50)
  reference_id    String?         @db.Uuid
  title           String          @db.VarChar(255)
  description     String?
  relevance_score Decimal         @default(0.5) @db.Decimal(3, 2)
  reason          String?
  is_dismissed    Boolean         @default(false)
  created_at      DateTime        @default(now()) @db.Timestamptz(6)
  expires_at      DateTime?       @db.Timestamptz(6)
  career_profiles career_profiles @relation(fields: [profile_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants         tenants         @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_career_recommendations_tenant_id")
  @@index([profile_id, type], map: "idx_career_recommendations_type")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model career_simulations {
  id                                                  String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                           String              @db.Uuid
  employee_id                                         String              @db.Uuid
  simulation_name                                     String?             @db.VarChar(200)
  target_job_id                                       String?             @db.Uuid
  target_path_id                                      String?             @db.Uuid
  target_level_id                                     String?             @db.Uuid
  current_skills_snapshot                             Json?
  current_gap_analysis                                Json?
  is_reachable                                        Boolean?
  skill_distance                                      Decimal?            @db.Decimal(5, 2)
  estimated_timeline_months                           Int?
  required_trainings                                  Json?
  required_experiences                                Json?
  milestone_plan                                      Json?
  alternative_paths                                   Json?
  created_at                                          DateTime?           @default(now()) @db.Timestamptz(6)
  created_by                                          String?             @db.Uuid
  employees_career_simulations_created_byToemployees  employees?          @relation("career_simulations_created_byToemployees", fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees_career_simulations_employee_idToemployees employees           @relation("career_simulations_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenant_jobs                                         tenant_jobs?        @relation(fields: [target_job_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  career_path_levels                                  career_path_levels? @relation(fields: [target_level_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  career_paths                                        career_paths?       @relation(fields: [target_path_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                                             tenants             @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_by], map: "idx_career_simulations_created_by")
  @@index([employee_id], map: "idx_career_simulations_employee")
  @@index([target_job_id], map: "idx_career_simulations_target_job")
  @@index([target_level_id], map: "idx_career_simulations_target_level_id")
  @@index([target_path_id], map: "idx_career_simulations_target_path_id")
  @@index([tenant_id], map: "idx_career_simulations_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model career_skills {
  id              String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id       String          @db.Uuid
  profile_id      String          @db.Uuid
  skill_id        String?         @db.Uuid
  skill_name      String          @db.VarChar(255)
  proficiency     Int             @default(1)
  source          String          @default("self") @db.VarChar(50)
  validated_by    String?         @db.Uuid
  validated_at    DateTime?       @db.Timestamptz(6)
  evidence_notes  String?
  created_at      DateTime        @default(now()) @db.Timestamptz(6)
  updated_at      DateTime        @default(now()) @db.Timestamptz(6)
  career_profiles career_profiles @relation(fields: [profile_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants         tenants         @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users           users?          @relation(fields: [validated_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  esco_skills     esco_skills?    @relation(fields: [skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_cs_skill")

  @@index([profile_id], map: "idx_career_skills_profile")
  @@index([skill_id], map: "idx_career_skills_skill")
  @@index([tenant_id], map: "idx_career_skills_tenant_id")
  @@index([validated_by], map: "idx_career_skills_validated_by")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ccnl_contracts {
  id                     String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  code                   String                   @unique @db.VarChar(50)
  name                   String                   @db.VarChar(200)
  name_en                String?                  @db.VarChar(200)
  sector                 String?                  @db.VarChar(100)
  effective_date         DateTime?                @db.Date
  expiry_date            DateTime?                @db.Date
  min_notice_days        Json?
  probation_period_days  Json?
  annual_leave_days      Int?                     @default(26)
  sick_leave_rules       Json?
  overtime_rates         Json?
  full_text              String?
  full_text_version      String?                  @db.VarChar(50)
  is_active              Boolean?                 @default(true)
  created_at             DateTime?                @default(now()) @db.Timestamp(6)
  updated_at             DateTime?                @default(now()) @db.Timestamp(6)
  deleted_at             DateTime?                @db.Timestamptz(6)
  name_it                String?                  @db.VarChar(255)
  ccnl_job_title_mapping ccnl_job_title_mapping[]
  ccnl_levels            ccnl_levels[]
  ccnl_seniority_rules   ccnl_seniority_rules[]
  industry_ccnl_mapping  industry_ccnl_mapping[]

  @@index([is_active], map: "idx_ccnl_active")
  @@index([sector], map: "idx_ccnl_sector")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ccnl_executive_bands {
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ccnl_code         String   @db.VarChar(50)
  job_title_pattern String
  match_type        String   @default("ilike") @db.VarChar(10)
  min_annual        Decimal  @db.Decimal(12, 2)
  max_annual        Decimal  @db.Decimal(12, 2)
  priority          Int      @default(100) @db.SmallInt
  notes             String?
  source_url        String?
  created_at        DateTime @default(now()) @db.Timestamptz(6)

  @@unique([ccnl_code, job_title_pattern], map: "uq_ccnl_exec_bands")
  @@index([ccnl_code, priority], map: "idx_ccnl_exec_bands_lookup")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model ccnl_job_title_mapping {
  id                String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ccnl_code         String         @db.VarChar(50)
  job_title_pattern String
  match_type        String         @default("ilike") @db.VarChar(10)
  base_level_code   String         @db.VarChar(20)
  is_management     Boolean        @default(false)
  priority          Int            @default(100) @db.SmallInt
  notes             String?
  created_at        DateTime       @default(now()) @db.Timestamptz(6)
  ccnl_contracts    ccnl_contracts @relation(fields: [ccnl_code], references: [code], onDelete: NoAction)

  @@unique([ccnl_code, job_title_pattern], map: "uq_ccnl_jtm")
  @@index([ccnl_code, priority], map: "idx_ccnl_jtm_lookup")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ccnl_levels {
  id                   String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ccnl_code            String         @db.VarChar(50)
  level_code           String         @db.VarChar(20)
  level_name           String         @db.VarChar(200)
  level_order          Int            @db.SmallInt
  category             String         @db.VarChar(30)
  monthly_salary       Decimal        @db.Decimal(10, 2)
  num_monthly_payments Int            @default(13) @db.SmallInt
  effective_date       DateTime       @db.Date
  expiry_date          DateTime?      @db.Date
  is_current           Boolean        @default(true)
  source_url           String?
  created_at           DateTime       @default(now()) @db.Timestamptz(6)
  updated_at           DateTime       @default(now()) @db.Timestamptz(6)
  ccnl_contracts       ccnl_contracts @relation(fields: [ccnl_code], references: [code], onDelete: Cascade)

  @@unique([ccnl_code, level_code, effective_date], map: "uq_ccnl_levels_code_effdate")
  @@index([ccnl_code, is_current], map: "idx_ccnl_levels_current")
}

model ccnl_seniority_rules {
  id                  String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ccnl_code           String         @db.VarChar(50)
  from_level_code     String         @db.VarChar(20)
  min_years_service   Decimal        @db.Decimal(4, 1)
  max_years_service   Decimal?       @db.Decimal(4, 1)
  requires_reports    Boolean        @default(false)
  requires_management Boolean        @default(false)
  target_level_code   String         @db.VarChar(20)
  priority            Int            @default(100) @db.SmallInt
  notes               String?
  created_at          DateTime       @default(now()) @db.Timestamptz(6)
  ccnl_contracts      ccnl_contracts @relation(fields: [ccnl_code], references: [code], onDelete: NoAction)

  @@index([ccnl_code, from_level_code, priority], map: "idx_ccnl_sr_lookup")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model certification_esco_skills {
  id                          String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  certification_id            String?   @db.Uuid
  esco_skill_uri              String    @db.VarChar(255)
  skill_name                  String    @db.VarChar(255)
  proficiency_level_validated Int?
  created_at                  DateTime? @default(now()) @db.Timestamptz(6)

  @@unique([certification_id, esco_skill_uri])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model certifications {
  id                         String                    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                  String                    @db.Uuid
  code                       String                    @db.VarChar(50)
  name                       String                    @db.VarChar(255)
  name_en                    String?                   @db.VarChar(255)
  issuing_organization       String                    @db.VarChar(255)
  description                String?
  validity_months            Int?
  renewal_requirements       String?
  verification_url           String?                   @db.VarChar(500)
  is_internal                Boolean?                  @default(false)
  required_for_job_templates String[]                  @db.Uuid
  is_active                  Boolean?                  @default(true)
  created_at                 DateTime?                 @default(now()) @db.Timestamptz(6)
  deleted_at                 DateTime?                 @db.Timestamptz(6)
  name_it                    String?                   @db.VarChar(255)
  description_it             String?
  description_en             String?
  tenants                    tenants                   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employee_certifications    employee_certifications[]

  @@index([tenant_id], map: "idx_certifications_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model check_ins {
  id                                         String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                  String                 @db.Uuid
  employee_id                                String                 @db.Uuid
  manager_id                                 String?                @db.Uuid
  scheduled_date                             DateTime               @db.Timestamptz(6)
  duration_minutes                           Int?                   @default(30)
  meeting_type                               String?                @default("weekly") @db.VarChar(50)
  agenda                                     String?
  employee_notes                             String?
  manager_notes                              String?
  action_items                               Json?
  employee_mood                              Int?
  status                                     String?                @default("scheduled") @db.VarChar(50)
  completed_at                               DateTime?              @db.Timestamptz(6)
  created_at                                 DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at                                 DateTime?              @default(now()) @db.Timestamptz(6)
  topics_discussed                           Json?                  @default("[]")
  employee_engagement                        Int?
  goals_discussed                            Json?                  @default("[]")
  follow_up_items                            Json?                  @default("[]")
  next_checkin_date                          DateTime?              @db.Date
  private_notes                              String?
  content_embedding                          Unsupported("vector")?
  embedding_text_hash                        String?                @db.VarChar(64)
  embedding_model                            String?                @db.VarChar(100)
  embedding_generated_at                     DateTime?              @db.Timestamptz(6)
  tenants                                    tenants                @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees_check_ins_employee_idToemployees employees              @relation("check_ins_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_check_ins_employee")
  employees_check_ins_manager_idToemployees  employees?             @relation("check_ins_manager_idToemployees", fields: [manager_id], references: [id], onUpdate: NoAction, map: "fk_check_ins_manager")

  @@index([content_embedding], map: "idx_check_ins_embedding")
  @@index([employee_id], map: "idx_check_ins_employee")
  @@index([manager_id], map: "idx_check_ins_manager")
  @@index([scheduled_date], map: "idx_check_ins_scheduled")
  @@index([status], map: "idx_check_ins_status")
  @@index([tenant_id], map: "idx_check_ins_tenant")
  @@index([tenant_id, employee_id], map: "idx_checkins_tenant_employee")
}

model club_events {
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  club_id         String?   @db.Uuid
  title           String    @db.VarChar(200)
  description     String?
  event_date      DateTime  @db.Timestamp(6)
  end_date        DateTime? @db.Timestamp(6)
  location        String?   @db.VarChar(300)
  is_virtual      Boolean?  @default(false)
  virtual_link    String?
  max_attendees   Int?
  attendees_count Int?      @default(0)
  created_by      String?   @db.Uuid
  created_at      DateTime? @default(now()) @db.Timestamp(6)

  @@index([club_id], map: "idx_club_events_club")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model club_memberships {
  id          String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  club_id     String?    @db.Uuid
  employee_id String?    @db.Uuid
  role        String?    @default("member") @db.VarChar(30)
  status      String?    @default("active") @db.VarChar(30)
  joined_at   DateTime?  @default(now()) @db.Timestamp(6)
  created_at  DateTime?  @default(now()) @db.Timestamptz(6)
  updated_at  DateTime?  @default(now()) @db.Timestamptz(6)
  employees   employees? @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_cm_employee")

  @@unique([club_id, employee_id])
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model company_profiles {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  profile_code  String    @db.VarChar(10)
  section_code  String    @db.Char(1)
  division_code String    @db.VarChar(2)
  group_code    String    @db.VarChar(5)
  size_code     String    @db.VarChar(2)
  full_name_it  String?   @db.VarChar(500)
  full_name_en  String?   @db.VarChar(500)
  is_active     Boolean?  @default(true)
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  updated_at    DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at    DateTime? @db.Timestamptz(6)

  @@index([profile_code], map: "idx_profiles_code")
  @@index([division_code], map: "idx_profiles_division")
  @@index([group_code], map: "idx_profiles_group")
  @@index([section_code], map: "idx_profiles_section")
  @@index([size_code], map: "idx_profiles_size")
}

model company_sizes {
  code              String              @id(map: "company_sizes_pkey1") @db.VarChar(20)
  name_it           String              @db.VarChar(100)
  name_en           String              @db.VarChar(100)
  min_employees     Int                 @default(0)
  max_employees     Int?
  max_revenue_eur   BigInt?
  max_balance_eur   BigInt?
  sort_order        Int                 @default(0) @db.SmallInt
  is_active         Boolean             @default(true)
  created_at        DateTime            @default(now()) @db.Timestamptz(6)
  industry_profiles industry_profiles[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model competencies {
  id                    String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id             String                @db.Uuid
  framework_id          String                @db.Uuid
  name                  String                @db.VarChar(100)
  description           String?
  category              String?               @db.VarChar(100)
  behavioral_indicators Json?                 @default("[]")
  weight                Decimal?              @default(100) @db.Decimal(5, 2)
  sort_order            Int?                  @default(0)
  is_active             Boolean?              @default(true)
  created_at            DateTime?             @default(now()) @db.Timestamptz(6)
  updated_at            DateTime?             @default(now()) @db.Timestamptz(6)
  deleted_at            DateTime?             @db.Timestamptz(6)
  competency_frameworks competency_frameworks @relation(fields: [framework_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants               tenants               @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([framework_id], map: "idx_competencies_framework")
  @@index([tenant_id], map: "idx_competencies_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model competency_frameworks {
  id             String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id      String         @db.Uuid
  name           String         @db.VarChar(100)
  description    String?
  version        String?        @default("1.0") @db.VarChar(20)
  framework_type String?        @default("core") @db.VarChar(30)
  is_active      Boolean?       @default(true)
  created_at     DateTime?      @default(now()) @db.Timestamptz(6)
  updated_at     DateTime?      @default(now()) @db.Timestamptz(6)
  deleted_at     DateTime?      @db.Timestamptz(6)
  competencies   competencies[]
  tenants        tenants        @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_competency_frameworks_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model competency_review_ratings {
  id                    String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id             String              @db.Uuid
  performance_review_id String              @db.Uuid
  employee_id           String              @db.Uuid
  competency_id         String?             @db.Uuid
  ksaba_dimension       String?             @db.VarChar(20)
  competency_name       String              @db.VarChar(100)
  self_rating           Decimal?            @db.Decimal(3, 2)
  self_comment          String?
  self_evidence         String[]
  manager_rating        Decimal?            @db.Decimal(3, 2)
  manager_comment       String?
  weight                Decimal?            @default(1.0) @db.Decimal(3, 2)
  created_at            DateTime?           @default(now()) @db.Timestamptz(6)
  updated_at            DateTime?           @default(now()) @db.Timestamptz(6)
  employees             employees           @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  performance_reviews   performance_reviews @relation(fields: [performance_review_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants               tenants             @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([performance_review_id, competency_name])
  @@index([ksaba_dimension], map: "idx_competency_review_ratings_dimension")
  @@index([employee_id], map: "idx_competency_review_ratings_employee")
  @@index([tenant_id], map: "idx_competency_review_ratings_tenant_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model compliance_training_requirements {
  id                       String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                String    @db.Uuid
  name                     String    @db.VarChar(255)
  description              String?
  course_id                String?   @db.Uuid
  applies_to_all           Boolean?  @default(false)
  applies_to_departments   String[]
  applies_to_job_templates String[]  @db.Uuid
  applies_to_locations     String[]  @db.Uuid
  recurrence_months        Int?
  grace_period_days        Int?      @default(30)
  is_active                Boolean?  @default(true)
  created_at               DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at               DateTime? @db.Timestamptz(6)
  tenants                  tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  courses                  courses?  @relation(fields: [course_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ctr_course")

  @@index([course_id], map: "idx_compliance_training_requirements_course_id")
  @@index([tenant_id], map: "idx_compliance_training_requirements_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model continuous_feedback {
  id                                                        String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                                 String               @db.Uuid
  from_employee_id                                          String?              @db.Uuid
  to_employee_id                                            String               @db.Uuid
  feedback_type                                             String?              @default("praise") @db.VarChar(30)
  message                                                   String
  is_private                                                Boolean?             @default(false)
  related_goal_id                                           String?              @db.Uuid
  created_at                                                DateTime?            @default(now()) @db.Timestamp(6)
  competency_id                                             String?              @db.Uuid
  sentiment_score                                           Decimal?             @db.Decimal(3, 2)
  acknowledged                                              Boolean?             @default(false)
  acknowledged_at                                           DateTime?            @db.Timestamptz(6)
  visibility                                                String?              @default("private") @db.VarChar(20)
  tags                                                      String[]
  category                                                  String?              @db.VarChar(50)
  performance_review_id                                     String?              @db.Uuid
  performance_reviews                                       performance_reviews? @relation(fields: [performance_review_id], references: [id], onUpdate: NoAction)
  tenants                                                   tenants              @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees_continuous_feedback_from_employee_idToemployees employees?           @relation("continuous_feedback_from_employee_idToemployees", fields: [from_employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_continuous_feedback_from")
  employees_continuous_feedback_to_employee_idToemployees   employees            @relation("continuous_feedback_to_employee_idToemployees", fields: [to_employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_continuous_feedback_to")

  @@index([created_at(sort: Desc)], map: "idx_continuous_feedback_created")
  @@index([from_employee_id], map: "idx_continuous_feedback_from")
  @@index([performance_review_id], map: "idx_continuous_feedback_review")
  @@index([tenant_id], map: "idx_continuous_feedback_tenant")
  @@index([to_employee_id], map: "idx_continuous_feedback_to")
  @@index([feedback_type], map: "idx_continuous_feedback_type")
  @@index([visibility], map: "idx_continuous_feedback_visibility")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model contract_amendments {
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  contract_id     String    @db.Uuid
  tenant_id       String    @db.Uuid
  amendment_type  String    @db.VarChar(50)
  effective_date  DateTime  @db.Date
  description     String?
  previous_values Json?
  new_values      Json?
  created_at      DateTime? @default(now()) @db.Timestamp(6)
  created_by      String?   @db.Uuid
  approved_by     String?   @db.Uuid
  approved_at     DateTime? @db.Timestamp(6)
  contracts       contracts @relation(fields: [contract_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants         tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([contract_id], map: "idx_contract_amendments_contract")
  @@index([tenant_id], map: "idx_contract_amendments_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model contracts {
  id                   String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id            String                @db.Uuid
  employee_id          String                @db.Uuid
  contract_type        String                @db.VarChar(50)
  contract_code        String?               @db.VarChar(50)
  start_date           DateTime              @db.Date
  end_date             DateTime?             @db.Date
  probation_end_date   DateTime?             @db.Date
  ccnl_type            String?               @db.VarChar(100)
  ccnl_level           String?               @db.VarChar(50)
  gross_annual_salary  Decimal?              @db.Decimal(12, 2)
  currency             String?               @default("EUR") @db.VarChar(3)
  salary_type          String?               @default("annual") @db.VarChar(20)
  payment_frequency    String?               @default("monthly") @db.VarChar(20)
  work_hours_weekly    Decimal?              @default(40) @db.Decimal(4, 1)
  work_schedule_type   String?               @default("full_time") @db.VarChar(20)
  part_time_percentage Decimal?              @db.Decimal(5, 2)
  job_title            String?               @db.VarChar(200)
  job_description      String?
  location_id          String?               @db.Uuid
  cost_center_id       String?               @db.Uuid
  status               String?               @default("active") @db.VarChar(20)
  termination_date     DateTime?             @db.Date
  termination_reason   String?
  notes                String?
  metadata             Json?                 @default("{}")
  created_at           DateTime?             @default(now()) @db.Timestamp(6)
  updated_at           DateTime?             @default(now()) @db.Timestamp(6)
  created_by           String?               @db.Uuid
  updated_by           String?               @db.Uuid
  org_unit_id          String?               @db.Uuid
  contract_amendments  contract_amendments[]
  cost_centers         cost_centers?         @relation(fields: [cost_center_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees            employees             @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  locations            locations?            @relation(fields: [location_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  org_units            org_units?            @relation(fields: [org_unit_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants              tenants               @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  salary_history       salary_history[]

  @@index([cost_center_id], map: "idx_contracts_cost_center")
  @@index([employee_id], map: "idx_contracts_employee_id")
  @@index([end_date], map: "idx_contracts_end_date")
  @@index([location_id], map: "idx_contracts_location")
  @@index([start_date], map: "idx_contracts_start_date")
  @@index([status], map: "idx_contracts_status")
  @@index([tenant_id], map: "idx_contracts_tenant_id")
  @@index([contract_type], map: "idx_contracts_type")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model cost_centers {
  id                String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String      @db.Uuid
  code              String      @db.VarChar(20)
  name              String      @db.VarChar(100)
  name_en           String?     @db.VarChar(100)
  parent_id         String?     @db.Uuid
  cost_center_type  String?     @db.VarChar(20)
  responsible_id    String?     @db.Uuid
  org_unit_id       String?     @db.Uuid
  budget_annual_eur Decimal?    @db.Decimal(15, 2)
  budget_headcount  Int?
  gl_account        String?     @db.VarChar(20)
  is_active         Boolean?    @default(true)
  valid_from        DateTime?   @default(dbgenerated("CURRENT_DATE")) @db.Date
  valid_to          DateTime?   @db.Date
  created_at        DateTime?   @default(now()) @db.Timestamptz(6)
  updated_at        DateTime?   @default(now()) @db.Timestamptz(6)
  sap_kostl         String?     @db.VarChar(20)
  sap_bukrs         String?     @db.VarChar(10)
  sap_kokrs         String?     @db.VarChar(10)
  sap_ktext         String?     @db.VarChar(100)
  sap_verak         String?     @db.VarChar(50)
  sap_sync_date     DateTime?   @db.Timestamp(6)
  deleted_at        DateTime?   @db.Timestamptz(6)
  name_it           String?     @db.VarChar(255)
  contracts         contracts[]
  tenants           tenants     @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees         employees[]

  @@index([org_unit_id], map: "idx_cost_centers_org_unit")
  @@index([parent_id], map: "idx_cost_centers_parent")
  @@index([tenant_id], map: "idx_cost_centers_tenant")
  @@index([cost_center_type], map: "idx_cost_centers_type")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model course_enrollments {
  id                                                              String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id                                                     String?    @db.Uuid
  course_id                                                       String?    @db.Uuid
  learning_path_enrollment_id                                     String?    @db.Uuid
  enrolled_at                                                     DateTime?  @default(now()) @db.Timestamptz(6)
  enrolled_by                                                     String?    @db.Uuid
  enrollment_source                                               String?    @default("self") @db.VarChar(50)
  status                                                          String?    @default("enrolled") @db.VarChar(20)
  progress_percent                                                Int?       @default(0)
  started_at                                                      DateTime?  @db.Timestamptz(6)
  completed_at                                                    DateTime?  @db.Timestamptz(6)
  due_date                                                        DateTime?  @db.Date
  score                                                           Decimal?   @db.Decimal(5, 2)
  passed                                                          Boolean?
  attempts                                                        Int?       @default(0)
  time_spent_minutes                                              Int?       @default(0)
  last_accessed_at                                                DateTime?  @db.Timestamptz(6)
  certificate_issued                                              Boolean?   @default(false)
  certificate_url                                                 String?    @db.VarChar(500)
  certificate_issued_at                                           DateTime?  @db.Timestamptz(6)
  notes                                                           String?
  created_at                                                      DateTime?  @default(now()) @db.Timestamptz(6)
  updated_at                                                      DateTime?  @default(now()) @db.Timestamptz(6)
  enrolled_by_employee_id                                         String?    @db.Uuid
  tenant_id                                                       String     @db.Uuid
  employees_course_enrollments_enrolled_by_employee_idToemployees employees? @relation("course_enrollments_enrolled_by_employee_idToemployees", fields: [enrolled_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "course_enrollments_enrolled_by_fkey_emp")
  tenants                                                         tenants    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  courses                                                         courses?   @relation(fields: [course_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_course_enrollments_course")
  employees_course_enrollments_employee_idToemployees             employees? @relation("course_enrollments_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_course_enrollments_employee")

  @@index([course_id], map: "idx_course_enrollments_course")
  @@index([employee_id], map: "idx_course_enrollments_employee")
  @@index([enrolled_by_employee_id], map: "idx_course_enrollments_enrolled_by_employee_id")
  @@index([status], map: "idx_course_enrollments_status")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model course_enrollments_semantic {
  id                     String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String                 @db.Uuid
  enrollment_id          String                 @unique(map: "uk_enrollment_semantic") @db.Uuid
  employee_id            String                 @db.Uuid
  course_id              String                 @db.Uuid
  enrollment_reason      String?
  learning_goals         String?
  completion_feedback    String?
  skills_acquired        String?
  context_embedding      Unsupported("vector")?
  embedding_model        String?                @db.VarChar(100)
  embedding_generated_at DateTime?              @db.Timestamptz(6)
  created_at             DateTime?              @default(now()) @db.Timestamptz(6)
  tenants                tenants                @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees              employees              @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ces_employee")
  courses                courses                @relation(fields: [course_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_cese_course")

  @@index([course_id], map: "idx_course_enrollments_semantic_course_id")
  @@index([context_embedding], map: "idx_course_enrollments_semantic_embedding")
  @@index([tenant_id, employee_id], map: "idx_course_enrollments_semantic_employee")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model course_esco_skills {
  id                       String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  course_id                String?   @db.Uuid
  esco_skill_uri           String    @db.VarChar(255)
  skill_name               String    @db.VarChar(255)
  skill_type               String?   @db.VarChar(50)
  proficiency_level_gained Int?
  is_primary               Boolean?  @default(false)
  created_at               DateTime? @default(now()) @db.Timestamptz(6)
  courses                  courses?  @relation(fields: [course_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ces_course")

  @@index([course_id], map: "idx_course_esco_skills_course_id")
}

model course_modules {
  id                 String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  course_id          String?              @db.Uuid
  title              String               @db.VarChar(255)
  description        String?
  sequence_order     Int
  duration_minutes   Int?
  content_type       String?              @db.VarChar(50)
  content_url        String?              @db.VarChar(500)
  is_mandatory       Boolean?             @default(true)
  passing_score      Decimal?             @db.Decimal(5, 2)
  created_at         DateTime?            @default(now()) @db.Timestamptz(6)
  courses            courses?             @relation(fields: [course_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_cm_course")
  module_completions module_completions[]

  @@index([course_id], map: "idx_course_modules_course_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model courses {
  id                               String                             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                        String                             @db.Uuid
  code                             String                             @db.VarChar(50)
  title                            String                             @db.VarChar(255)
  title_en                         String?                            @db.VarChar(255)
  description                      String?
  description_en                   String?
  course_type                      String                             @default("online") @db.VarChar(50)
  category                         String?                            @db.VarChar(100)
  duration_hours                   Decimal?                           @db.Decimal(6, 2)
  skill_level                      String?                            @default("beginner") @db.VarChar(20)
  provider                         String?                            @db.VarChar(100)
  provider_course_id               String?                            @db.VarChar(100)
  provider_url                     String?                            @db.VarChar(500)
  thumbnail_url                    String?                            @db.VarChar(500)
  video_url                        String?                            @db.VarChar(500)
  is_mandatory                     Boolean?                           @default(false)
  is_certification                 Boolean?                           @default(false)
  certification_validity_months    Int?
  max_enrollments                  Int?
  language                         String?                            @default("it") @db.VarChar(10)
  tags                             String[]
  prerequisites                    String[]                           @db.Uuid
  status                           String?                            @default("draft") @db.VarChar(20)
  published_at                     DateTime?                          @db.Timestamptz(6)
  created_by                       String?                            @db.Uuid
  created_at                       DateTime?                          @default(now()) @db.Timestamptz(6)
  updated_at                       DateTime?                          @default(now()) @db.Timestamptz(6)
  created_by_employee_id           String?                            @db.Uuid
  embedding_en                     Unsupported("vector")?
  embedding_it                     Unsupported("vector")?
  embedding_model                  String?                            @db.VarChar(100)
  embedding_generated_at           DateTime?                          @db.Timestamptz(6)
  compliance_training_requirements compliance_training_requirements[]
  course_enrollments               course_enrollments[]
  course_enrollments_semantic      course_enrollments_semantic[]
  course_esco_skills               course_esco_skills[]
  course_modules                   course_modules[]
  employees                        employees?                         @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "courses_created_by_fkey_emp")
  tenants                          tenants                            @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employee_training_records        employee_training_records[]
  job_title_courses                job_title_courses[]
  learning_bookmarks               learning_bookmarks[]
  learning_path_courses            learning_path_courses[]
  learning_ratings                 learning_ratings[]
  learning_recommendations         learning_recommendations[]

  @@index([created_by_employee_id], map: "idx_courses_created_by")
  @@index([embedding_en], map: "idx_courses_embedding_en")
  @@index([embedding_it], map: "idx_courses_embedding_it")
  @@index([status], map: "idx_courses_status")
  @@index([tenant_id], map: "idx_courses_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model crawl_runs {
  id               String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id        String             @db.Uuid
  config_id        String             @db.Uuid
  status           String             @default("pending") @db.VarChar(50)
  started_at       DateTime?          @db.Timestamptz(6)
  completed_at     DateTime?          @db.Timestamptz(6)
  postings_found   Int?               @default(0)
  postings_saved   Int?               @default(0)
  skills_extracted Int?               @default(0)
  errors           Json?              @default("[]")
  created_at       DateTime?          @default(now()) @db.Timestamptz(6)
  crawler_configs  crawler_configs    @relation(fields: [config_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants          tenants            @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  job_postings_raw job_postings_raw[]

  @@index([config_id], map: "idx_crawl_runs_config")
  @@index([status], map: "idx_crawl_runs_status")
  @@index([tenant_id], map: "idx_crawl_runs_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model crawler_configs {
  id                  String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String       @db.Uuid
  name                String       @db.VarChar(255)
  source              String       @db.VarChar(100)
  is_active           Boolean?     @default(true)
  schedule_cron       String?      @default("0 0 * * 0") @db.VarChar(100)
  search_criteria     Json         @default("{}")
  rate_limit_delay_ms Int?         @default(5000)
  max_pages_per_run   Int?         @default(50)
  last_run_at         DateTime?    @db.Timestamptz(6)
  last_run_status     String?      @db.VarChar(50)
  last_run_stats      Json?
  created_at          DateTime?    @default(now()) @db.Timestamptz(6)
  updated_at          DateTime?    @default(now()) @db.Timestamptz(6)
  deleted_at          DateTime?    @db.Timestamptz(6)
  crawl_runs          crawl_runs[]
  tenants             tenants      @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([source], map: "idx_crawler_configs_source")
  @@index([tenant_id], map: "idx_crawler_configs_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model critical_roles {
  id                    String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id             String    @db.Uuid
  role_name             String    @db.VarChar(255)
  department            String?   @db.VarChar(100)
  current_incumbent_id  String?   @db.Uuid
  criticality_level     String?   @default("High") @db.VarChar(20)
  impact_if_vacant      String?
  time_to_fill_estimate Int?
  succession_status     String?   @default("at_risk") @db.VarChar(50)
  created_at            DateTime? @default(now()) @db.Timestamp(6)
  updated_at            DateTime? @default(now()) @db.Timestamp(6)
  tenants               tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_critical_roles_tenant")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model cross_entity_relations {
  id                 String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String    @db.Uuid
  source_entity_type String    @db.VarChar(50)
  source_entity_id   String    @db.Uuid
  target_entity_type String    @db.VarChar(50)
  target_entity_id   String    @db.Uuid
  relation_type      String    @db.VarChar(50)
  confidence_score   Decimal?  @db.Decimal(5, 4)
  source             String?   @default("manual") @db.VarChar(30)
  metadata           Json?     @default("{}")
  created_at         DateTime? @default(now()) @db.Timestamptz(6)
  tenants            tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([tenant_id, source_entity_type, source_entity_id, target_entity_type, target_entity_id, relation_type])
  @@index([source_entity_type, source_entity_id], map: "idx_cross_entity_relations_source")
  @@index([target_entity_type, target_entity_id], map: "idx_cross_entity_relations_target")
  @@index([tenant_id], map: "idx_cross_entity_relations_tenant")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model cross_entity_searches {
  id                 String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  query_text         String
  query_embedding    Unsupported("vector")?
  entity_types       String[]
  results_count      Int?
  top_results        Json?
  search_duration_ms Int?
  tenant_id          String                 @db.Uuid
  user_id            String?                @db.Uuid
  created_at         DateTime?              @default(now()) @db.Timestamptz(6)
  tenants            tenants                @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users              users?                 @relation(fields: [user_id], references: [id], onUpdate: NoAction, map: "fk_ces_user")

  @@index([created_at(sort: Desc)], map: "idx_ces_created")
  @@index([tenant_id], map: "idx_ces_tenant")
  @@index([user_id], map: "idx_cross_entity_searches_user_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model dashboard_widgets {
  id               String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id        String      @db.Uuid
  dashboard_id     String?     @db.Uuid
  widget_type      String      @db.VarChar(50)
  title            String?     @db.VarChar(200)
  data_source      String      @db.VarChar(100)
  query_config     Json        @default("{}")
  display_config   Json        @default("{}")
  position_x       Int?        @default(0)
  position_y       Int?        @default(0)
  width            Int?        @default(4)
  height           Int?        @default(3)
  refresh_interval Int?
  is_active        Boolean?    @default(true)
  created_at       DateTime?   @default(now()) @db.Timestamptz(6)
  updated_at       DateTime?   @default(now()) @db.Timestamptz(6)
  deleted_at       DateTime?   @db.Timestamptz(6)
  dashboards       dashboards? @relation(fields: [dashboard_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants          tenants     @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([dashboard_id], map: "idx_widgets_dashboard")
  @@index([tenant_id], map: "idx_widgets_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model dashboards {
  id                String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String              @db.Uuid
  owner_id          String?             @db.Uuid
  name              String              @db.VarChar(100)
  description       String?
  layout            Json?               @default("{}")
  is_default        Boolean?            @default(false)
  is_shared         Boolean?            @default(false)
  shared_with_roles Json?               @default("[]")
  created_at        DateTime?           @default(now()) @db.Timestamptz(6)
  updated_at        DateTime?           @default(now()) @db.Timestamptz(6)
  dashboard_widgets dashboard_widgets[]
  employees         employees?          @relation(fields: [owner_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants           tenants             @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([owner_id], map: "idx_dashboards_owner")
  @@index([tenant_id], map: "idx_dashboards_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model data_retention_policies {
  id                     String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                   String    @db.VarChar(100)
  description            String?
  data_type              String    @db.VarChar(100)
  retention_days         Int
  action_after_retention String?   @default("anonymize") @db.VarChar(50)
  is_active              Boolean?  @default(true)
  created_at             DateTime? @default(now()) @db.Timestamp(6)
  updated_at             DateTime? @default(now()) @db.Timestamp(6)
  deleted_at             DateTime? @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model data_subject_requests {
  id                       String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  request_type             String     @db.VarChar(50)
  subject_email            String     @db.VarChar(255)
  subject_id               String?    @db.Uuid
  status                   String?    @default("pending") @db.VarChar(50)
  requested_at             DateTime?  @default(now()) @db.Timestamp(6)
  due_date                 DateTime?  @db.Date
  completed_at             DateTime?  @db.Timestamp(6)
  completed_by             String?    @db.Uuid
  notes                    String?
  response_data            Json?
  tenant_id                String     @db.Uuid
  completed_by_employee_id String?    @db.Uuid
  created_at               DateTime?  @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?  @default(now()) @db.Timestamptz(6)
  employees                employees? @relation(fields: [completed_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "data_subject_requests_completed_by_fkey_emp")
  tenants                  tenants    @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([completed_by_employee_id], map: "idx_data_subject_requests_completed_by_employee_id")
  @@index([due_date], map: "idx_dsr_due")
  @@index([subject_email], map: "idx_dsr_email")
  @@index([status], map: "idx_dsr_status")
  @@index([tenant_id], map: "idx_dsr_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model db_table_registry {
  table_name     String   @id @db.VarChar(128)
  table_category String   @default("business") @db.VarChar(50)
  module         String?  @db.VarChar(50)
  row_count      BigInt?  @default(0)
  column_count   Int?     @default(0)
  has_tenant_id  Boolean? @default(false)
  has_rls        Boolean? @default(false)
  has_created_at Boolean? @default(false)
  has_updated_at Boolean? @default(false)
  fk_out_count   Int?     @default(0)
  fk_in_count    Int?     @default(0)
  last_synced_at DateTime @default(now()) @db.Timestamptz(6)
  created_at     DateTime @default(now()) @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model document_acknowledgments {
  id                 String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String             @db.Uuid
  document_id        String             @db.Uuid
  employee_id        String             @db.Uuid
  acknowledged_at    DateTime?          @default(now()) @db.Timestamp(6)
  ip_address         String?            @db.VarChar(45)
  user_agent         String?
  created_at         DateTime?          @default(now()) @db.Timestamptz(6)
  updated_at         DateTime?          @default(now()) @db.Timestamptz(6)
  employee_documents employee_documents @relation(fields: [document_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees          employees          @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants            tenants            @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([document_id, employee_id])
  @@index([tenant_id], map: "idx_document_acknowledgments_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model document_comments {
  id                  String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  document_id         String        @db.Uuid
  version_id          String?       @db.Uuid
  user_id             String        @db.Uuid
  comment             String
  created_at          DateTime?     @default(now()) @db.Timestamptz(6)
  updated_at          DateTime?     @default(now()) @db.Timestamptz(6)
  is_resolved         Boolean?      @default(false)
  user_id_employee_id String?       @db.Uuid
  rag_documents       rag_documents @relation(fields: [document_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees           employees?    @relation(fields: [user_id_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "document_comments_user_id_fkey_emp")
  users               users         @relation(fields: [user_id], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_dc_user")

  @@index([document_id], map: "idx_document_comments_doc")
  @@index([user_id_employee_id], map: "idx_document_comments_user_id_employee_id")
}

model document_locks {
  id                    String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  document_id           String        @unique @db.Uuid
  locked_by             String        @db.Uuid
  locked_at             DateTime?     @default(now()) @db.Timestamptz(6)
  lock_reason           String?
  expires_at            DateTime?     @db.Timestamptz(6)
  locked_by_employee_id String?       @db.Uuid
  created_at            DateTime?     @default(now()) @db.Timestamptz(6)
  rag_documents         rag_documents @relation(fields: [document_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees             employees?    @relation(fields: [locked_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "document_locks_locked_by_fkey_emp")

  @@index([locked_by_employee_id], map: "idx_document_locks_locked_by_employee_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model document_requests {
  id                                                 String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                          String              @db.Uuid
  employee_id                                        String              @db.Uuid
  document_type                                      String              @db.VarChar(50)
  purpose                                            String?
  additional_notes                                   String?
  status                                             String?             @default("pending") @db.VarChar(20)
  priority                                           String?             @default("normal") @db.VarChar(20)
  assigned_to                                        String?             @db.Uuid
  assigned_at                                        DateTime?           @db.Timestamp(6)
  completed_at                                       DateTime?           @db.Timestamp(6)
  rejection_reason                                   String?
  result_document_id                                 String?             @db.Uuid
  created_at                                         DateTime?           @default(now()) @db.Timestamp(6)
  updated_at                                         DateTime?           @default(now()) @db.Timestamp(6)
  employees_document_requests_assigned_toToemployees employees?          @relation("document_requests_assigned_toToemployees", fields: [assigned_to], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees_document_requests_employee_idToemployees employees           @relation("document_requests_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employee_documents                                 employee_documents? @relation(fields: [result_document_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                                            tenants             @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([employee_id], map: "idx_document_requests_employee")
  @@index([result_document_id], map: "idx_document_requests_result_document_id")
  @@index([status], map: "idx_document_requests_status")
  @@index([tenant_id], map: "idx_document_requests_tenant")
}

model document_versions {
  id                     String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  document_id            String        @db.Uuid
  version                Int
  filename               String        @db.VarChar(255)
  file_path              String        @db.VarChar(500)
  file_size              Int?
  checksum               String?       @db.VarChar(64)
  version_notes          String?
  created_by             String?       @db.Uuid
  created_at             DateTime?     @default(now()) @db.Timestamptz(6)
  is_active              Boolean?      @default(true)
  created_by_employee_id String?       @db.Uuid
  deleted_at             DateTime?     @db.Timestamptz(6)
  employees              employees?    @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "document_versions_created_by_fkey_emp")
  rag_documents          rag_documents @relation(fields: [document_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_by_employee_id], map: "idx_document_versions_created_by_employee_id")
  @@index([document_id, version], map: "idx_document_versions_version")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model embedding_queue {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  entity_type   String    @db.VarChar(50)
  entity_id     String    @db.Uuid
  tenant_id     String    @db.Uuid
  operation     String    @default("upsert") @db.VarChar(20)
  priority      Int?      @default(5)
  status        String?   @default("pending") @db.VarChar(20)
  attempts      Int?      @default(0)
  max_attempts  Int?      @default(3)
  error_message String?
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  processed_at  DateTime? @db.Timestamptz(6)
  tenants       tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([entity_type, entity_id, status], map: "uk_embedding_queue_entity")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_addresses {
  id           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id    String    @db.Uuid
  employee_id  String    @db.Uuid
  address_type String    @default("home") @db.VarChar(20)
  street       String?   @db.VarChar(200)
  city         String?   @db.VarChar(100)
  postal_code  String?   @db.VarChar(20)
  province     String?   @db.VarChar(10)
  country_code String?   @default("IT") @db.VarChar(3)
  is_primary   Boolean?  @default(true)
  valid_from   DateTime? @default(dbgenerated("CURRENT_DATE")) @db.Date
  valid_to     DateTime? @db.Date
  created_at   DateTime? @default(now()) @db.Timestamptz(6)
  updated_at   DateTime? @default(now()) @db.Timestamptz(6)
  employees    employees @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants      tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([employee_id], map: "idx_employee_addresses_employee")
  @@index([tenant_id], map: "idx_employee_addresses_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_attendance {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id        String    @db.Uuid
  employee_id      String    @db.Uuid
  attendance_date  DateTime  @db.Date
  clock_in         DateTime? @db.Time(6)
  clock_out        DateTime? @db.Time(6)
  break_start      DateTime? @db.Time(6)
  break_end        DateTime? @db.Time(6)
  hours_regular    Decimal?  @default(0) @db.Decimal(5, 2)
  hours_overtime   Decimal?  @default(0) @db.Decimal(5, 2)
  hours_night      Decimal?  @default(0) @db.Decimal(5, 2)
  hours_holiday    Decimal?  @default(0) @db.Decimal(5, 2)
  hours_total      Decimal?  @default(dbgenerated("(((hours_regular + hours_overtime) + hours_night) + hours_holiday)")) @db.Decimal(5, 2)
  status           String?   @default("present") @db.VarChar(30)
  source           String?   @default("manual") @db.VarChar(30)
  source_reference String?
  is_validated     Boolean?  @default(false)
  validated_by     String?   @db.Uuid
  validated_at     DateTime? @db.Timestamp(6)
  notes            String?
  created_at       DateTime? @default(now()) @db.Timestamp(6)
  updated_at       DateTime? @default(now()) @db.Timestamp(6)
  created_by       String?   @db.Uuid
  employees        employees @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants          tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, employee_id, attendance_date])
  @@index([employee_id, attendance_date], map: "idx_employee_attendance_emp_date")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_bank_details {
  id                  String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String    @db.Uuid
  employee_id         String    @db.Uuid
  iban                String?   @db.VarChar(34)
  swift_bic           String?   @db.VarChar(11)
  bank_name           String?   @db.VarChar(200)
  bank_account_number String?   @db.VarChar(30)
  bank_key            String?   @db.VarChar(15)
  bank_control_key    String?   @db.VarChar(5)
  bank_country        String?   @default("IT") @db.VarChar(3)
  is_primary          Boolean?  @default(true)
  valid_from          DateTime? @default(dbgenerated("CURRENT_DATE")) @db.Date
  valid_to            DateTime? @db.Date
  created_at          DateTime? @default(now()) @db.Timestamptz(6)
  updated_at          DateTime? @default(now()) @db.Timestamptz(6)
  employees           employees @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants             tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([employee_id], map: "idx_employee_bank_details_employee")
  @@index([tenant_id], map: "idx_employee_bank_details_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model employee_benefit_enrollments {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id    String    @db.Uuid
  benefit_id     String    @db.Uuid
  coverage_level String?   @db.VarChar(50)
  enrolled_at    DateTime? @default(now()) @db.Timestamp(6)
  effective_date DateTime? @db.Date
  is_active      Boolean?  @default(true)
  created_at     DateTime? @default(now()) @db.Timestamptz(6)
  updated_at     DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at     DateTime? @db.Timestamptz(6)
  employees      employees @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ebe_employee")

  @@index([employee_id], map: "idx_employee_benefit_enrollments_employee_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_benefits {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id        String    @db.Uuid
  benefit_name     String    @db.VarChar(100)
  benefit_type     String    @db.VarChar(50)
  description      String?
  monthly_cost     Decimal?  @default(0) @db.Decimal(10, 2)
  coverage_options Json?     @default("[]")
  is_active        Boolean?  @default(true)
  created_at       DateTime? @default(now()) @db.Timestamp(6)
  deleted_at       DateTime? @db.Timestamptz(6)
  tenants          tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_employee_benefits_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model employee_career_paths {
  id                                                                 String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id                                                        String?    @db.Uuid
  path_id                                                            String?    @db.Uuid
  current_level_id                                                   String?    @db.Uuid
  status                                                             String?    @default("active") @db.VarChar(30)
  started_at                                                         DateTime?  @default(now()) @db.Timestamp(6)
  target_completion                                                  DateTime?  @db.Date
  notes                                                              String?
  assigned_by                                                        String?    @db.Uuid
  created_at                                                         DateTime?  @default(now()) @db.Timestamp(6)
  updated_at                                                         DateTime?  @default(now()) @db.Timestamp(6)
  assigned_by_employee_id                                            String?    @db.Uuid
  employees_employee_career_paths_assigned_by_employee_idToemployees employees? @relation("employee_career_paths_assigned_by_employee_idToemployees", fields: [assigned_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "employee_career_paths_assigned_by_fkey_emp")
  employees_employee_career_paths_employee_idToemployees             employees? @relation("employee_career_paths_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ecp_employee")

  @@index([assigned_by_employee_id], map: "idx_employee_career_paths_assigned_by_employee_id")
  @@index([employee_id], map: "idx_employee_career_paths_emp")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_career_progress {
  id                        String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                 String             @db.Uuid
  employee_id               String             @db.Uuid
  path_id                   String             @db.Uuid
  level_id                  String             @db.Uuid
  status                    String?            @default("not_started") @db.VarChar(20)
  started_at                DateTime?          @db.Timestamptz(6)
  completed_at              DateTime?          @db.Timestamptz(6)
  overall_fit_score         Decimal?           @db.Decimal(3, 2)
  skill_coverage_pct        Decimal?           @db.Decimal(5, 2)
  avg_skill_gap             Decimal?           @db.Decimal(3, 2)
  critical_gaps_count       Int?               @default(0)
  estimated_months_to_ready Int?
  actual_months_spent       Int?
  last_analyzed_at          DateTime?          @db.Timestamptz(6)
  analysis_version          Int?               @default(1)
  created_at                DateTime?          @default(now()) @db.Timestamptz(6)
  updated_at                DateTime?          @default(now()) @db.Timestamptz(6)
  employees                 employees          @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  career_path_levels        career_path_levels @relation(fields: [level_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  career_paths              career_paths       @relation(fields: [path_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                   tenants            @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([employee_id, level_id])
  @@index([path_id], map: "idx_career_progress_path")
  @@index([status], map: "idx_career_progress_status")
  @@index([tenant_id], map: "idx_employee_career_progress_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model employee_certifications {
  id                                                                   String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id                                                          String?         @db.Uuid
  certification_id                                                     String?         @db.Uuid
  credential_id                                                        String?         @db.VarChar(100)
  issued_date                                                          DateTime        @db.Date
  expiry_date                                                          DateTime?       @db.Date
  status                                                               String?         @default("active") @db.VarChar(20)
  credential_url                                                       String?         @db.VarChar(500)
  verification_status                                                  String?         @default("unverified") @db.VarChar(20)
  verified_by                                                          String?         @db.Uuid
  verified_at                                                          DateTime?       @db.Timestamptz(6)
  document_url                                                         String?         @db.VarChar(500)
  notes                                                                String?
  created_at                                                           DateTime?       @default(now()) @db.Timestamptz(6)
  updated_at                                                           DateTime?       @default(now()) @db.Timestamptz(6)
  verified_by_employee_id                                              String?         @db.Uuid
  employees_employee_certifications_verified_by_employee_idToemployees employees?      @relation("employee_certifications_verified_by_employee_idToemployees", fields: [verified_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "employee_certifications_verified_by_fkey_emp")
  certifications                                                       certifications? @relation(fields: [certification_id], references: [id], onUpdate: NoAction, map: "fk_employee_certifications_cert")
  employees_employee_certifications_employee_idToemployees             employees?      @relation("employee_certifications_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_employee_certifications_employee")

  @@index([certification_id], map: "idx_employee_certifications_certification_id")
  @@index([employee_id], map: "idx_employee_certifications_employee")
  @@index([status], map: "idx_employee_certifications_status")
  @@index([verified_by_employee_id], map: "idx_employee_certifications_verified_by_employee_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_clubs {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String    @db.Uuid
  name              String    @db.VarChar(200)
  description       String?
  category          String?   @db.VarChar(100)
  icon              String?   @db.VarChar(50)
  cover_image_url   String?
  is_public         Boolean?  @default(true)
  requires_approval Boolean?  @default(false)
  max_members       Int?
  members_count     Int?      @default(0)
  owner_id          String?   @db.Uuid
  is_active         Boolean?  @default(true)
  created_at        DateTime? @default(now()) @db.Timestamp(6)
  updated_at        DateTime? @default(now()) @db.Timestamp(6)
  deleted_at        DateTime? @db.Timestamptz(6)
  tenants           tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_employee_clubs_tenant")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_contract_amendments {
  id                   String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_contract_id String             @db.Uuid
  tenant_id            String             @db.Uuid
  amendment_type       String             @db.VarChar(50)
  effective_date       DateTime           @default(dbgenerated("CURRENT_DATE")) @db.Date
  description          String?
  previous_values      Json?
  new_values           Json?
  created_at           DateTime           @default(now()) @db.Timestamptz(6)
  employee_contracts   employee_contracts @relation(fields: [employee_contract_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants              tenants            @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([employee_contract_id], map: "idx_eca_contract")
  @@index([tenant_id, amendment_type], map: "idx_eca_tenant_type")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_contracts {
  id                           String                         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                    String                         @db.Uuid
  employee_id                  String                         @db.Uuid
  contract_type                String                         @db.VarChar(50)
  start_date                   DateTime                       @db.Date
  end_date                     DateTime?                      @db.Date
  pay_scale_area               String?                        @db.VarChar(10)
  ccnl_code                    String?                        @db.VarChar(50)
  level                        String?                        @db.VarChar(20)
  annual_salary                Decimal?                       @db.Decimal(12, 2)
  currency                     String?                        @default("EUR") @db.VarChar(5)
  fte_percentage               Decimal?                       @default(100.00) @db.Decimal(5, 2)
  probation_end_date           DateTime?                      @db.Date
  notice_period_days           Int?
  is_current                   Boolean?                       @default(true)
  notes                        String?
  created_at                   DateTime?                      @default(now()) @db.Timestamptz(6)
  updated_at                   DateTime?                      @default(now()) @db.Timestamptz(6)
  monthly_salary               Decimal?                       @db.Decimal(10, 2)
  num_monthly_payments         Int?                           @db.SmallInt
  salary_override_reason       String?                        @db.VarChar(50)
  employee_contract_amendments employee_contract_amendments[]
  employees                    employees                      @relation(fields: [employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                      tenants                        @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([employee_id], map: "idx_employee_contracts_employee")
  @@index([tenant_id], map: "idx_employee_contracts_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_documents {
  id                                                  String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                           String                     @db.Uuid
  employee_id                                         String                     @db.Uuid
  title                                               String                     @db.VarChar(255)
  description                                         String?
  document_type                                       String                     @db.VarChar(50)
  category                                            String?                    @db.VarChar(100)
  filename                                            String                     @db.VarChar(255)
  original_name                                       String                     @db.VarChar(255)
  mime_type                                           String                     @db.VarChar(100)
  file_size                                           Int?
  file_path                                           String                     @db.VarChar(500)
  document_date                                       DateTime?                  @db.Date
  expiry_date                                         DateTime?                  @db.Date
  reference_number                                    String?                    @db.VarChar(100)
  visibility                                          String?                    @default("private") @db.VarChar(20)
  requires_signature                                  Boolean?                   @default(false)
  signed_at                                           DateTime?                  @db.Timestamp(6)
  signed_by                                           String?                    @db.Uuid
  uploaded_by                                         String?                    @db.Uuid
  uploaded_at                                         DateTime?                  @default(now()) @db.Timestamp(6)
  status                                              String?                    @default("active") @db.VarChar(20)
  is_verified                                         Boolean?                   @default(false)
  verified_by                                         String?                    @db.Uuid
  verified_at                                         DateTime?                  @db.Timestamp(6)
  version                                             Int?                       @default(1)
  parent_document_id                                  String?                    @db.Uuid
  is_latest                                           Boolean?                   @default(true)
  created_at                                          DateTime?                  @default(now()) @db.Timestamp(6)
  updated_at                                          DateTime?                  @default(now()) @db.Timestamp(6)
  document_acknowledgments                            document_acknowledgments[]
  document_requests                                   document_requests[]
  employees_employee_documents_employee_idToemployees employees                  @relation("employee_documents_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employee_documents                                  employee_documents?        @relation("employee_documentsToemployee_documents", fields: [parent_document_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  other_employee_documents                            employee_documents[]       @relation("employee_documentsToemployee_documents")
  employees_employee_documents_signed_byToemployees   employees?                 @relation("employee_documents_signed_byToemployees", fields: [signed_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                                             tenants                    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees_employee_documents_uploaded_byToemployees employees?                 @relation("employee_documents_uploaded_byToemployees", fields: [uploaded_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees_employee_documents_verified_byToemployees employees?                 @relation("employee_documents_verified_byToemployees", fields: [verified_by], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([parent_document_id, is_latest], map: "unique_latest_version")
  @@index([signed_by], map: "idx_employee_docs_signed")
  @@index([uploaded_by], map: "idx_employee_docs_uploaded")
  @@index([category], map: "idx_employee_documents_category")
  @@index([employee_id], map: "idx_employee_documents_employee")
  @@index([status], map: "idx_employee_documents_status")
  @@index([tenant_id], map: "idx_employee_documents_tenant")
  @@index([document_type], map: "idx_employee_documents_type")
  @@index([verified_by], map: "idx_employee_documents_verified_by")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_emergency_contacts {
  id           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id    String    @db.Uuid
  employee_id  String    @db.Uuid
  name         String    @db.VarChar(200)
  phone        String?   @db.VarChar(30)
  relationship String?   @db.VarChar(100)
  is_primary   Boolean?  @default(true)
  created_at   DateTime? @default(now()) @db.Timestamptz(6)
  updated_at   DateTime? @default(now()) @db.Timestamptz(6)
  employees    employees @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants      tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([employee_id], map: "idx_employee_emergency_contacts_employee")
  @@index([tenant_id], map: "idx_employee_emergency_contacts_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model employee_job_assignments {
  id              String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id     String      @db.Uuid
  tenant_job_id   String      @db.Uuid
  assignment_type String?     @default("primary") @db.VarChar(20)
  fte_percentage  Decimal?    @default(100.00) @db.Decimal(5, 2)
  start_date      DateTime    @db.Date
  end_date        DateTime?   @db.Date
  is_current      Boolean?    @default(true)
  created_at      DateTime?   @default(now()) @db.Timestamptz(6)
  updated_at      DateTime?   @default(now()) @db.Timestamptz(6)
  tenant_jobs     tenant_jobs @relation(fields: [tenant_job_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees       employees   @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_eja_employee")

  @@index([start_date, end_date], map: "idx_emp_assignments_dates")
  @@index([employee_id], map: "idx_emp_assignments_employee")
  @@index([tenant_job_id], map: "idx_emp_assignments_job")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model employee_kpi_targets {
  id                  String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id         String           @db.Uuid
  tenant_job_kpi_id   String?          @db.Uuid
  kpi_code            String           @db.VarChar(20)
  kpi_name            String           @db.VarChar(255)
  period_start        DateTime         @db.Date
  period_end          DateTime         @db.Date
  target_value        Decimal          @db.Decimal(12, 2)
  min_acceptable      Decimal?         @db.Decimal(12, 2)
  stretch_target      Decimal?         @db.Decimal(12, 2)
  actual_value        Decimal?         @db.Decimal(12, 2)
  achievement_percent Decimal?         @default(dbgenerated("\nCASE\n    WHEN (target_value <> (0)::numeric) THEN ((actual_value / target_value) * (100)::numeric)\n    ELSE NULL::numeric\nEND")) @db.Decimal(5, 2)
  status              String?          @default("active") @db.VarChar(20)
  created_at          DateTime?        @default(now()) @db.Timestamptz(6)
  updated_at          DateTime?        @default(now()) @db.Timestamptz(6)
  tenant_job_kpis     tenant_job_kpis? @relation(fields: [tenant_job_kpi_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees           employees        @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ekt_employee")

  @@index([employee_id], map: "idx_emp_kpis_employee")
  @@index([tenant_job_kpi_id], map: "idx_emp_kpis_job_kpi")
  @@index([period_start, period_end], map: "idx_emp_kpis_period")
}

model employee_occupations {
  id                 String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  employee_id        String    @db.Uuid
  esco_occupation_id String    @db.Uuid
  is_current         Boolean?  @default(true)
  start_date         DateTime? @db.Date
  end_date           DateTime? @db.Date
  created_at         DateTime? @default(now()) @db.Timestamp(6)
  employees          employees @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_eo_employee")

  @@index([employee_id], map: "idx_employee_occupations_employee")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_overtime {
  id                  String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String               @db.Uuid
  employee_id         String               @db.Uuid
  overtime_date       DateTime             @db.Date
  overtime_type       String               @db.VarChar(30)
  hours               Decimal              @db.Decimal(5, 2)
  rate_multiplier     Decimal?             @db.Decimal(3, 2)
  hourly_rate         Decimal?             @db.Decimal(8, 2)
  total_compensation  Decimal?             @db.Decimal(10, 2)
  status              String?              @default("pending") @db.VarChar(20)
  requested_by        String?              @db.Uuid
  requested_at        DateTime?            @default(now()) @db.Timestamp(6)
  approved_by         String?              @db.Uuid
  approved_at         DateTime?            @db.Timestamp(6)
  rejection_reason    String?
  payroll_job_id      String?              @db.Uuid
  exported_at         DateTime?            @db.Timestamp(6)
  reason              String?
  notes               String?
  created_at          DateTime?            @default(now()) @db.Timestamp(6)
  updated_at          DateTime?            @default(now()) @db.Timestamp(6)
  employees           employees            @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  payroll_export_jobs payroll_export_jobs? @relation(fields: [payroll_job_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants             tenants              @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([overtime_date], map: "idx_employee_overtime_date")
  @@index([employee_id], map: "idx_employee_overtime_employee")
  @@index([payroll_job_id], map: "idx_employee_overtime_payroll_job_id")
  @@index([status], map: "idx_employee_overtime_status")
  @@index([tenant_id], map: "idx_employee_overtime_tenant")
}

model employee_pay_stubs {
  id           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id  String    @db.Uuid
  period       String    @db.VarChar(20)
  period_start DateTime  @db.Date
  period_end   DateTime  @db.Date
  gross_pay    Decimal   @db.Decimal(12, 2)
  net_pay      Decimal   @db.Decimal(12, 2)
  deductions   Json?     @default("{}")
  payment_date DateTime? @db.Date
  status       String?   @default("processing") @db.VarChar(20)
  created_at   DateTime? @default(now()) @db.Timestamp(6)
  employees    employees @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_eps_employee")

  @@index([employee_id], map: "idx_employee_pay_stubs_employee_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_permission_overrides {
  id                                                             String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id                                                    String            @db.Uuid
  permission_id                                                  String            @db.Uuid
  scope                                                          permission_scope?
  is_granted                                                     Boolean           @default(true)
  conditions                                                     Json?             @default("{}")
  granted_at                                                     DateTime?         @default(now()) @db.Timestamptz(6)
  granted_by                                                     String?           @db.Uuid
  expires_at                                                     DateTime?         @db.Timestamptz(6)
  reason                                                         String?
  tenant_id                                                      String            @db.Uuid
  created_at                                                     DateTime?         @default(now()) @db.Timestamptz(6)
  updated_at                                                     DateTime?         @default(now()) @db.Timestamptz(6)
  employees_employee_permission_overrides_employee_idToemployees employees         @relation("employee_permission_overrides_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees_employee_permission_overrides_granted_byToemployees  employees?        @relation("employee_permission_overrides_granted_byToemployees", fields: [granted_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  permissions                                                    permissions       @relation(fields: [permission_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                                                        tenants           @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([employee_id, permission_id], map: "uq_employee_permission")
  @@index([granted_by], map: "idx_employee_permission_overrides_granted_by")
  @@index([tenant_id], map: "idx_employee_permission_overrides_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model employee_requests {
  id                                                 String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id                                        String     @db.Uuid
  request_type                                       String     @db.VarChar(50)
  title                                              String     @db.VarChar(200)
  description                                        String?
  status                                             String?    @default("pending") @db.VarChar(20)
  priority                                           String?    @default("normal") @db.VarChar(20)
  attachments                                        Json?      @default("[]")
  approver_id                                        String?    @db.Uuid
  resolved_at                                        DateTime?  @db.Timestamp(6)
  resolution_notes                                   String?
  created_at                                         DateTime?  @default(now()) @db.Timestamp(6)
  updated_at                                         DateTime?  @default(now()) @db.Timestamp(6)
  employees_employee_requests_approver_idToemployees employees? @relation("employee_requests_approver_idToemployees", fields: [approver_id], references: [id], onUpdate: NoAction, map: "fk_er_approver")
  employees_employee_requests_employee_idToemployees employees  @relation("employee_requests_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_er_employee")

  @@index([approver_id], map: "idx_employee_requests_approver_id")
  @@index([employee_id], map: "idx_employee_requests_employee_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model employee_skill_assessments {
  id                  String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id         String             @db.Uuid
  tenant_job_skill_id String?            @db.Uuid
  esco_skill_uri      String?            @db.VarChar(255)
  skill_name          String             @db.VarChar(255)
  assessed_level      Int
  required_level      Int?
  gap                 Int?               @default(dbgenerated("(COALESCE(required_level, assessed_level) - assessed_level)"))
  assessment_date     DateTime           @db.Date
  assessed_by         String?            @db.Uuid
  assessment_method   String?            @db.VarChar(50)
  evidence_notes      String?
  certification_url   String?            @db.VarChar(255)
  created_at          DateTime?          @default(now()) @db.Timestamptz(6)
  updated_at          DateTime?          @default(now()) @db.Timestamptz(6)
  tenant_job_skills   tenant_job_skills? @relation(fields: [tenant_job_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees           employees          @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_esa_employee")

  @@index([assessment_date], map: "idx_emp_skills_date")
  @@index([employee_id], map: "idx_emp_skills_employee")
  @@index([esco_skill_uri], map: "idx_emp_skills_esco")
  @@index([tenant_job_skill_id], map: "idx_emp_skills_job_skill")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model employee_skill_history {
  id                       String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  profile_id               String                  @db.Uuid
  previous_knowledge_level Int?                    @db.SmallInt
  previous_skill_level     Int?                    @db.SmallInt
  previous_ability_level   Int?                    @db.SmallInt
  previous_behavior_level  Int?                    @db.SmallInt
  previous_attitude_level  Int?                    @db.SmallInt
  previous_composite_score Decimal?                @db.Decimal(4, 2)
  new_knowledge_level      Int?                    @db.SmallInt
  new_skill_level          Int?                    @db.SmallInt
  new_ability_level        Int?                    @db.SmallInt
  new_behavior_level       Int?                    @db.SmallInt
  new_attitude_level       Int?                    @db.SmallInt
  new_composite_score      Decimal?                @db.Decimal(4, 2)
  change_type              String                  @db.VarChar(50)
  change_reason            String?
  changed_by               String?                 @db.Uuid
  changed_at               DateTime?               @default(now()) @db.Timestamptz(6)
  created_at               DateTime?               @default(now()) @db.Timestamptz(6)
  employees                employees?              @relation(fields: [changed_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employee_skill_profiles  employee_skill_profiles @relation(fields: [profile_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([changed_by], map: "idx_employee_skill_history_changed_by")
  @@index([changed_at(sort: Desc)], map: "idx_esh_changed_at")
  @@index([profile_id], map: "idx_esh_profile")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_skill_mappings {
  id                                                                   String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                                            String       @db.Uuid
  employee_id                                                          String       @db.Uuid
  original_text                                                        String
  esco_skill_id                                                        String?      @db.Uuid
  mapping_confidence                                                   Decimal?     @db.Decimal(3, 2)
  mapping_method                                                       String?      @db.VarChar(50)
  proficiency_level                                                    Int?
  years_experience                                                     Decimal?     @db.Decimal(4, 1)
  verified_by                                                          String?      @db.Uuid
  verified_at                                                          DateTime?    @db.Timestamptz(6)
  created_at                                                           DateTime?    @default(now()) @db.Timestamptz(6)
  updated_at                                                           DateTime?    @default(now()) @db.Timestamptz(6)
  verified_by_employee_id                                              String?      @db.Uuid
  employees_employee_skill_mappings_employee_idToemployees             employees    @relation("employee_skill_mappings_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  esco_skills                                                          esco_skills? @relation(fields: [esco_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                                                              tenants      @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees_employee_skill_mappings_verified_by_employee_idToemployees employees?   @relation("employee_skill_mappings_verified_by_employee_idToemployees", fields: [verified_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([tenant_id, employee_id, original_text])
  @@index([esco_skill_id], map: "idx_employee_skill_mappings_esco")
  @@index([tenant_id], map: "idx_employee_skill_mappings_tenant")
  @@index([verified_by_employee_id], map: "idx_employee_skill_mappings_verified_by_employee_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_skill_profiles {
  id                                                       String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                                String                     @db.Uuid
  employee_id                                              String                     @db.Uuid
  skill_id                                                 String                     @db.Uuid
  knowledge_level                                          Int?                       @default(0) @db.SmallInt
  skill_level                                              Int?                       @default(0) @db.SmallInt
  ability_level                                            Int?                       @default(0) @db.SmallInt
  behavior_level                                           Int?                       @default(0) @db.SmallInt
  attitude_level                                           Int?                       @default(0) @db.SmallInt
  composite_score                                          Decimal?                   @default(0.00) @db.Decimal(4, 2)
  source                                                   skill_source_type          @default(self_declaration)
  source_description                                       String?
  acquired_date                                            DateTime?                  @db.Date
  last_demonstrated                                        DateTime?                  @db.Date
  evidence_type                                            String?                    @db.VarChar(50)
  evidence_id                                              String?                    @db.Uuid
  evidence_url                                             String?
  evidence_notes                                           String?
  verification_status                                      skill_verification_status? @default(pending)
  verified_by                                              String?                    @db.Uuid
  verified_at                                              DateTime?                  @db.Timestamptz(6)
  verification_notes                                       String?
  verification_expires_at                                  DateTime?                  @db.Date
  confidence_score                                         Decimal?                   @default(0.500) @db.Decimal(4, 3)
  is_primary                                               Boolean?                   @default(false)
  is_target                                                Boolean?                   @default(false)
  target_level                                             Int?                       @db.SmallInt
  created_at                                               DateTime?                  @default(now()) @db.Timestamptz(6)
  updated_at                                               DateTime?                  @default(now()) @db.Timestamptz(6)
  employee_skill_history                                   employee_skill_history[]
  employees_employee_skill_profiles_employee_idToemployees employees                  @relation("employee_skill_profiles_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  esco_skills                                              esco_skills                @relation(fields: [skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                                                  tenants                    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees_employee_skill_profiles_verified_byToemployees employees?                 @relation("employee_skill_profiles_verified_byToemployees", fields: [verified_by], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([tenant_id, employee_id, skill_id], map: "unique_employee_skill")
  @@index([verified_by], map: "idx_employee_skill_profiles_verified")
  @@index([composite_score(sort: Desc)], map: "idx_esp_composite")
  @@index([employee_id], map: "idx_esp_employee")
  @@index([skill_id], map: "idx_esp_skill")
  @@index([source], map: "idx_esp_source")
  @@index([tenant_id, employee_id], map: "idx_esp_tenant_employee")
  @@index([verification_status], map: "idx_esp_verification")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_skills {
  id                                                           String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                                    String       @db.Uuid
  employee_id                                                  String       @db.Uuid
  esco_skill_id                                                String?      @db.Uuid
  custom_skill_name                                            String?      @db.VarChar(255)
  proficiency_level                                            Int?
  proficiency_label                                            String?      @db.VarChar(50)
  years_experience                                             Decimal?     @db.Decimal(4, 1)
  is_primary                                                   Boolean?     @default(false)
  is_verified                                                  Boolean?     @default(false)
  verified_by                                                  String?      @db.Uuid
  verified_at                                                  DateTime?    @db.Timestamptz(6)
  source                                                       String?      @default("self_assessment") @db.VarChar(50)
  confidence_score                                             Decimal?     @db.Decimal(3, 2)
  last_used_at                                                 DateTime?    @db.Date
  notes                                                        String?
  created_at                                                   DateTime?    @default(now()) @db.Timestamptz(6)
  updated_at                                                   DateTime?    @default(now()) @db.Timestamptz(6)
  verified_by_employee_id                                      String?      @db.Uuid
  primary_category                                             String?      @db.VarChar(20)
  cognitive_level_achieved                                     Int?
  transferability_demonstrated                                 String?      @db.VarChar(20)
  employees_employee_skills_employee_idToemployees             employees    @relation("employee_skills_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  esco_skills                                                  esco_skills? @relation(fields: [esco_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                                                      tenants      @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees_employee_skills_verified_by_employee_idToemployees employees?   @relation("employee_skills_verified_by_employee_idToemployees", fields: [verified_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([employee_id], map: "idx_employee_skills_employee")
  @@index([esco_skill_id], map: "idx_employee_skills_esco")
  @@index([proficiency_level], map: "idx_employee_skills_proficiency")
  @@index([tenant_id], map: "idx_employee_skills_tenant")
  @@index([tenant_id, employee_id], map: "idx_employee_skills_tenant_employee")
  @@index([verified_by_employee_id], map: "idx_employee_skills_verified")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_time_off_balances {
  id                         String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id                String                       @db.Uuid
  leave_type                 String                       @db.VarChar(50)
  total_days                 Decimal?                     @default(0) @db.Decimal(5, 2)
  used_days                  Decimal?                     @default(0) @db.Decimal(5, 2)
  pending_days               Decimal?                     @default(0) @db.Decimal(5, 2)
  year                       Int?                         @default(dbgenerated("EXTRACT(year FROM CURRENT_DATE)"))
  created_at                 DateTime?                    @default(now()) @db.Timestamp(6)
  updated_at                 DateTime?                    @default(now()) @db.Timestamp(6)
  tenant_id                  String                       @db.Uuid
  carryover_days             Decimal?                     @default(0) @db.Decimal(5, 2)
  carryover_expires_at       DateTime?                    @db.Date
  accrued_days               Decimal?                     @default(0) @db.Decimal(5, 2)
  adjustment_days            Decimal?                     @default(0) @db.Decimal(5, 2)
  adjustment_reason          String?
  tenants                    tenants                      @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees                  employees                    @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_etob_employee")
  leave_balance_transactions leave_balance_transactions[]

  @@index([employee_id], map: "idx_time_off_balances_employee")
  @@index([tenant_id], map: "idx_time_off_balances_tenant")
  @@index([leave_type], map: "idx_time_off_balances_type")
  @@index([year], map: "idx_time_off_balances_year")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_time_off_requests {
  id                                                          String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id                                                 String                 @db.Uuid
  leave_type                                                  String                 @db.VarChar(50)
  start_date                                                  DateTime               @db.Date
  end_date                                                    DateTime               @db.Date
  days_requested                                              Decimal                @db.Decimal(5, 2)
  reason                                                      String?
  status                                                      String?                @default("pending") @db.VarChar(20)
  approver_id                                                 String?                @db.Uuid
  approved_at                                                 DateTime?              @db.Timestamp(6)
  rejection_reason                                            String?
  created_at                                                  DateTime?              @default(now()) @db.Timestamp(6)
  updated_at                                                  DateTime?              @default(now()) @db.Timestamp(6)
  tenant_id                                                   String                 @db.Uuid
  half_day_start                                              Boolean?               @default(false)
  half_day_end                                                Boolean?               @default(false)
  overlap_approved                                            Boolean?               @default(false)
  medical_certificate_required                                Boolean?               @default(false)
  medical_certificate_uploaded                                Boolean?               @default(false)
  cancellation_requested                                      Boolean?               @default(false)
  cancellation_reason                                         String?
  cancelled_at                                                DateTime?              @db.Timestamp(6)
  cancelled_by                                                String?                @db.Uuid
  tenants                                                     tenants                @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees_employee_time_off_requests_approver_idToemployees employees?             @relation("employee_time_off_requests_approver_idToemployees", fields: [approver_id], references: [id], onUpdate: NoAction, map: "fk_etor_approver")
  employees_employee_time_off_requests_employee_idToemployees employees              @relation("employee_time_off_requests_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_etor_employee")
  leave_approval_steps                                        leave_approval_steps[]
  medical_certificates                                        medical_certificates[]

  @@index([approver_id], map: "idx_time_off_requests_approver")
  @@index([start_date, end_date], map: "idx_time_off_requests_dates")
  @@index([employee_id], map: "idx_time_off_requests_employee")
  @@index([status], map: "idx_time_off_requests_status")
  @@index([tenant_id], map: "idx_time_off_requests_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_timeline {
  id           String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id    String                 @db.Uuid
  employee_id  String                 @db.Uuid
  event_type   String                 @db.VarChar(50)
  event_date   DateTime               @db.Timestamptz(6)
  source_table String?                @db.VarChar(50)
  source_id    String?                @db.Uuid
  payload      Json?
  ai_summary   String?
  embedding    Unsupported("vector")?
  created_at   DateTime               @default(now()) @db.Timestamptz(6)
  employees    employees              @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants      tenants                @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([employee_id, source_table, source_id, event_type], map: "uq_emp_timeline_source")
  @@index([embedding], map: "idx_emp_timeline_embedding")
  @@index([employee_id, event_date(sort: Desc)], map: "idx_emp_timeline_employee")
  @@index([tenant_id, event_type, event_date(sort: Desc)], map: "idx_emp_timeline_type")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employee_training_records {
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id       String    @db.Uuid
  employee_id     String    @db.Uuid
  course_id       String?   @db.Uuid
  training_title  String    @db.VarChar(255)
  training_type   String?   @default("internal") @db.VarChar(50)
  provider        String?   @db.VarChar(200)
  start_date      DateTime? @db.Date
  end_date        DateTime? @db.Date
  completion_date DateTime? @db.Date
  status          String?   @default("enrolled") @db.VarChar(30)
  score           Decimal?  @db.Decimal(5, 2)
  passed          Boolean?
  certificate_url String?
  credit_hours    Decimal?  @db.Decimal(5, 1)
  cost            Decimal?  @db.Decimal(10, 2)
  cost_currency   String?   @default("EUR") @db.VarChar(3)
  notes           String?
  created_at      DateTime? @default(now()) @db.Timestamptz(6)
  updated_at      DateTime? @default(now()) @db.Timestamptz(6)
  courses         courses?  @relation(fields: [course_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees       employees @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants         tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([course_id], map: "idx_employee_training_records_course_id")
  @@index([employee_id], map: "idx_employee_training_records_employee")
  @@index([tenant_id], map: "idx_employee_training_records_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employees_staging {
  id                                                          String                        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  session_id                                                  String                        @db.Uuid
  original_employee_id                                        String?                       @db.Uuid
  tenant_id                                                   String                        @db.Uuid
  first_name                                                  String                        @db.VarChar(100)
  last_name                                                   String                        @db.VarChar(100)
  middle_name                                                 String?                       @db.VarChar(100)
  email                                                       String                        @db.VarChar(255)
  personal_email                                              String?                       @db.VarChar(255)
  phone_mobile                                                String?                       @db.VarChar(30)
  phone_work                                                  String?                       @db.VarChar(30)
  phone_home                                                  String?                       @db.VarChar(30)
  job_title                                                   String?                       @db.VarChar(255)
  department                                                  String?                       @db.VarChar(100)
  location                                                    String?                       @db.VarChar(255)
  hire_date                                                   DateTime?                     @db.Date
  is_active                                                   Boolean?                      @default(true)
  manager_id                                                  String?                       @db.Uuid
  org_unit_id                                                 String?                       @db.Uuid
  cost_center_id                                              String?                       @db.Uuid
  position_id                                                 String?                       @db.VarChar(50)
  cost_center                                                 String?                       @db.VarChar(50)
  hierarchy_level                                             Int?
  reports_to_staging_id                                       String?                       @db.Uuid
  position_code                                               String?                       @db.VarChar(50)
  unit_code                                                   String?                       @db.VarChar(50)
  birth_date                                                  DateTime?                     @db.Date
  birth_place                                                 String?                       @db.VarChar(100)
  gender                                                      String?                       @db.VarChar(10)
  nationality                                                 String?                       @db.VarChar(50)
  marital_status                                              String?                       @db.VarChar(20)
  address_street                                              String?                       @db.VarChar(255)
  address_city                                                String?                       @db.VarChar(100)
  address_postal_code                                         String?                       @db.VarChar(20)
  address_country                                             String?                       @db.VarChar(3)
  address_region                                              String?                       @db.VarChar(50)
  salary                                                      Decimal?                      @db.Decimal(15, 2)
  currency                                                    String?                       @default("EUR") @db.VarChar(3)
  pay_scale_area                                              String?                       @db.VarChar(10)
  pay_scale_type                                              String?                       @db.VarChar(10)
  pay_scale_group                                             String?                       @db.VarChar(10)
  pay_scale_level                                             String?                       @db.VarChar(10)
  pay_periods_per_year                                        Int?                          @default(12)
  iban                                                        String?                       @db.VarChar(34)
  swift_bic                                                   String?                       @db.VarChar(11)
  bank_name                                                   String?                       @db.VarChar(100)
  bank_account_number                                         String?                       @db.VarChar(50)
  skills                                                      String[]                      @default([])
  performance_rating                                          Decimal?                      @db.Decimal(3, 2)
  potential                                                   String?                       @db.VarChar(20)
  employee_group                                              String?                       @db.VarChar(50)
  employee_subgroup                                           String?                       @db.VarChar(50)
  work_schedule_percentage                                    Decimal?                      @default(100.00) @db.Decimal(5, 2)
  pernr                                                       String?                       @db.VarChar(8)
  legacy_org_unit_code                                        String?                       @db.VarChar(50)
  tax_id                                                      String?                       @db.VarChar(20)
  original_values                                             Json                          @default("{}")
  diff_fields                                                 String[]                      @default([])
  change_type                                                 String                        @default("unchanged") @db.VarChar(30)
  assignment_confidence                                       Decimal?                      @db.Decimal(5, 4)
  assignment_method                                           String?                       @db.VarChar(50)
  assignment_notes                                            String?
  is_approved                                                 Boolean?                      @default(false)
  approved_by                                                 String?                       @db.Uuid
  approved_at                                                 DateTime?                     @db.Timestamptz(6)
  created_at                                                  DateTime?                     @default(now()) @db.Timestamptz(6)
  updated_at                                                  DateTime?                     @default(now()) @db.Timestamptz(6)
  deleted_at                                                  DateTime?                     @db.Timestamptz(6)
  employees_employees_staging_original_employee_idToemployees employees?                    @relation("employees_staging_original_employee_idToemployees", fields: [original_employee_id], references: [id], onUpdate: NoAction)
  employees_staging                                           employees_staging?            @relation("employees_stagingToemployees_staging", fields: [reports_to_staging_id], references: [id], onUpdate: NoAction)
  other_employees_staging                                     employees_staging[]           @relation("employees_stagingToemployees_staging")
  org_chart_generation_sessions                               org_chart_generation_sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                                                     tenants                       @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees_employees_staging_manager_idToemployees           employees?                    @relation("employees_staging_manager_idToemployees", fields: [manager_id], references: [id], onUpdate: NoAction, map: "fk_es_manager")

  @@index([manager_id], map: "idx_employees_staging_manager_id")
  @@index([session_id, is_approved], map: "idx_es_approved")
  @@index([change_type], map: "idx_es_change_type")
  @@index([session_id, hierarchy_level], map: "idx_es_hierarchy")
  @@index([original_employee_id], map: "idx_es_original_employee")
  @@index([session_id, position_code], map: "idx_es_position")
  @@index([reports_to_staging_id], map: "idx_es_reports_to")
  @@index([session_id], map: "idx_es_session")
  @@index([tenant_id], map: "idx_es_tenant")
  @@index([session_id, unit_code], map: "idx_es_unit")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model engagement_action_plans {
  id                                              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                       String    @db.Uuid
  source_type                                     String    @db.VarChar(50)
  source_id                                       String?   @db.Uuid
  title                                           String    @db.VarChar(255)
  description                                     String?
  owner_id                                        String    @db.Uuid
  status                                          String    @default("planned") @db.VarChar(20)
  priority                                        String    @default("medium") @db.VarChar(20)
  due_date                                        DateTime? @db.Date
  completed_at                                    DateTime? @db.Timestamptz(6)
  created_by                                      String    @db.Uuid
  created_at                                      DateTime  @default(now()) @db.Timestamptz(6)
  updated_at                                      DateTime  @default(now()) @db.Timestamptz(6)
  users_engagement_action_plans_created_byTousers users     @relation("engagement_action_plans_created_byTousers", fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users_engagement_action_plans_owner_idTousers   users     @relation("engagement_action_plans_owner_idTousers", fields: [owner_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                                         tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_by], map: "idx_engagement_action_plans_created_by")
  @@index([owner_id], map: "idx_engagement_actions_owner")
  @@index([tenant_id, status], map: "idx_engagement_actions_status")
  @@index([tenant_id], map: "idx_engagement_actions_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model engagement_feedback {
  id           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id    String    @db.Uuid
  category     String    @default("other") @db.VarChar(50)
  message      String
  status       String    @default("new") @db.VarChar(20)
  reviewed_by  String?   @db.Uuid
  reviewed_at  DateTime? @db.Timestamptz(6)
  action_notes String?
  created_at   DateTime  @default(now()) @db.Timestamptz(6)
  users        users?    @relation(fields: [reviewed_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants      tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([tenant_id, created_at(sort: Desc)], map: "idx_engagement_feedback_created")
  @@index([reviewed_by], map: "idx_engagement_feedback_reviewed_by")
  @@index([tenant_id, status], map: "idx_engagement_feedback_status")
  @@index([tenant_id], map: "idx_engagement_feedback_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model engagement_pulse_configs {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id      String    @db.Uuid
  name           String    @db.VarChar(255)
  questions      Json      @default("[]")
  frequency      String    @default("biweekly") @db.VarChar(20)
  audience_type  String    @default("all") @db.VarChar(20)
  audience_ids   String[]  @default([]) @db.Uuid
  is_active      Boolean   @default(true)
  last_sent_at   DateTime? @db.Timestamptz(6)
  next_send_date DateTime? @db.Timestamptz(6)
  created_by     String    @db.Uuid
  created_at     DateTime  @default(now()) @db.Timestamptz(6)
  updated_at     DateTime  @default(now()) @db.Timestamptz(6)
  deleted_at     DateTime? @db.Timestamptz(6)
  users          users     @relation(fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants        tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([tenant_id, is_active], map: "idx_engagement_pulse_active")
  @@index([created_by], map: "idx_engagement_pulse_configs_created_by")
  @@index([tenant_id], map: "idx_engagement_pulse_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model engagement_survey_responses {
  id                 String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String             @db.Uuid
  survey_id          String             @db.Uuid
  employee_id        String?            @db.Uuid
  anonymous_token    String?            @db.VarChar(64)
  answers            Json               @default("[]")
  started_at         DateTime           @default(now()) @db.Timestamptz(6)
  completed_at       DateTime?          @db.Timestamptz(6)
  is_complete        Boolean            @default(false)
  created_at         DateTime?          @default(now()) @db.Timestamptz(6)
  employees          employees?         @relation(fields: [employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  engagement_surveys engagement_surveys @relation(fields: [survey_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants            tenants            @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([survey_id, is_complete], map: "idx_engagement_responses_complete")
  @@index([survey_id], map: "idx_engagement_responses_survey")
  @@index([tenant_id], map: "idx_engagement_survey_responses_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model engagement_survey_templates {
  id                 String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String               @db.Uuid
  name               String               @db.VarChar(255)
  description        String?
  category           String               @default("custom") @db.VarChar(50)
  questions          Json                 @default("[]")
  is_system          Boolean              @default(false)
  is_active          Boolean              @default(true)
  created_by         String?              @db.Uuid
  created_at         DateTime             @default(now()) @db.Timestamptz(6)
  updated_at         DateTime             @default(now()) @db.Timestamptz(6)
  deleted_at         DateTime?            @db.Timestamptz(6)
  users              users?               @relation(fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants            tenants              @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  engagement_surveys engagement_surveys[]

  @@index([created_by], map: "idx_engagement_survey_templates_created_by")
  @@index([tenant_id, category], map: "idx_engagement_templates_category")
  @@index([tenant_id], map: "idx_engagement_templates_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model engagement_surveys {
  id                          String                        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                   String                        @db.Uuid
  template_id                 String?                       @db.Uuid
  title                       String                        @db.VarChar(255)
  description                 String?
  questions                   Json                          @default("[]")
  is_anonymous                Boolean                       @default(true)
  status                      String                        @default("draft") @db.VarChar(20)
  audience_type               String                        @default("all") @db.VarChar(20)
  audience_ids                String[]                      @default([]) @db.Uuid
  start_date                  DateTime                      @default(now()) @db.Timestamptz(6)
  end_date                    DateTime?                     @db.Timestamptz(6)
  reminder_days               Int[]                         @default([3, 7])
  total_invitations           Int                           @default(0)
  total_responses             Int                           @default(0)
  created_by                  String                        @db.Uuid
  created_at                  DateTime                      @default(now()) @db.Timestamptz(6)
  updated_at                  DateTime                      @default(now()) @db.Timestamptz(6)
  engagement_survey_responses engagement_survey_responses[]
  users                       users                         @relation(fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  engagement_survey_templates engagement_survey_templates?  @relation(fields: [template_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                     tenants                       @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_by], map: "idx_engagement_surveys_created_by")
  @@index([tenant_id, start_date, end_date], map: "idx_engagement_surveys_dates")
  @@index([tenant_id, status], map: "idx_engagement_surveys_status")
  @@index([template_id], map: "idx_engagement_surveys_template")
  @@index([tenant_id], map: "idx_engagement_surveys_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model enrichment_candidates {
  id                          String                       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                   String                       @db.Uuid
  job_id                      String                       @db.Uuid
  source_snapshot_id          String?                      @db.Uuid
  entity_type                 String                       @db.VarChar(100)
  entity_anchor               String?                      @db.VarChar(500)
  field_name                  String                       @db.VarChar(100)
  candidate_value             Json
  confidence                  Decimal                      @default(0) @db.Decimal(3, 2)
  extraction_method           String                       @db.VarChar(50)
  llm_provider_code           String?                      @db.VarChar(50)
  fact_hash                   String                       @db.Char(64)
  created_at                  DateTime                     @default(now()) @db.Timestamptz(6)
  enrichment_jobs             enrichment_jobs              @relation(fields: [job_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  enrichment_llm_providers    enrichment_llm_providers?    @relation(fields: [llm_provider_code], references: [code], onDelete: NoAction, onUpdate: NoAction)
  enrichment_source_snapshots enrichment_source_snapshots? @relation(fields: [source_snapshot_id], references: [id], onUpdate: NoAction)
  tenants                     tenants                      @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  enrichment_lineage          enrichment_lineage[]
  enrichment_matches          enrichment_matches[]
  enrichment_writes           enrichment_writes[]

  @@unique([tenant_id, fact_hash], map: "enrichment_candidates_fact_hash_unique")
  @@index([entity_type, field_name, confidence(sort: Desc)], map: "idx_enrichment_candidates_field")
  @@index([job_id], map: "idx_enrichment_candidates_job")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model enrichment_entity_descriptors {
  id                            String                         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                     String?                        @db.Uuid
  entity_name                   String                         @db.VarChar(100)
  target_table                  String                         @db.VarChar(100)
  pk_field                      String                         @default("id") @db.VarChar(100)
  match_keys                    String[]                       @default([])
  source_strategy_jsonb         Json                           @default("{}")
  extraction_schema_id          String?                        @db.Uuid
  default_merge_policy_id       String?                        @db.Uuid
  default_mode                  String                         @default("suggest") @db.VarChar(20)
  scope_level                   String                         @default("platform") @db.VarChar(20)
  is_active                     Boolean                        @default(true)
  description                   String?
  created_at                    DateTime                       @default(now()) @db.Timestamptz(6)
  updated_at                    DateTime                       @default(now()) @db.Timestamptz(6)
  crawl_config                  Json                           @default("{}")
  description_it                String?
  description_en                String?
  enrichment_merge_policies     enrichment_merge_policies?     @relation(fields: [default_merge_policy_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  enrichment_extraction_schemas enrichment_extraction_schemas? @relation(fields: [extraction_schema_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  tenants                       tenants?                       @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  enrichment_jobs               enrichment_jobs[]

  @@unique([tenant_id, entity_name], map: "enrichment_entity_descriptors_unique")
  @@index([target_table, is_active], map: "idx_enrichment_entity_descriptors_target")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an index with non-default null sort order and requires additional setup for migrations. Visit https://pris.ly/d/default-index-null-ordering for more info.
model enrichment_extraction_schemas {
  id                            String                          @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                     String?                         @db.Uuid
  code                          String                          @db.VarChar(100)
  version                       Int                             @default(1)
  schema_jsonb                  Json
  description                   String?
  is_active                     Boolean                         @default(true)
  created_at                    DateTime                        @default(now()) @db.Timestamptz(6)
  updated_at                    DateTime                        @default(now()) @db.Timestamptz(6)
  enrichment_entity_descriptors enrichment_entity_descriptors[]
  tenants                       tenants?                        @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, code, version], map: "enrichment_extraction_schemas_unique")
  @@index([code, tenant_id], map: "idx_enrichment_extraction_schemas_code")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model enrichment_job_events {
  id              BigInt          @id @default(autoincrement())
  job_id          String          @db.Uuid
  tenant_id       String          @db.Uuid
  event_type      String          @db.VarChar(50)
  payload_jsonb   Json?
  created_at      DateTime        @default(now()) @db.Timestamptz(6)
  enrichment_jobs enrichment_jobs @relation(fields: [job_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants         tenants         @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([job_id, created_at], map: "idx_enrichment_job_events_job")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model enrichment_jobs {
  id                                                                  String                        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                                                           String                        @db.Uuid
  descriptor_id                                                       String                        @db.Uuid
  target_table                                                        String                        @db.VarChar(100)
  target_pk_field                                                     String                        @db.VarChar(100)
  target_record_id                                                    String
  semantic_scope_jsonb                                                Json                          @default("{}")
  policy_id                                                           String?                       @db.Uuid
  mode                                                                String                        @default("suggest") @db.VarChar(20)
  idempotency_key                                                     String                        @db.VarChar(64)
  status                                                              String                        @default("pending") @db.VarChar(30)
  requested_by_user_id                                                String?                       @db.Uuid
  freshness_days                                                      Int                           @default(7)
  llm_cost_eur                                                        Decimal                       @default(0) @db.Decimal(10, 4)
  error_details                                                       Json?
  created_at                                                          DateTime                      @default(now()) @db.Timestamptz(6)
  completed_at                                                        DateTime?                     @db.Timestamptz(6)
  enrichment_candidates                                               enrichment_candidates[]
  enrichment_job_events                                               enrichment_job_events[]
  enrichment_entity_descriptors                                       enrichment_entity_descriptors @relation(fields: [descriptor_id], references: [id], onUpdate: NoAction)
  enrichment_merge_policies                                           enrichment_merge_policies?    @relation(fields: [policy_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  users                                                               users?                        @relation(fields: [requested_by_user_id], references: [id], onUpdate: NoAction)
  tenants                                                             tenants                       @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  enrichment_lineage                                                  enrichment_lineage[]
  enrichment_matches                                                  enrichment_matches[]
  enrichment_merges                                                   enrichment_merges[]
  enrichment_observations                                             enrichment_observations[]
  enrichment_sources                                                  enrichment_sources[]
  enrichment_writes_enrichment_writes_job_idToenrichment_jobs         enrichment_writes[]           @relation("enrichment_writes_job_idToenrichment_jobs")
  enrichment_writes_enrichment_writes_rolled_back_byToenrichment_jobs enrichment_writes[]           @relation("enrichment_writes_rolled_back_byToenrichment_jobs")

  @@unique([tenant_id, idempotency_key], map: "enrichment_jobs_idempotency_unique")
  @@index([target_table, target_record_id], map: "idx_enrichment_jobs_target")
  @@index([tenant_id, status, created_at(sort: Desc)], map: "idx_enrichment_jobs_tenant_status")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model enrichment_lineage {
  id                      String                   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id               String                   @db.Uuid
  job_id                  String                   @db.Uuid
  candidate_id            String?                  @db.Uuid
  match_id                String?                  @db.Uuid
  merge_id                String?                  @db.Uuid
  observation_id          String?                  @db.Uuid
  lineage_type            String                   @db.VarChar(50)
  created_at              DateTime                 @default(now()) @db.Timestamptz(6)
  enrichment_candidates   enrichment_candidates?   @relation(fields: [candidate_id], references: [id], onUpdate: NoAction)
  enrichment_jobs         enrichment_jobs          @relation(fields: [job_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  enrichment_matches      enrichment_matches?      @relation(fields: [match_id], references: [id], onUpdate: NoAction)
  enrichment_merges       enrichment_merges?       @relation(fields: [merge_id], references: [id], onUpdate: NoAction)
  enrichment_observations enrichment_observations? @relation(fields: [observation_id], references: [id], onUpdate: NoAction)
  tenants                 tenants                  @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([job_id], map: "idx_enrichment_lineage_job")
  @@index([merge_id], map: "idx_enrichment_lineage_merge")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model enrichment_llm_providers {
  id                         Int                     @id @default(autoincrement())
  code                       String                  @unique @db.VarChar(50)
  display_name               String                  @db.VarChar(100)
  api_key_env_ref            String                  @db.VarChar(100)
  base_url                   String?                 @db.VarChar(500)
  default_model              String                  @db.VarChar(100)
  priority                   Int                     @default(100)
  is_active                  Boolean                 @default(true)
  cost_per_1k_input_tokens   Decimal                 @default(0) @db.Decimal(10, 6)
  cost_per_1k_output_tokens  Decimal                 @default(0) @db.Decimal(10, 6)
  supports_structured_output Boolean                 @default(false)
  notes                      String?
  created_at                 DateTime                @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime                @default(now()) @db.Timestamptz(6)
  enrichment_candidates      enrichment_candidates[]

  @@index([is_active, priority], map: "idx_enrichment_llm_providers_active")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model enrichment_matches {
  id                    String                @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id             String                @db.Uuid
  job_id                String                @db.Uuid
  candidate_id          String                @db.Uuid
  target_table          String                @db.VarChar(100)
  target_pk_field       String                @db.VarChar(100)
  target_record_id      String
  match_score           Decimal               @default(0) @db.Decimal(3, 2)
  match_reason          Json?
  accepted              Boolean               @default(false)
  reviewed_at           DateTime?             @db.Timestamptz(6)
  reviewed_by_user_id   String?               @db.Uuid
  created_at            DateTime              @default(now()) @db.Timestamptz(6)
  enrichment_lineage    enrichment_lineage[]
  enrichment_candidates enrichment_candidates @relation(fields: [candidate_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  enrichment_jobs       enrichment_jobs       @relation(fields: [job_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                 users?                @relation(fields: [reviewed_by_user_id], references: [id], onUpdate: NoAction)
  tenants               tenants               @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  enrichment_merges     enrichment_merges[]

  @@index([job_id, accepted], map: "idx_enrichment_matches_job")
  @@index([target_table, target_record_id], map: "idx_enrichment_matches_target")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an index with non-default null sort order and requires additional setup for migrations. Visit https://pris.ly/d/default-index-null-ordering for more info.
model enrichment_merge_policies {
  id                            String                          @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                     String?                         @db.Uuid
  code                          String                          @db.VarChar(100)
  version                       Int                             @default(1)
  rules_jsonb                   Json
  budget_cap_eur                Decimal                         @default(10.00) @db.Decimal(10, 2)
  current_usage_eur             Decimal                         @default(0) @db.Decimal(10, 2)
  budget_reset_at               DateTime                        @default(now()) @db.Timestamptz(6)
  description                   String?
  is_active                     Boolean                         @default(true)
  created_at                    DateTime                        @default(now()) @db.Timestamptz(6)
  updated_at                    DateTime                        @default(now()) @db.Timestamptz(6)
  budget_reset_interval         Unsupported("interval")         @default(dbgenerated("'30 days'::interval"))
  enrichment_entity_descriptors enrichment_entity_descriptors[]
  enrichment_jobs               enrichment_jobs[]
  tenants                       tenants?                        @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, code, version], map: "enrichment_merge_policies_unique")
  @@index([code, tenant_id], map: "idx_enrichment_merge_policies_code")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model enrichment_merges {
  id                                                    String               @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                                             String               @db.Uuid
  job_id                                                String               @db.Uuid
  match_id                                              String               @db.Uuid
  target_table                                          String               @db.VarChar(100)
  target_pk_field                                       String               @db.VarChar(100)
  target_record_id                                      String
  target_column                                         String               @db.VarChar(100)
  previous_value                                        Json?
  new_value                                             Json
  merge_rule                                            String               @db.VarChar(100)
  confidence                                            Decimal              @db.Decimal(3, 2)
  committed_by_user_id                                  String?              @db.Uuid
  committed_at                                          DateTime             @default(now()) @db.Timestamptz(6)
  rolled_back_at                                        DateTime?            @db.Timestamptz(6)
  rolled_back_by_user_id                                String?              @db.Uuid
  enrichment_lineage                                    enrichment_lineage[]
  users_enrichment_merges_committed_by_user_idTousers   users?               @relation("enrichment_merges_committed_by_user_idTousers", fields: [committed_by_user_id], references: [id], onUpdate: NoAction)
  enrichment_jobs                                       enrichment_jobs      @relation(fields: [job_id], references: [id], onUpdate: NoAction)
  enrichment_matches                                    enrichment_matches   @relation(fields: [match_id], references: [id], onUpdate: NoAction)
  users_enrichment_merges_rolled_back_by_user_idTousers users?               @relation("enrichment_merges_rolled_back_by_user_idTousers", fields: [rolled_back_by_user_id], references: [id], onUpdate: NoAction)
  tenants                                               tenants              @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, target_table, target_record_id, target_column, match_id], map: "enrichment_merges_idempotent")
  @@index([job_id, committed_at(sort: Desc)], map: "idx_enrichment_merges_job")
  @@index([target_table, target_record_id], map: "idx_enrichment_merges_target")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model enrichment_observations {
  id                          String                       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                   String                       @db.Uuid
  job_id                      String                       @db.Uuid
  target_table                String                       @db.VarChar(100)
  target_record_id            String
  field_name                  String                       @db.VarChar(100)
  observed_value              Json
  confidence                  Decimal                      @default(0) @db.Decimal(3, 2)
  source_snapshot_id          String?                      @db.Uuid
  observed_at                 DateTime                     @default(now()) @db.Timestamptz(6)
  enrichment_lineage          enrichment_lineage[]
  enrichment_jobs             enrichment_jobs              @relation(fields: [job_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  enrichment_source_snapshots enrichment_source_snapshots? @relation(fields: [source_snapshot_id], references: [id], onUpdate: NoAction)
  tenants                     tenants                      @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([target_table, target_record_id, field_name, observed_at(sort: Desc)], map: "idx_enrichment_observations_target_time")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model enrichment_source_snapshots {
  id                      String                    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id               String                    @db.Uuid
  source_id               String                    @db.Uuid
  content_hash            String                    @db.Char(64)
  content_text            String?
  content_markdown        String?
  content_metadata_jsonb  Json?
  retrieved_at            DateTime                  @default(now()) @db.Timestamptz(6)
  crawler_used            String                    @db.VarChar(50)
  http_status             Int?
  bytes_size              Int?
  enrichment_candidates   enrichment_candidates[]
  enrichment_observations enrichment_observations[]
  enrichment_sources      enrichment_sources        @relation(fields: [source_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                 tenants                   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, source_id, content_hash], map: "enrichment_source_snapshots_unique")
  @@index([content_hash], map: "idx_enrichment_source_snapshots_hash")
  @@index([retrieved_at(sort: Desc)], map: "idx_enrichment_source_snapshots_retrieval")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model enrichment_sources {
  id                          String                        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                   String                        @db.Uuid
  job_id                      String                        @db.Uuid
  url                         String                        @db.VarChar(2048)
  canonical_url               String                        @db.VarChar(2048)
  source_type                 String                        @db.VarChar(50)
  discovered_via              String                        @db.VarChar(50)
  trust_score                 Decimal                       @default(0) @db.Decimal(3, 2)
  relevance_score             Decimal                       @default(0) @db.Decimal(3, 2)
  language                    String?                       @db.VarChar(10)
  created_at                  DateTime                      @default(now()) @db.Timestamptz(6)
  enrichment_source_snapshots enrichment_source_snapshots[]
  enrichment_jobs             enrichment_jobs               @relation(fields: [job_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                     tenants                       @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([tenant_id, canonical_url], map: "idx_enrichment_sources_canonical")
  @@index([job_id], map: "idx_enrichment_sources_job")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an index with non-default null sort order and requires additional setup for migrations. Visit https://pris.ly/d/default-index-null-ordering for more info.
model enrichment_trust_rules {
  id             Int      @id @default(autoincrement())
  tenant_id      String?  @db.Uuid
  source_type    String   @db.VarChar(50)
  domain_pattern String?  @db.VarChar(255)
  trust_score    Decimal  @db.Decimal(3, 2)
  notes          String?
  created_at     DateTime @default(now()) @db.Timestamptz(6)
  updated_at     DateTime @default(now()) @db.Timestamptz(6)
  tenants        tenants? @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, source_type, domain_pattern], map: "enrichment_trust_rules_unique")
  @@index([source_type, tenant_id], map: "idx_enrichment_trust_rules_lookup")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model enrichment_writes {
  id                                                                String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                                                         String                 @db.Uuid
  job_id                                                            String                 @db.Uuid
  candidate_id                                                      String?                @db.Uuid
  entity_name                                                       String                 @db.VarChar(100)
  target_table                                                      String                 @db.VarChar(100)
  target_record_id                                                  String
  entity_anchor                                                     String                 @db.VarChar(500)
  field_name                                                        String                 @db.VarChar(100)
  written_value                                                     Json
  previous_value                                                    Json?
  fact_hash                                                         String                 @db.Char(64)
  committed_at                                                      DateTime               @default(now()) @db.Timestamptz(6)
  committed_by_job                                                  String                 @db.Uuid
  rolled_back_at                                                    DateTime?              @db.Timestamptz(6)
  rolled_back_by                                                    String?                @db.Uuid
  enrichment_candidates                                             enrichment_candidates? @relation(fields: [candidate_id], references: [id], onUpdate: NoAction)
  enrichment_jobs_enrichment_writes_job_idToenrichment_jobs         enrichment_jobs        @relation("enrichment_writes_job_idToenrichment_jobs", fields: [job_id], references: [id], onUpdate: NoAction)
  enrichment_jobs_enrichment_writes_rolled_back_byToenrichment_jobs enrichment_jobs?       @relation("enrichment_writes_rolled_back_byToenrichment_jobs", fields: [rolled_back_by], references: [id], onUpdate: NoAction)
  tenants                                                           tenants                @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, target_table, target_record_id, field_name, fact_hash], map: "enrichment_writes_unique_fact")
  @@index([tenant_id, entity_anchor], map: "idx_enrichment_writes_anchor")
  @@index([job_id], map: "idx_enrichment_writes_job")
  @@index([tenant_id, target_table, target_record_id], map: "idx_enrichment_writes_target")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model error_analytics_hourly {
  id              String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  hour_start      DateTime @db.Timestamptz(6)
  tenant_id       String   @db.Uuid
  category        String   @db.VarChar(30)
  severity        String   @db.VarChar(20)
  error_count     Int      @default(0)
  unique_errors   Int      @default(0)
  affected_users  Int      @default(0)
  top_error_codes String[]
  created_at      DateTime @default(now()) @db.Timestamptz(6)
  tenants         tenants  @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([hour_start, tenant_id, category, severity])
  @@index([category], map: "idx_error_analytics_hourly_category")
  @@index([top_error_codes], map: "idx_error_analytics_hourly_codes", type: Gin)
  @@index([hour_start(sort: Desc)], map: "idx_error_analytics_hourly_hour")
  @@index([tenant_id], map: "idx_error_analytics_hourly_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model error_logs {
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  error_id         String   @db.VarChar(50)
  code             String   @db.VarChar(50)
  category         String   @db.VarChar(30)
  severity         String   @db.VarChar(20)
  message          String
  http_status      Int
  tenant_id        String   @db.Uuid
  user_id          String?  @db.Uuid
  request_method   String?  @db.VarChar(10)
  request_path     String?
  request_ip       String?  @db.VarChar(45)
  request_id       String?  @db.VarChar(100)
  user_agent       String?
  details          Json?
  database_context Json?
  stack_trace      String?
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  tenants          tenants  @relation(fields: [tenant_id], references: [id], onDelete: SetNull, onUpdate: NoAction)
  users            users?   @relation(fields: [user_id], references: [id], onUpdate: NoAction)

  @@index([category], map: "idx_error_logs_category")
  @@index([code], map: "idx_error_logs_code")
  @@index([created_at(sort: Desc)], map: "idx_error_logs_created_at")
  @@index([details], map: "idx_error_logs_details", type: Gin)
  @@index([http_status], map: "idx_error_logs_http_status")
  @@index([severity], map: "idx_error_logs_severity")
  @@index([tenant_id, created_at(sort: Desc)], map: "idx_error_logs_tenant_created")
  @@index([tenant_id], map: "idx_error_logs_tenant_id")
  @@index([user_id], map: "idx_error_logs_user_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model error_patterns {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  pattern_type     String    @db.VarChar(50)
  description      String
  error_code       String?   @db.VarChar(50)
  category         String?   @db.VarChar(30)
  first_seen       DateTime  @db.Timestamptz(6)
  last_seen        DateTime  @db.Timestamptz(6)
  occurrence_count Int       @default(1)
  affected_tenants String[]  @db.Uuid
  is_resolved      Boolean?  @default(false)
  resolution_notes String?
  resolved_at      DateTime? @db.Timestamptz(6)
  resolved_by      String?   @db.Uuid
  created_at       DateTime  @default(now()) @db.Timestamptz(6)
  updated_at       DateTime  @default(now()) @db.Timestamptz(6)
  users            users?    @relation(fields: [resolved_by], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([error_code], map: "idx_error_patterns_code")
  @@index([last_seen(sort: Desc)], map: "idx_error_patterns_last_seen")
  @@index([is_resolved], map: "idx_error_patterns_resolved")
  @@index([resolved_by], map: "idx_error_patterns_resolved_by")
  @@index([pattern_type], map: "idx_error_patterns_type")
}

model esco_isco_groups {
  id                 String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  uri                String?  @unique @db.VarChar(500)
  code               String   @unique @db.VarChar(20)
  preferred_label_en String?  @db.VarChar(500)
  preferred_label_it String?  @db.VarChar(500)
  description_en     String?
  description_it     String?
  parent_uri         String?  @db.VarChar(500)
  level              Int?     @default(1)
  created_at         DateTime @default(now()) @db.Timestamptz(6)
}

model esco_occupation_skills {
  id               String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  occupation_id    String           @db.Uuid
  skill_id         String           @db.Uuid
  relation_type    String?          @default("essential") @db.VarChar(50)
  created_at       DateTime         @default(now()) @db.Timestamptz(6)
  esco_occupations esco_occupations @relation(fields: [occupation_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  esco_skills      esco_skills      @relation(fields: [skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([occupation_id, skill_id])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model esco_occupations {
  id                                  String                                @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  uri                                 String                                @unique @db.VarChar(255)
  preferred_label                     String                                @db.VarChar(500)
  description                         String?
  isco_code                           String?                               @db.VarChar(10)
  nace_codes                          String[]
  created_at                          DateTime?                             @default(now()) @db.Timestamp(6)
  embedding_en                        Unsupported("vector")?
  embedding_it                        Unsupported("vector")?
  embedding_model                     String?                               @db.VarChar(100)
  embedding_generated_at              DateTime?                             @db.Timestamptz(6)
  preferred_label_en                  String?                               @db.VarChar(500)
  preferred_label_it                  String?                               @db.VarChar(500)
  code                                String?                               @db.VarChar(20)
  description_en                      String?
  description_it                      String?
  parent_uri                          String?                               @db.VarChar(500)
  level                               Int?                                  @default(1)
  alt_labels                          String?
  alt_labels_it                       String?
  esco_occupation_skills              esco_occupation_skills[]
  occupation_industry_classifications occupation_industry_classifications[]
  onet_esco_mappings                  onet_esco_mappings[]
  process_roles                       process_roles[]

  @@index([embedding_en], map: "idx_esco_occupations_embedding_en")
  @@index([embedding_it], map: "idx_esco_occupations_embedding_it")
  @@index([isco_code], map: "idx_esco_occupations_isco")
  @@index([preferred_label], map: "idx_esco_occupations_label")
}

model esco_skill_groups {
  id                 String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  uri                String   @unique @db.VarChar(500)
  preferred_label_en String?  @db.VarChar(500)
  preferred_label_it String?  @db.VarChar(500)
  description_en     String?
  description_it     String?
  broader_uri        String?  @db.VarChar(500)
  created_at         DateTime @default(now()) @db.Timestamptz(6)
}

model esco_skill_relations {
  id                                                            String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  source_skill_id                                               String      @db.Uuid
  target_skill_id                                               String      @db.Uuid
  relation_type                                                 String      @db.VarChar(50)
  created_at                                                    DateTime    @default(now()) @db.Timestamptz(6)
  skill_uri                                                     String?     @db.VarChar(500)
  related_skill_uri                                             String?     @db.VarChar(500)
  esco_skills_esco_skill_relations_source_skill_idToesco_skills esco_skills @relation("esco_skill_relations_source_skill_idToesco_skills", fields: [source_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  esco_skills_esco_skill_relations_target_skill_idToesco_skills esco_skills @relation("esco_skill_relations_target_skill_idToesco_skills", fields: [target_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([source_skill_id, target_skill_id, relation_type])
  @@index([related_skill_uri], map: "idx_esco_skill_relations_related_uri")
  @@index([skill_uri], map: "idx_esco_skill_relations_skill_uri")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model esco_skills {
  id                                                                             String                         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  uri                                                                            String                         @unique @db.VarChar(255)
  preferred_label                                                                String                         @db.VarChar(500)
  description                                                                    String?
  skill_type                                                                     String                         @db.VarChar(50)
  reuse_level                                                                    String?                        @db.VarChar(50)
  is_digital                                                                     Boolean?                       @default(false)
  is_green                                                                       Boolean?                       @default(false)
  created_at                                                                     DateTime?                      @default(now()) @db.Timestamp(6)
  alt_labels                                                                     Json?                          @default("[]")
  broader_uri                                                                    String?                        @db.VarChar(500)
  narrower_uris                                                                  Json?                          @default("[]")
  related_uris                                                                   Json?                          @default("[]")
  isco_groups                                                                    Json?                          @default("[]")
  updated_at                                                                     DateTime?                      @default(now()) @db.Timestamptz(6)
  primary_category                                                               String?                        @db.VarChar(20)
  cognitive_level                                                                Int?
  is_classified                                                                  Boolean?                       @default(false)
  embedding_en                                                                   Unsupported("vector")?
  embedding_it                                                                   Unsupported("vector")?
  embedding_model                                                                String?                        @db.VarChar(100)
  embedding_generated_at                                                         DateTime?                      @db.Timestamptz(6)
  skill_group_uri                                                                String?                        @db.VarChar(500)
  preferred_label_en                                                             String?                        @db.VarChar(500)
  preferred_label_it                                                             String?                        @db.VarChar(500)
  is_transversal                                                                 Boolean?                       @default(false)
  description_en                                                                 String?
  description_it                                                                 String?
  alt_labels_it                                                                  String?
  career_path_level_skills                                                       career_path_level_skills[]
  career_skills                                                                  career_skills[]
  employee_skill_mappings                                                        employee_skill_mappings[]
  employee_skill_profiles                                                        employee_skill_profiles[]
  employee_skills                                                                employee_skills[]
  esco_occupation_skills                                                         esco_occupation_skills[]
  esco_skill_relations_esco_skill_relations_source_skill_idToesco_skills         esco_skill_relations[]         @relation("esco_skill_relations_source_skill_idToesco_skills")
  esco_skill_relations_esco_skill_relations_target_skill_idToesco_skills         esco_skill_relations[]         @relation("esco_skill_relations_target_skill_idToesco_skills")
  extracted_skills                                                               extracted_skills[]
  import_skill_links                                                             import_skill_links[]
  job_template_skills                                                            job_template_skills[]
  mentor_match_scores                                                            mentor_match_scores[]
  onet_esco_mappings                                                             onet_esco_mappings[]
  onet_skills_onet_skills_esco_skill_idToesco_skills                             onet_skills[]                  @relation("onet_skills_esco_skill_idToesco_skills")
  onet_skills_onet_skills_mapped_esco_skill_idToesco_skills                      onet_skills[]                  @relation("onet_skills_mapped_esco_skill_idToesco_skills")
  ontology_skill_dimensions                                                      ontology_skill_dimensions[]
  ontology_skill_relations_ontology_skill_relations_source_skill_idToesco_skills ontology_skill_relations[]     @relation("ontology_skill_relations_source_skill_idToesco_skills")
  ontology_skill_relations_ontology_skill_relations_target_skill_idToesco_skills ontology_skill_relations[]     @relation("ontology_skill_relations_target_skill_idToesco_skills")
  performance_skill_links                                                        performance_skill_links[]
  position_skill_requirements                                                    position_skill_requirements[]
  process_skill_requirements                                                     process_skill_requirements[]
  role_skill_requirements                                                        role_skill_requirements[]
  skill_adjacencies_skill_adjacencies_adjacent_skill_idToesco_skills             skill_adjacencies[]            @relation("skill_adjacencies_adjacent_skill_idToesco_skills")
  skill_adjacencies_skill_adjacencies_skill_idToesco_skills                      skill_adjacencies[]            @relation("skill_adjacencies_skill_idToesco_skills")
  skill_aliases                                                                  skill_aliases[]
  skill_classifications                                                          skill_classifications?
  skill_demand_metrics                                                           skill_demand_metrics[]
  skill_pair_usage_skill_pair_usage_skill_id_1Toesco_skills                      skill_pair_usage[]             @relation("skill_pair_usage_skill_id_1Toesco_skills")
  skill_pair_usage_skill_pair_usage_skill_id_2Toesco_skills                      skill_pair_usage[]             @relation("skill_pair_usage_skill_id_2Toesco_skills")
  skill_relationships_skill_relationships_source_skill_idToesco_skills           skill_relationships[]          @relation("skill_relationships_source_skill_idToesco_skills")
  skill_relationships_skill_relationships_target_skill_idToesco_skills           skill_relationships[]          @relation("skill_relationships_target_skill_idToesco_skills")
  skill_requirements_templates                                                   skill_requirements_templates[]
  skill_supply_metrics                                                           skill_supply_metrics[]
  skill_synonyms                                                                 skill_synonyms[]
  skill_taxonomy_extensions                                                      skill_taxonomy_extensions[]
  tenant_custom_skills                                                           tenant_custom_skills[]
  unknown_skills_unknown_skills_mapped_to_esco_idToesco_skills                   unknown_skills[]               @relation("unknown_skills_mapped_to_esco_idToesco_skills")
  unknown_skills_unknown_skills_suggested_esco_idToesco_skills                   unknown_skills[]               @relation("unknown_skills_suggested_esco_idToesco_skills")

  @@index([alt_labels], map: "idx_esco_skills_alt_labels", type: Gin)
  @@index([broader_uri], map: "idx_esco_skills_broader")
  @@index([preferred_label], map: "idx_esco_skills_label")
  @@index([preferred_label(ops: raw("gin_trgm_ops"))], map: "idx_esco_skills_label_trgm", type: Gin)
  @@index([reuse_level], map: "idx_esco_skills_reuse")
  @@index([skill_type], map: "idx_esco_skills_type")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model export_configurations {
  id              String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id       String        @db.Uuid
  name            String        @db.VarChar(200)
  description     String?
  data_source     String        @db.VarChar(100)
  columns         Json          @default("[]")
  filters         Json?         @default("{}")
  format          String?       @default("csv") @db.VarChar(20)
  delimiter       String?       @default(",") @db.VarChar(5)
  include_headers Boolean?      @default(true)
  date_format     String?       @default("YYYY-MM-DD") @db.VarChar(50)
  encoding        String?       @default("UTF-8") @db.VarChar(20)
  compression     String?       @db.VarChar(10)
  is_template     Boolean?      @default(false)
  created_by      String?       @db.Uuid
  created_at      DateTime?     @default(now()) @db.Timestamptz(6)
  updated_at      DateTime?     @default(now()) @db.Timestamptz(6)
  employees       employees?    @relation(fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants         tenants       @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  export_jobs     export_jobs[]

  @@index([tenant_id], map: "idx_export_configs_tenant")
  @@index([created_by], map: "idx_export_configurations_created_by")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model export_jobs {
  id                    String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id             String                 @db.Uuid
  config_id             String?                @db.Uuid
  triggered_by          String?                @db.Uuid
  status                String?                @default("pending") @db.VarChar(20)
  row_count             Int?
  file_size             BigInt?
  file_path             String?
  file_url              String?
  expires_at            DateTime?              @db.Timestamptz(6)
  error_message         String?
  started_at            DateTime?              @db.Timestamptz(6)
  completed_at          DateTime?              @db.Timestamptz(6)
  created_at            DateTime?              @default(now()) @db.Timestamptz(6)
  export_configurations export_configurations? @relation(fields: [config_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants               tenants                @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees             employees?             @relation(fields: [triggered_by], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([config_id], map: "idx_export_jobs_config")
  @@index([status], map: "idx_export_jobs_status")
  @@index([tenant_id], map: "idx_export_jobs_tenant")
  @@index([triggered_by], map: "idx_export_jobs_triggered")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ext_hrp1007 {
  objid                   String    @db.VarChar(8)
  subty                   String    @default("0001") @db.VarChar(4)
  tenant_id               String    @db.Uuid
  title                   String?   @db.VarChar(255)
  description             String?
  requirements            String?
  salary_min              Decimal?  @db.Decimal(12, 2)
  salary_max              Decimal?  @db.Decimal(12, 2)
  salary_currency         String?   @default("EUR") @db.VarChar(3)
  hiring_manager_pernr    String?   @db.VarChar(8)
  recruiter_pernr         String?   @db.VarChar(8)
  priority                String?   @db.VarChar(10)
  original_requisition_id String?   @db.Uuid
  created_at              DateTime? @default(now()) @db.Timestamp(6)
  tenants                 tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@id([objid, subty])
  @@index([tenant_id], map: "idx_ext_hrp1007_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ext_pa0002 {
  pernr              String    @db.VarChar(8)
  tenant_id          String    @db.Uuid
  manager_pernr      String?   @db.VarChar(8)
  skills             String[]
  performance_rating Decimal?  @db.Decimal(3, 2)
  potential          String?   @db.VarChar(10)
  is_active          Boolean?  @default(true)
  created_at         DateTime? @default(now()) @db.Timestamp(6)
  updated_at         DateTime? @default(now()) @db.Timestamp(6)
  deleted_at         DateTime? @db.Timestamptz(6)
  tenants            tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_ext_pa0002_tenant_id")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ext_pa0024 {
  pernr         String    @db.VarChar(8)
  quali         String    @db.VarChar(50)
  is_primary    Boolean?  @default(false)
  acquired_date DateTime? @db.Date
  created_at    DateTime? @default(now()) @db.Timestamp(6)

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ext_pa0025 {
  pernr                   String    @db.VarChar(8)
  appraisal_id            String    @db.VarChar(50)
  goal_achievement_rating Decimal?  @db.Decimal(3, 2)
  competency_rating       Decimal?  @db.Decimal(3, 2)
  strengths               String?
  areas_for_improvement   String?
  employee_comments       String?
  created_at              DateTime? @default(now()) @db.Timestamp(6)

  @@id([pernr, appraisal_id])
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ext_pb0002 {
  aplnr                 String    @db.VarChar(20)
  tenant_id             String    @db.Uuid
  linkedin_url          String?   @db.VarChar(255)
  skills                Json?
  consent_given         Boolean?  @default(false)
  fit_score             Decimal?  @db.Decimal(3, 2)
  original_candidate_id String?   @db.Uuid
  created_at            DateTime? @default(now()) @db.Timestamp(6)
  tenants               tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_ext_pb0002_tenant_id")
  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model extracted_skills {
  id                 String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String           @db.Uuid
  job_posting_id     String           @db.Uuid
  raw_text           String
  esco_skill_id      String?          @db.Uuid
  mapping_confidence Decimal?         @db.Decimal(3, 2)
  is_required        Boolean?         @default(true)
  mention_count      Int?             @default(1)
  context_snippet    String?
  created_at         DateTime?        @default(now()) @db.Timestamptz(6)
  esco_skills        esco_skills?     @relation(fields: [esco_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  job_postings_raw   job_postings_raw @relation(fields: [job_posting_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants            tenants          @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([esco_skill_id], map: "idx_extracted_skills_esco")
  @@index([job_posting_id], map: "idx_extracted_skills_posting")
  @@index([tenant_id], map: "idx_extracted_skills_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model feature_categories {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name          String    @db.VarChar(100)
  display_order Int?      @default(0)
  icon          String?   @db.VarChar(50)
  description   String?
  created_at    DateTime? @default(now()) @db.Timestamp(6)
  updated_at    DateTime? @default(now()) @db.Timestamp(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model feature_modules {
  id              String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  code            String        @unique(map: "uq_feature_modules_code") @db.VarChar(50)
  name            String        @db.VarChar(100)
  description     String?
  category        String        @db.VarChar(50)
  api_prefix      String?       @db.VarChar(100)
  frontend_path   String?       @db.VarChar(100)
  is_active       Boolean?      @default(true)
  requires_tenant Boolean?      @default(true)
  sort_order      Int?          @default(0)
  created_at      DateTime?     @default(now()) @db.Timestamptz(6)
  updated_at      DateTime?     @default(now()) @db.Timestamptz(6)
  deleted_at      DateTime?     @db.Timestamptz(6)
  permissions     permissions[]

  @@index([category], map: "idx_feature_modules_category")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model features {
  id                    String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  category_id           String?   @db.Uuid
  name                  String    @db.VarChar(200)
  description           String?
  status                String?   @default("planned") @db.VarChar(20)
  notes                 String?
  priority              Int?      @default(0)
  phase                 String?   @db.VarChar(50)
  target_date           DateTime? @db.Date
  completion_percentage Int?      @default(0)
  created_at            DateTime? @default(now()) @db.Timestamp(6)
  updated_at            DateTime? @default(now()) @db.Timestamp(6)

  @@index([phase], map: "idx_features_phase")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model feedback_360 {
  id                                                                String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                                         String                       @db.Uuid
  target_employee_id                                                String?                      @db.Uuid
  reviewer_employee_id                                              String?                      @db.Uuid
  review_cycle_id                                                   String?                      @db.Uuid
  relationship_type                                                 String?                      @db.VarChar(50)
  overall_rating                                                    Decimal?                     @db.Decimal(3, 2)
  strengths                                                         String?
  areas_for_improvement                                             String?
  is_anonymous                                                      Boolean?                     @default(true)
  status                                                            String?                      @default("pending") @db.VarChar(50)
  created_at                                                        DateTime?                    @default(now()) @db.Timestamp(6)
  completed_at                                                      DateTime?                    @db.Timestamp(6)
  content_embedding                                                 Unsupported("vector")?
  embedding_text_hash                                               String?                      @db.VarChar(64)
  embedding_model                                                   String?                      @db.VarChar(100)
  embedding_generated_at                                            DateTime?                    @db.Timestamptz(6)
  questionnaire_id                                                  String?                      @db.Uuid
  performance_review_id                                             String?                      @db.Uuid
  request_id                                                        String?                      @db.Uuid
  question_responses                                                Json?
  sentiment_score                                                   Decimal?                     @db.Decimal(3, 2)
  submission_time_seconds                                           Int?
  performance_reviews                                               performance_reviews?         @relation(fields: [performance_review_id], references: [id], onUpdate: NoAction)
  feedback_360_questionnaires                                       feedback_360_questionnaires? @relation(fields: [questionnaire_id], references: [id], onUpdate: NoAction)
  feedback_requests_feedback_360_request_idTofeedback_requests      feedback_requests?           @relation("feedback_360_request_idTofeedback_requests", fields: [request_id], references: [id], onUpdate: NoAction)
  tenants                                                           tenants                      @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees_feedback_360_reviewer_employee_idToemployees            employees?                   @relation("feedback_360_reviewer_employee_idToemployees", fields: [reviewer_employee_id], references: [id], onUpdate: NoAction, map: "fk_feedback_360_reviewer")
  employees_feedback_360_target_employee_idToemployees              employees?                   @relation("feedback_360_target_employee_idToemployees", fields: [target_employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_feedback_360_target")
  feedback_requests_feedback_requests_feedback_360_idTofeedback_360 feedback_requests[]          @relation("feedback_requests_feedback_360_idTofeedback_360")

  @@index([review_cycle_id], map: "idx_feedback_360_cycle")
  @@index([content_embedding], map: "idx_feedback_360_embedding")
  @@index([questionnaire_id], map: "idx_feedback_360_questionnaire_id")
  @@index([request_id], map: "idx_feedback_360_request_id")
  @@index([performance_review_id], map: "idx_feedback_360_review")
  @@index([reviewer_employee_id], map: "idx_feedback_360_reviewer")
  @@index([status], map: "idx_feedback_360_status")
  @@index([target_employee_id], map: "idx_feedback_360_target")
  @@index([tenant_id], map: "idx_feedback_360_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model feedback_360_peer_suggestions {
  id                                                                    String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                                             String         @db.Uuid
  target_employee_id                                                    String         @db.Uuid
  suggested_peer_id                                                     String         @db.Uuid
  review_cycle_id                                                       String?        @db.Uuid
  suggestion_source                                                     String?        @default("algorithm") @db.VarChar(50)
  relationship_type                                                     String?        @db.VarChar(50)
  confidence_score                                                      Decimal?       @db.Decimal(3, 2)
  reason                                                                String?
  is_selected                                                           Boolean?       @default(false)
  created_at                                                            DateTime?      @default(now()) @db.Timestamptz(6)
  review_cycles                                                         review_cycles? @relation(fields: [review_cycle_id], references: [id], onUpdate: NoAction)
  employees_feedback_360_peer_suggestions_suggested_peer_idToemployees  employees      @relation("feedback_360_peer_suggestions_suggested_peer_idToemployees", fields: [suggested_peer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees_feedback_360_peer_suggestions_target_employee_idToemployees employees      @relation("feedback_360_peer_suggestions_target_employee_idToemployees", fields: [target_employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                                                               tenants        @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, target_employee_id, suggested_peer_id, review_cycle_id])
  @@index([review_cycle_id], map: "idx_peer_suggestions_cycle")
  @@index([target_employee_id], map: "idx_peer_suggestions_target")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model feedback_360_questionnaires {
  id                     String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String                   @db.Uuid
  name                   String                   @db.VarChar(255)
  description            String?
  is_default             Boolean?                 @default(false)
  relationship_types     String[]                 @default(["manager", "peer", "direct_report", "self"])
  is_active              Boolean?                 @default(true)
  created_by             String?                  @db.Uuid
  created_at             DateTime?                @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?                @default(now()) @db.Timestamptz(6)
  deleted_at             DateTime?                @db.Timestamptz(6)
  feedback_360           feedback_360[]
  employees              employees?               @relation(fields: [created_by], references: [id], onUpdate: NoAction)
  tenants                tenants                  @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  feedback_360_questions feedback_360_questions[]
  feedback_requests      feedback_requests[]

  @@index([is_active], map: "idx_feedback_360_questionnaires_active")
  @@index([created_by], map: "idx_feedback_360_questionnaires_created_by")
  @@index([tenant_id], map: "idx_feedback_360_questionnaires_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model feedback_360_questions {
  id                          String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                   String                      @db.Uuid
  questionnaire_id            String                      @db.Uuid
  question_text               String
  question_type               String?                     @default("rating") @db.VarChar(50)
  category                    String?                     @db.VarChar(100)
  ksaba_dimension             String?                     @db.VarChar(20)
  competency_id               String?                     @db.Uuid
  options                     Json?
  rating_scale_min            Int?                        @default(1)
  rating_scale_max            Int?                        @default(5)
  is_required                 Boolean?                    @default(true)
  display_order               Int?                        @default(0)
  relationship_types          String[]
  created_at                  DateTime?                   @default(now()) @db.Timestamptz(6)
  feedback_360_questionnaires feedback_360_questionnaires @relation(fields: [questionnaire_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                     tenants                     @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([category], map: "idx_feedback_360_questions_category")
  @@index([questionnaire_id], map: "idx_feedback_360_questions_questionnaire")
  @@index([tenant_id], map: "idx_feedback_360_questions_tenant_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model feedback_categories {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id     String    @db.Uuid
  name          String    @db.VarChar(100)
  description   String?
  icon          String?   @db.VarChar(50)
  color         String?   @db.VarChar(20)
  display_order Int?      @default(0)
  is_active     Boolean?  @default(true)
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at    DateTime? @db.Timestamptz(6)
  tenants       tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_feedback_categories_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model feedback_requests {
  id                                                           String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                                    String                       @db.Uuid
  requestee_id                                                 String                       @db.Uuid
  reviewer_id                                                  String                       @db.Uuid
  feedback_type                                                String?                      @default("peer") @db.VarChar(50)
  status                                                       String?                      @default("pending") @db.VarChar(50)
  due_date                                                     DateTime?                    @db.Date
  completed_at                                                 DateTime?                    @db.Timestamptz(6)
  is_anonymous                                                 Boolean?                     @default(true)
  created_at                                                   DateTime?                    @default(now()) @db.Timestamptz(6)
  review_cycle_id                                              String?                      @db.Uuid
  performance_review_id                                        String?                      @db.Uuid
  questionnaire_id                                             String?                      @db.Uuid
  relationship_type                                            String?                      @db.VarChar(50)
  reminder_sent_at                                             DateTime?                    @db.Timestamptz(6)
  feedback_360_id                                              String?                      @db.Uuid
  feedback_360_feedback_360_request_idTofeedback_requests      feedback_360[]               @relation("feedback_360_request_idTofeedback_requests")
  feedback_360_feedback_requests_feedback_360_idTofeedback_360 feedback_360?                @relation("feedback_requests_feedback_360_idTofeedback_360", fields: [feedback_360_id], references: [id], onUpdate: NoAction)
  performance_reviews                                          performance_reviews?         @relation(fields: [performance_review_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  feedback_360_questionnaires                                  feedback_360_questionnaires? @relation(fields: [questionnaire_id], references: [id], onUpdate: NoAction)
  review_cycles                                                review_cycles?               @relation(fields: [review_cycle_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                                                      tenants                      @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([feedback_360_id], map: "idx_feedback_requests_feedback_360_id")
  @@index([performance_review_id], map: "idx_feedback_requests_performance_review_id")
  @@index([questionnaire_id], map: "idx_feedback_requests_questionnaire_id")
  @@index([review_cycle_id], map: "idx_feedback_requests_review_cycle_id")
  @@index([tenant_id], map: "idx_feedback_requests_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model feedback_responses {
  id                    String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  request_id            String    @db.Uuid
  overall_rating        Int?
  strengths             String?
  areas_for_improvement String?
  additional_comments   String?
  competency_ratings    Json?
  created_at            DateTime? @default(now()) @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model goal_alignments {
  id                                           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                    String    @db.Uuid
  goal_id                                      String    @db.Uuid
  aligned_goal_id                              String    @db.Uuid
  alignment_type                               String?   @default("supports") @db.VarChar(50)
  alignment_weight                             Decimal?  @default(100) @db.Decimal(5, 2)
  created_at                                   DateTime? @default(now()) @db.Timestamptz(6)
  goals_goal_alignments_aligned_goal_idTogoals goals     @relation("goal_alignments_aligned_goal_idTogoals", fields: [aligned_goal_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  goals_goal_alignments_goal_idTogoals         goals     @relation("goal_alignments_goal_idTogoals", fields: [goal_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                                      tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([goal_id, aligned_goal_id], map: "unique_goal_alignment")
  @@index([aligned_goal_id], map: "idx_goal_alignments_aligned")
  @@index([goal_id], map: "idx_goal_alignments_goal")
  @@index([tenant_id], map: "idx_goal_alignments_tenant_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model goal_check_ins {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String    @db.Uuid
  goal_id           String    @db.Uuid
  employee_id       String    @db.Uuid
  check_in_date     DateTime  @default(dbgenerated("CURRENT_DATE")) @db.Date
  previous_progress Int?
  new_progress      Int
  status_update     String?   @db.VarChar(50)
  notes             String?
  blockers          String?
  next_steps        String?
  confidence_level  Int?
  created_at        DateTime? @default(now()) @db.Timestamptz(6)
  employees         employees @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  goals             goals     @relation(fields: [goal_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants           tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([check_in_date], map: "idx_goal_check_ins_date")
  @@index([employee_id], map: "idx_goal_check_ins_employee")
  @@index([goal_id], map: "idx_goal_check_ins_goal")
  @@index([tenant_id], map: "idx_goal_check_ins_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model goal_comments {
  id                  String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String          @db.Uuid
  goal_id             String          @db.Uuid
  author_id           String?         @db.Uuid
  parent_comment_id   String?         @db.Uuid
  content             String
  is_private          Boolean?        @default(false)
  created_at          DateTime?       @default(now()) @db.Timestamptz(6)
  updated_at          DateTime?       @default(now()) @db.Timestamptz(6)
  employees           employees?      @relation(fields: [author_id], references: [id], onUpdate: NoAction)
  goals               goals           @relation(fields: [goal_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  goal_comments       goal_comments?  @relation("goal_commentsTogoal_comments", fields: [parent_comment_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  other_goal_comments goal_comments[] @relation("goal_commentsTogoal_comments")
  tenants             tenants         @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([author_id], map: "idx_goal_comments_author")
  @@index([goal_id], map: "idx_goal_comments_goal")
  @@index([parent_comment_id], map: "idx_goal_comments_parent_comment_id")
  @@index([tenant_id], map: "idx_goal_comments_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model goal_milestones {
  id           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id    String    @db.Uuid
  goal_id      String    @db.Uuid
  title        String    @db.VarChar(255)
  description  String?
  target_date  DateTime? @db.Date
  completed_at DateTime? @db.Timestamptz(6)
  status       String?   @default("pending") @db.VarChar(20)
  weight       Decimal?  @default(0) @db.Decimal(5, 2)
  created_at   DateTime? @default(now()) @db.Timestamptz(6)
  updated_at   DateTime? @default(now()) @db.Timestamptz(6)
  goals        goals     @relation(fields: [goal_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants      tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([goal_id], map: "idx_goal_milestones_goal")
  @@index([tenant_id], map: "idx_goal_milestones_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model goal_review_ratings {
  id                      String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id               String              @db.Uuid
  performance_review_id   String              @db.Uuid
  goal_id                 String              @db.Uuid
  employee_id             String              @db.Uuid
  self_rating             Decimal?            @db.Decimal(3, 2)
  self_comment            String?
  achievement_description String?
  manager_rating          Decimal?            @db.Decimal(3, 2)
  manager_comment         String?
  weight                  Decimal?            @default(1.0) @db.Decimal(3, 2)
  created_at              DateTime?           @default(now()) @db.Timestamptz(6)
  updated_at              DateTime?           @default(now()) @db.Timestamptz(6)
  employees               employees           @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  goals                   goals               @relation(fields: [goal_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  performance_reviews     performance_reviews @relation(fields: [performance_review_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                 tenants             @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([performance_review_id, goal_id])
  @@index([employee_id], map: "idx_goal_review_ratings_employee")
  @@index([goal_id], map: "idx_goal_review_ratings_goal")
  @@index([performance_review_id], map: "idx_goal_review_ratings_review")
  @@index([tenant_id], map: "idx_goal_review_ratings_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model goal_templates {
  id                      String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id               String     @db.Uuid
  name                    String     @db.VarChar(255)
  description             String?
  category                String?    @db.VarChar(100)
  goal_type               String?    @default("objective") @db.VarChar(50)
  suggested_metrics       String[]
  suggested_duration_days Int?
  suggested_weight        Decimal?   @default(1.0) @db.Decimal(3, 2)
  difficulty_level        String?    @default("medium") @db.VarChar(20)
  role_id                 String?    @db.Uuid
  is_company_wide         Boolean?   @default(false)
  usage_count             Int?       @default(0)
  is_active               Boolean?   @default(true)
  created_by              String?    @db.Uuid
  created_at              DateTime?  @default(now()) @db.Timestamptz(6)
  updated_at              DateTime?  @default(now()) @db.Timestamptz(6)
  deleted_at              DateTime?  @db.Timestamptz(6)
  org_unit_id             String?    @db.Uuid
  employees               employees? @relation(fields: [created_by], references: [id], onUpdate: NoAction)
  org_units               org_units? @relation(fields: [org_unit_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                 tenants    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  goals                   goals[]

  @@index([is_active], map: "idx_goal_templates_active")
  @@index([category], map: "idx_goal_templates_category")
  @@index([created_by], map: "idx_goal_templates_created_by")
  @@index([tenant_id], map: "idx_goal_templates_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model goal_updates {
  id                String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String     @db.Uuid
  goal_id           String     @db.Uuid
  author_id         String?    @db.Uuid
  update_type       String?    @default("progress") @db.VarChar(30)
  previous_progress Decimal?   @db.Decimal(5, 2)
  new_progress      Decimal?   @db.Decimal(5, 2)
  previous_status   String?    @db.VarChar(30)
  new_status        String?    @db.VarChar(30)
  content           String?
  attachments       Json?      @default("[]")
  created_at        DateTime?  @default(now()) @db.Timestamptz(6)
  employees         employees? @relation(fields: [author_id], references: [id], onUpdate: NoAction)
  goals             goals      @relation(fields: [goal_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants           tenants    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([author_id], map: "idx_goal_updates_author")
  @@index([created_at(sort: Desc)], map: "idx_goal_updates_created")
  @@index([goal_id], map: "idx_goal_updates_goal")
  @@index([tenant_id], map: "idx_goal_updates_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model goals {
  id                                                     String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                              String                     @db.Uuid
  employee_id                                            String?                    @db.Uuid
  title                                                  String                     @db.VarChar(255)
  description                                            String?
  goal_type                                              String?                    @default("objective") @db.VarChar(50)
  parent_goal_id                                         String?                    @db.Uuid
  start_date                                             DateTime?                  @db.Date
  due_date                                               DateTime?                  @db.Date
  status                                                 String?                    @default("not_started") @db.VarChar(50)
  progress_percent                                       Int?                       @default(0)
  weight                                                 Decimal?                   @default(1.0) @db.Decimal(3, 2)
  created_at                                             DateTime?                  @default(now()) @db.Timestamp(6)
  updated_at                                             DateTime?                  @default(now()) @db.Timestamp(6)
  completed_at                                           DateTime?                  @db.Timestamp(6)
  category                                               String?                    @db.VarChar(100)
  owner_id                                               String?                    @db.Uuid
  priority                                               String?                    @default("medium") @db.VarChar(20)
  tags                                                   Json?                      @default("[]")
  custom_fields                                          Json?                      @default("{}")
  embedding                                              Unsupported("vector")?
  embedding_model                                        String?                    @db.VarChar(100)
  embedding_generated_at                                 DateTime?                  @db.Timestamptz(6)
  smart_criteria                                         Json?
  is_smart_validated                                     Boolean?                   @default(false)
  smart_score                                            Int?
  template_id                                            String?                    @db.Uuid
  goal_alignments_goal_alignments_aligned_goal_idTogoals goal_alignments[]          @relation("goal_alignments_aligned_goal_idTogoals")
  goal_alignments_goal_alignments_goal_idTogoals         goal_alignments[]          @relation("goal_alignments_goal_idTogoals")
  goal_check_ins                                         goal_check_ins[]
  goal_comments                                          goal_comments[]
  goal_milestones                                        goal_milestones[]
  goal_review_ratings                                    goal_review_ratings[]
  goal_updates                                           goal_updates[]
  employees_goals_employee_idToemployees                 employees?                 @relation("goals_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_goals_employee")
  employees_goals_owner_idToemployees                    employees?                 @relation("goals_owner_idToemployees", fields: [owner_id], references: [id], onUpdate: NoAction, map: "fk_goals_owner")
  goals                                                  goals?                     @relation("goalsTogoals", fields: [parent_goal_id], references: [id], onUpdate: NoAction, map: "fk_goals_parent")
  other_goals                                            goals[]                    @relation("goalsTogoals")
  goal_templates                                         goal_templates?            @relation(fields: [template_id], references: [id], onUpdate: NoAction)
  tenants                                                tenants                    @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  self_assessment_evidence                               self_assessment_evidence[]

  @@index([tenant_id, due_date], map: "idx_goals_due_date")
  @@index([embedding], map: "idx_goals_embedding")
  @@index([employee_id], map: "idx_goals_employee")
  @@index([owner_id], map: "idx_goals_owner")
  @@index([parent_goal_id], map: "idx_goals_parent")
  @@index([tenant_id, status], map: "idx_goals_status")
  @@index([template_id], map: "idx_goals_template")
  @@index([tenant_id], map: "idx_goals_tenant")
  @@index([tenant_id, employee_id], map: "idx_goals_tenant_employee")
  @@index([tenant_id, goal_type], map: "idx_goals_type")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model heuresys_sap_mapping {
  id                   Int       @id @default(autoincrement())
  heuresys_table       String    @db.VarChar(100)
  heuresys_field       String    @db.VarChar(100)
  heuresys_description String?   @db.VarChar(255)
  sap_table            String    @db.VarChar(100)
  sap_field            String    @db.VarChar(100)
  sap_description      String?   @db.VarChar(255)
  mapping_type         String    @db.VarChar(20)
  sync_direction       String    @db.VarChar(20)
  transform_rule       String?
  is_key_field         Boolean?  @default(false)
  is_required          Boolean?  @default(false)
  is_active            Boolean?  @default(true)
  domain               String?   @db.VarChar(50)
  priority             Int?      @default(100)
  created_at           DateTime? @default(now()) @db.Timestamp(6)
  updated_at           DateTime? @default(now()) @db.Timestamp(6)
  deleted_at           DateTime? @db.Timestamptz(6)

  @@index([domain], map: "idx_mapping_domain")
  @@index([heuresys_table, heuresys_field], map: "idx_mapping_heuresys")
  @@index([sap_table, sap_field], map: "idx_mapping_sap")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model holidays {
  id           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id    String    @db.Uuid
  date         DateTime  @db.Date
  name         String    @db.VarChar(100)
  name_en      String?   @db.VarChar(100)
  holiday_type String?   @default("national") @db.VarChar(50)
  country_code String?   @default("ITA") @db.VarChar(3)
  region_code  String?   @db.VarChar(10)
  is_recurring Boolean?  @default(true)
  is_active    Boolean?  @default(true)
  created_at   DateTime? @default(now()) @db.Timestamp(6)
  deleted_at   DateTime? @db.Timestamptz(6)
  name_it      String?   @db.VarChar(255)
  tenants      tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([date], map: "idx_holidays_date")
  @@index([tenant_id], map: "idx_holidays_tenant")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model hrp1000 {
  id         Int       @default(autoincrement())
  plvar      String    @default("01") @db.VarChar(2)
  otype      String    @db.VarChar(2)
  objid      String    @db.VarChar(8)
  endda      DateTime  @db.Date
  begda      DateTime  @db.Date
  istat      String?   @default("1") @db.VarChar(1)
  short      String?   @db.VarChar(12)
  stext      String?   @db.VarChar(40)
  langu      String?   @default("EN") @db.VarChar(2)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@index([objid], map: "idx_hrp1000_objid")
  @@index([otype], map: "idx_hrp1000_otype")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model hrp1001 {
  id         Int       @default(autoincrement())
  plvar      String    @default("01") @db.VarChar(2)
  otype      String    @db.VarChar(2)
  objid      String    @db.VarChar(8)
  rsign      String    @db.VarChar(1)
  relat      String    @db.VarChar(3)
  endda      DateTime  @db.Date
  begda      DateTime  @db.Date
  sclas      String?   @db.VarChar(2)
  sobid      String?   @db.VarChar(45)
  prozt      Decimal?  @db.Decimal(5, 2)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@index([objid], map: "idx_hrp1001_objid")
  @@index([sobid], map: "idx_hrp1001_sobid")
  @@ignore
}

model hrp1002 {
  id         Int       @id @default(autoincrement())
  plvar      String    @default("01") @db.VarChar(2)
  otype      String    @db.VarChar(2)
  objid      String    @db.VarChar(8)
  subty      String    @default("EN") @db.VarChar(4)
  endda      DateTime  @db.Date
  begda      DateTime  @db.Date
  tline      String?
  created_at DateTime? @default(now()) @db.Timestamp(6)
}

model hrp1003 {
  id            Int       @id @default(autoincrement())
  plvar         String?   @default("01") @db.VarChar(2)
  otype         String    @db.VarChar(2)
  objid         String    @db.VarChar(8)
  subty         String?   @default("0001") @db.VarChar(4)
  endda         DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda         DateTime  @db.Date
  stession      Int?
  besession     Int?
  vacancy_count Int?
  created_at    DateTime? @default(now()) @db.Timestamp(6)

  @@unique([plvar, otype, objid, subty, begda], map: "hrp1003_unique")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model hrp1005 {
  id            Int       @default(autoincrement())
  plvar         String    @default("01") @db.VarChar(2)
  otype         String    @db.VarChar(2)
  objid         String    @db.VarChar(8)
  subty         String    @default("0001") @db.VarChar(4)
  endda         DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda         DateTime  @db.Date
  waession      String?   @db.VarChar(4)
  min_salary    Decimal?  @db.Decimal(15, 2)
  mid_salary    Decimal?  @db.Decimal(15, 2)
  max_salary    Decimal?  @db.Decimal(15, 2)
  currency      String?   @default("EUR") @db.VarChar(3)
  pay_frequency String?   @default("M") @db.VarChar(2)
  created_at    DateTime? @default(now()) @db.Timestamp(6)

  @@index([objid], map: "idx_hrp1005_objid")
  @@ignore
}

model hrp1006 {
  id                Int       @id @default(autoincrement())
  plvar             String?   @default("01") @db.VarChar(2)
  otype             String    @db.VarChar(2)
  objid             String    @db.VarChar(8)
  subty             String?   @default("0001") @db.VarChar(4)
  endda             DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda             DateTime  @db.Date
  restriction_type  String?   @db.VarChar(4)
  restriction_value String?   @db.VarChar(40)
  created_at        DateTime? @default(now()) @db.Timestamp(6)

  @@unique([plvar, otype, objid, subty, begda], map: "hrp1006_unique")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model hrp1007 {
  id               Int       @id @default(autoincrement())
  plvar            String    @default("01") @db.VarChar(2)
  otype            String    @default("S") @db.VarChar(2)
  objid            String    @db.VarChar(8)
  subty            String    @default("0001") @db.VarChar(4)
  endda            DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda            DateTime  @db.Date
  vacancy_status   String?   @db.VarChar(2)
  vacancy_reason   String?   @db.VarChar(4)
  target_fill_date DateTime? @db.Date
  headcount        Int?      @default(1)
  filled_count     Int?      @default(0)
  requisition_id   String?   @db.VarChar(20)
  created_at       DateTime? @default(now()) @db.Timestamp(6)

  @@unique([plvar, otype, objid, subty, endda])
  @@index([objid], map: "idx_hrp1007_objid")
}

model hrp1008 {
  id         Int       @id @default(autoincrement())
  plvar      String?   @default("01") @db.VarChar(2)
  otype      String    @db.VarChar(2)
  objid      String    @db.VarChar(8)
  subty      String?   @default("0001") @db.VarChar(4)
  endda      DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda      DateTime  @db.Date
  bukrs      String?   @db.VarChar(4)
  kostl      String?   @db.VarChar(10)
  prctr      String?   @db.VarChar(10)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@unique([plvar, otype, objid, subty, begda], map: "hrp1008_unique")
}

model hrp1010 {
  id               Int       @id @default(autoincrement())
  plvar            String?   @default("01") @db.VarChar(2)
  otype            String?   @default("S") @db.VarChar(2)
  objid            String    @db.VarChar(8)
  subty            String?   @default("0001") @db.VarChar(4)
  endda            DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda            DateTime  @db.Date
  exam_type        String?   @db.VarChar(4)
  exam_required    Boolean?  @default(false)
  frequency_months Int?
  created_at       DateTime? @default(now()) @db.Timestamp(6)

  @@unique([plvar, otype, objid, subty, begda], map: "hrp1010_unique")
}

model hrp1011 {
  id              Int       @id @default(autoincrement())
  plvar           String?   @default("01") @db.VarChar(2)
  otype           String    @db.VarChar(2)
  objid           String    @db.VarChar(8)
  subty           String?   @default("0001") @db.VarChar(4)
  endda           DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda           DateTime  @db.Date
  work_schedule   String?   @db.VarChar(8)
  weekly_hours    Decimal?  @db.Decimal(5, 2)
  daily_hours     Decimal?  @db.Decimal(5, 2)
  shift_indicator String?   @db.VarChar(2)
  created_at      DateTime? @default(now()) @db.Timestamp(6)

  @@unique([plvar, otype, objid, subty, begda], map: "hrp1011_unique")
}

model hrp1013 {
  id         Int       @id @default(autoincrement())
  plvar      String?   @default("01") @db.VarChar(2)
  otype      String    @db.VarChar(2)
  objid      String    @db.VarChar(8)
  subty      String?   @default("0001") @db.VarChar(4)
  endda      DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda      DateTime  @db.Date
  persg      String?   @db.VarChar(1)
  persk      String?   @db.VarChar(2)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@unique([plvar, otype, objid, subty, begda], map: "hrp1013_unique")
}

model hrp1014 {
  id                Int       @id @default(autoincrement())
  plvar             String?   @default("01") @db.VarChar(2)
  otype             String    @db.VarChar(2)
  objid             String    @db.VarChar(8)
  subty             String?   @default("0001") @db.VarChar(4)
  endda             DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda             DateTime  @db.Date
  obsolete          Boolean?  @default(false)
  obsolete_reason   String?   @db.VarChar(4)
  replacement_objid String?   @db.VarChar(8)
  created_at        DateTime? @default(now()) @db.Timestamp(6)

  @@unique([plvar, otype, objid, subty, begda], map: "hrp1014_unique")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model hrp1035 {
  id                    Int       @default(autoincrement())
  plvar                 String?   @default("01") @db.VarChar(2)
  otype                 String    @db.VarChar(2)
  objid                 String    @db.VarChar(8)
  subty                 String?   @default("0001") @db.VarChar(4)
  endda                 DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda                 DateTime  @default(dbgenerated("'1900-01-01'::date")) @db.Date
  quali                 String    @db.VarChar(8)
  proficiency_required  String?   @db.VarChar(2)
  proficiency_essential Boolean?  @default(false)
  weight                Decimal?  @default(1.00) @db.Decimal(3, 2)
  esco_skill_uri        String?   @db.VarChar(255)
  esco_skill_type       String?   @db.VarChar(50)
  created_at            DateTime? @default(now()) @db.Timestamp(6)
  updated_at            DateTime? @default(now()) @db.Timestamp(6)

  @@index([esco_skill_uri], map: "idx_hrp1035_esco")
  @@index([otype, objid], map: "idx_hrp1035_obj")
  @@index([quali], map: "idx_hrp1035_quali")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model hrp1036 {
  id               Int       @default(autoincrement())
  plvar            String?   @default("01") @db.VarChar(2)
  otype            String?   @default("Q") @db.VarChar(2)
  objid            String    @db.VarChar(8)
  endda            DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda            DateTime  @default(dbgenerated("'1900-01-01'::date")) @db.Date
  langu            String?   @default("EN") @db.VarChar(2)
  short_name       String?   @db.VarChar(12)
  qual_name        String    @db.VarChar(80)
  qual_description String?
  qual_group       String?   @db.VarChar(8)
  qual_type        String?   @db.VarChar(4)
  esco_skill_uri   String?   @db.VarChar(255)
  esco_skill_type  String?   @db.VarChar(50)
  esco_reuse_level String?   @db.VarChar(20)
  is_active        Boolean?  @default(true)
  created_at       DateTime? @default(now()) @db.Timestamp(6)
  updated_at       DateTime? @default(now()) @db.Timestamp(6)
  deleted_at       DateTime? @db.Timestamptz(6)

  @@index([esco_skill_uri], map: "idx_hrp1036_esco")
  @@index([qual_group], map: "idx_hrp1036_group")
  @@index([objid], map: "idx_hrp1036_objid")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model hrp5001 {
  id                   Int       @default(autoincrement())
  plvar                String?   @default("01") @db.VarChar(2)
  otype                String?   @default("VA") @db.VarChar(2)
  objid                String    @db.VarChar(8)
  subty                String?   @default("0001") @db.VarChar(4)
  endda                DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda                DateTime  @default(dbgenerated("'1900-01-01'::date")) @db.Date
  template_name        String    @db.VarChar(80)
  template_description String?
  template_type        String?   @db.VarChar(4)
  rating_scale         String?   @default("5PT") @db.VarChar(4)
  include_goals        Boolean?  @default(true)
  include_competencies Boolean?  @default(true)
  include_development  Boolean?  @default(true)
  self_assessment      Boolean?  @default(true)
  manager_assessment   Boolean?  @default(true)
  peer_assessment      Boolean?  @default(false)
  is_active            Boolean?  @default(true)
  created_at           DateTime? @default(now()) @db.Timestamp(6)
  updated_at           DateTime? @default(now()) @db.Timestamp(6)
  deleted_at           DateTime? @db.Timestamptz(6)

  @@index([template_type], map: "idx_hrp5001_type")
  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model hrp5002 {
  id                    Int       @id @default(autoincrement())
  plvar                 String?   @default("01") @db.VarChar(2)
  otype                 String?   @default("VC") @db.VarChar(2)
  objid                 String    @db.VarChar(8)
  template_id           String    @db.VarChar(8)
  subty                 String?   @default("0001") @db.VarChar(4)
  endda                 DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda                 DateTime  @default(dbgenerated("'1900-01-01'::date")) @db.Date
  criterion_name        String    @db.VarChar(80)
  criterion_description String?
  criterion_type        String?   @db.VarChar(4)
  weight                Decimal?  @default(1.00) @db.Decimal(5, 2)
  sequence              Int?      @default(1)
  quali                 String?   @db.VarChar(8)
  min_rating            Int?      @default(1)
  max_rating            Int?      @default(5)
  created_at            DateTime? @default(now()) @db.Timestamp(6)

  @@unique([plvar, template_id, objid], map: "hrp5002_unique")
  @@index([quali], map: "idx_hrp5002_quali")
  @@index([template_id], map: "idx_hrp5002_template")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model hrpdev1 {
  id                  Int       @default(autoincrement())
  plvar               String?   @default("01") @db.VarChar(2)
  position_id         String    @db.VarChar(8)
  successor_pernr     String    @db.VarChar(8)
  endda               DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda               DateTime  @default(dbgenerated("'1900-01-01'::date")) @db.Date
  readiness           String?   @db.VarChar(2)
  readiness_pct       Decimal?  @db.Decimal(5, 2)
  ranking             Int?
  talent_pool         String?   @db.VarChar(4)
  potential_rating    String?   @db.VarChar(2)
  performance_rating  String?   @db.VarChar(2)
  mobility            String?   @db.VarChar(2)
  development_plan_id String?   @db.VarChar(20)
  gap_analysis        String?
  status              String?   @default("01") @db.VarChar(2)
  reviewed_date       DateTime? @db.Date
  reviewed_by         String?   @db.VarChar(8)
  created_at          DateTime? @default(now()) @db.Timestamp(6)
  updated_at          DateTime? @default(now()) @db.Timestamp(6)

  @@index([position_id], map: "idx_hrpdev1_position")
  @@index([readiness], map: "idx_hrpdev1_readiness")
  @@index([successor_pernr], map: "idx_hrpdev1_successor")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model import_jobs {
  id                 String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String               @db.Uuid
  import_type        String               @db.VarChar(50)
  file_name          String               @db.VarChar(500)
  file_size_bytes    Int
  mime_type          String               @db.VarChar(100)
  status             String               @default("pending_review") @db.VarChar(20)
  total_rows         Int                  @default(0)
  valid_rows         Int                  @default(0)
  error_rows         Int                  @default(0)
  created_rows       Int?
  updated_rows       Int?
  skipped_rows       Int?
  errors             Json?                @default("[]")
  warnings           Json?                @default("[]")
  preview_data       Json?
  uploaded_by        String               @db.Uuid
  executed_at        DateTime?            @db.Timestamptz(6)
  completed_at       DateTime?            @db.Timestamptz(6)
  created_at         DateTime             @default(now()) @db.Timestamptz(6)
  updated_at         DateTime             @default(now()) @db.Timestamptz(6)
  tenants            tenants              @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  import_skill_links import_skill_links[]

  @@index([tenant_id, status], map: "idx_import_jobs_status")
  @@index([tenant_id], map: "idx_import_jobs_tenant")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model import_skill_links {
  id            String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  import_job_id String       @db.Uuid
  tenant_id     String       @db.Uuid
  input_text    String       @db.VarChar(500)
  esco_skill_id String?      @db.Uuid
  similarity    Decimal?     @db.Decimal(5, 4)
  confidence    String       @default("none") @db.VarChar(10)
  accepted      Boolean?
  created_at    DateTime     @default(now()) @db.Timestamptz(6)
  esco_skills   esco_skills? @relation(fields: [esco_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  import_jobs   import_jobs  @relation(fields: [import_job_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants       tenants      @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([import_job_id], map: "idx_import_skill_links_job")
  @@index([tenant_id], map: "idx_import_skill_links_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model industry_ccnl_mapping {
  id             String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  industry_code  String         @db.VarChar(10)
  ccnl_code      String         @db.VarChar(50)
  is_primary     Boolean        @default(true)
  notes          String?
  created_at     DateTime       @default(now()) @db.Timestamptz(6)
  updated_at     DateTime       @default(now()) @db.Timestamptz(6)
  ccnl_contracts ccnl_contracts @relation(fields: [ccnl_code], references: [code], onDelete: NoAction)

  @@index([ccnl_code], map: "idx_industry_ccnl_code")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model industry_classifications {
  code                                String                                @id @db.VarChar(10)
  parent_code                         String?                               @db.VarChar(10)
  level                               Int                                   @db.SmallInt
  classification_system               String                                @default("NACE") @db.VarChar(10)
  name_it                             String                                @db.VarChar(500)
  name_en                             String?                               @db.VarChar(500)
  description_it                      String?
  description_en                      String?
  icon                                String?                               @db.VarChar(50)
  color                               String?                               @db.VarChar(7)
  is_active                           Boolean                               @default(true)
  embedding_it                        Unsupported("vector")?
  embedding_en                        Unsupported("vector")?
  embedding_model                     String?                               @db.VarChar(50)
  embedding_generated_at              DateTime?                             @db.Timestamptz(6)
  created_at                          DateTime                              @default(now()) @db.Timestamptz(6)
  updated_at                          DateTime                              @default(now()) @db.Timestamptz(6)
  deleted_at                          DateTime?                             @db.Timestamptz(6)
  industry_classifications            industry_classifications?             @relation("industry_classificationsToindustry_classifications", fields: [parent_code], references: [code], onDelete: NoAction, onUpdate: NoAction)
  other_industry_classifications      industry_classifications[]            @relation("industry_classificationsToindustry_classifications")
  industry_profiles                   industry_profiles[]
  occupation_industry_classifications occupation_industry_classifications[]
  tenant_industry_classifications     tenant_industry_classifications[]

  @@index([level], map: "idx_ic_level")
  @@index([name_en(ops: raw("gin_trgm_ops"))], map: "idx_ic_name_en_trgm", type: Gin)
  @@index([name_it(ops: raw("gin_trgm_ops"))], map: "idx_ic_name_it_trgm", type: Gin)
  @@index([parent_code], map: "idx_ic_parent")
  @@index([classification_system], map: "idx_ic_system")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model industry_occupation_mapping {
  id                    String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  nace_division_code    String    @db.VarChar(10)
  esco_occupation_uri   String    @db.VarChar(255)
  relevance_score       Decimal?  @default(1.0) @db.Decimal(3, 2)
  is_core_occupation    Boolean?  @default(false)
  typical_headcount_pct Decimal?  @db.Decimal(5, 2)
  min_company_size      Int?
  source                String?   @default("inferred") @db.VarChar(50)
  created_at            DateTime? @default(now()) @db.Timestamptz(6)
  updated_at            DateTime? @default(now()) @db.Timestamptz(6)

  @@unique([nace_division_code, esco_occupation_uri], map: "uk_industry_occupation")
  @@index([is_core_occupation], map: "idx_iom_core")
  @@index([nace_division_code], map: "idx_iom_nace")
  @@index([esco_occupation_uri], map: "idx_iom_occupation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model industry_profiles {
  id                       String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  code                     String                   @unique @db.VarChar(50)
  name                     String                   @db.VarChar(200)
  description              String?
  nace_class_code          String                   @db.VarChar(10)
  company_size_code        String                   @db.VarChar(20)
  typical_hierarchy        Json?
  typical_roles            String[]
  typical_departments      String[]
  typical_span_of_control  Json?
  department_templates     Json?
  esco_occupation_codes    String[]
  esco_skill_uris          String[]
  min_employees            Int?
  max_employees            Int?
  is_active                Boolean                  @default(true)
  created_at               DateTime                 @default(now()) @db.Timestamptz(6)
  updated_at               DateTime                 @default(now()) @db.Timestamptz(6)
  blueprint_templates      blueprint_templates[]
  business_processes       business_processes[]
  company_sizes            company_sizes            @relation(fields: [company_size_code], references: [code], onDelete: NoAction, onUpdate: NoAction)
  industry_classifications industry_classifications @relation(fields: [nace_class_code], references: [code], onDelete: NoAction, onUpdate: NoAction)
  tenants                  tenants[]

  @@unique([nace_class_code, company_size_code], map: "uq_profile_nace_size")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model integration_sync_logs {
  id                String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  integration_id    String       @db.Uuid
  tenant_id         String       @db.Uuid
  status            String?      @default("pending") @db.VarChar(50)
  sync_type         String?      @default("incremental") @db.VarChar(50)
  records_processed Int?         @default(0)
  records_created   Int?         @default(0)
  records_updated   Int?         @default(0)
  records_failed    Int?         @default(0)
  error_message     String?
  started_at        DateTime?    @default(now()) @db.Timestamptz(6)
  completed_at      DateTime?    @db.Timestamptz(6)
  created_at        DateTime?    @default(now()) @db.Timestamptz(6)
  integrations      integrations @relation(fields: [integration_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants           tenants      @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([integration_id], map: "idx_integration_sync_logs_int")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model integrations {
  id                    String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id             String                  @db.Uuid
  name                  String                  @db.VarChar(255)
  type                  String                  @db.VarChar(50)
  provider              String                  @db.VarChar(100)
  status                String?                 @default("pending_setup") @db.VarChar(50)
  config                Json?
  sync_frequency        String?                 @default("daily") @db.VarChar(50)
  sync_direction        String?                 @default("bidirectional") @db.VarChar(50)
  field_mappings        Json?
  last_sync_at          DateTime?               @db.Timestamptz(6)
  last_sync_status      String?                 @db.VarChar(50)
  error_count           Int?                    @default(0)
  created_at            DateTime?               @default(now()) @db.Timestamptz(6)
  updated_at            DateTime?               @default(now()) @db.Timestamptz(6)
  integration_sync_logs integration_sync_logs[]
  tenants               tenants                 @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([tenant_id, status], map: "idx_integrations_status")
  @@index([tenant_id], map: "idx_integrations_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model internal_applications {
  id                      String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  job_posting_id          String?                @db.Uuid
  employee_id             String?                @db.Uuid
  cover_letter            String?
  motivation              String?
  relevant_experience     String?
  matched_skills          String[]
  skill_match_score       Int?
  status                  String?                @default("submitted") @db.VarChar(30)
  current_manager_id      String?                @db.Uuid
  manager_approval_status String?                @db.VarChar(20)
  manager_approval_date   DateTime?              @db.Timestamp(6)
  manager_notes           String?
  hr_reviewer_id          String?                @db.Uuid
  hr_notes                String?
  hr_score                Int?
  interview_scheduled     Boolean?               @default(false)
  interview_date          DateTime?              @db.Timestamp(6)
  interview_feedback      String?
  outcome_notes           String?
  rejected_reason         String?                @db.VarChar(200)
  submitted_at            DateTime?              @default(now()) @db.Timestamp(6)
  reviewed_at             DateTime?              @db.Timestamp(6)
  decided_at              DateTime?              @db.Timestamp(6)
  created_at              DateTime?              @default(now()) @db.Timestamp(6)
  updated_at              DateTime?              @default(now()) @db.Timestamp(6)
  employees               employees?             @relation(fields: [employee_id], references: [id], onUpdate: NoAction, map: "fk_ia_employee")
  internal_job_postings   internal_job_postings? @relation(fields: [job_posting_id], references: [id], onUpdate: NoAction, map: "fk_ia_posting")

  @@index([employee_id], map: "idx_applications_employee")
  @@index([job_posting_id], map: "idx_applications_job")
  @@index([status], map: "idx_applications_status")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model internal_job_alerts {
  id           String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id  String?    @db.Uuid
  keywords     String?
  departments  String[]
  job_families String[]
  job_levels   String[]
  locations    String[]
  work_types   String[]
  frequency    String?    @default("daily") @db.VarChar(20)
  is_active    Boolean?   @default(true)
  created_at   DateTime?  @default(now()) @db.Timestamp(6)
  updated_at   DateTime?  @default(now()) @db.Timestamp(6)
  deleted_at   DateTime?  @db.Timestamptz(6)
  employees    employees? @relation(fields: [employee_id], references: [id], onUpdate: NoAction, map: "fk_ija_employee")

  @@index([employee_id], map: "idx_internal_job_alerts_employee_id")
}

model internal_job_bookmarks {
  id                    String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  job_posting_id        String?                @db.Uuid
  employee_id           String?                @db.Uuid
  created_at            DateTime?              @default(now()) @db.Timestamp(6)
  employees             employees?             @relation(fields: [employee_id], references: [id], onUpdate: NoAction, map: "fk_ijb_employee")
  internal_job_postings internal_job_postings? @relation(fields: [job_posting_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ijb_posting")

  @@unique([job_posting_id, employee_id])
  @@index([employee_id], map: "idx_bookmarks_employee")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model internal_job_postings {
  id                     String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String                   @db.Uuid
  title                  String                   @db.VarChar(255)
  department             String?                  @db.VarChar(100)
  team                   String?                  @db.VarChar(100)
  location               String?                  @db.VarChar(200)
  work_type              String?                  @default("hybrid") @db.VarChar(30)
  summary                String?
  responsibilities       String?
  requirements           String?
  nice_to_have           String?
  job_level              String?                  @db.VarChar(50)
  job_family             String?                  @db.VarChar(100)
  salary_min             Int?
  salary_max             Int?
  currency               String?                  @default("EUR") @db.VarChar(10)
  show_salary            Boolean?                 @default(false)
  status                 String?                  @default("draft") @db.VarChar(30)
  visibility             String?                  @default("all_employees") @db.VarChar(30)
  min_tenure_months      Int?                     @default(12)
  min_rating             Int?
  required_skills        String[]
  posted_at              DateTime?                @db.Timestamp(6)
  expires_at             DateTime?                @db.Timestamp(6)
  target_start_date      DateTime?                @db.Date
  hiring_manager_id      String?                  @db.Uuid
  hr_contact_id          String?                  @db.Uuid
  views_count            Int?                     @default(0)
  applications_count     Int?                     @default(0)
  created_by             String?                  @db.Uuid
  created_at             DateTime?                @default(now()) @db.Timestamp(6)
  updated_at             DateTime?                @default(now()) @db.Timestamp(6)
  created_by_employee_id String?                  @db.Uuid
  internal_applications  internal_applications[]
  internal_job_bookmarks internal_job_bookmarks[]
  employees              employees?               @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "internal_job_postings_created_by_fkey_emp")
  tenants                tenants                  @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  internal_job_views     internal_job_views[]
  recruiting_interviews  recruiting_interviews[]
  saved_jobs             saved_jobs[]

  @@index([created_by_employee_id], map: "idx_internal_job_postings_created_by_employee_id")
  @@index([department], map: "idx_job_postings_department")
  @@index([status], map: "idx_job_postings_status")
  @@index([tenant_id], map: "idx_job_postings_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model internal_job_views {
  id                    String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  job_posting_id        String?                @db.Uuid
  employee_id           String?                @db.Uuid
  viewed_at             DateTime?              @default(now()) @db.Timestamp(6)
  created_at            DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at            DateTime?              @default(now()) @db.Timestamptz(6)
  employees             employees?             @relation(fields: [employee_id], references: [id], onUpdate: NoAction, map: "fk_ijv_employee")
  internal_job_postings internal_job_postings? @relation(fields: [job_posting_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ijv_posting")

  @@index([employee_id], map: "idx_internal_job_views_employee_id")
  @@index([job_posting_id], map: "idx_job_views_posting")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model internal_mobility_postings {
  id                                                                String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                                         String     @db.Uuid
  title                                                             String     @db.VarChar(255)
  location_id                                                       String?    @db.Uuid
  work_type                                                         String?    @default("hybrid") @db.VarChar(30)
  summary                                                           String?
  job_level                                                         String?    @db.VarChar(50)
  job_family                                                        String?    @db.VarChar(100)
  required_skills                                                   String[]
  min_tenure_months                                                 Int?       @default(6)
  min_rating                                                        Decimal?   @db.Decimal(3, 1)
  salary_min                                                        Decimal?   @db.Decimal(12, 2)
  salary_max                                                        Decimal?   @db.Decimal(12, 2)
  currency                                                          String?    @default("EUR") @db.VarChar(10)
  status                                                            String?    @default("draft") @db.VarChar(30)
  posted_at                                                         DateTime?  @db.Timestamptz(6)
  expires_at                                                        DateTime?  @db.Timestamptz(6)
  hiring_manager_id                                                 String?    @db.Uuid
  created_by                                                        String?    @db.Uuid
  created_at                                                        DateTime?  @default(now()) @db.Timestamptz(6)
  updated_at                                                        DateTime?  @default(now()) @db.Timestamptz(6)
  org_unit_id                                                       String?    @db.Uuid
  employees_internal_mobility_postings_created_byToemployees        employees? @relation("internal_mobility_postings_created_byToemployees", fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees_internal_mobility_postings_hiring_manager_idToemployees employees? @relation("internal_mobility_postings_hiring_manager_idToemployees", fields: [hiring_manager_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  locations                                                         locations? @relation(fields: [location_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  org_units                                                         org_units? @relation(fields: [org_unit_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                                                           tenants    @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([created_by], map: "idx_internal_mobility_postings_created_by")
  @@index([hiring_manager_id], map: "idx_internal_mobility_postings_hiring_manager_id")
  @@index([location_id], map: "idx_internal_mobility_postings_location_id")
  @@index([status], map: "idx_internal_mobility_postings_status")
  @@index([tenant_id], map: "idx_internal_mobility_postings_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model internal_mobility_requests {
  id                     String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String                 @db.Uuid
  employee_id            String                 @db.Uuid
  current_position       String?                @db.VarChar(200)
  current_department_id  String?                @db.Uuid
  target_position        String?                @db.VarChar(200)
  target_department_id   String?                @db.Uuid
  target_location_id     String?                @db.Uuid
  motivation             String?
  career_goals           String?
  timeline               String?                @db.VarChar(50)
  status                 String?                @default("pending") @db.VarChar(50)
  manager_notes          String?
  hr_notes               String?
  created_at             DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?              @default(now()) @db.Timestamptz(6)
  request_embedding      Unsupported("vector")?
  embedding_model        String?                @db.VarChar(100)
  embedding_generated_at DateTime?              @db.Timestamptz(6)
  employees              employees              @relation(fields: [employee_id], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_imr_employee")
  tenants                tenants                @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([request_embedding], map: "idx_mobility_requests_embedding")
  @@index([employee_id], map: "idx_mobility_requests_employee")
  @@index([tenant_id, status], map: "idx_mobility_requests_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model interview_feedback {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  interview_id      String    @db.Uuid
  interviewer_id    String    @db.Uuid
  overall_rating    Int?
  recommendation    String?   @db.VarChar(50)
  strengths         String?
  concerns          String?
  notes             String?
  competency_scores Json?
  submitted_at      DateTime? @default(now()) @db.Timestamptz(6)
  created_at        DateTime? @default(now()) @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model interviews {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  application_id   String    @db.Uuid
  interview_type   String?   @default("general") @db.VarChar(50)
  interview_round  Int?      @default(1)
  scheduled_at     DateTime? @db.Timestamptz(6)
  duration_minutes Int?      @default(60)
  location         String?   @db.VarChar(255)
  meeting_link     String?   @db.VarChar(500)
  interviewers     Json?
  status           String?   @default("scheduled") @db.VarChar(50)
  overall_rating   Int?
  recommendation   String?   @db.VarChar(50)
  feedback_notes   String?
  strengths        String?
  concerns         String?
  scorecard        Json?
  completed_at     DateTime? @db.Timestamptz(6)
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  updated_at       DateTime? @default(now()) @db.Timestamptz(6)

  @@index([application_id], map: "idx_interviews_application")
  @@index([scheduled_at], map: "idx_interviews_scheduled")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model job_analysis {
  id                      String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id               String            @db.Uuid
  job_title               String            @db.VarChar(200)
  job_family_id           String?           @db.Uuid
  job_level               String?           @db.VarChar(50)
  scope_of_work           String?
  key_responsibilities    Json?             @default("[]")
  required_qualifications Json?             @default("[]")
  esco_occupation_uri     String?           @db.VarChar(500)
  complexity_score        Int?
  analyst_id              String?           @db.Uuid
  analysis_date           DateTime?         @db.Date
  status                  String?           @default("draft") @db.VarChar(30)
  created_at              DateTime?         @default(now()) @db.Timestamptz(6)
  updated_at              DateTime?         @default(now()) @db.Timestamptz(6)
  org_unit_id             String?           @db.Uuid
  employees               employees?        @relation(fields: [analyst_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  job_families            job_families?     @relation(fields: [job_family_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  org_units               org_units?        @relation(fields: [org_unit_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                 tenants           @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  job_evaluations         job_evaluations[]

  @@index([analyst_id], map: "idx_job_analysis_analyst_id")
  @@index([job_family_id], map: "idx_job_analysis_family")
  @@index([tenant_id], map: "idx_job_analysis_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model job_evaluations {
  id                     String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String        @db.Uuid
  job_analysis_id        String?       @db.Uuid
  job_title              String        @db.VarChar(200)
  evaluation_method      String?       @default("point_factor") @db.VarChar(50)
  total_points           Int?
  job_grade              String?       @db.VarChar(20)
  knowledge_points       Int?
  problem_solving_points Int?
  accountability_points  Int?
  evaluated_by           String?       @db.Uuid
  evaluation_date        DateTime?     @db.Date
  status                 String?       @default("draft") @db.VarChar(30)
  notes                  String?
  created_at             DateTime?     @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?     @default(now()) @db.Timestamptz(6)
  employees              employees?    @relation(fields: [evaluated_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  job_analysis           job_analysis? @relation(fields: [job_analysis_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                tenants       @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([job_analysis_id], map: "idx_job_evaluations_analysis")
  @@index([evaluated_by], map: "idx_job_evaluations_evaluated_by")
  @@index([tenant_id], map: "idx_job_evaluations_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model job_families {
  id                 String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String         @db.Uuid
  code               String         @db.VarChar(50)
  name               String         @db.VarChar(200)
  description        String?
  parent_id          String?        @db.Uuid
  is_active          Boolean?       @default(true)
  created_at         DateTime?      @default(now()) @db.Timestamptz(6)
  updated_at         DateTime?      @default(now()) @db.Timestamptz(6)
  deleted_at         DateTime?      @db.Timestamptz(6)
  job_analysis       job_analysis[]
  job_families       job_families?  @relation("job_familiesTojob_families", fields: [parent_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  other_job_families job_families[] @relation("job_familiesTojob_families")
  tenants            tenants        @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([tenant_id, code])
  @@index([parent_id], map: "idx_job_families_parent_id")
  @@index([tenant_id], map: "idx_job_families_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model job_kpi_distribution {
  id                   String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  job_template_id      String        @db.Uuid
  org_unit_kpi_id      String        @db.Uuid
  accountability_level String        @db.VarChar(20)
  weight_percentage    Int
  notes                String?
  created_at           DateTime?     @default(now()) @db.Timestamptz(6)
  updated_at           DateTime?     @default(now()) @db.Timestamptz(6)
  job_templates        job_templates @relation(fields: [job_template_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  org_unit_kpis        org_unit_kpis @relation(fields: [org_unit_kpi_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([job_template_id, org_unit_kpi_id])
  @@index([job_template_id], map: "idx_job_kpi_distribution_job")
  @@index([org_unit_kpi_id], map: "idx_job_kpi_distribution_kpi")
  @@index([accountability_level], map: "idx_job_kpi_distribution_level")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model job_kpis {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  job_template_id  String    @db.Uuid
  kpi_code         String    @db.VarChar(20)
  name_it          String    @db.VarChar(255)
  name_en          String?   @db.VarChar(255)
  description      String?
  measurement_type String?   @db.VarChar(20)
  unit             String?   @db.VarChar(20)
  target_value     Decimal?  @db.Decimal(12, 2)
  min_acceptable   Decimal?  @db.Decimal(12, 2)
  stretch_target   Decimal?  @db.Decimal(12, 2)
  kpi_category     String?   @db.VarChar(50)
  frequency        String?   @db.VarChar(20)
  weight           Decimal?  @db.Decimal(5, 2)
  created_at       DateTime? @default(now()) @db.Timestamptz(6)

  @@index([kpi_category], map: "idx_job_kpis_category")
  @@index([job_template_id], map: "idx_job_kpis_job")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model job_market_postings {
  id                   String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  source_id            String?             @db.Uuid
  external_id          String?             @db.VarChar(255)
  job_title            String              @db.VarChar(500)
  company_name         String?             @db.VarChar(255)
  company_size         String?             @db.VarChar(50)
  location             String?             @db.VarChar(255)
  location_type        String?             @db.VarChar(50)
  country_code         String?             @db.VarChar(3)
  region               String?             @db.VarChar(100)
  job_description      String?
  salary_min           Decimal?            @db.Decimal(12, 2)
  salary_max           Decimal?            @db.Decimal(12, 2)
  salary_currency      String?             @default("EUR") @db.VarChar(3)
  salary_period        String?             @default("yearly") @db.VarChar(20)
  employment_type      String?             @db.VarChar(50)
  experience_level     String?             @db.VarChar(50)
  experience_years_min Int?
  experience_years_max Int?
  education_level      String?             @db.VarChar(100)
  industry             String?             @db.VarChar(100)
  nace_code            String?             @db.VarChar(10)
  isco_code            String?             @db.VarChar(10)
  extracted_skills     Json?               @default("[]")
  mapped_esco_skills   Json?               @default("[]")
  posting_date         DateTime?           @db.Date
  expiry_date          DateTime?           @db.Date
  source_url           String?             @db.VarChar(1000)
  is_active            Boolean?            @default(true)
  raw_data             Json?
  created_at           DateTime?           @default(now()) @db.Timestamptz(6)
  updated_at           DateTime?           @default(now()) @db.Timestamptz(6)
  deleted_at           DateTime?           @db.Timestamptz(6)
  job_market_sources   job_market_sources? @relation(fields: [source_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([source_id, external_id])
  @@index([posting_date], map: "idx_job_postings_date")
  @@index([industry], map: "idx_job_postings_industry")
  @@index([isco_code], map: "idx_job_postings_isco")
  @@index([country_code, region], map: "idx_job_postings_location")
  @@index([salary_min, salary_max], map: "idx_job_postings_salary")
  @@index([mapped_esco_skills], map: "idx_job_postings_skills", type: Gin)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model job_market_sources {
  id                    String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  source_name           String                @db.VarChar(100)
  source_type           String                @db.VarChar(50)
  source_url            String?               @db.VarChar(500)
  api_config            Json?
  scraper_config        Json?
  is_active             Boolean?              @default(true)
  crawl_frequency_hours Int?                  @default(24)
  last_crawl_at         DateTime?             @db.Timestamptz(6)
  next_crawl_at         DateTime?             @db.Timestamptz(6)
  total_jobs_collected  Int?                  @default(0)
  created_at            DateTime?             @default(now()) @db.Timestamptz(6)
  updated_at            DateTime?             @default(now()) @db.Timestamptz(6)
  deleted_at            DateTime?             @db.Timestamptz(6)
  job_market_postings   job_market_postings[]
}

model job_market_statistics {
  id                           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  stat_date                    DateTime  @db.Date
  stat_period                  String?   @default("daily") @db.VarChar(20)
  country_code                 String?   @db.VarChar(3)
  region                       String?   @db.VarChar(100)
  industry                     String?   @db.VarChar(100)
  isco_code                    String?   @db.VarChar(10)
  total_postings               Int?      @default(0)
  avg_salary_min               Decimal?  @db.Decimal(12, 2)
  avg_salary_max               Decimal?  @db.Decimal(12, 2)
  median_salary                Decimal?  @db.Decimal(12, 2)
  top_skills                   Json?     @default("[]")
  top_companies                Json?     @default("[]")
  experience_distribution      Json?     @default("{}")
  employment_type_distribution Json?     @default("{}")
  remote_percentage            Decimal?  @db.Decimal(5, 2)
  created_at                   DateTime? @default(now()) @db.Timestamptz(6)

  @@unique([stat_date, stat_period, country_code, region, industry, isco_code])
  @@index([stat_date], map: "idx_job_stats_date")
  @@index([country_code, region], map: "idx_job_stats_location")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model job_postings {
  id                     String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String                 @db.Uuid
  requisition_id         String?                @db.Uuid
  job_template_id        String?                @db.Uuid
  title                  String                 @db.VarChar(200)
  location_id            String?                @db.Uuid
  description            String?
  requirements           String?
  responsibilities       String?
  qualifications         String?
  benefits               String?
  salary_range_min       Decimal?               @db.Decimal(12, 2)
  salary_range_max       Decimal?               @db.Decimal(12, 2)
  salary_currency        String?                @default("EUR") @db.VarChar(3)
  employment_type        String?                @db.VarChar(50)
  experience_level       String?                @db.VarChar(50)
  remote_policy          String?                @db.VarChar(50)
  status                 String?                @default("draft") @db.VarChar(50)
  published_at           DateTime?              @db.Timestamptz(6)
  closes_at              DateTime?              @db.Timestamptz(6)
  filled_at              DateTime?              @db.Timestamptz(6)
  views_count            Int?                   @default(0)
  applications_count     Int?                   @default(0)
  created_by             String?                @db.Uuid
  created_at             DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?              @default(now()) @db.Timestamptz(6)
  content_embedding      Unsupported("vector")?
  embedding_text_hash    String?                @db.VarChar(64)
  embedding_model        String?                @db.VarChar(100)
  embedding_generated_at DateTime?              @db.Timestamptz(6)
  org_unit_id            String?                @db.Uuid
  org_units              org_units?             @relation(fields: [org_unit_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                tenants                @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([content_embedding], map: "idx_job_postings_embedding")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model job_postings_raw {
  id               String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id        String             @db.Uuid
  crawl_run_id     String             @db.Uuid
  source           String             @db.VarChar(100)
  source_url       String
  source_id        String?            @db.VarChar(255)
  job_title        String
  company_name     String?
  company_industry String?            @db.VarChar(100)
  ateco_code       String?            @db.VarChar(20)
  location_city    String?            @db.VarChar(255)
  location_region  String?            @db.VarChar(255)
  location_country String?            @default("IT") @db.VarChar(100)
  remote_type      String?            @db.VarChar(50)
  job_type         String?            @db.VarChar(50)
  experience_level String?            @db.VarChar(50)
  salary_min       Decimal?           @db.Decimal(12, 2)
  salary_max       Decimal?           @db.Decimal(12, 2)
  salary_currency  String?            @default("EUR") @db.VarChar(10)
  salary_period    String?            @default("year") @db.VarChar(20)
  description_raw  String?
  requirements_raw String?
  benefits_raw     String?
  skills_required  Json?              @default("[]")
  skills_preferred Json?              @default("[]")
  posted_at        DateTime?          @db.Date
  expires_at       DateTime?          @db.Date
  extracted_at     DateTime?          @default(now()) @db.Timestamptz(6)
  created_at       DateTime?          @default(now()) @db.Timestamptz(6)
  extracted_skills extracted_skills[]
  crawl_runs       crawl_runs         @relation(fields: [crawl_run_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants          tenants            @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, source, source_url])
  @@index([ateco_code], map: "idx_job_postings_raw_ateco")
  @@index([location_city, location_region], map: "idx_job_postings_raw_location")
  @@index([crawl_run_id], map: "idx_job_postings_raw_run")
  @@index([source], map: "idx_job_postings_raw_source")
  @@index([tenant_id], map: "idx_job_postings_raw_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model job_qualifications {
  id                     String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  job_template_id        String    @db.Uuid
  qualification_type     String    @db.VarChar(30)
  description_it         String
  description_en         String?
  education_level        String?   @db.VarChar(50)
  field_of_study         String?   @db.VarChar(100)
  years_required         Int?
  certification_name     String?   @db.VarChar(100)
  certification_provider String?   @db.VarChar(100)
  language_code          String?   @db.VarChar(5)
  language_level         String?   @db.VarChar(5)
  is_required            Boolean?  @default(true)
  created_at             DateTime? @default(now()) @db.Timestamptz(6)

  @@index([job_template_id], map: "idx_job_qualifications_job")
  @@index([qualification_type], map: "idx_job_qualifications_type")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model job_skills {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  job_template_id  String    @db.Uuid
  skill_name_it    String    @db.VarChar(255)
  skill_name_en    String?   @db.VarChar(255)
  esco_skill_uri   String?   @db.VarChar(255)
  esco_skill_code  String?   @db.VarChar(50)
  esco_skill_type  String?   @db.VarChar(20)
  required_level   Int?
  is_required      Boolean?  @default(true)
  importance       String?   @db.VarChar(20)
  skill_category   String?   @db.VarChar(50)
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  primary_category String?   @db.VarChar(20)
  cognitive_level  Int?
  transferability  String?   @db.VarChar(20)
  skill_cluster_id String?   @db.Uuid

  @@index([skill_category], map: "idx_job_skills_category")
  @@index([job_template_id], map: "idx_job_skills_job")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model job_task_distribution {
  id                        String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  job_template_id           String         @db.Uuid
  org_unit_task_id          String         @db.Uuid
  responsibility_percentage Int
  is_primary_owner          Boolean?       @default(false)
  notes                     String?
  created_at                DateTime?      @default(now()) @db.Timestamptz(6)
  updated_at                DateTime?      @default(now()) @db.Timestamptz(6)
  job_templates             job_templates  @relation(fields: [job_template_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  org_unit_tasks            org_unit_tasks @relation(fields: [org_unit_task_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([job_template_id, org_unit_task_id])
  @@index([job_template_id], map: "idx_job_task_distribution_job")
  @@index([org_unit_task_id], map: "idx_job_task_distribution_task")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model job_tasks {
  id                      String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  job_template_id         String    @db.Uuid
  task_order              Int
  description_it          String
  description_en          String?
  frequency               String?   @db.VarChar(20)
  importance              String?   @db.VarChar(20)
  time_allocation_percent Int?
  esco_skill_uri          String?   @db.VarChar(255)
  created_at              DateTime? @default(now()) @db.Timestamptz(6)

  @@index([job_template_id], map: "idx_job_tasks_job")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model job_template_skills {
  id                   String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id            String                @db.Uuid
  job_template_id      String                @db.Uuid
  skill_id             String?               @db.Uuid
  custom_skill_id      String?               @db.Uuid
  required_proficiency Int                   @default(3) @db.SmallInt
  importance           String                @default("core") @db.VarChar(20)
  source               String                @default("esco_inferred") @db.VarChar(20)
  rationale            String?
  created_at           DateTime              @default(now()) @db.Timestamptz(6)
  tenant_custom_skills tenant_custom_skills? @relation(fields: [custom_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  job_templates        job_templates         @relation(fields: [job_template_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  esco_skills          esco_skills?          @relation(fields: [skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants              tenants               @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([job_template_id, skill_id, custom_skill_id], map: "uq_jts_template_skill")
  @@index([job_template_id], map: "idx_jts_template")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model job_templates {
  id                          String                        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  org_unit_template_id        String?                       @db.Uuid
  profile_id                  String?                       @db.Uuid
  job_code                    String                        @db.VarChar(20)
  title_it                    String                        @db.VarChar(255)
  title_en                    String?                       @db.VarChar(255)
  description                 String?
  summary                     String?
  org_level                   Int?
  is_management               Boolean?                      @default(false)
  employment_type             String?                       @default("full_time") @db.VarChar(20)
  esco_occupation_uri         String?                       @db.VarChar(255)
  esco_occupation_code        String?                       @db.VarChar(20)
  esco_occupation_title       String?                       @db.VarChar(255)
  salary_band_code            String?                       @db.VarChar(20)
  salary_min                  Decimal?                      @db.Decimal(12, 2)
  salary_max                  Decimal?                      @db.Decimal(12, 2)
  currency                    String?                       @default("EUR") @db.VarChar(3)
  source_type                 String?                       @db.VarChar(50)
  source_reference            String?
  is_active                   Boolean?                      @default(true)
  created_at                  DateTime?                     @default(now()) @db.Timestamptz(6)
  updated_at                  DateTime?                     @default(now()) @db.Timestamptz(6)
  sap_stell                   String?                       @db.VarChar(20)
  sap_stext                   String?                       @db.VarChar(100)
  sap_persk                   String?                       @db.VarChar(10)
  sap_sync_date               DateTime?                     @db.Timestamp(6)
  embedding_en                Unsupported("vector")?
  embedding_it                Unsupported("vector")?
  embedding_model             String?                       @db.VarChar(100)
  embedding_generated_at      DateTime?                     @db.Timestamptz(6)
  deleted_at                  DateTime?                     @db.Timestamptz(6)
  is_prototype_generated      Boolean?                      @default(false)
  tenant_id                   String                        @db.Uuid
  job_kpi_distribution        job_kpi_distribution[]
  job_task_distribution       job_task_distribution[]
  job_template_skills         job_template_skills[]
  tenants                     tenants                       @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  position_skill_requirements position_skill_requirements[]
  role_skill_requirements     role_skill_requirements[]

  @@index([embedding_en], map: "idx_job_templates_embedding_en")
  @@index([embedding_it], map: "idx_job_templates_embedding_it")
  @@index([esco_occupation_code], map: "idx_job_templates_esco")
  @@index([org_level], map: "idx_job_templates_level")
  @@index([profile_id], map: "idx_job_templates_profile")
  @@index([tenant_id], map: "idx_job_templates_tenant")
  @@index([org_unit_template_id], map: "idx_job_templates_unit")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model job_title_courses {
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id        String   @db.Uuid
  job_title        String   @db.VarChar(100)
  course_id        String   @db.Uuid
  requirement_type String   @default("recommended") @db.VarChar(20)
  seniority_level  String   @default("all") @db.VarChar(20)
  priority         Int      @default(100) @db.SmallInt
  rationale        String?
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  courses          courses  @relation(fields: [course_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants          tenants  @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, job_title, course_id], map: "uq_jtc")
  @@index([tenant_id, job_title], map: "idx_jtc_tenant_role")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model job_title_learning_paths {
  id               String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id        String         @db.Uuid
  job_title        String         @db.VarChar(100)
  learning_path_id String         @db.Uuid
  seniority_level  String         @default("all") @db.VarChar(20)
  is_primary       Boolean        @default(true)
  created_at       DateTime       @default(now()) @db.Timestamptz(6)
  learning_paths   learning_paths @relation(fields: [learning_path_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants          tenants        @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, job_title, learning_path_id], map: "uq_jtlp")
  @@index([tenant_id, job_title], map: "idx_jtlp_tenant_role")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model key_results {
  id               String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  okr_id           String          @db.Uuid
  description      String
  metric_type      String?         @default("percentage") @db.VarChar(50)
  start_value      Decimal?        @default(0) @db.Decimal(15, 2)
  target_value     Decimal         @db.Decimal(15, 2)
  current_value    Decimal?        @default(0) @db.Decimal(15, 2)
  unit             String?         @db.VarChar(50)
  progress_percent Decimal?        @default(0) @db.Decimal(5, 2)
  status           String?         @default("on_track") @db.VarChar(50)
  weight           Decimal?        @default(1.0) @db.Decimal(5, 2)
  owner_id         String?         @db.Uuid
  created_at       DateTime?       @default(now()) @db.Timestamptz(6)
  updated_at       DateTime?       @default(now()) @db.Timestamptz(6)
  tenant_id        String          @db.Uuid
  last_check_in_at DateTime?       @db.Timestamptz(6)
  confidence_level Int?            @default(3)
  tenants          tenants         @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  okr_check_ins    okr_check_ins[]

  @@index([okr_id], map: "idx_key_results_okr")
  @@index([owner_id], map: "idx_key_results_owner")
  @@index([status], map: "idx_key_results_status")
  @@index([tenant_id], map: "idx_key_results_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model learning_bookmarks {
  id               String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id      String?    @db.Uuid
  course_id        String?    @db.Uuid
  learning_path_id String?    @db.Uuid
  created_at       DateTime?  @default(now()) @db.Timestamptz(6)
  courses          courses?   @relation(fields: [course_id], references: [id], onUpdate: NoAction, map: "fk_lb_course")
  employees        employees? @relation(fields: [employee_id], references: [id], onUpdate: NoAction, map: "fk_lb_employee")

  @@index([course_id], map: "idx_learning_bookmarks_course_id")
  @@index([employee_id], map: "idx_learning_bookmarks_employee_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model learning_content_providers {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String    @db.Uuid
  provider_name     String    @db.VarChar(100)
  provider_type     String?   @db.VarChar(50)
  api_endpoint      String?   @db.VarChar(500)
  api_key_encrypted String?   @db.VarChar(500)
  sync_enabled      Boolean?  @default(false)
  last_sync_at      DateTime? @db.Timestamptz(6)
  config            Json?
  is_active         Boolean?  @default(true)
  created_at        DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at        DateTime? @db.Timestamptz(6)
  tenants           tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_learning_content_providers_tenant_id")
}

model learning_path_courses {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  learning_path_id  String?   @db.Uuid
  course_id         String?   @db.Uuid
  sequence_order    Int
  is_mandatory      Boolean?  @default(true)
  unlock_after_days Int?      @default(0)
  created_at        DateTime? @default(now()) @db.Timestamptz(6)
  courses           courses?  @relation(fields: [course_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_lpc_course")

  @@index([course_id], map: "idx_learning_path_courses_course_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model learning_path_enrollments {
  id                                                                     String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id                                                            String?         @db.Uuid
  learning_path_id                                                       String?         @db.Uuid
  enrolled_at                                                            DateTime?       @default(now()) @db.Timestamptz(6)
  enrolled_by                                                            String?         @db.Uuid
  status                                                                 String?         @default("enrolled") @db.VarChar(20)
  progress_percent                                                       Int?            @default(0)
  started_at                                                             DateTime?       @db.Timestamptz(6)
  completed_at                                                           DateTime?       @db.Timestamptz(6)
  due_date                                                               DateTime?       @db.Date
  created_at                                                             DateTime?       @default(now()) @db.Timestamptz(6)
  enrolled_by_employee_id                                                String?         @db.Uuid
  tenant_id                                                              String          @db.Uuid
  employees_learning_path_enrollments_employee_idToemployees             employees?      @relation("learning_path_enrollments_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_learning_path_enrollments_employee")
  learning_paths                                                         learning_paths? @relation(fields: [learning_path_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_learning_path_enrollments_path")
  employees_learning_path_enrollments_enrolled_by_employee_idToemployees employees?      @relation("learning_path_enrollments_enrolled_by_employee_idToemployees", fields: [enrolled_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "learning_path_enrollments_enrolled_by_fkey_emp")
  tenants                                                                tenants         @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([employee_id], map: "idx_learning_path_enroll_emp")
  @@index([learning_path_id], map: "idx_learning_path_enroll_path")
  @@index([enrolled_by_employee_id], map: "idx_learning_path_enrollments_enrolled_by_employee_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model learning_paths {
  id                        String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                 String                      @db.Uuid
  code                      String                      @db.VarChar(50)
  title                     String                      @db.VarChar(255)
  title_en                  String?                     @db.VarChar(255)
  description               String?
  target_role               String?                     @db.VarChar(100)
  target_job_template_id    String?                     @db.Uuid
  estimated_duration_hours  Decimal?                    @db.Decimal(6, 2)
  skill_level               String?                     @db.VarChar(20)
  path_type                 String?                     @default("career") @db.VarChar(50)
  is_active                 Boolean?                    @default(true)
  is_mandatory              Boolean?                    @default(false)
  created_by                String?                     @db.Uuid
  created_at                DateTime?                   @default(now()) @db.Timestamptz(6)
  updated_at                DateTime?                   @default(now()) @db.Timestamptz(6)
  created_by_employee_id    String?                     @db.Uuid
  embedding                 Unsupported("vector")?
  embedding_text_hash       String?                     @db.VarChar(64)
  embedding_model           String?                     @db.VarChar(100)
  embedding_generated_at    DateTime?                   @db.Timestamptz(6)
  deleted_at                DateTime?                   @db.Timestamptz(6)
  title_it                  String?                     @db.VarChar(255)
  description_it            String?
  description_en            String?
  job_title_learning_paths  job_title_learning_paths[]
  learning_path_enrollments learning_path_enrollments[]
  employees                 employees?                  @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "learning_paths_created_by_fkey_emp")
  tenants                   tenants                     @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_by_employee_id], map: "idx_learning_paths_created_by_employee_id")
  @@index([embedding], map: "idx_learning_paths_embedding")
  @@index([tenant_id], map: "idx_learning_paths_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model learning_ratings {
  id            String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  course_id     String?    @db.Uuid
  employee_id   String?    @db.Uuid
  rating        Int?
  review        String?
  helpful_count Int?       @default(0)
  created_at    DateTime?  @default(now()) @db.Timestamptz(6)
  courses       courses?   @relation(fields: [course_id], references: [id], onUpdate: NoAction, map: "fk_lra_course")
  employees     employees? @relation(fields: [employee_id], references: [id], onUpdate: NoAction, map: "fk_lra_employee")

  @@index([course_id], map: "idx_learning_ratings_course_id")
  @@index([employee_id], map: "idx_learning_ratings_employee_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model learning_recommendations {
  id                   String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id          String?    @db.Uuid
  course_id            String?    @db.Uuid
  learning_path_id     String?    @db.Uuid
  certification_id     String?    @db.Uuid
  recommendation_type  String     @db.VarChar(50)
  reason               String
  priority             Int?       @default(5)
  confidence_score     Decimal?   @db.Decimal(3, 2)
  skill_gaps_addressed String[]
  source_context       Json?
  status               String?    @default("pending") @db.VarChar(20)
  created_at           DateTime?  @default(now()) @db.Timestamptz(6)
  viewed_at            DateTime?  @db.Timestamptz(6)
  actioned_at          DateTime?  @db.Timestamptz(6)
  dismissed_at         DateTime?  @db.Timestamptz(6)
  dismiss_reason       String?    @db.VarChar(255)
  courses              courses?   @relation(fields: [course_id], references: [id], onUpdate: NoAction, map: "fk_lrec_course")
  employees            employees? @relation(fields: [employee_id], references: [id], onUpdate: NoAction, map: "fk_lrec_employee")

  @@index([course_id], map: "idx_learning_recommendations_course_id")
  @@index([employee_id], map: "idx_learning_recommendations_employee")
  @@index([status], map: "idx_learning_recommendations_status")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model leave_accrual_rules {
  id                      String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id               String    @db.Uuid
  leave_type              String    @db.VarChar(50)
  name                    String    @db.VarChar(100)
  description             String?
  accrual_method          String?   @default("monthly") @db.VarChar(20)
  accrual_amount          Decimal   @db.Decimal(5, 2)
  max_accrual             Decimal?  @db.Decimal(5, 2)
  allow_carryover         Boolean?  @default(true)
  max_carryover_days      Decimal?  @db.Decimal(5, 2)
  carryover_expiry_months Int?
  min_tenure_months       Int?      @default(0)
  prorated_first_year     Boolean?  @default(true)
  ccnl_type               String?   @db.VarChar(100)
  is_ccnl_default         Boolean?  @default(false)
  is_active               Boolean?  @default(true)
  created_at              DateTime? @default(now()) @db.Timestamp(6)
  updated_at              DateTime? @default(now()) @db.Timestamp(6)
  deleted_at              DateTime? @db.Timestamptz(6)
  tenants                 tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([ccnl_type], map: "idx_leave_accrual_rules_ccnl")
  @@index([tenant_id], map: "idx_leave_accrual_rules_tenant")
  @@index([leave_type], map: "idx_leave_accrual_rules_type")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model leave_approval_steps {
  id                         String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                  String                     @db.Uuid
  request_id                 String                     @db.Uuid
  step_order                 Int                        @default(1)
  approver_id                String?                    @db.Uuid
  approver_type              String?                    @db.VarChar(50)
  status                     String?                    @default("pending") @db.VarChar(20)
  decision_at                DateTime?                  @db.Timestamp(6)
  decision_notes             String?
  created_at                 DateTime?                  @default(now()) @db.Timestamp(6)
  employees                  employees?                 @relation(fields: [approver_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employee_time_off_requests employee_time_off_requests @relation(fields: [request_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                    tenants                    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([approver_id], map: "idx_leave_approval_steps_approver")
  @@index([request_id], map: "idx_leave_approval_steps_request")
  @@index([tenant_id], map: "idx_leave_approval_steps_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model leave_balance_transactions {
  id                         String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                  String                     @db.Uuid
  balance_id                 String                     @db.Uuid
  transaction_type           String                     @db.VarChar(50)
  days_amount                Decimal                    @db.Decimal(5, 2)
  reference_type             String?                    @db.VarChar(50)
  reference_id               String?                    @db.Uuid
  description                String?
  performed_by               String?                    @db.Uuid
  created_at                 DateTime?                  @default(now()) @db.Timestamp(6)
  employee_time_off_balances employee_time_off_balances @relation(fields: [balance_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                    tenants                    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([balance_id], map: "idx_leave_transactions_balance")
  @@index([created_at], map: "idx_leave_transactions_date")
  @@index([tenant_id], map: "idx_leave_transactions_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model locations {
  id                         String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                  String                       @db.Uuid
  code                       String                       @db.VarChar(20)
  name                       String                       @db.VarChar(100)
  location_type              String?                      @db.VarChar(30)
  address                    String?                      @db.VarChar(255)
  city                       String?                      @db.VarChar(100)
  province                   String?                      @db.VarChar(50)
  postal_code                String?                      @db.VarChar(10)
  country                    String?                      @default("ITA") @db.VarChar(3)
  latitude                   Decimal?                     @db.Decimal(10, 8)
  longitude                  Decimal?                     @db.Decimal(11, 8)
  phone                      String?                      @db.VarChar(30)
  email                      String?                      @db.VarChar(100)
  is_active                  Boolean?                     @default(true)
  capacity_headcount         Int?
  square_meters              Decimal?                     @db.Decimal(10, 2)
  opening_date               DateTime?                    @db.Date
  closing_date               DateTime?                    @db.Date
  created_at                 DateTime?                    @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime?                    @default(now()) @db.Timestamptz(6)
  sap_werks                  String?                      @db.VarChar(10)
  sap_btrtl                  String?                      @db.VarChar(10)
  sap_name1                  String?                      @db.VarChar(100)
  sap_stras                  String?                      @db.VarChar(100)
  sap_ort01                  String?                      @db.VarChar(50)
  sap_pstlz                  String?                      @db.VarChar(20)
  sap_land1                  String?                      @db.VarChar(5)
  sap_sync_date              DateTime?                    @db.Timestamp(6)
  embedding                  Unsupported("vector")?
  embedding_text_hash        String?                      @db.VarChar(64)
  embedding_model            String?                      @db.VarChar(100)
  embedding_generated_at     DateTime?                    @db.Timestamptz(6)
  deleted_at                 DateTime?                    @db.Timestamptz(6)
  contracts                  contracts[]
  employees                  employees[]
  internal_mobility_postings internal_mobility_postings[]
  tenants                    tenants                      @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([city], map: "idx_locations_city")
  @@index([embedding], map: "idx_locations_embedding")
  @@index([tenant_id], map: "idx_locations_tenant")
  @@index([location_type], map: "idx_locations_type")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model login_attempts {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  username       String    @unique @db.VarChar(255)
  attempt_count  Int       @default(0)
  last_failed_at DateTime? @db.Timestamptz(6)
  locked_until   DateTime? @db.Timestamptz(6)
  created_at     DateTime  @default(now()) @db.Timestamptz(6)
  updated_at     DateTime  @default(now()) @db.Timestamptz(6)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model market_benchmarks {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id        String    @db.Uuid
  benchmark_type   String    @db.VarChar(50)
  segment          Json      @default("{}")
  period_start     DateTime  @db.Date
  period_end       DateTime  @db.Date
  metrics          Json      @default("{}")
  sample_size      Int?      @default(0)
  confidence_level Decimal?  @db.Decimal(3, 2)
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  tenants          tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([period_start, period_end], map: "idx_market_benchmarks_period")
  @@index([tenant_id], map: "idx_market_benchmarks_tenant")
  @@index([benchmark_type], map: "idx_market_benchmarks_type")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model market_salary_data {
  id          String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id   String    @db.Uuid
  job_title   String    @db.VarChar(200)
  job_family  String?   @db.VarChar(100)
  job_level   String?   @db.VarChar(50)
  geo_region  String?   @db.VarChar(100)
  source      String?   @db.VarChar(100)
  survey_year Int?
  p25_salary  Decimal?  @db.Decimal(12, 2)
  p50_salary  Decimal?  @db.Decimal(12, 2)
  p75_salary  Decimal?  @db.Decimal(12, 2)
  p90_salary  Decimal?  @db.Decimal(12, 2)
  currency    String?   @default("EUR") @db.VarChar(3)
  created_at  DateTime? @default(now()) @db.Timestamp(6)
  updated_at  DateTime? @default(now()) @db.Timestamp(6)
  tenants     tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([job_title, geo_region], map: "idx_market_salary_data_job")
  @@index([tenant_id], map: "idx_market_salary_data_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model medical_certificates {
  id                         String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                  String                      @db.Uuid
  employee_id                String                      @db.Uuid
  request_id                 String?                     @db.Uuid
  certificate_number         String?                     @db.VarChar(100)
  issue_date                 DateTime                    @db.Date
  start_date                 DateTime                    @db.Date
  end_date                   DateTime                    @db.Date
  diagnosis_code             String?                     @db.VarChar(50)
  doctor_name                String?                     @db.VarChar(200)
  hospital_name              String?                     @db.VarChar(200)
  document_url               String?
  verified                   Boolean?                    @default(false)
  verified_at                DateTime?                   @db.Timestamp(6)
  verified_by                String?                     @db.Uuid
  notes                      String?
  created_at                 DateTime?                   @default(now()) @db.Timestamp(6)
  updated_at                 DateTime?                   @default(now()) @db.Timestamp(6)
  employees                  employees                   @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employee_time_off_requests employee_time_off_requests? @relation(fields: [request_id], references: [id], onUpdate: NoAction)
  tenants                    tenants                     @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([start_date, end_date], map: "idx_medical_certificates_dates")
  @@index([employee_id], map: "idx_medical_certificates_employee")
  @@index([request_id], map: "idx_medical_certificates_request_id")
  @@index([tenant_id], map: "idx_medical_certificates_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mentor_match_scores {
  id                                                 String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                          String       @db.Uuid
  mentee_id                                          String       @db.Uuid
  mentor_id                                          String       @db.Uuid
  skill_id                                           String?      @db.Uuid
  skill_name                                         String?      @db.VarChar(200)
  mentee_level                                       Decimal?     @db.Decimal(3, 2)
  mentor_level                                       Decimal?     @db.Decimal(3, 2)
  match_score                                        Decimal?     @db.Decimal(5, 4)
  match_factors                                      Json?        @default("{}")
  is_recommended                                     Boolean?     @default(false)
  recommendation_rank                                Int?
  created_at                                         DateTime?    @default(now()) @db.Timestamptz(6)
  expires_at                                         DateTime?    @default(dbgenerated("(now() + '30 days'::interval)")) @db.Timestamptz(6)
  employees_mentor_match_scores_mentee_idToemployees employees    @relation("mentor_match_scores_mentee_idToemployees", fields: [mentee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees_mentor_match_scores_mentor_idToemployees employees    @relation("mentor_match_scores_mentor_idToemployees", fields: [mentor_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  esco_skills                                        esco_skills? @relation(fields: [skill_id], references: [id], onUpdate: NoAction)
  tenants                                            tenants      @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([mentee_id, mentor_id, skill_id])
  @@index([mentee_id], map: "idx_mentor_match_mentee")
  @@index([match_score(sort: Desc)], map: "idx_mentor_match_score")
  @@index([tenant_id], map: "idx_mentor_match_scores_tenant_id")
  @@index([skill_id], map: "idx_mentor_match_skill")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mentorship_programs {
  id                   String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id            String    @db.Uuid
  name                 String    @db.VarChar(255)
  description          String?
  program_type         String?   @default("general") @db.VarChar(50)
  status               String?   @default("active") @db.VarChar(20)
  duration_months      Int?      @default(6)
  max_participants     Int?      @default(50)
  focus_areas          String[]
  eligibility_criteria Json?
  start_date           DateTime? @db.Date
  end_date             DateTime? @db.Date
  created_at           DateTime? @default(now()) @db.Timestamp(6)
  updated_at           DateTime? @default(now()) @db.Timestamp(6)
  tenants              tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_mentorship_programs_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model mentorship_sessions {
  id               String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  mentorship_id    String      @db.Uuid
  session_date     DateTime    @db.Timestamp(6)
  duration_minutes Int?        @default(60)
  status           String?     @default("scheduled") @db.VarChar(20)
  topics           String[]
  notes            String?
  rating           Int?
  created_at       DateTime?   @default(now()) @db.Timestamp(6)
  mentorships      mentorships @relation(fields: [mentorship_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_mentorship_sessions_mentorship")

  @@index([mentorship_id], map: "idx_mentorship_sessions_mentorship_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mentorships {
  id                  String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String                @db.Uuid
  program_id          String?               @db.Uuid
  mentor_id           String                @db.Uuid
  mentee_id           String                @db.Uuid
  status              String?               @default("active") @db.VarChar(30)
  focus_areas         String[]
  meeting_frequency   String?               @default("bi-weekly") @db.VarChar(30)
  goals               String[]
  match_score         Decimal?              @db.Decimal(5, 2)
  start_date          DateTime?             @default(dbgenerated("CURRENT_DATE")) @db.Date
  end_date            DateTime?             @db.Date
  notes               String?
  created_at          DateTime?             @default(now()) @db.Timestamp(6)
  updated_at          DateTime?             @default(now()) @db.Timestamp(6)
  mentorship_sessions mentorship_sessions[]
  tenants             tenants               @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_mentorships_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model merit_cycles {
  id                     String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String     @db.Uuid
  name                   String     @db.VarChar(200)
  description            String?
  effective_date         DateTime   @db.Date
  submission_deadline    DateTime?  @db.Date
  approval_deadline      DateTime?  @db.Date
  total_budget           Decimal?   @db.Decimal(15, 2)
  budget_spent           Decimal?   @default(0) @db.Decimal(15, 2)
  min_increase_percent   Decimal?   @default(0) @db.Decimal(5, 2)
  max_increase_percent   Decimal?   @default(20) @db.Decimal(5, 2)
  guideline_matrix       Json?
  status                 String?    @default("draft") @db.VarChar(30)
  created_by             String?    @db.Uuid
  created_at             DateTime?  @default(now()) @db.Timestamp(6)
  updated_at             DateTime?  @default(now()) @db.Timestamp(6)
  created_by_employee_id String?    @db.Uuid
  employees              employees? @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "merit_cycles_created_by_fkey_emp")
  tenants                tenants    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_by_employee_id], map: "idx_merit_cycles_created_by_employee_id")
  @@index([status], map: "idx_merit_cycles_status")
  @@index([tenant_id], map: "idx_merit_cycles_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model merit_recommendations {
  id                                                                  String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  cycle_id                                                            String?    @db.Uuid
  employee_id                                                         String?    @db.Uuid
  current_salary                                                      Decimal?   @db.Decimal(12, 2)
  current_bonus_target                                                Decimal?   @db.Decimal(12, 2)
  recommended_increase_percent                                        Decimal?   @db.Decimal(5, 2)
  recommended_increase_amount                                         Decimal?   @db.Decimal(12, 2)
  new_salary                                                          Decimal?   @db.Decimal(12, 2)
  performance_rating                                                  String?    @db.VarChar(20)
  justification                                                       String?
  status                                                              String?    @default("pending") @db.VarChar(30)
  submitted_by                                                        String?    @db.Uuid
  submitted_at                                                        DateTime?  @db.Timestamp(6)
  approved_by                                                         String?    @db.Uuid
  approved_at                                                         DateTime?  @db.Timestamp(6)
  created_at                                                          DateTime?  @default(now()) @db.Timestamp(6)
  updated_at                                                          DateTime?  @default(now()) @db.Timestamp(6)
  approved_by_employee_id                                             String?    @db.Uuid
  submitted_by_employee_id                                            String?    @db.Uuid
  employees_merit_recommendations_employee_idToemployees              employees? @relation("merit_recommendations_employee_idToemployees", fields: [employee_id], references: [id], onUpdate: NoAction, map: "fk_mr_employee")
  employees_merit_recommendations_approved_by_employee_idToemployees  employees? @relation("merit_recommendations_approved_by_employee_idToemployees", fields: [approved_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "merit_recommendations_approved_by_fkey_emp")
  employees_merit_recommendations_submitted_by_employee_idToemployees employees? @relation("merit_recommendations_submitted_by_employee_idToemployees", fields: [submitted_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "merit_recommendations_submitted_by_fkey_emp")

  @@index([approved_by_employee_id], map: "idx_merit_recommendations_approved_by_employee_id")
  @@index([cycle_id], map: "idx_merit_recommendations_cycle")
  @@index([employee_id], map: "idx_merit_recommendations_emp")
  @@index([submitted_by_employee_id], map: "idx_merit_recommendations_submitted_by_employee_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model model_predictions {
  id                 String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String             @db.Uuid
  model_id           String?            @db.Uuid
  entity_type        String             @db.VarChar(50)
  entity_id          String             @db.Uuid
  prediction_value   Decimal?           @db.Decimal(20, 4)
  prediction_label   String?            @db.VarChar(100)
  confidence_score   Decimal?           @db.Decimal(5, 4)
  prediction_details Json?              @default("{}")
  feature_importance Json?              @default("{}")
  valid_until        DateTime?          @db.Timestamptz(6)
  created_at         DateTime?          @default(now()) @db.Timestamptz(6)
  predictive_models  predictive_models? @relation(fields: [model_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants            tenants            @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([created_at], map: "idx_predictions_created")
  @@index([tenant_id, entity_type, entity_id], map: "idx_predictions_entity")
  @@index([model_id], map: "idx_predictions_model")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model module_completions {
  id                 String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  enrollment_id      String?         @db.Uuid
  module_id          String?         @db.Uuid
  status             String?         @default("not_started") @db.VarChar(20)
  started_at         DateTime?       @db.Timestamptz(6)
  completed_at       DateTime?       @db.Timestamptz(6)
  score              Decimal?        @db.Decimal(5, 2)
  time_spent_minutes Int?            @default(0)
  attempts           Int?            @default(1)
  created_at         DateTime?       @default(now()) @db.Timestamptz(6)
  updated_at         DateTime?       @default(now()) @db.Timestamptz(6)
  course_modules     course_modules? @relation(fields: [module_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([enrollment_id, module_id])
}

model news_article_tags {
  article_id    String        @db.Uuid
  tag_id        String        @db.Uuid
  created_at    DateTime?     @default(now()) @db.Timestamptz(6)
  news_articles news_articles @relation(fields: [article_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  news_tags     news_tags     @relation(fields: [tag_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([article_id, tag_id])
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model news_articles {
  id                      String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id               String              @db.Uuid
  category_id             String?             @db.Uuid
  author_id               String              @db.Uuid
  title                   String              @db.VarChar(255)
  slug                    String              @db.VarChar(255)
  excerpt                 String?
  content                 String
  cover_image_url         String?
  status                  String              @default("draft") @db.VarChar(20)
  is_featured             Boolean             @default(false)
  is_pinned               Boolean             @default(false)
  allow_comments          Boolean             @default(true)
  requires_acknowledgment Boolean             @default(false)
  audience_type           String              @default("all") @db.VarChar(20)
  audience_ids            Json?               @default("[]")
  publish_at              DateTime?           @db.Timestamptz(6)
  published_at            DateTime?           @db.Timestamptz(6)
  archived_at             DateTime?           @db.Timestamptz(6)
  views_count             Int                 @default(0)
  created_at              DateTime            @default(now()) @db.Timestamptz(6)
  updated_at              DateTime            @default(now()) @db.Timestamptz(6)
  news_article_tags       news_article_tags[]
  users                   users               @relation(fields: [author_id], references: [id], onDelete: SetNull, onUpdate: NoAction)
  news_categories         news_categories?    @relation(fields: [category_id], references: [id], onUpdate: NoAction)
  tenants                 tenants             @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  news_bookmarks          news_bookmarks[]
  news_comments           news_comments[]
  news_reactions          news_reactions[]
  news_reads              news_reads[]

  @@unique([tenant_id, slug], map: "unique_article_slug")
  @@index([author_id], map: "idx_news_articles_author")
  @@index([category_id], map: "idx_news_articles_category")
  @@index([tenant_id, status], map: "idx_news_articles_status")
  @@index([tenant_id], map: "idx_news_articles_tenant")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model news_bookmarks {
  id            String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id     String        @db.Uuid
  article_id    String        @db.Uuid
  user_id       String        @db.Uuid
  created_at    DateTime      @default(now()) @db.Timestamptz(6)
  news_articles news_articles @relation(fields: [article_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants       tenants       @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users         users         @relation(fields: [user_id], references: [id], onDelete: SetNull, onUpdate: NoAction)

  @@unique([article_id, user_id], map: "unique_bookmark")
  @@index([tenant_id], map: "idx_news_bookmarks_tenant_id")
  @@index([user_id], map: "idx_news_bookmarks_user")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model news_categories {
  id                    String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id             String            @db.Uuid
  name                  String            @db.VarChar(100)
  slug                  String            @db.VarChar(100)
  description           String?
  parent_id             String?           @db.Uuid
  icon                  String?           @default("folder") @db.VarChar(50)
  color                 String?           @default("#6366f1") @db.VarChar(20)
  sort_order            Int               @default(0)
  is_active             Boolean           @default(true)
  created_at            DateTime          @default(now()) @db.Timestamptz(6)
  updated_at            DateTime          @default(now()) @db.Timestamptz(6)
  deleted_at            DateTime?         @db.Timestamptz(6)
  news_articles         news_articles[]
  news_categories       news_categories?  @relation("news_categoriesTonews_categories", fields: [parent_id], references: [id], onUpdate: NoAction)
  other_news_categories news_categories[] @relation("news_categoriesTonews_categories")
  tenants               tenants           @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, slug], map: "unique_category_slug")
  @@index([parent_id], map: "idx_news_categories_parent")
  @@index([tenant_id], map: "idx_news_categories_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model news_comments {
  id                  String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String          @db.Uuid
  article_id          String          @db.Uuid
  user_id             String          @db.Uuid
  parent_id           String?         @db.Uuid
  content             String
  is_hidden           Boolean         @default(false)
  created_at          DateTime        @default(now()) @db.Timestamptz(6)
  updated_at          DateTime        @default(now()) @db.Timestamptz(6)
  news_articles       news_articles   @relation(fields: [article_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  news_comments       news_comments?  @relation("news_commentsTonews_comments", fields: [parent_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  other_news_comments news_comments[] @relation("news_commentsTonews_comments")
  tenants             tenants         @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users               users           @relation(fields: [user_id], references: [id], onDelete: SetNull, onUpdate: NoAction)

  @@index([article_id], map: "idx_news_comments_article")
  @@index([parent_id], map: "idx_news_comments_parent_id")
  @@index([tenant_id], map: "idx_news_comments_tenant_id")
  @@index([user_id], map: "idx_news_comments_user_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model news_reactions {
  id            String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id     String        @db.Uuid
  article_id    String        @db.Uuid
  user_id       String        @db.Uuid
  type          String        @default("like") @db.VarChar(20)
  created_at    DateTime      @default(now()) @db.Timestamptz(6)
  news_articles news_articles @relation(fields: [article_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants       tenants       @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users         users         @relation(fields: [user_id], references: [id], onDelete: SetNull, onUpdate: NoAction)

  @@unique([article_id, user_id], map: "unique_user_reaction")
  @@index([article_id], map: "idx_news_reactions_article")
  @@index([tenant_id], map: "idx_news_reactions_tenant_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model news_reads {
  id                   String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id            String        @db.Uuid
  article_id           String        @db.Uuid
  user_id              String        @db.Uuid
  read_at              DateTime      @default(now()) @db.Timestamptz(6)
  acknowledged_at      DateTime?     @db.Timestamptz(6)
  reading_time_seconds Int?          @default(0)
  created_at           DateTime?     @default(now()) @db.Timestamptz(6)
  news_articles        news_articles @relation(fields: [article_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants              tenants       @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                users         @relation(fields: [user_id], references: [id], onDelete: SetNull, onUpdate: NoAction)

  @@unique([article_id, user_id], map: "unique_user_read")
  @@index([article_id], map: "idx_news_reads_article")
  @@index([tenant_id], map: "idx_news_reads_tenant_id")
  @@index([user_id], map: "idx_news_reads_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model news_tags {
  id                String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String              @db.Uuid
  name              String              @db.VarChar(50)
  slug              String              @db.VarChar(50)
  created_at        DateTime            @default(now()) @db.Timestamptz(6)
  news_article_tags news_article_tags[]
  tenants           tenants             @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, slug], map: "unique_tag_slug")
  @@index([tenant_id], map: "idx_news_tags_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model notification_preferences {
  id                        String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id                   String     @unique(map: "notification_preferences_user_id_unique") @db.Uuid
  email_enabled             Boolean?   @default(true)
  in_app_enabled            Boolean?   @default(true)
  goal_reminders            Boolean?   @default(true)
  review_reminders          Boolean?   @default(true)
  flight_risk_alerts        Boolean?   @default(true)
  checkin_reminders         Boolean?   @default(true)
  survey_notifications      Boolean?   @default(true)
  recognition_notifications Boolean?   @default(true)
  system_notifications      Boolean?   @default(true)
  quiet_hours_start         DateTime?  @db.Time(6)
  quiet_hours_end           DateTime?  @db.Time(6)
  created_at                DateTime?  @default(now()) @db.Timestamp(6)
  updated_at                DateTime?  @default(now()) @db.Timestamp(6)
  mentorship_notifications  Boolean?   @default(true)
  mobility_notifications    Boolean?   @default(true)
  wellbeing_notifications   Boolean?   @default(true)
  user_id_employee_id       String?    @db.Uuid
  users                     users      @relation(fields: [user_id], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_np_user")
  employees                 employees? @relation(fields: [user_id_employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "notification_preferences_user_id_fkey_emp")

  @@index([user_id_employee_id], map: "idx_notification_preferences_user_id_employee_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model notifications {
  id                  String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  type                String     @db.VarChar(50)
  title               String     @db.VarChar(255)
  message             String
  priority            String?    @default("medium") @db.VarChar(20)
  read                Boolean?   @default(false)
  read_at             DateTime?  @db.Timestamp(6)
  user_id             String     @db.Uuid
  tenant_id           String     @db.Uuid
  action_url          String?    @db.VarChar(500)
  action_label        String?    @db.VarChar(100)
  metadata            Json?      @default("{}")
  created_at          DateTime?  @default(now()) @db.Timestamp(6)
  expires_at          DateTime?  @db.Timestamp(6)
  user_id_employee_id String?    @db.Uuid
  users               users      @relation(fields: [user_id], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_n_user")
  tenants             tenants    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees           employees? @relation(fields: [user_id_employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "notifications_user_id_fkey_emp")

  @@index([created_at(sort: Desc)], map: "idx_notifications_created_at")
  @@index([tenant_id], map: "idx_notifications_tenant")
  @@index([type], map: "idx_notifications_type")
  @@index([user_id], map: "idx_notifications_user_id")
  @@index([user_id_employee_id], map: "idx_notifications_user_id_employee_id")
  @@index([user_id, read], map: "idx_notifications_user_read")
}

model occupation_industry_classifications {
  id                       String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  occupation_id            String                   @db.Uuid
  classification_code      String                   @db.VarChar(10)
  source                   String                   @default("ESCO") @db.VarChar(20)
  relevance_score          Decimal?                 @default(1.00) @db.Decimal(3, 2)
  created_at               DateTime?                @default(now()) @db.Timestamptz(6)
  industry_classifications industry_classifications @relation(fields: [classification_code], references: [code], onDelete: Cascade, onUpdate: NoAction)
  esco_occupations         esco_occupations         @relation(fields: [occupation_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([occupation_id, classification_code], map: "occupation_industry_classific_occupation_id_classification__key")
  @@index([classification_code], map: "idx_oic_classification")
  @@index([occupation_id], map: "idx_oic_occupation")
  @@index([source], map: "idx_oic_source")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model okr_check_ins {
  id                String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String       @db.Uuid
  okr_id            String       @db.Uuid
  key_result_id     String?      @db.Uuid
  employee_id       String       @db.Uuid
  check_in_date     DateTime     @default(dbgenerated("CURRENT_DATE")) @db.Date
  previous_value    Decimal?     @db.Decimal(15, 2)
  new_value         Decimal      @db.Decimal(15, 2)
  previous_progress Decimal?     @db.Decimal(5, 2)
  new_progress      Decimal?     @db.Decimal(5, 2)
  confidence_level  Int?
  notes             String?
  blockers          String?
  created_at        DateTime?    @default(now()) @db.Timestamptz(6)
  employees         employees    @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  key_results       key_results? @relation(fields: [key_result_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  okrs              okrs         @relation(fields: [okr_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants           tenants      @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([check_in_date], map: "idx_okr_check_ins_date")
  @@index([employee_id], map: "idx_okr_check_ins_employee")
  @@index([key_result_id], map: "idx_okr_check_ins_kr")
  @@index([okr_id], map: "idx_okr_check_ins_okr")
  @@index([tenant_id], map: "idx_okr_check_ins_tenant_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model okr_checkins {
  id                 String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String     @db.Uuid
  okr_id             String     @db.Uuid
  author_id          String?    @db.Uuid
  checkin_date       DateTime   @default(dbgenerated("CURRENT_DATE")) @db.Date
  overall_progress   Decimal?   @db.Decimal(5, 2)
  confidence_level   Decimal?   @db.Decimal(5, 2)
  status_update      String?
  blockers           String?
  next_steps         String?
  key_result_updates Json?      @default("[]")
  created_at         DateTime?  @default(now()) @db.Timestamptz(6)
  employees          employees? @relation(fields: [author_id], references: [id], onUpdate: NoAction)
  okrs               okrs       @relation(fields: [okr_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants            tenants    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([author_id], map: "idx_okr_checkins_author_id")
  @@index([checkin_date(sort: Desc)], map: "idx_okr_checkins_date")
  @@index([okr_id], map: "idx_okr_checkins_okr")
  @@index([tenant_id], map: "idx_okr_checkins_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model okrs {
  id               String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id        String          @db.Uuid
  objective        String
  okr_type         String?         @default("company") @db.VarChar(50)
  department       String?         @db.VarChar(100)
  period_type      String?         @default("quarterly") @db.VarChar(20)
  period_start     DateTime        @db.Date
  period_end       DateTime        @db.Date
  status           String?         @default("active") @db.VarChar(50)
  overall_progress Decimal?        @default(0) @db.Decimal(5, 2)
  confidence_level Decimal?        @db.Decimal(3, 2)
  owner_id         String?         @db.Uuid
  created_by       String?         @db.Uuid
  created_at       DateTime?       @default(now()) @db.Timestamptz(6)
  updated_at       DateTime?       @default(now()) @db.Timestamptz(6)
  description      String?
  parent_okr_id    String?         @db.Uuid
  tags             Json?           @default("[]")
  fiscal_year      Int?
  fiscal_quarter   Int?
  okr_check_ins    okr_check_ins[]
  okr_checkins     okr_checkins[]
  employees        employees?      @relation(fields: [owner_id], references: [id], onUpdate: NoAction, map: "fk_okrs_owner")
  okrs             okrs?           @relation("okrsTookrs", fields: [parent_okr_id], references: [id], onUpdate: NoAction)
  other_okrs       okrs[]          @relation("okrsTookrs")
  tenants          tenants         @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([tenant_id, owner_id], map: "idx_okrs_owner")
  @@index([parent_okr_id], map: "idx_okrs_parent_okr_id")
  @@index([tenant_id, period_start, period_end], map: "idx_okrs_period")
  @@index([tenant_id, status], map: "idx_okrs_status")
  @@index([tenant_id], map: "idx_okrs_tenant")
  @@index([tenant_id, okr_type], map: "idx_okrs_type")
}

model onboarding_checklist {
  id                       String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  instance_id              String?    @db.Uuid
  item                     String     @db.VarChar(300)
  is_completed             Boolean?   @default(false)
  completed_by             String?    @db.Uuid
  completed_at             DateTime?  @db.Timestamp(6)
  notes                    String?
  created_at               DateTime?  @default(now()) @db.Timestamp(6)
  completed_by_employee_id String?    @db.Uuid
  employees                employees? @relation(fields: [completed_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "onboarding_checklist_completed_by_fkey_emp")

  @@index([completed_by_employee_id], map: "idx_onboarding_checklist_completed_by_employee_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model onboarding_documents {
  id                      String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  instance_id             String?    @db.Uuid
  task_id                 String?    @db.Uuid
  document_type           String     @db.VarChar(100)
  file_name               String?    @db.VarChar(500)
  file_path               String?    @db.VarChar(1000)
  file_size               Int?
  mime_type               String?    @db.VarChar(100)
  status                  String?    @default("pending") @db.VarChar(20)
  verified_by             String?    @db.Uuid
  verified_at             DateTime?  @db.Timestamp(6)
  notes                   String?
  uploaded_at             DateTime?  @default(now()) @db.Timestamp(6)
  verified_by_employee_id String?    @db.Uuid
  created_at              DateTime?  @default(now()) @db.Timestamptz(6)
  employees               employees? @relation(fields: [verified_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "onboarding_documents_verified_by_fkey_emp")

  @@index([instance_id], map: "idx_onboarding_documents_instance")
  @@index([verified_by_employee_id], map: "idx_onboarding_documents_verified_by_employee_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model onboarding_instances {
  id                                                                  String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                                           String     @db.Uuid
  employee_id                                                         String?    @db.Uuid
  template_id                                                         String?    @db.Uuid
  status                                                              String?    @default("pending") @db.VarChar(20)
  start_date                                                          DateTime   @db.Date
  target_completion_date                                              DateTime?  @db.Date
  actual_completion_date                                              DateTime?  @db.Date
  progress_percent                                                    Int?       @default(0)
  assigned_buddy_id                                                   String?    @db.Uuid
  hr_contact_id                                                       String?    @db.Uuid
  notes                                                               String?
  created_by                                                          String?    @db.Uuid
  created_at                                                          DateTime?  @default(now()) @db.Timestamp(6)
  updated_at                                                          DateTime?  @default(now()) @db.Timestamp(6)
  created_by_employee_id                                              String?    @db.Uuid
  hr_contact_id_employee_id                                           String?    @db.Uuid
  employees_onboarding_instances_employee_idToemployees               employees? @relation("onboarding_instances_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_oi_employee")
  employees_onboarding_instances_created_by_employee_idToemployees    employees? @relation("onboarding_instances_created_by_employee_idToemployees", fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "onboarding_instances_created_by_fkey_emp")
  employees_onboarding_instances_hr_contact_id_employee_idToemployees employees? @relation("onboarding_instances_hr_contact_id_employee_idToemployees", fields: [hr_contact_id_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "onboarding_instances_hr_contact_id_fkey_emp")
  tenants                                                             tenants    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_by_employee_id], map: "idx_onboarding_instances_created_by_employee_id")
  @@index([employee_id], map: "idx_onboarding_instances_employee")
  @@index([hr_contact_id_employee_id], map: "idx_onboarding_instances_hr_contact_id_employee_id")
  @@index([status], map: "idx_onboarding_instances_status")
  @@index([tenant_id], map: "idx_onboarding_instances_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model onboarding_tasks {
  id                       String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  instance_id              String?    @db.Uuid
  template_task_id         String?    @db.Uuid
  title                    String     @db.VarChar(300)
  description              String?
  category                 String?    @default("general") @db.VarChar(50)
  priority                 Int?       @default(1)
  status                   String?    @default("pending") @db.VarChar(20)
  assignee_type            String?    @default("employee") @db.VarChar(20)
  assigned_to_id           String?    @db.Uuid
  due_date                 DateTime?  @db.Date
  completed_at             DateTime?  @db.Timestamp(6)
  completed_by             String?    @db.Uuid
  notes                    String?
  sequence_order           Int?       @default(0)
  created_at               DateTime?  @default(now()) @db.Timestamp(6)
  updated_at               DateTime?  @default(now()) @db.Timestamp(6)
  completed_by_employee_id String?    @db.Uuid
  employees                employees? @relation(fields: [completed_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "onboarding_tasks_completed_by_fkey_emp")

  @@index([completed_by_employee_id], map: "idx_onboarding_tasks_completed_by_employee_id")
  @@index([instance_id], map: "idx_onboarding_tasks_instance")
  @@index([status], map: "idx_onboarding_tasks_status")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model onboarding_template_tasks {
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  template_id     String?   @db.Uuid
  title           String    @db.VarChar(300)
  description     String?
  category        String?   @default("general") @db.VarChar(50)
  priority        Int?      @default(1)
  due_day         Int?      @default(1)
  is_required     Boolean?  @default(true)
  assignee_type   String?   @default("employee") @db.VarChar(20)
  estimated_hours Decimal?  @db.Decimal(4, 1)
  sequence_order  Int?      @default(0)
  created_at      DateTime? @default(now()) @db.Timestamp(6)

  @@index([template_id], map: "idx_onboarding_template_tasks_template")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model onboarding_templates {
  id                     String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String     @db.Uuid
  name                   String     @db.VarChar(200)
  description            String?
  department             String?    @db.VarChar(100)
  role_type              String?    @db.VarChar(100)
  duration_days          Int?       @default(30)
  is_active              Boolean?   @default(true)
  is_default             Boolean?   @default(false)
  created_by             String?    @db.Uuid
  created_at             DateTime?  @default(now()) @db.Timestamp(6)
  updated_at             DateTime?  @default(now()) @db.Timestamp(6)
  created_by_employee_id String?    @db.Uuid
  deleted_at             DateTime?  @db.Timestamptz(6)
  employees              employees? @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "onboarding_templates_created_by_fkey_emp")
  tenants                tenants    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([is_active], map: "idx_onboarding_templates_active")
  @@index([created_by_employee_id], map: "idx_onboarding_templates_created_by_employee_id")
  @@index([tenant_id], map: "idx_onboarding_templates_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model onet_abilities {
  id                        String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  element_id                String                      @unique @db.VarChar(50)
  element_name              String                      @db.VarChar(500)
  description               String?
  category                  String?                     @db.VarChar(100)
  created_at                DateTime                    @default(now()) @db.Timestamptz(6)
  updated_at                DateTime                    @default(now()) @db.Timestamptz(6)
  onet_occupation_abilities onet_occupation_abilities[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model onet_esco_mappings {
  id                 String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  onet_element_id    String            @db.VarChar(50)
  onet_element_type  String            @db.VarChar(50)
  esco_uri           String?           @db.VarChar(500)
  esco_skill_id      String?           @db.Uuid
  esco_occupation_id String?           @db.Uuid
  confidence         Decimal           @default(0.0) @db.Decimal(5, 4)
  mapping_method     String?           @db.VarChar(50)
  created_at         DateTime          @default(now()) @db.Timestamptz(6)
  updated_at         DateTime          @default(now()) @db.Timestamptz(6)
  esco_occupations   esco_occupations? @relation(fields: [esco_occupation_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  esco_skills        esco_skills?      @relation(fields: [esco_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([confidence(sort: Desc)], map: "idx_onet_esco_mappings_confidence")
  @@index([esco_occupation_id], map: "idx_onet_esco_mappings_esco_occupation_id")
  @@index([esco_skill_id], map: "idx_onet_esco_mappings_esco_skill_id")
  @@index([onet_element_id, onet_element_type], map: "idx_onet_esco_mappings_onet")
}

model onet_import_jobs {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  import_type       String    @db.VarChar(50)
  source_version    String?   @db.VarChar(50)
  status            String    @default("pending") @db.VarChar(20)
  total_records     Int?      @default(0)
  processed_records Int?      @default(0)
  error_message     String?
  started_at        DateTime? @db.Timestamptz(6)
  completed_at      DateTime? @db.Timestamptz(6)
  created_at        DateTime  @default(now()) @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model onet_knowledge {
  id                        String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  element_id                String                      @unique @db.VarChar(50)
  element_name              String                      @db.VarChar(500)
  description               String?
  domain                    String?                     @db.VarChar(200)
  created_at                DateTime                    @default(now()) @db.Timestamptz(6)
  updated_at                DateTime                    @default(now()) @db.Timestamptz(6)
  onet_occupation_knowledge onet_occupation_knowledge[]
}

model onet_occupation_abilities {
  id               String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  occupation_id    String           @db.Uuid
  ability_id       String           @db.Uuid
  importance       Decimal?         @db.Decimal(5, 2)
  level            Decimal?         @db.Decimal(5, 2)
  created_at       DateTime         @default(now()) @db.Timestamptz(6)
  onet_abilities   onet_abilities   @relation(fields: [ability_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  onet_occupations onet_occupations @relation(fields: [occupation_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([occupation_id, ability_id])
}

model onet_occupation_knowledge {
  id               String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  occupation_id    String           @db.Uuid
  knowledge_id     String           @db.Uuid
  importance       Decimal?         @db.Decimal(5, 2)
  level            Decimal?         @db.Decimal(5, 2)
  created_at       DateTime         @default(now()) @db.Timestamptz(6)
  onet_knowledge   onet_knowledge   @relation(fields: [knowledge_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  onet_occupations onet_occupations @relation(fields: [occupation_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([occupation_id, knowledge_id])
}

model onet_occupation_skills {
  id               String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  occupation_id    String           @db.Uuid
  skill_id         String           @db.Uuid
  importance       Decimal?         @db.Decimal(5, 2)
  level            Decimal?         @db.Decimal(5, 2)
  created_at       DateTime         @default(now()) @db.Timestamptz(6)
  onet_occupations onet_occupations @relation(fields: [occupation_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  onet_skills      onet_skills      @relation(fields: [skill_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([occupation_id, skill_id])
}

model onet_occupation_work_activities {
  id                   String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  occupation_id        String               @db.Uuid
  work_activity_id     String               @db.Uuid
  importance           Decimal?             @db.Decimal(5, 2)
  level                Decimal?             @db.Decimal(5, 2)
  created_at           DateTime             @default(now()) @db.Timestamptz(6)
  onet_occupations     onet_occupations     @relation(fields: [occupation_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  onet_work_activities onet_work_activities @relation(fields: [work_activity_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([occupation_id, work_activity_id], map: "onet_occupation_work_activiti_occupation_id_work_activity_i_key")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model onet_occupations {
  id                              String                            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  onet_soc_code                   String                            @unique @db.VarChar(20)
  title                           String                            @db.VarChar(500)
  description                     String?
  job_zone                        Int?
  related_experience              String?
  education_required              String?
  on_job_training                 String?
  source_version                  String?                           @db.VarChar(50)
  created_at                      DateTime                          @default(now()) @db.Timestamptz(6)
  updated_at                      DateTime                          @default(now()) @db.Timestamptz(6)
  embedding_en                    Unsupported("vector")?
  title_it                        String?                           @db.VarChar(255)
  title_en                        String?                           @db.VarChar(255)
  description_it                  String?
  description_en                  String?
  needs_translation_it            Boolean?                          @default(false)
  onet_occupation_abilities       onet_occupation_abilities[]
  onet_occupation_knowledge       onet_occupation_knowledge[]
  onet_occupation_skills          onet_occupation_skills[]
  onet_occupation_work_activities onet_occupation_work_activities[]

  @@index([title(ops: raw("gin_trgm_ops"))], map: "idx_onet_occupations_title", type: Gin)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model onet_skills {
  id                                                        String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  element_id                                                String                   @unique @db.VarChar(50)
  element_name                                              String                   @db.VarChar(500)
  description                                               String?
  category                                                  String?                  @db.VarChar(100)
  esco_skill_id                                             String?                  @db.Uuid
  similarity_score                                          Decimal?                 @db.Decimal(5, 4)
  created_at                                                DateTime                 @default(now()) @db.Timestamptz(6)
  updated_at                                                DateTime                 @default(now()) @db.Timestamptz(6)
  mapped_esco_skill_id                                      String?                  @db.Uuid
  mapping_confidence                                        Decimal?                 @db.Decimal(5, 4)
  onet_occupation_skills                                    onet_occupation_skills[]
  esco_skills_onet_skills_esco_skill_idToesco_skills        esco_skills?             @relation("onet_skills_esco_skill_idToesco_skills", fields: [esco_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  esco_skills_onet_skills_mapped_esco_skill_idToesco_skills esco_skills?             @relation("onet_skills_mapped_esco_skill_idToesco_skills", fields: [mapped_esco_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([esco_skill_id], map: "idx_onet_skills_esco_skill_id")
  @@index([mapped_esco_skill_id], map: "idx_onet_skills_mapped_esco_skill_id")
  @@index([element_name(ops: raw("gin_trgm_ops"))], map: "idx_onet_skills_name", type: Gin)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model onet_work_activities {
  id                              String                            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  element_id                      String                            @unique @db.VarChar(50)
  element_name                    String                            @db.VarChar(500)
  description                     String?
  activity_type                   String?                           @db.VarChar(100)
  parent_element_id               String?                           @db.VarChar(50)
  created_at                      DateTime                          @default(now()) @db.Timestamptz(6)
  updated_at                      DateTime                          @default(now()) @db.Timestamptz(6)
  onet_occupation_work_activities onet_occupation_work_activities[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ontology_categories {
  id                        String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  parent_id                 String?                @db.Uuid
  code                      String                 @unique(map: "ontology_categories_code_unique") @db.VarChar(50)
  name_en                   String                 @db.VarChar(255)
  name_it                   String?                @db.VarChar(255)
  description_en            String?
  description_it            String?
  level                     Int                    @default(0)
  esco_pillar               String?                @db.VarChar(100)
  isced_field               String?                @db.VarChar(20)
  is_active                 Boolean?               @default(true)
  created_at                DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at                DateTime?              @default(now()) @db.Timestamptz(6)
  deleted_at                DateTime?              @db.Timestamptz(6)
  ontology_categories       ontology_categories?   @relation("ontology_categoriesToontology_categories", fields: [parent_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  other_ontology_categories ontology_categories[]  @relation("ontology_categoriesToontology_categories")
  tenant_custom_skills      tenant_custom_skills[]

  @@index([level], map: "idx_ontology_categories_level")
  @@index([parent_id], map: "idx_ontology_categories_parent")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ontology_embedding_jobs {
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  job_type        String    @db.VarChar(50)
  target_table    String    @db.VarChar(100)
  tenant_id       String    @db.Uuid
  status          String    @default("pending") @db.VarChar(20)
  total_items     Int?
  processed_items Int?      @default(0)
  failed_items    Int?      @default(0)
  provider        String    @db.VarChar(50)
  model           String    @db.VarChar(100)
  tokens_used     Int?      @default(0)
  estimated_cost  Decimal?  @db.Decimal(10, 4)
  started_at      DateTime? @db.Timestamptz(6)
  completed_at    DateTime? @db.Timestamptz(6)
  last_error      String?
  retry_count     Int?      @default(0)
  created_at      DateTime? @default(now()) @db.Timestamptz(6)
  tenants         tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([status], map: "idx_embedding_jobs_status")
  @@index([tenant_id], map: "idx_embedding_jobs_tenant")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ontology_feedback {
  id            String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id     String     @db.Uuid
  entity_type   String     @db.VarChar(50)
  entity_id     String     @db.Uuid
  feedback_type String     @db.VarChar(30)
  feedback_text String?
  submitted_by  String?    @db.Uuid
  metadata      Json?      @default("{}")
  created_at    DateTime?  @default(now()) @db.Timestamptz(6)
  employees     employees? @relation(fields: [submitted_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants       tenants    @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([entity_type, entity_id], map: "idx_ontology_feedback_entity")
  @@index([submitted_by], map: "idx_ontology_feedback_submitted_by")
  @@index([tenant_id], map: "idx_ontology_feedback_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ontology_inference_jobs {
  id                   String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  job_type             String    @db.VarChar(50)
  status               String    @default("pending") @db.VarChar(20)
  total_pairs          Int?
  processed_pairs      Int?      @default(0)
  relations_found      Int?      @default(0)
  similarity_threshold Decimal?  @default(0.75) @db.Decimal(3, 2)
  started_at           DateTime? @db.Timestamptz(6)
  completed_at         DateTime? @db.Timestamptz(6)
  error                String?
  created_at           DateTime? @default(now()) @db.Timestamptz(6)
  tenant_id            String    @db.Uuid
  tenants              tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([status], map: "idx_inference_jobs_status")
  @@index([tenant_id], map: "idx_inference_jobs_tenant")
}

model ontology_quality_metrics {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  entity_type      String    @db.VarChar(50)
  entity_id        String    @db.Uuid
  metric_name      String    @db.VarChar(100)
  metric_value     Decimal?  @db.Decimal(5, 4)
  measurement_date DateTime? @default(dbgenerated("CURRENT_DATE")) @db.Date
  model_version    String?   @db.VarChar(50)
  notes            String?
  created_at       DateTime? @default(now()) @db.Timestamptz(6)

  @@index([entity_type, entity_id], map: "idx_ontology_quality_entity")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ontology_skill_dimensions {
  id              String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  esco_skill_id   String                 @db.Uuid
  dimension_type  String                 @db.VarChar(20)
  description_en  String?
  description_it  String?
  level_scale     String?                @default("basic_to_expert") @db.VarChar(50)
  min_level       Int?                   @default(1)
  max_level       Int?                   @default(5)
  embedding       Unsupported("vector")?
  embedding_model String?                @db.VarChar(100)
  is_primary      Boolean?               @default(false)
  created_at      DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at      DateTime?              @default(now()) @db.Timestamptz(6)
  esco_skills     esco_skills            @relation(fields: [esco_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([esco_skill_id, dimension_type], map: "ontology_dimensions_unique")
  @@index([esco_skill_id], map: "idx_ontology_dimensions_skill")
  @@index([dimension_type], map: "idx_ontology_dimensions_type")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ontology_skill_relations {
  id                                                                String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  source_skill_id                                                   String      @db.Uuid
  target_skill_id                                                   String      @db.Uuid
  relation_type                                                     String      @db.VarChar(50)
  strength                                                          Decimal?    @default(1.00) @db.Decimal(3, 2)
  context                                                           String?     @db.VarChar(255)
  source                                                            String      @default("manual") @db.VarChar(50)
  confidence                                                        Decimal?    @db.Decimal(3, 2)
  model_version                                                     String?     @db.VarChar(100)
  inference_date                                                    DateTime?   @db.Timestamptz(6)
  approval_status                                                   String?     @default("approved") @db.VarChar(20)
  approved_by                                                       String?     @db.Uuid
  approved_at                                                       DateTime?   @db.Timestamptz(6)
  created_at                                                        DateTime?   @default(now()) @db.Timestamptz(6)
  updated_at                                                        DateTime?   @default(now()) @db.Timestamptz(6)
  esco_skills_ontology_skill_relations_source_skill_idToesco_skills esco_skills @relation("ontology_skill_relations_source_skill_idToesco_skills", fields: [source_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  esco_skills_ontology_skill_relations_target_skill_idToesco_skills esco_skills @relation("ontology_skill_relations_target_skill_idToesco_skills", fields: [target_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([source_skill_id, target_skill_id, relation_type], map: "ontology_relations_unique")
  @@index([source_skill_id], map: "idx_ontology_relations_source")
  @@index([target_skill_id], map: "idx_ontology_relations_target")
  @@index([relation_type], map: "idx_ontology_relations_type")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ontology_source_mappings {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id        String    @db.Uuid
  source_system    String    @db.VarChar(50)
  source_id        String    @db.VarChar(200)
  source_type      String?   @db.VarChar(50)
  target_table     String    @db.VarChar(100)
  target_id        String    @db.Uuid
  confidence_score Decimal?  @db.Decimal(5, 4)
  mapping_method   String?   @default("manual") @db.VarChar(50)
  verified         Boolean?  @default(false)
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  updated_at       DateTime? @default(now()) @db.Timestamptz(6)
  tenants          tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([source_system, source_id], map: "idx_ontology_source_mappings_source")
  @@index([tenant_id], map: "idx_ontology_source_mappings_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model org_areas {
  code               String    @id @db.VarChar(10)
  cost_center_series String    @db.VarChar(4)
  name_it            String    @db.VarChar(100)
  name_en            String    @db.VarChar(100)
  description        String?
  icon               String?   @db.VarChar(50)
  color              String?   @db.VarChar(7)
  sort_order         Int?
  is_active          Boolean?  @default(true)
  created_at         DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at         DateTime? @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model org_chart_generation_sessions {
  id                  String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String                @db.Uuid
  session_name        String                @db.VarChar(255)
  session_description String?
  generation_method   String                @default("combined") @db.VarChar(50)
  nace_section        String?               @db.Char(1)
  nace_division       String?               @db.VarChar(2)
  nace_group          String?               @db.VarChar(4)
  nace_code           String?               @db.VarChar(10)
  industry_name       String?               @db.VarChar(255)
  company_size        String?               @default("medium") @db.VarChar(20)
  ai_provider         String?               @db.VarChar(50)
  ai_model            String?               @db.VarChar(100)
  ai_prompt           String?
  ai_response         Json?
  generated_structure Json?
  tenant_context      Json?
  status              String                @default("pending") @db.VarChar(20)
  error_message       String?
  created_at          DateTime?             @default(now()) @db.Timestamptz(6)
  started_at          DateTime?             @db.Timestamptz(6)
  completed_at        DateTime?             @db.Timestamptz(6)
  updated_at          DateTime?             @default(now()) @db.Timestamptz(6)
  employees_staging   employees_staging[]
  tenants             tenants               @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  org_chart_snapshots org_chart_snapshots[]

  @@index([created_at(sort: Desc)], map: "idx_ocgs_created")
  @@index([nace_code], map: "idx_ocgs_nace")
  @@index([status], map: "idx_ocgs_status")
  @@index([tenant_id], map: "idx_ocgs_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model org_chart_snapshots {
  id                            String                        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  session_id                    String                        @db.Uuid
  tenant_id                     String                        @db.Uuid
  snapshot_name                 String                        @db.VarChar(255)
  snapshot_type                 String                        @db.VarChar(30)
  snapshot_version              String?                       @default("1.0.0") @db.VarChar(20)
  tree_structure                Json?
  excalidraw_format             Json?
  employees_map                 Json?
  statistics                    Json?
  is_active                     Boolean?                      @default(true)
  created_at                    DateTime?                     @default(now()) @db.Timestamptz(6)
  updated_at                    DateTime?                     @default(now()) @db.Timestamptz(6)
  deleted_at                    DateTime?                     @db.Timestamptz(6)
  org_chart_generation_sessions org_chart_generation_sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                       tenants                       @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([session_id], map: "idx_ocs_session")
  @@index([tenant_id], map: "idx_ocs_tenant")
  @@index([snapshot_type], map: "idx_ocs_type")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model org_chart_templates {
  id                   String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  profile_id           String    @db.Uuid
  name                 String    @db.VarChar(255)
  description          String?
  version              String?   @default("1.0.0") @db.VarChar(20)
  is_default           Boolean?  @default(false)
  source_type          String?   @db.VarChar(50)
  source_reference     String?
  is_active            Boolean?  @default(true)
  created_at           DateTime? @default(now()) @db.Timestamptz(6)
  updated_at           DateTime? @default(now()) @db.Timestamptz(6)
  template_structure   Json?
  position_definitions Json?
  nace_section         String?   @db.Char(1)
  nace_division        String?   @db.VarChar(2)
  size_class           String?   @db.VarChar(20)
  level_count          Int?      @default(7)
  deleted_at           DateTime? @db.Timestamptz(6)

  @@index([profile_id], map: "idx_org_templates_profile")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model org_levels {
  level                   Int       @id
  name_it                 String    @db.VarChar(50)
  name_en                 String    @db.VarChar(50)
  nature                  String    @db.VarChar(20)
  is_management           Boolean?  @default(false)
  min_salary_multiplier   Decimal?  @db.Decimal(4, 2)
  max_salary_multiplier   Decimal?  @db.Decimal(4, 2)
  typical_span_of_control Int?
  sort_order              Int?
  created_at              DateTime? @default(now()) @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model org_prototype_rules {
  id                         String                    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  company_size               String                    @db.VarChar(10)
  nace_section               String?                   @db.VarChar(5)
  max_hierarchy_levels       Int
  min_departments            Int
  max_departments            Int
  role_specialization_factor Float
  merge_support_functions    Boolean?                  @default(false)
  department_merge_rules     Json?                     @default("{}")
  mandatory_departments      Json?                     @default("[]")
  optional_departments       Json?                     @default("[]")
  created_at                 DateTime?                 @default(now()) @db.Timestamptz(6)
  org_prototype_templates    org_prototype_templates[]

  @@unique([company_size, nace_section])
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model org_prototype_templates {
  id                    String               @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  rule_id               String?              @db.Uuid
  isco_major_group      String               @db.VarChar(2)
  department_name_en    String               @db.VarChar(100)
  department_name_it    String?              @db.VarChar(100)
  function_type         String?              @db.VarChar(20)
  default_headcount_pct Int?
  typical_positions     Json?                @default("[]")
  created_at            DateTime?            @default(now()) @db.Timestamptz(6)
  org_prototype_rules   org_prototype_rules? @relation(fields: [rule_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([rule_id], map: "idx_org_prototype_templates_rule")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model org_scenarios {
  id                String     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id         String     @db.Uuid
  name              String     @db.VarChar(255)
  description       String?
  scenario_type     String     @default("restructuring") @db.VarChar(50)
  status            String     @default("draft") @db.VarChar(50)
  is_baseline       Boolean?   @default(false)
  base_org_unit_id  String?    @db.Uuid
  changes           Json?      @default("[]")
  impact_analysis   Json?      @default("{}")
  headcount         Int?
  departments_count Int?
  total_cost        Decimal?   @db.Decimal(15, 2)
  span_of_control   Decimal?   @db.Decimal(5, 2)
  created_by        String?    @db.Uuid
  created_at        DateTime?  @default(now()) @db.Timestamp(6)
  updated_at        DateTime?  @default(now()) @db.Timestamp(6)
  deleted_at        DateTime?  @db.Timestamp(6)
  org_units         org_units? @relation(fields: [base_org_unit_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users             users?     @relation(fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants           tenants    @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([base_org_unit_id], map: "idx_org_scenarios_base_org_unit_id")
  @@index([created_by], map: "idx_org_scenarios_created_by")
  @@index([tenant_id, status], map: "idx_org_scenarios_status")
  @@index([tenant_id], map: "idx_org_scenarios_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model org_templates {
  id                 String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  nace_section_code  String    @db.Char(1)
  nace_division_code String?   @db.VarChar(10)
  company_size_min   Int
  company_size_max   Int
  template_name      String    @db.VarChar(200)
  description        String?
  org_structure      Json
  recommended_roles  Json
  created_at         DateTime? @default(now()) @db.Timestamptz(6)
  updated_at         DateTime? @default(now()) @db.Timestamptz(6)

  @@index([nace_section_code, nace_division_code], map: "idx_org_templates_nace")
  @@index([company_size_min, company_size_max], map: "idx_org_templates_size")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model org_unit_kpis {
  id                   String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  org_unit_template_id String                 @db.Uuid
  kpi_code             String                 @db.VarChar(20)
  kpi_name             String                 @db.VarChar(200)
  kpi_description      String?
  measurement_unit     String                 @db.VarChar(50)
  target_direction     String                 @db.VarChar(20)
  benchmark_value      Decimal?               @db.Decimal(15, 2)
  benchmark_min        Decimal?               @db.Decimal(15, 2)
  benchmark_max        Decimal?               @db.Decimal(15, 2)
  data_source          String?                @db.VarChar(100)
  calculation_formula  String?
  created_at           DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at           DateTime?              @default(now()) @db.Timestamptz(6)
  job_kpi_distribution job_kpi_distribution[]
  org_unit_templates   org_unit_templates     @relation(fields: [org_unit_template_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([org_unit_template_id, kpi_code])
  @@index([target_direction], map: "idx_org_unit_kpis_direction")
  @@index([org_unit_template_id], map: "idx_org_unit_kpis_unit")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model org_unit_process_mapping {
  id                   String                @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  org_unit_template_id String                @db.Uuid
  process_id           String                @db.Uuid
  cost_center_id       String?               @db.Uuid
  responsibility_level String                @db.VarChar(20)
  created_at           DateTime?             @default(now()) @db.Timestamptz(6)
  updated_at           DateTime?             @default(now()) @db.Timestamptz(6)
  process_cost_centers process_cost_centers? @relation(fields: [cost_center_id], references: [id], onUpdate: NoAction)
  org_unit_templates   org_unit_templates    @relation(fields: [org_unit_template_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  business_processes   business_processes    @relation(fields: [process_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([org_unit_template_id, process_id])
  @@index([cost_center_id], map: "idx_org_unit_process_mapping_cost_center")
  @@index([process_id], map: "idx_org_unit_process_mapping_process")
  @@index([org_unit_template_id], map: "idx_org_unit_process_mapping_unit")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model org_unit_tasks {
  id                    String                  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  org_unit_template_id  String                  @db.Uuid
  task_code             String                  @db.VarChar(20)
  task_name             String                  @db.VarChar(200)
  task_description      String?
  frequency             String                  @db.VarChar(50)
  complexity_level      Int
  estimated_hours       Decimal?                @db.Decimal(6, 2)
  requires_approval     Boolean?                @default(false)
  created_at            DateTime?               @default(now()) @db.Timestamptz(6)
  updated_at            DateTime?               @default(now()) @db.Timestamptz(6)
  job_task_distribution job_task_distribution[]
  org_unit_templates    org_unit_templates      @relation(fields: [org_unit_template_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([org_unit_template_id, task_code])
  @@index([complexity_level], map: "idx_org_unit_tasks_complexity")
  @@index([frequency], map: "idx_org_unit_tasks_frequency")
  @@index([org_unit_template_id], map: "idx_org_unit_tasks_unit")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model org_unit_templates {
  id                       String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  template_id              String                     @db.Uuid
  parent_id                String?                    @db.Uuid
  path                     Unsupported("ltree")?
  depth                    Int                        @default(0)
  sort_order               Int?                       @default(0)
  code                     String                     @db.VarChar(20)
  cost_center              String?                    @db.VarChar(10)
  name_it                  String                     @db.VarChar(255)
  name_en                  String?                    @db.VarChar(255)
  short_name               String?                    @db.VarChar(50)
  area_code                String?                    @db.VarChar(10)
  level                    Int?
  level_name               String?                    @db.VarChar(50)
  nature                   String?                    @db.VarChar(20)
  is_line                  Boolean?                   @default(true)
  is_management            Boolean?                   @default(false)
  headcount_min            Int?
  headcount_max            Int?
  typical_span             Int?
  description              String?
  responsibilities         String[]
  created_at               DateTime?                  @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?                  @default(now()) @db.Timestamptz(6)
  org_unit_kpis            org_unit_kpis[]
  org_unit_process_mapping org_unit_process_mapping[]
  org_unit_tasks           org_unit_tasks[]

  @@index([area_code], map: "idx_org_units_area")
  @@index([level], map: "idx_org_units_level")
  @@index([parent_id], map: "idx_org_units_parent")
  @@index([path], map: "idx_org_units_path", type: Gist)
  @@index([template_id], map: "idx_org_units_template")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model org_units {
  id                                         String                         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                  String                         @db.Uuid
  code                                       String                         @db.VarChar(30)
  name                                       String                         @db.VarChar(150)
  name_en                                    String?                        @db.VarChar(150)
  parent_id                                  String?                        @db.Uuid
  org_level                                  Int                            @default(1)
  org_type                                   String?                        @db.VarChar(30)
  legacy_department_id                       String?                        @db.Uuid
  default_location_id                        String?                        @db.Uuid
  manager_id                                 String?                        @db.Uuid
  deputy_manager_id                          String?                        @db.Uuid
  headcount_budget                           Int?
  is_active                                  Boolean?                       @default(true)
  valid_from                                 DateTime?                      @default(dbgenerated("CURRENT_DATE")) @db.Date
  valid_to                                   DateTime?                      @db.Date
  sort_order                                 Int?                           @default(0)
  created_at                                 DateTime?                      @default(now()) @db.Timestamptz(6)
  updated_at                                 DateTime?                      @default(now()) @db.Timestamptz(6)
  sap_objid                                  String?                        @db.VarChar(20)
  sap_short                                  String?                        @db.VarChar(20)
  sap_stext                                  String?                        @db.VarChar(100)
  sap_istat                                  String?                        @db.VarChar(5)
  sap_plvar                                  String?                        @default("01") @db.VarChar(5)
  sap_sync_date                              DateTime?                      @db.Timestamp(6)
  embedding                                  Unsupported("vector")?
  embedding_text_hash                        String?                        @db.VarChar(64)
  embedding_model                            String?                        @db.VarChar(100)
  embedding_generated_at                     DateTime?                      @db.Timestamptz(6)
  deleted_at                                 DateTime?                      @db.Timestamptz(6)
  description                                String?
  color                                      String?                        @db.VarChar(50)
  icon                                       String?                        @db.VarChar(100)
  sap_kostl                                  String?                        @db.VarChar(50)
  name_it                                    String?                        @db.VarChar(255)
  description_it                             String?
  description_en                             String?
  calibration_sessions                       calibration_sessions[]
  contracts                                  contracts[]
  employees_employees_org_unit_idToorg_units employees[]                    @relation("employees_org_unit_idToorg_units")
  goal_templates                             goal_templates[]
  internal_mobility_postings                 internal_mobility_postings[]
  job_analysis                               job_analysis[]
  job_postings                               job_postings[]
  org_scenarios                              org_scenarios[]
  employees_org_units_manager_idToemployees  employees?                     @relation("org_units_manager_idToemployees", fields: [manager_id], references: [id], onUpdate: NoAction, map: "fk_ou_manager")
  tenants                                    tenants                        @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  skill_requirements_templates               skill_requirements_templates[]
  workforce_plan_actions                     workforce_plan_actions[]

  @@index([legacy_department_id], map: "idx_org_units_department")
  @@index([embedding], map: "idx_org_units_embedding")
  @@index([manager_id], map: "idx_org_units_manager_id")
  @@index([tenant_id], map: "idx_org_units_tenant")
  @@index([org_type], map: "idx_org_units_type")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model pa0000 {
  id         Int       @default(autoincrement())
  pernr      String    @db.VarChar(8)
  subty      String    @default("") @db.VarChar(4)
  endda      DateTime  @db.Date
  begda      DateTime  @db.Date
  massn      String?   @db.VarChar(2)
  massg      String?   @db.VarChar(2)
  stat2      String?   @db.VarChar(1)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@index([pernr], map: "idx_pa0000_pernr")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model pa0001 {
  id         Int       @default(autoincrement())
  pernr      String    @db.VarChar(8)
  endda      DateTime  @db.Date
  begda      DateTime  @db.Date
  bukrs      String?   @db.VarChar(4)
  werks      String?   @db.VarChar(4)
  btrtl      String?   @db.VarChar(4)
  persg      String?   @db.VarChar(1)
  persk      String?   @db.VarChar(2)
  plans      String?   @db.VarChar(8)
  orgeh      String?   @db.VarChar(8)
  stell      String?   @db.VarChar(8)
  kostl      String?   @db.VarChar(10)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@index([orgeh], map: "idx_pa0001_orgeh")
  @@index([pernr], map: "idx_pa0001_pernr")
  @@index([plans], map: "idx_pa0001_plans")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model pa0002 {
  id         Int       @default(autoincrement())
  pernr      String    @db.VarChar(8)
  endda      DateTime  @db.Date
  begda      DateTime  @db.Date
  nachn      String    @db.VarChar(40)
  vorna      String    @db.VarChar(40)
  midnm      String?   @db.VarChar(40)
  gbdat      DateTime? @db.Date
  gbort      String?   @db.VarChar(40)
  natio      String?   @db.VarChar(3)
  gesch      String?   @db.VarChar(1)
  famst      String?   @db.VarChar(1)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@index([pernr], map: "idx_pa0002_pernr")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa0003 {
  id                  Int       @default(autoincrement())
  pernr               String    @db.VarChar(8)
  subty               String?   @default("0001") @db.VarChar(4)
  endda               DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda               DateTime  @db.Date
  abession            String?   @db.VarChar(4)
  aession             String?   @db.VarChar(2)
  stat2               String?   @db.VarChar(2)
  most_recent_payroll DateTime? @db.Date
  next_payroll        DateTime? @db.Date
  retro_from          DateTime? @db.Date
  created_at          DateTime? @default(now()) @db.Timestamp(6)
  updated_at          DateTime? @default(now()) @db.Timestamp(6)

  @@index([pernr], map: "idx_pa0003_pernr")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa0005 {
  id                 Int       @default(autoincrement())
  pernr              String    @db.VarChar(8)
  subty              String    @db.VarChar(4)
  endda              DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda              DateTime  @db.Date
  annual_entitlement Decimal?  @db.Decimal(7, 2)
  carry_over_max     Decimal?  @db.Decimal(7, 2)
  accrual_rule       String?   @db.VarChar(4)
  service_years_rule Boolean?  @default(false)
  service_increment  Decimal?  @db.Decimal(5, 2)
  created_at         DateTime? @default(now()) @db.Timestamp(6)
  updated_at         DateTime? @default(now()) @db.Timestamp(6)

  @@index([pernr], map: "idx_pa0005_pernr")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model pa0006 {
  id         Int       @default(autoincrement())
  pernr      String    @db.VarChar(8)
  subty      String    @db.VarChar(4)
  endda      DateTime  @db.Date
  begda      DateTime  @db.Date
  stras      String?   @db.VarChar(60)
  ort01      String?   @db.VarChar(40)
  pstlz      String?   @db.VarChar(10)
  land1      String?   @db.VarChar(3)
  regio      String?   @db.VarChar(3)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@index([pernr], map: "idx_pa0006_pernr")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa0007 {
  id         Int       @default(autoincrement())
  pernr      String    @db.VarChar(8)
  subty      String    @default("0") @db.VarChar(4)
  endda      DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda      DateTime  @db.Date
  schkz      String?   @db.VarChar(8)
  zession    Decimal?  @db.Decimal(5, 2)
  wession    Decimal?  @db.Decimal(5, 2)
  arbst      Decimal?  @db.Decimal(5, 2)
  wkwdy      Int?      @default(5)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@index([pernr], map: "idx_pa0007_pernr")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa0008 {
  id         Int       @default(autoincrement())
  pernr      String    @db.VarChar(8)
  subty      String    @default("0") @db.VarChar(4)
  endda      DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda      DateTime  @db.Date
  trfar      String?   @db.VarChar(2)
  trfgb      String?   @db.VarChar(2)
  trfgr      String?   @db.VarChar(8)
  trfst      String?   @db.VarChar(2)
  ansal      Decimal?  @db.Decimal(15, 2)
  bet01      Decimal?  @db.Decimal(15, 2)
  lga01      String?   @db.VarChar(4)
  bet02      Decimal?  @db.Decimal(15, 2)
  lga02      String?   @db.VarChar(4)
  divgv      Int?      @default(12)
  waession   String?   @default("EUR") @db.VarChar(5)
  bession    Decimal?  @default(100.00) @db.Decimal(5, 2)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@index([pernr], map: "idx_pa0008_pernr")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model pa0009 {
  id         Int       @default(autoincrement())
  pernr      String    @db.VarChar(8)
  subty      String    @default("0") @db.VarChar(4)
  endda      DateTime  @db.Date
  begda      DateTime  @db.Date
  banks      String?   @db.VarChar(3)
  bankl      String?   @db.VarChar(15)
  bankn      String?   @db.VarChar(18)
  iban       String?   @db.VarChar(34)
  swift      String?   @db.VarChar(11)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa0014 {
  id         Int       @default(autoincrement())
  pernr      String    @db.VarChar(8)
  subty      String    @db.VarChar(4)
  lgart      String    @db.VarChar(4)
  endda      DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda      DateTime  @db.Date
  seqnr      Int?      @default(1)
  betrg      Decimal?  @db.Decimal(13, 2)
  anzhl      Decimal?  @db.Decimal(7, 3)
  waession   String?   @default("USD") @db.VarChar(5)
  zession    String?   @db.VarChar(2)
  kostl      String?   @db.VarChar(10)
  created_at DateTime? @default(now()) @db.Timestamp(6)
  updated_at DateTime? @default(now()) @db.Timestamp(6)

  @@index([lgart], map: "idx_pa0014_lgart")
  @@index([pernr], map: "idx_pa0014_pernr")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa0015 {
  id         Int       @default(autoincrement())
  pernr      String    @db.VarChar(8)
  subty      String?   @default("0001") @db.VarChar(4)
  lgart      String    @db.VarChar(4)
  endda      DateTime  @db.Date
  begda      DateTime  @db.Date
  seqnr      Int?      @default(1)
  betrg      Decimal   @db.Decimal(13, 2)
  waession   String?   @default("USD") @db.VarChar(5)
  reason     String?   @db.VarChar(4)
  pay_date   DateTime? @db.Date
  processed  Boolean?  @default(false)
  kostl      String?   @db.VarChar(10)
  created_at DateTime? @default(now()) @db.Timestamp(6)
  updated_at DateTime? @default(now()) @db.Timestamp(6)

  @@index([lgart], map: "idx_pa0015_lgart")
  @@index([pernr], map: "idx_pa0015_pernr")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa0016 {
  id               Int       @default(autoincrement())
  pernr            String    @db.VarChar(8)
  subty            String    @default("01") @db.VarChar(4)
  endda            DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda            DateTime  @db.Date
  cttyp            String?   @db.VarChar(2)
  cession          String?   @db.VarChar(20)
  pession          String?   @db.VarChar(3)
  massn            String?   @db.VarChar(2)
  termination_date DateTime? @db.Date
  probation_end    DateTime? @db.Date
  created_at       DateTime? @default(now()) @db.Timestamp(6)

  @@index([pernr], map: "idx_pa0016_pernr")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model pa0017 {
  id            Int       @default(autoincrement())
  pernr         String    @db.VarChar(8)
  subty         String?   @default("0001") @db.VarChar(4)
  endda         DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda         DateTime  @db.Date
  travel_class  String?   @db.VarChar(4)
  car_policy    String?   @db.VarChar(4)
  hotel_policy  String?   @db.VarChar(4)
  per_diem_rate Decimal?  @db.Decimal(9, 2)
  currency      String?   @default("USD") @db.VarChar(5)
  created_at    DateTime? @default(now()) @db.Timestamp(6)

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa0019 {
  id             Int       @default(autoincrement())
  pernr          String    @db.VarChar(8)
  subty          String    @db.VarChar(4)
  endda          DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda          DateTime  @default(dbgenerated("'1900-01-01'::date")) @db.Date
  dat01          DateTime? @db.Date
  dat02          DateTime? @db.Date
  dat03          DateTime? @db.Date
  date_type_text String?   @db.VarChar(40)
  reminder_days  Int?
  reminder_sent  Boolean?  @default(false)
  created_at     DateTime? @default(now()) @db.Timestamp(6)
  updated_at     DateTime? @default(now()) @db.Timestamp(6)

  @@index([dat01, dat02, dat03], map: "idx_pa0019_dates")
  @@index([pernr], map: "idx_pa0019_pernr")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa0021 {
  id         Int       @default(autoincrement())
  pernr      String    @db.VarChar(8)
  subty      String    @db.VarChar(4)
  endda      DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda      DateTime  @db.Date
  famsa      String?   @db.VarChar(2)
  faession   String?   @db.VarChar(40)
  fession    String?   @db.VarChar(40)
  gbdat      DateTime? @db.Date
  fasession  String?   @db.VarChar(1)
  favor      Boolean?  @default(false)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@index([pernr], map: "idx_pa0021_pernr")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa0022 {
  id                     Int       @default(autoincrement())
  pernr                  String    @db.VarChar(8)
  subty                  String    @db.VarChar(4)
  endda                  DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda                  DateTime  @db.Date
  slart                  String?   @db.VarChar(4)
  insti                  String?   @db.VarChar(80)
  slabs                  String?   @db.VarChar(80)
  anession               String?   @db.VarChar(4)
  sltp1                  String?   @db.VarChar(40)
  sltp2                  String?   @db.VarChar(40)
  end_year               Int?
  grade                  String?   @db.VarChar(10)
  esco_qualification_uri String?   @db.VarChar(255)
  created_at             DateTime? @default(now()) @db.Timestamp(6)

  @@index([pernr], map: "idx_pa0022_pernr")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa0024 {
  id                 Int       @default(autoincrement())
  pernr              String    @db.VarChar(8)
  subty              String    @default("0001") @db.VarChar(4)
  endda              DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda              DateTime  @db.Date
  quali              String    @db.VarChar(8)
  proficiency        String?   @db.VarChar(2)
  target_proficiency String?   @db.VarChar(2)
  years_experience   Decimal?  @db.Decimal(4, 1)
  verified_date      DateTime? @db.Date
  verified_by        String?   @db.VarChar(8)
  esco_skill_uri     String?   @db.VarChar(255)
  created_at         DateTime? @default(now()) @db.Timestamp(6)

  @@index([esco_skill_uri], map: "idx_pa0024_esco")
  @@index([pernr], map: "idx_pa0024_pernr")
  @@index([quali], map: "idx_pa0024_quali")
  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa0025 {
  id                  Int       @id @default(autoincrement())
  pernr               String    @db.VarChar(8)
  subty               String    @db.VarChar(4)
  endda               DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda               DateTime  @db.Date
  appraisal_id        String?   @db.VarChar(20)
  bession             String?   @db.VarChar(2)
  potential_rating    String?   @db.VarChar(2)
  reviewer_pernr      String?   @db.VarChar(8)
  appraisal_date      DateTime? @db.Date
  review_period_start DateTime? @db.Date
  review_period_end   DateTime? @db.Date
  status              String?   @db.VarChar(2)
  comments            String?
  development_notes   String?
  created_at          DateTime? @default(now()) @db.Timestamp(6)

  @@unique([pernr, subty, endda, appraisal_id])
  @@index([pernr], map: "idx_pa0025_pernr")
  @@index([reviewer_pernr], map: "idx_pa0025_reviewer")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa0027 {
  id         Int       @default(autoincrement())
  pernr      String    @db.VarChar(8)
  subty      String?   @default("0001") @db.VarChar(4)
  endda      DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda      DateTime  @db.Date
  seqnr      Int?      @default(1)
  kostl      String    @db.VarChar(10)
  pession    Decimal   @db.Decimal(5, 2)
  bukrs      String?   @db.VarChar(4)
  gsber      String?   @db.VarChar(4)
  created_at DateTime? @default(now()) @db.Timestamp(6)
  updated_at DateTime? @default(now()) @db.Timestamp(6)

  @@index([kostl], map: "idx_pa0027_kostl")
  @@index([pernr], map: "idx_pa0027_pernr")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model pa0032 {
  id              Int       @default(autoincrement())
  pernr           String    @db.VarChar(8)
  subty           String?   @default("0001") @db.VarChar(4)
  endda           DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda           DateTime  @db.Date
  internal_field1 String?   @db.VarChar(40)
  internal_field2 String?   @db.VarChar(40)
  internal_field3 String?   @db.VarChar(40)
  internal_field4 String?   @db.VarChar(40)
  notes           String?
  created_at      DateTime? @default(now()) @db.Timestamp(6)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model pa0041 {
  id         Int       @default(autoincrement())
  pernr      String    @db.VarChar(8)
  subty      String?   @default("0001") @db.VarChar(4)
  endda      DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda      DateTime  @db.Date
  dar01      DateTime? @db.Date
  dar02      DateTime? @db.Date
  dar03      DateTime? @db.Date
  dar04      DateTime? @db.Date
  dar05      DateTime? @db.Date
  dar06      DateTime? @db.Date
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model pa0105 {
  id         Int       @default(autoincrement())
  pernr      String    @db.VarChar(8)
  subty      String    @db.VarChar(4)
  endda      DateTime  @db.Date
  begda      DateTime  @db.Date
  usrid      String?   @db.VarChar(30)
  usrid_long String?   @db.VarChar(241)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@index([pernr], map: "idx_pa0105_pernr")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa0167 {
  id               Int       @default(autoincrement())
  pernr            String    @db.VarChar(8)
  subty            String    @db.VarChar(4)
  endda            DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda            DateTime  @db.Date
  seqnr            Int?      @default(1)
  bession          String    @db.VarChar(4)
  bession_option   String?   @db.VarChar(4)
  coverage_level   String?   @db.VarChar(2)
  enrollment_date  DateTime? @db.Date
  effective_date   DateTime? @db.Date
  ee_contribution  Decimal?  @db.Decimal(13, 2)
  er_contribution  Decimal?  @db.Decimal(13, 2)
  total_premium    Decimal?  @db.Decimal(13, 2)
  currency         String?   @default("USD") @db.VarChar(5)
  dependents_count Int?      @default(0)
  created_at       DateTime? @default(now()) @db.Timestamp(6)
  updated_at       DateTime? @default(now()) @db.Timestamp(6)

  @@index([bession], map: "idx_pa0167_bession")
  @@index([pernr], map: "idx_pa0167_pernr")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa0168 {
  id                Int       @default(autoincrement())
  pernr             String    @db.VarChar(8)
  subty             String    @db.VarChar(4)
  endda             DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda             DateTime  @db.Date
  bession           String    @db.VarChar(4)
  coverage_amount   Decimal?  @db.Decimal(13, 2)
  coverage_multiple Decimal?  @db.Decimal(3, 1)
  ee_contribution   Decimal?  @db.Decimal(13, 2)
  er_contribution   Decimal?  @db.Decimal(13, 2)
  currency          String?   @default("USD") @db.VarChar(5)
  beneficiary_type  String?   @db.VarChar(4)
  beneficiary_name  String?   @db.VarChar(80)
  beneficiary_pct   Decimal?  @default(100.00) @db.Decimal(5, 2)
  created_at        DateTime? @default(now()) @db.Timestamp(6)
  updated_at        DateTime? @default(now()) @db.Timestamp(6)

  @@index([bession], map: "idx_pa0168_bession")
  @@index([pernr], map: "idx_pa0168_pernr")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa0169 {
  id                  Int       @default(autoincrement())
  pernr               String    @db.VarChar(8)
  subty               String    @db.VarChar(4)
  endda               DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda               DateTime  @db.Date
  bession             String    @db.VarChar(4)
  ee_contribution_pct Decimal?  @db.Decimal(5, 2)
  ee_contribution_amt Decimal?  @db.Decimal(13, 2)
  er_match_pct        Decimal?  @db.Decimal(5, 2)
  er_match_cap        Decimal?  @db.Decimal(5, 2)
  investment_option   String?   @db.VarChar(4)
  vesting_pct         Decimal?  @db.Decimal(5, 2)
  vesting_date        DateTime? @db.Date
  current_balance     Decimal?  @db.Decimal(15, 2)
  created_at          DateTime? @default(now()) @db.Timestamp(6)
  updated_at          DateTime? @default(now()) @db.Timestamp(6)

  @@index([bession], map: "idx_pa0169_bession")
  @@index([pernr], map: "idx_pa0169_pernr")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa0170 {
  id                Int       @default(autoincrement())
  pernr             String    @db.VarChar(8)
  subty             String    @db.VarChar(4)
  endda             DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda             DateTime  @db.Date
  bession           String    @db.VarChar(4)
  annual_election   Decimal?  @db.Decimal(13, 2)
  er_contribution   Decimal?  @db.Decimal(13, 2)
  ytd_contributions Decimal?  @db.Decimal(13, 2)
  ytd_disbursements Decimal?  @db.Decimal(13, 2)
  current_balance   Decimal?  @db.Decimal(13, 2)
  rollover_balance  Decimal?  @db.Decimal(13, 2)
  plan_year         Int?
  grace_period_end  DateTime? @db.Date
  created_at        DateTime? @default(now()) @db.Timestamp(6)
  updated_at        DateTime? @default(now()) @db.Timestamp(6)

  @@index([bession], map: "idx_pa0170_bession")
  @@index([pernr], map: "idx_pa0170_pernr")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa0171 {
  id                Int       @default(autoincrement())
  pernr             String    @db.VarChar(8)
  subty             String    @db.VarChar(4)
  endda             DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda             DateTime  @db.Date
  bession           String    @db.VarChar(4)
  bession_text      String?   @db.VarChar(80)
  annual_allowance  Decimal?  @db.Decimal(13, 2)
  ytd_used          Decimal?  @db.Decimal(13, 2)
  remaining         Decimal?  @db.Decimal(13, 2)
  enrollment_status String?   @default("01") @db.VarChar(2)
  eligible_date     DateTime? @db.Date
  created_at        DateTime? @default(now()) @db.Timestamp(6)
  updated_at        DateTime? @default(now()) @db.Timestamp(6)

  @@index([bession], map: "idx_pa0171_bession")
  @@index([pernr], map: "idx_pa0171_pernr")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa0185 {
  id          Int       @default(autoincrement())
  pernr       String    @db.VarChar(8)
  subty       String    @db.VarChar(4)
  endda       DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda       DateTime  @db.Date
  ictyp       String?   @db.VarChar(2)
  icnum       String?   @db.VarChar(40)
  icnam       String?   @db.VarChar(80)
  fpession    String?   @db.VarChar(3)
  issue_date  DateTime? @db.Date
  expiry_date DateTime? @db.Date
  created_at  DateTime? @default(now()) @db.Timestamp(6)

  @@unique([pernr, subty, endda, ictyp])
  @@index([pernr], map: "idx_pa0185_pernr")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa2000 {
  id         Int       @default(autoincrement())
  pernr      String    @db.VarChar(8)
  subty      String?   @default("0001") @db.VarChar(4)
  objps      String?   @default("01") @db.VarChar(2)
  sprps      Boolean?  @default(false)
  endda      DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda      DateTime  @db.Date
  seqnr      Int?      @default(1)
  mession    String?   @db.VarChar(2)
  zession    String?   @db.VarChar(2)
  schkz      String?   @db.VarChar(8)
  zterf      String?   @db.VarChar(4)
  empct      Decimal?  @default(100.00) @db.Decimal(5, 2)
  mostd      Decimal?  @db.Decimal(5, 2)
  wostd      Decimal?  @db.Decimal(5, 2)
  arbst      Decimal?  @db.Decimal(5, 2)
  wkmod      String?   @db.VarChar(4)
  aession    String?   @db.VarChar(12)
  uession    DateTime? @default(now()) @db.Timestamp(6)
  created_at DateTime? @default(now()) @db.Timestamp(6)
  updated_at DateTime? @default(now()) @db.Timestamp(6)

  @@index([begda, endda], map: "idx_pa2000_dates")
  @@index([pernr], map: "idx_pa2000_pernr")
  @@index([schkz], map: "idx_pa2000_schkz")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa2001 {
  id         Int       @default(autoincrement())
  pernr      String    @db.VarChar(8)
  subty      String    @db.VarChar(4)
  endda      DateTime  @db.Date
  begda      DateTime  @db.Date
  abwtg      Decimal?  @db.Decimal(9, 2)
  stdaz      Decimal?  @db.Decimal(9, 2)
  awart      String?   @db.VarChar(30)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@index([begda, endda], map: "idx_pa2001_dates")
  @@index([pernr], map: "idx_pa2001_pernr")
  @@index([subty], map: "idx_pa2001_subty")
  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa2002 {
  id         Int       @id @default(autoincrement())
  pernr      String    @db.VarChar(8)
  subty      String    @db.VarChar(4)
  ldate      DateTime  @db.Date
  beguz      DateTime? @db.Time(6)
  enduz      DateTime? @db.Time(6)
  anzhl      Decimal?  @db.Decimal(5, 2)
  awart      String?   @db.VarChar(4)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@index([pernr], map: "idx_pa2002_pernr")
}

model pa2003 {
  id                  Int       @id @default(autoincrement())
  pernr               String    @db.VarChar(8)
  subty               String?   @default("0001") @db.VarChar(4)
  endda               DateTime  @db.Date
  begda               DateTime  @db.Date
  substitute_pernr    String?   @db.VarChar(8)
  substitute_position String?   @db.VarChar(8)
  reason              String?   @db.VarChar(4)
  created_at          DateTime? @default(now()) @db.Timestamp(6)

  @@unique([pernr, subty, begda], map: "pa2003_unique")
}

model pa2004 {
  id                Int       @id @default(autoincrement())
  pernr             String    @db.VarChar(8)
  subty             String?   @default("0001") @db.VarChar(4)
  endda             DateTime  @db.Date
  begda             DateTime  @db.Date
  oncall_type       String?   @db.VarChar(4)
  start_time        DateTime? @db.Time(6)
  end_time          DateTime? @db.Time(6)
  compensation_type String?   @db.VarChar(4)
  created_at        DateTime? @default(now()) @db.Timestamp(6)

  @@unique([pernr, subty, begda], map: "pa2004_unique")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa2005 {
  id           Int       @default(autoincrement())
  pernr        String    @db.VarChar(8)
  subty        String?   @default("0001") @db.VarChar(4)
  objps        String?   @default("01") @db.VarChar(2)
  sprps        Boolean?  @default(false)
  endda        DateTime  @db.Date
  begda        DateTime  @db.Date
  seqnr        Int?      @default(1)
  uession_type String?   @db.VarChar(4)
  stdaz        Decimal?  @db.Decimal(7, 2)
  beguz        DateTime? @db.Time(6)
  enduz        DateTime? @db.Time(6)
  zession_type String?   @db.VarChar(2)
  multi        Decimal?  @default(1.50) @db.Decimal(3, 2)
  kostl        String?   @db.VarChar(10)
  approved     Boolean?  @default(false)
  approved_by  String?   @db.VarChar(12)
  approved_at  DateTime? @db.Timestamp(6)
  aession      String?   @db.VarChar(12)
  uession      DateTime? @default(now()) @db.Timestamp(6)
  created_at   DateTime? @default(now()) @db.Timestamp(6)
  updated_at   DateTime? @default(now()) @db.Timestamp(6)

  @@index([begda, endda], map: "idx_pa2005_dates")
  @@index([pernr], map: "idx_pa2005_pernr")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa2006 {
  id           Int       @default(autoincrement())
  pernr        String    @db.VarChar(8)
  subty        String    @db.VarChar(4)
  objps        String?   @default("01") @db.VarChar(2)
  sprps        Boolean?  @default(false)
  endda        DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda        DateTime  @db.Date
  seqnr        Int?      @default(1)
  ktart        String?   @db.VarChar(4)
  kession_text String?   @db.VarChar(40)
  anzhl        Decimal?  @db.Decimal(7, 2)
  kession      Decimal?  @db.Decimal(7, 2)
  remainder    Decimal?  @db.Decimal(7, 2)
  unit         String?   @default("DAY") @db.VarChar(3)
  desta        DateTime? @db.Date
  deession     DateTime? @db.Date
  quession     DateTime? @db.Date
  aession      String?   @db.VarChar(12)
  uession      DateTime? @default(now()) @db.Timestamp(6)
  created_at   DateTime? @default(now()) @db.Timestamp(6)
  updated_at   DateTime? @default(now()) @db.Timestamp(6)

  @@index([begda, endda], map: "idx_pa2006_dates")
  @@index([ktart], map: "idx_pa2006_ktart")
  @@index([pernr], map: "idx_pa2006_pernr")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa2007 {
  id              Int       @default(autoincrement())
  pernr           String    @db.VarChar(8)
  subty           String    @db.VarChar(4)
  objps           String?   @default("01") @db.VarChar(2)
  sprps           Boolean?  @default(false)
  endda           DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda           DateTime  @db.Date
  seqnr           Int?      @default(1)
  ktart           String?   @db.VarChar(4)
  anzhl           Decimal?  @db.Decimal(7, 2)
  kession         Decimal?  @db.Decimal(7, 2)
  remainder       Decimal?  @db.Decimal(7, 2)
  unit            String?   @default("HRS") @db.VarChar(3)
  quession_source String?   @db.VarChar(4)
  quession_rule   String?   @db.VarChar(8)
  aession         String?   @db.VarChar(12)
  uession         DateTime? @default(now()) @db.Timestamp(6)
  created_at      DateTime? @default(now()) @db.Timestamp(6)
  updated_at      DateTime? @default(now()) @db.Timestamp(6)

  @@index([ktart], map: "idx_pa2007_ktart")
  @@index([pernr], map: "idx_pa2007_pernr")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa2010 {
  id             Int       @id @default(autoincrement())
  pernr          String    @db.VarChar(8)
  subty          String?   @default("0001") @db.VarChar(4)
  objps          String?   @default("01") @db.VarChar(2)
  sprps          Boolean?  @default(false)
  endda          DateTime  @db.Date
  begda          DateTime  @db.Date
  seqnr          Int?      @default(1)
  lgart          String    @db.VarChar(4)
  beession       Decimal?  @db.Decimal(13, 2)
  anzhl          Decimal?  @db.Decimal(7, 3)
  eession        String?   @db.VarChar(3)
  waession       String?   @default("USD") @db.VarChar(5)
  zession_period String?   @db.VarChar(2)
  kostl          String?   @db.VarChar(10)
  aession        String?   @db.VarChar(12)
  uession        DateTime? @default(now()) @db.Timestamp(6)
  created_at     DateTime? @default(now()) @db.Timestamp(6)
  updated_at     DateTime? @default(now()) @db.Timestamp(6)

  @@unique([pernr, lgart, begda, seqnr], map: "pa2010_unique")
  @@index([begda, endda], map: "idx_pa2010_dates")
  @@index([lgart], map: "idx_pa2010_lgart")
  @@index([pernr], map: "idx_pa2010_pernr")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pa2011 {
  id         Int       @id @default(autoincrement())
  pernr      String    @db.VarChar(8)
  subty      String?   @default("0001") @db.VarChar(4)
  ldate      DateTime  @db.Date
  ltime      DateTime  @db.Time(6)
  seession   Int?      @default(1)
  satession  String    @db.VarChar(2)
  terminal   String?   @db.VarChar(8)
  origession String?   @default("M") @db.VarChar(2)
  status     String?   @default("0") @db.VarChar(2)
  processed  Boolean?  @default(false)
  aession    String?   @db.VarChar(12)
  uession    DateTime? @default(now()) @db.Timestamp(6)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@unique([pernr, ldate, ltime, satession], map: "pa2011_unique")
  @@index([ldate], map: "idx_pa2011_date")
  @@index([satession], map: "idx_pa2011_event")
  @@index([pernr], map: "idx_pa2011_pernr")
}

model pa2012 {
  id            Int       @id @default(autoincrement())
  pernr         String    @db.VarChar(8)
  subty         String?   @default("0001") @db.VarChar(4)
  endda         DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda         DateTime  @db.Date
  transfer_type String?   @db.VarChar(4)
  source_quota  String?   @db.VarChar(4)
  target_quota  String?   @db.VarChar(4)
  amount        Decimal?  @db.Decimal(7, 2)
  created_at    DateTime? @default(now()) @db.Timestamp(6)

  @@unique([pernr, subty, begda], map: "pa2012_unique")
}

model pa2013 {
  id                Int       @id @default(autoincrement())
  pernr             String    @db.VarChar(8)
  subty             String    @db.VarChar(4)
  endda             DateTime  @db.Date
  begda             DateTime  @db.Date
  quota_type        String?   @db.VarChar(4)
  correction_amount Decimal?  @db.Decimal(7, 2)
  correction_reason String?   @db.VarChar(4)
  approved_by       String?   @db.VarChar(8)
  created_at        DateTime? @default(now()) @db.Timestamp(6)

  @@unique([pernr, subty, quota_type, begda], map: "pa2013_unique")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model page_table_relations {
  id             String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  page_id        String         @db.Uuid
  table_name     String         @db.VarChar(128)
  relation_type  String         @default("primary") @db.VarChar(20)
  source         String         @default("manual") @db.VarChar(20)
  confidence     Int            @default(100) @db.SmallInt
  created_at     DateTime       @default(now()) @db.Timestamptz(6)
  updated_at     DateTime       @default(now()) @db.Timestamptz(6)
  platform_pages platform_pages @relation(fields: [page_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([page_id, table_name, relation_type])
  @@index([page_id], map: "idx_ptr_page")
  @@index([table_name], map: "idx_ptr_table")
  @@index([relation_type], map: "idx_ptr_type")
}

model page_table_sync_log {
  id                BigInt   @id @default(autoincrement())
  sync_type         String   @db.VarChar(30)
  tables_discovered Int?     @default(0)
  relations_created Int?     @default(0)
  relations_removed Int?     @default(0)
  orphans_found     Int?     @default(0)
  duration_ms       Int?
  details           Json?
  created_at        DateTime @default(now()) @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model payroll_anomaly_patterns {
  id                  String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String    @db.Uuid
  pattern_code        String    @db.VarChar(50)
  pattern_name        String    @db.VarChar(200)
  pattern_description String?
  detection_type      String    @db.VarChar(30)
  detection_config    Json
  severity            String?   @default("warning") @db.VarChar(20)
  requires_approval   Boolean?  @default(false)
  is_active           Boolean?  @default(true)
  created_at          DateTime? @default(now()) @db.Timestamp(6)
  updated_at          DateTime? @default(now()) @db.Timestamp(6)
  deleted_at          DateTime? @db.Timestamptz(6)
  tenants             tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_payroll_anomaly_patterns_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model payroll_export_employees {
  id                   String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  job_id               String              @db.Uuid
  employee_id          String              @db.Uuid
  tenant_id            String              @db.Uuid
  employee_code        String?             @db.VarChar(50)
  fiscal_code          String?             @db.VarChar(20)
  status               String?             @default("pending") @db.VarChar(30)
  sections_included    String[]
  validation_errors    Json?               @default("[]")
  validation_warnings  Json?               @default("[]")
  validation_overrides Json?               @default("[]")
  anagrafica_data      Json?
  presenze_data        Json?
  straordinari_data    Json?
  variazioni_data      Json?
  assenze_data         Json?
  days_worked          Int?
  days_absent          Int?
  hours_regular        Decimal?            @db.Decimal(6, 2)
  hours_overtime       Decimal?            @db.Decimal(6, 2)
  hours_night          Decimal?            @db.Decimal(6, 2)
  hours_holiday        Decimal?            @db.Decimal(6, 2)
  leave_breakdown      Json?
  processed_at         DateTime?           @db.Timestamp(6)
  export_line_number   Int?
  created_at           DateTime?           @default(now()) @db.Timestamp(6)
  updated_at           DateTime?           @default(now()) @db.Timestamp(6)
  employees            employees           @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  payroll_export_jobs  payroll_export_jobs @relation(fields: [job_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants              tenants             @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([employee_id], map: "idx_payroll_export_employees_employee")
  @@index([job_id], map: "idx_payroll_export_employees_job")
  @@index([status], map: "idx_payroll_export_employees_status")
  @@index([tenant_id], map: "idx_payroll_export_employees_tenant_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model payroll_export_files {
  id                  String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  job_id              String              @db.Uuid
  tenant_id           String              @db.Uuid
  file_type           String              @db.VarChar(30)
  file_name           String              @db.VarChar(255)
  file_path           String
  file_size           BigInt?
  file_hash           String?             @db.VarChar(64)
  mime_type           String?             @db.VarChar(100)
  storage_type        String?             @default("local") @db.VarChar(30)
  storage_reference   String?
  expires_at          DateTime?           @db.Timestamp(6)
  is_archived         Boolean?            @default(false)
  archived_at         DateTime?           @db.Timestamp(6)
  created_at          DateTime?           @default(now()) @db.Timestamp(6)
  downloaded_count    Int?                @default(0)
  last_downloaded_at  DateTime?           @db.Timestamp(6)
  last_downloaded_by  String?             @db.Uuid
  payroll_export_jobs payroll_export_jobs @relation(fields: [job_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants             tenants             @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([job_id], map: "idx_payroll_export_files_job")
  @@index([tenant_id], map: "idx_payroll_export_files_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model payroll_export_jobs {
  id                        String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                 String                     @db.Uuid
  integration_id            String?                    @db.Uuid
  job_number                String                     @db.VarChar(50)
  job_name                  String?                    @db.VarChar(200)
  pay_period_year           Int
  pay_period_month          Int
  period_start_date         DateTime                   @db.Date
  period_end_date           DateTime                   @db.Date
  export_type               String                     @db.VarChar(30)
  export_sections           String[]                   @default(["anagrafica", "presenze", "straordinari", "variazioni"])
  status                    String?                    @default("draft") @db.VarChar(30)
  progress_percent          Int?                       @default(0)
  current_phase             String?                    @db.VarChar(50)
  total_employees           Int?                       @default(0)
  exported_employees        Int?                       @default(0)
  employees_with_errors     Int?                       @default(0)
  employees_with_warnings   Int?                       @default(0)
  validation_started_at     DateTime?                  @db.Timestamp(6)
  validation_completed_at   DateTime?                  @db.Timestamp(6)
  validation_errors         Json?                      @default("[]")
  validation_warnings       Json?                      @default("[]")
  validation_summary        Json?
  export_started_at         DateTime?                  @db.Timestamp(6)
  export_completed_at       DateTime?                  @db.Timestamp(6)
  export_file_path          String?
  export_file_size          BigInt?
  export_file_hash          String?                    @db.VarChar(64)
  export_file_name          String?                    @db.VarChar(255)
  transmission_method       String?                    @db.VarChar(20)
  transmission_started_at   DateTime?                  @db.Timestamp(6)
  transmission_completed_at DateTime?                  @db.Timestamp(6)
  transmission_reference    String?                    @db.VarChar(100)
  transmission_response     Json?
  acknowledged_at           DateTime?                  @db.Timestamp(6)
  acknowledgment_reference  String?                    @db.VarChar(100)
  acknowledgment_details    Json?
  records_accepted          Int?
  records_rejected          Int?
  error_code                String?                    @db.VarChar(50)
  error_message             String?
  error_details             Json?
  retry_count               Int?                       @default(0)
  last_retry_at             DateTime?                  @db.Timestamp(6)
  created_at                DateTime?                  @default(now()) @db.Timestamp(6)
  updated_at                DateTime?                  @default(now()) @db.Timestamp(6)
  created_by                String?                    @db.Uuid
  submitted_by              String?                    @db.Uuid
  submitted_at              DateTime?                  @db.Timestamp(6)
  approved_by               String?                    @db.Uuid
  approved_at               DateTime?                  @db.Timestamp(6)
  employee_overtime         employee_overtime[]
  payroll_export_employees  payroll_export_employees[]
  payroll_export_files      payroll_export_files[]
  payroll_integrations      payroll_integrations?      @relation(fields: [integration_id], references: [id], onUpdate: NoAction)
  tenants                   tenants                    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  payroll_transmission_log  payroll_transmission_log[]

  @@index([integration_id], map: "idx_payroll_export_jobs_integration")
  @@index([job_number], map: "idx_payroll_export_jobs_number")
  @@index([pay_period_year, pay_period_month], map: "idx_payroll_export_jobs_period")
  @@index([status], map: "idx_payroll_export_jobs_status")
  @@index([tenant_id], map: "idx_payroll_export_jobs_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model payroll_field_mappings {
  id                   String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id            String                @db.Uuid
  integration_id       String?               @db.Uuid
  section              String                @db.VarChar(50)
  source_table         String                @db.VarChar(100)
  source_field         String                @db.VarChar(100)
  target_field         String                @db.VarChar(100)
  target_field_name    String?               @db.VarChar(200)
  target_position      Int?
  target_length        Int?
  transform_type       String?               @default("direct") @db.VarChar(30)
  transform_config     Json?
  format_pattern       String?               @db.VarChar(100)
  pad_character        String?               @db.VarChar(1)
  pad_direction        String?               @db.VarChar(5)
  is_required          Boolean?              @default(false)
  default_value        String?
  priority             Int?                  @default(100)
  is_active            Boolean?              @default(true)
  created_at           DateTime?             @default(now()) @db.Timestamp(6)
  updated_at           DateTime?             @default(now()) @db.Timestamp(6)
  deleted_at           DateTime?             @db.Timestamptz(6)
  payroll_integrations payroll_integrations? @relation(fields: [integration_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants              tenants               @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([integration_id], map: "idx_payroll_field_mappings_integration")
  @@index([section], map: "idx_payroll_field_mappings_section")
  @@index([tenant_id], map: "idx_payroll_field_mappings_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model payroll_integrations {
  id                      String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id               String                   @db.Uuid
  provider_name           String                   @db.VarChar(100)
  provider_code           String                   @db.VarChar(50)
  display_name            String?                  @db.VarChar(200)
  integration_type        String                   @default("file") @db.VarChar(30)
  api_endpoint            String?
  api_key_encrypted       String?
  api_secret_encrypted    String?
  sftp_host               String?
  sftp_port               Int?                     @default(22)
  sftp_username           String?
  sftp_password_encrypted String?
  sftp_path               String?
  company_code            String?                  @db.VarChar(50)
  fiscal_code             String?                  @db.VarChar(20)
  vat_number              String?                  @db.VarChar(20)
  inps_code               String?                  @db.VarChar(20)
  inail_code              String?                  @db.VarChar(20)
  export_format           String?                  @default("csv") @db.VarChar(20)
  file_encoding           String?                  @default("UTF-8") @db.VarChar(20)
  date_format             String?                  @default("DD/MM/YYYY") @db.VarChar(20)
  decimal_separator       String?                  @default(",") @db.VarChar(1)
  field_delimiter         String?                  @default(";") @db.VarChar(5)
  include_headers         Boolean?                 @default(true)
  auto_export_enabled     Boolean?                 @default(false)
  export_schedule         String?                  @db.VarChar(100)
  export_day_of_month     Int?
  export_cutoff_day       Int?
  notification_recipients String[]
  last_connection_test    DateTime?                @db.Timestamp(6)
  connection_status       String?                  @default("unknown") @db.VarChar(20)
  connection_error        String?
  is_active               Boolean?                 @default(true)
  is_primary              Boolean?                 @default(false)
  settings                Json?                    @default("{}")
  field_mappings          Json?                    @default("{}")
  created_at              DateTime?                @default(now()) @db.Timestamp(6)
  updated_at              DateTime?                @default(now()) @db.Timestamp(6)
  created_by              String?                  @db.Uuid
  updated_by              String?                  @db.Uuid
  deleted_at              DateTime?                @db.Timestamptz(6)
  payroll_export_jobs     payroll_export_jobs[]
  payroll_field_mappings  payroll_field_mappings[]
  tenants                 tenants                  @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, provider_code])
  @@index([is_active], map: "idx_payroll_integrations_active")
  @@index([provider_code], map: "idx_payroll_integrations_provider")
  @@index([tenant_id], map: "idx_payroll_integrations_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model payroll_transmission_log {
  id                   String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  job_id               String              @db.Uuid
  tenant_id            String              @db.Uuid
  transmission_type    String              @db.VarChar(30)
  transmission_attempt Int?                @default(1)
  request_timestamp    DateTime            @default(now()) @db.Timestamp(6)
  request_method       String?             @db.VarChar(20)
  request_url          String?
  request_headers      Json?
  request_size         BigInt?
  response_timestamp   DateTime?           @db.Timestamp(6)
  response_status      Int?
  response_headers     Json?
  response_body        String?
  response_size        BigInt?
  success              Boolean?
  error_code           String?             @db.VarChar(50)
  error_message        String?
  provider_reference   String?             @db.VarChar(100)
  provider_message     String?
  created_at           DateTime?           @default(now()) @db.Timestamp(6)
  initiated_by         String?             @db.Uuid
  payroll_export_jobs  payroll_export_jobs @relation(fields: [job_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants              tenants             @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([job_id], map: "idx_payroll_transmission_log_job")
  @@index([tenant_id], map: "idx_payroll_transmission_log_tenant")
  @@index([request_timestamp], map: "idx_payroll_transmission_log_timestamp")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model payroll_validation_rules {
  id                  String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String    @db.Uuid
  rule_code           String    @db.VarChar(50)
  rule_name           String    @db.VarChar(200)
  rule_description    String?
  category            String    @db.VarChar(50)
  rule_type           String    @db.VarChar(30)
  rule_config         Json
  severity            String?   @default("warning") @db.VarChar(20)
  is_blocking         Boolean?  @default(false)
  applies_to_sections String[]
  ccnl_types          String[]
  is_active           Boolean?  @default(true)
  created_at          DateTime? @default(now()) @db.Timestamp(6)
  updated_at          DateTime? @default(now()) @db.Timestamp(6)
  deleted_at          DateTime? @db.Timestamptz(6)
  tenants             tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([is_active], map: "idx_payroll_validation_rules_active")
  @@index([category], map: "idx_payroll_validation_rules_category")
  @@index([tenant_id], map: "idx_payroll_validation_rules_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pb0001 {
  id          Int       @default(autoincrement())
  aplnr       String    @db.VarChar(8)
  subty       String?   @default("0001") @db.VarChar(4)
  endda       DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda       DateTime  @db.Date
  seqnr       Int?      @default(1)
  massn       String    @db.VarChar(2)
  massg       String?   @db.VarChar(2)
  stat2       String?   @default("01") @db.VarChar(2)
  processor   String?   @db.VarChar(8)
  action_date DateTime? @db.Date
  created_at  DateTime? @default(now()) @db.Timestamp(6)
  updated_at  DateTime? @default(now()) @db.Timestamp(6)

  @@index([aplnr], map: "idx_pb0001_aplnr")
  @@index([massn], map: "idx_pb0001_massn")
  @@index([stat2], map: "idx_pb0001_stat2")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pb0002 {
  id             Int       @default(autoincrement())
  aplnr          String    @db.VarChar(8)
  endda          DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda          DateTime  @default(dbgenerated("'1900-01-01'::date")) @db.Date
  nachn          String    @db.VarChar(40)
  vorna          String    @db.VarChar(40)
  midnm          String?   @db.VarChar(40)
  gbdat          DateTime? @db.Date
  gbort          String?   @db.VarChar(40)
  natio          String?   @db.VarChar(3)
  gesch          String?   @db.VarChar(1)
  famst          String?   @db.VarChar(1)
  email          String?   @db.VarChar(241)
  phone          String?   @db.VarChar(30)
  mobile         String?   @db.VarChar(30)
  source_channel String?   @db.VarChar(4)
  referral_pernr String?   @db.VarChar(8)
  created_at     DateTime? @default(now()) @db.Timestamp(6)
  updated_at     DateTime? @default(now()) @db.Timestamp(6)

  @@index([aplnr], map: "idx_pb0002_aplnr")
  @@index([email], map: "idx_pb0002_email")
  @@index([nachn, vorna], map: "idx_pb0002_name")
  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pb0003 {
  id         Int       @id @default(autoincrement())
  aplnr      String    @db.VarChar(8)
  subty      String?   @default("1") @db.VarChar(4)
  endda      DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda      DateTime  @default(dbgenerated("'1900-01-01'::date")) @db.Date
  stras      String?   @db.VarChar(60)
  ort01      String?   @db.VarChar(40)
  pstlz      String?   @db.VarChar(10)
  land1      String?   @db.VarChar(3)
  regio      String?   @db.VarChar(3)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@unique([aplnr, subty, begda], map: "pb0003_unique")
  @@index([aplnr], map: "idx_pb0003_aplnr")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pb0022 {
  id                     Int       @default(autoincrement())
  aplnr                  String    @db.VarChar(8)
  subty                  String?   @default("0001") @db.VarChar(4)
  endda                  DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda                  DateTime  @default(dbgenerated("'1900-01-01'::date")) @db.Date
  seqnr                  Int?      @default(1)
  slart                  String?   @db.VarChar(4)
  insti                  String?   @db.VarChar(80)
  slabs                  String?   @db.VarChar(80)
  anession               String?   @db.VarChar(4)
  sltp1                  String?   @db.VarChar(40)
  end_year               Int?
  grade                  String?   @db.VarChar(10)
  esco_qualification_uri String?   @db.VarChar(255)
  created_at             DateTime? @default(now()) @db.Timestamp(6)

  @@index([aplnr], map: "idx_pb0022_aplnr")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pb0024 {
  id               Int       @default(autoincrement())
  aplnr            String    @db.VarChar(8)
  subty            String?   @default("0001") @db.VarChar(4)
  quali            String    @db.VarChar(8)
  endda            DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda            DateTime  @default(dbgenerated("'1900-01-01'::date")) @db.Date
  proficiency      String?   @db.VarChar(2)
  years_experience Decimal?  @db.Decimal(4, 1)
  verified         Boolean?  @default(false)
  self_assessed    Boolean?  @default(true)
  esco_skill_uri   String?   @db.VarChar(255)
  created_at       DateTime? @default(now()) @db.Timestamp(6)

  @@index([aplnr], map: "idx_pb0024_aplnr")
  @@index([esco_skill_uri], map: "idx_pb0024_esco")
  @@index([quali], map: "idx_pb0024_quali")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pb4000 {
  id                Int       @default(autoincrement())
  aplnr             String    @db.VarChar(8)
  activity_id       String    @db.VarChar(10)
  activity_date     DateTime  @db.Date
  activity_time     DateTime? @db.Time(6)
  activity_type     String    @db.VarChar(4)
  activity_status   String?   @default("01") @db.VarChar(2)
  activity_result   String?   @db.VarChar(2)
  interviewer_pernr String?   @db.VarChar(8)
  location          String?   @db.VarChar(80)
  notes             String?
  rating            String?   @db.VarChar(2)
  document_ref      String?   @db.VarChar(50)
  created_at        DateTime? @default(now()) @db.Timestamp(6)
  updated_at        DateTime? @default(now()) @db.Timestamp(6)

  @@index([aplnr], map: "idx_pb4000_aplnr")
  @@index([activity_date], map: "idx_pb4000_date")
  @@index([activity_type], map: "idx_pb4000_type")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pb4001 {
  id                Int       @default(autoincrement())
  aplnr             String    @db.VarChar(8)
  vacancy_id        String    @db.VarChar(10)
  endda             DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda             DateTime  @db.Date
  assignment_status String?   @default("01") @db.VarChar(2)
  assignment_date   DateTime? @db.Date
  priority          Int?      @default(0)
  position_id       String?   @db.VarChar(8)
  job_id            String?   @db.VarChar(8)
  decision_date     DateTime? @db.Date
  decision_by       String?   @db.VarChar(8)
  rejection_reason  String?   @db.VarChar(4)
  created_at        DateTime? @default(now()) @db.Timestamp(6)
  updated_at        DateTime? @default(now()) @db.Timestamp(6)

  @@index([aplnr], map: "idx_pb4001_aplnr")
  @@index([position_id], map: "idx_pb4001_position")
  @@index([assignment_status], map: "idx_pb4001_status")
  @@index([vacancy_id], map: "idx_pb4001_vacancy")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pb4005 {
  id                  Int       @default(autoincrement())
  aplnr               String    @db.VarChar(8)
  vacancy_id          String?   @db.VarChar(10)
  evaluation_id       String    @db.VarChar(10)
  evaluation_date     DateTime  @db.Date
  evaluator_pernr     String    @db.VarChar(8)
  evaluation_type     String?   @db.VarChar(4)
  technical_score     Decimal?  @db.Decimal(3, 1)
  communication_score Decimal?  @db.Decimal(3, 1)
  cultural_fit_score  Decimal?  @db.Decimal(3, 1)
  experience_score    Decimal?  @db.Decimal(3, 1)
  overall_score       Decimal?  @db.Decimal(3, 1)
  recommendation      String?   @db.VarChar(2)
  comments            String?
  created_at          DateTime? @default(now()) @db.Timestamp(6)

  @@index([aplnr], map: "idx_pb4005_aplnr")
  @@index([evaluator_pernr], map: "idx_pb4005_evaluator")
  @@index([vacancy_id], map: "idx_pb4005_vacancy")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pcl1 {
  id         Int       @id @default(autoincrement())
  pernr      String    @db.VarChar(8)
  seqnr      Int       @default(1)
  relession  String    @db.VarChar(4)
  paession   Int
  pession_no Int
  time_data  Json?
  rgdate     DateTime? @default(now()) @db.Timestamp(6)
  status     String?   @default("0") @db.VarChar(2)
  created_at DateTime? @default(now()) @db.Timestamp(6)
  updated_at DateTime? @default(now()) @db.Timestamp(6)

  @@unique([pernr, relession, paession, pession_no, seqnr], map: "pcl1_unique")
  @@index([paession, pession_no], map: "idx_pcl1_period")
  @@index([pernr], map: "idx_pcl1_pernr")
  @@index([relession], map: "idx_pcl1_relession")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pcl2 {
  id           Int       @default(autoincrement())
  pernr        String    @db.VarChar(8)
  seqnr        Int       @default(1)
  relession    String    @db.VarChar(4)
  paession     Int
  pession_no   Int
  abkrs        String?   @db.VarChar(4)
  payroll_data Json?
  gross_pay    Decimal?  @db.Decimal(13, 2)
  net_pay      Decimal?  @db.Decimal(13, 2)
  rgdate       DateTime? @default(now()) @db.Timestamp(6)
  status       String?   @default("0") @db.VarChar(2)
  created_at   DateTime? @default(now()) @db.Timestamp(6)
  updated_at   DateTime? @default(now()) @db.Timestamp(6)

  @@index([abkrs], map: "idx_pcl2_abkrs")
  @@index([paession, pession_no], map: "idx_pcl2_period")
  @@index([pernr], map: "idx_pcl2_pernr")
  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model performance_predictions {
  id                       String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                String     @db.Uuid
  employee_id              String?    @db.Uuid
  prediction_period        String     @db.VarChar(20)
  predicted_rating         Decimal?   @db.Decimal(3, 2)
  confidence_interval_low  Decimal?   @db.Decimal(3, 2)
  confidence_interval_high Decimal?   @db.Decimal(3, 2)
  influencing_factors      Json?      @default("[]")
  improvement_suggestions  Json?      @default("[]")
  model_version            Int?
  is_current               Boolean?   @default(true)
  created_at               DateTime?  @default(now()) @db.Timestamptz(6)
  risk_score               Int?
  risk_level               String?    @db.VarChar(20)
  is_high_potential        Boolean?   @default(false)
  hipo_score               Decimal?   @db.Decimal(3, 2)
  hipo_justification       String?
  contributing_factors     Json?      @default("[]")
  recommended_actions      Json?      @default("[]")
  data_sources             Json?      @default("[]")
  prediction_confidence    Decimal?   @db.Decimal(5, 4)
  actual_rating            Decimal?   @db.Decimal(3, 2)
  prediction_accuracy      Decimal?   @db.Decimal(5, 4)
  validated_at             DateTime?  @db.Timestamptz(6)
  model_name               String?    @default("heuresys-perf-v1") @db.VarChar(100)
  feature_weights          Json?      @default("{}")
  employees                employees? @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                  tenants    @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([employee_id], map: "idx_perf_predictions_employee")
  @@index([model_name, model_version], map: "idx_perf_predictions_model")
  @@index([tenant_id], map: "idx_perf_predictions_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model performance_review_templates {
  id                       String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                String                @db.Uuid
  name                     String                @db.VarChar(255)
  description              String?
  template_type            String?               @default("standard") @db.VarChar(50)
  rating_scale_type        String?               @default("1-5") @db.VarChar(20)
  rating_scale_config      Json?                 @default("{\"max\": 5, \"min\": 1, \"labels\": [\"Needs Improvement\", \"Below Expectations\", \"Meets Expectations\", \"Exceeds Expectations\", \"Outstanding\"]}")
  sections                 Json                  @default("[]")
  competencies             Json?
  include_goals            Boolean?              @default(true)
  include_development_plan Boolean?              @default(true)
  is_default               Boolean?              @default(false)
  is_active                Boolean?              @default(true)
  created_at               DateTime?             @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?             @default(now()) @db.Timestamptz(6)
  deleted_at               DateTime?             @db.Timestamptz(6)
  tenants                  tenants               @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  performance_reviews      performance_reviews[]

  @@index([is_active], map: "idx_perf_review_templates_active")
  @@index([tenant_id], map: "idx_perf_review_templates_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model performance_reviews {
  id                                                     String                        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                              String                        @db.Uuid
  employee_id                                            String?                       @db.Uuid
  reviewer_id                                            String?                       @db.Uuid
  review_period_start                                    DateTime                      @db.Date
  review_period_end                                      DateTime                      @db.Date
  review_type                                            String?                       @default("annual") @db.VarChar(50)
  overall_rating                                         Decimal?                      @db.Decimal(3, 2)
  goal_achievement_rating                                Decimal?                      @db.Decimal(3, 2)
  competency_rating                                      Decimal?                      @db.Decimal(3, 2)
  potential_rating                                       String?                       @db.VarChar(20)
  strengths                                              String?
  areas_for_improvement                                  String?
  manager_comments                                       String?
  employee_comments                                      String?
  status                                                 String?                       @default("draft") @db.VarChar(50)
  submitted_at                                           DateTime?                     @db.Timestamp(6)
  acknowledged_at                                        DateTime?                     @db.Timestamp(6)
  created_at                                             DateTime?                     @default(now()) @db.Timestamp(6)
  updated_at                                             DateTime?                     @default(now()) @db.Timestamp(6)
  review_cycle_id                                        String?                       @db.Uuid
  competency_ratings                                     Json?                         @default("[]")
  goal_ratings                                           Json?                         @default("[]")
  development_plan                                       String?
  career_aspirations                                     String?
  recommended_actions                                    Json?                         @default("[]")
  performance_box                                        Int?
  potential_box                                          Int?
  pre_calibration_rating                                 Decimal?                      @db.Decimal(3, 1)
  calibration_notes                                      String?
  self_review_completed_at                               DateTime?                     @db.Timestamptz(6)
  calibrated_at                                          DateTime?                     @db.Timestamptz(6)
  shared_at                                              DateTime?                     @db.Timestamptz(6)
  content_embedding                                      Unsupported("vector")?
  embedding_text_hash                                    String?                       @db.VarChar(64)
  embedding_model                                        String?                       @db.VarChar(100)
  embedding_generated_at                                 DateTime?                     @db.Timestamptz(6)
  template_id                                            String?                       @db.Uuid
  self_rating                                            Decimal?                      @db.Decimal(3, 2)
  self_comments                                          String?
  self_submitted_at                                      DateTime?                     @db.Timestamptz(6)
  manager_submitted_at                                   DateTime?                     @db.Timestamptz(6)
  calibrated_rating                                      Decimal?                      @db.Decimal(3, 2)
  calibrated_by                                          String?                       @db.Uuid
  finalized_at                                           DateTime?                     @db.Timestamptz(6)
  finalized_by                                           String?                       @db.Uuid
  section_ratings                                        Json?
  self_assessment_status                                 String?                       @default("not_started") @db.VarChar(20)
  self_assessment_started_at                             DateTime?                     @db.Timestamptz(6)
  goals_auto_populated                                   Boolean?                      @default(false)
  goals_count                                            Int?                          @default(0)
  competencies_count                                     Int?                          @default(0)
  calibration_results                                    calibration_results[]
  competency_review_ratings                              competency_review_ratings[]
  continuous_feedback                                    continuous_feedback[]
  feedback_360                                           feedback_360[]
  feedback_requests                                      feedback_requests[]
  goal_review_ratings                                    goal_review_ratings[]
  employees_performance_reviews_employee_idToemployees   employees?                    @relation("performance_reviews_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_performance_reviews_employee")
  employees_performance_reviews_reviewer_idToemployees   employees?                    @relation("performance_reviews_reviewer_idToemployees", fields: [reviewer_id], references: [id], onUpdate: NoAction, map: "fk_performance_reviews_reviewer")
  employees_performance_reviews_calibrated_byToemployees employees?                    @relation("performance_reviews_calibrated_byToemployees", fields: [calibrated_by], references: [id], onUpdate: NoAction)
  employees_performance_reviews_finalized_byToemployees  employees?                    @relation("performance_reviews_finalized_byToemployees", fields: [finalized_by], references: [id], onUpdate: NoAction)
  review_cycles                                          review_cycles?                @relation(fields: [review_cycle_id], references: [id], onUpdate: NoAction)
  performance_review_templates                           performance_review_templates? @relation(fields: [template_id], references: [id], onUpdate: NoAction)
  tenants                                                tenants                       @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  performance_skill_links                                performance_skill_links[]
  self_assessment_evidence                               self_assessment_evidence[]
  self_reviews                                           self_reviews?

  @@index([calibrated_by], map: "idx_performance_reviews_calibrated_by")
  @@index([review_cycle_id], map: "idx_performance_reviews_cycle")
  @@index([content_embedding], map: "idx_performance_reviews_embedding")
  @@index([tenant_id, employee_id], map: "idx_performance_reviews_employee")
  @@index([finalized_by], map: "idx_performance_reviews_finalized_by")
  @@index([review_period_start, review_period_end], map: "idx_performance_reviews_period")
  @@index([reviewer_id], map: "idx_performance_reviews_reviewer")
  @@index([tenant_id, status], map: "idx_performance_reviews_status")
  @@index([template_id], map: "idx_performance_reviews_template")
  @@index([tenant_id], map: "idx_performance_reviews_tenant")
  @@index([employee_id], map: "idx_reviews_employee")
  @@index([tenant_id, review_cycle_id], map: "idx_reviews_tenant_cycle")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model performance_skill_links {
  id                       String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                String              @db.Uuid
  performance_review_id    String              @db.Uuid
  employee_id              String              @db.Uuid
  competency_name          String              @db.VarChar(200)
  competency_rating        Decimal?            @db.Decimal(3, 2)
  rating_level             String?             @db.VarChar(20)
  linked_skill_id          String?             @db.Uuid
  linked_gap_analysis_id   String?             @db.Uuid
  recommended_actions      Json?               @default("[]")
  learning_path_id         String?             @db.Uuid
  mentor_recommendation_id String?             @db.Uuid
  is_addressed             Boolean?            @default(false)
  addressed_at             DateTime?           @db.Timestamptz(6)
  created_at               DateTime?           @default(now()) @db.Timestamptz(6)
  employees                employees           @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  skill_gap_analyses       skill_gap_analyses? @relation(fields: [linked_gap_analysis_id], references: [id], onUpdate: NoAction)
  esco_skills              esco_skills?        @relation(fields: [linked_skill_id], references: [id], onUpdate: NoAction)
  performance_reviews      performance_reviews @relation(fields: [performance_review_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                  tenants             @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([performance_review_id, competency_name])
  @@index([employee_id], map: "idx_perf_skill_links_employee")
  @@index([tenant_id], map: "idx_perf_skill_links_tenant")
  @@index([linked_gap_analysis_id], map: "idx_performance_skill_links_linked_gap_analysis_id")
  @@index([linked_skill_id], map: "idx_performance_skill_links_linked_skill_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model performance_trends {
  id                   String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id            String                 @db.Uuid
  employee_id          String                 @db.Uuid
  period_start         DateTime               @db.Date
  period_end           DateTime               @db.Date
  overall_rating       Decimal?               @db.Decimal(3, 2)
  goal_achievement_pct Decimal?               @db.Decimal(5, 2)
  competency_score     Decimal?               @db.Decimal(3, 2)
  engagement_score     Decimal?               @db.Decimal(3, 2)
  performance_tier     String?                @db.VarChar(20)
  trend_direction      String?                @db.VarChar(20)
  summary_text         String?
  summary_embedding    Unsupported("vector")?
  created_at           DateTime?              @default(now()) @db.Timestamptz(6)
  employees            employees              @relation(fields: [employee_id], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_pt_employee")
  tenants              tenants                @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, employee_id, period_start, period_end], map: "uk_performance_trend")
  @@index([summary_embedding], map: "idx_performance_trends_embedding")
  @@index([tenant_id, employee_id, period_end(sort: Desc)], map: "idx_performance_trends_employee")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model permission_overrides {
  id            String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id     String      @db.Uuid
  role          String      @db.VarChar(50)
  permission_id String      @db.Uuid
  is_granted    Boolean     @default(true)
  conditions    Json?       @default("{}")
  granted_at    DateTime?   @default(now()) @db.Timestamptz(6)
  granted_by    String?     @db.Uuid
  expires_at    DateTime?   @db.Timestamptz(6)
  reason        String?
  created_at    DateTime?   @default(now()) @db.Timestamptz(6)
  updated_at    DateTime?   @default(now()) @db.Timestamptz(6)
  employees     employees?  @relation(fields: [granted_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  permissions   permissions @relation(fields: [permission_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants       tenants     @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([tenant_id, role, permission_id])
  @@index([granted_by], map: "idx_permission_overrides_granted_by")
  @@index([role], map: "idx_permission_overrides_role")
  @@index([tenant_id], map: "idx_permission_overrides_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model permissions {
  id                            String                          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  code                          String                          @unique(map: "uq_permissions_code") @db.VarChar(100)
  module_id                     String?                         @db.Uuid
  operation                     crud_operation
  default_scope                 permission_scope?               @default(own)
  name                          String                          @db.VarChar(200)
  description                   String?
  is_sensitive                  Boolean?                        @default(false)
  is_active                     Boolean?                        @default(true)
  created_at                    DateTime?                       @default(now()) @db.Timestamptz(6)
  updated_at                    DateTime?                       @default(now()) @db.Timestamptz(6)
  deleted_at                    DateTime?                       @db.Timestamptz(6)
  employee_permission_overrides employee_permission_overrides[]
  permission_overrides          permission_overrides[]
  feature_modules               feature_modules?                @relation(fields: [module_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  role_permissions              role_permissions[]

  @@index([module_id], map: "idx_permissions_module")
  @@index([operation], map: "idx_permissions_operation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model platform_features {
  id                     Int       @id @default(autoincrement())
  feature_code           String    @db.VarChar(50)
  feature_name           String    @db.VarChar(255)
  category               String    @db.VarChar(100)
  subcategory            String?   @db.VarChar(100)
  description            String
  business_value         String?
  current_coverage_pct   Int?      @default(0)
  implementation_status  String?   @default("not_started") @db.VarChar(50)
  priority               String?   @default("medium") @db.VarChar(20)
  estimated_effort_days  Int?
  market_leaders         String[]
  market_standard_score  Int?      @default(100)
  requires_tables        String[]
  requires_views         String[]
  requires_api_endpoints String[]
  requires_ui_components String[]
  created_at             DateTime? @default(now()) @db.Timestamp(6)
  updated_at             DateTime? @default(now()) @db.Timestamp(6)
  implemented_at         DateTime? @db.Timestamp(6)
  notes                  String?

  @@index([category], map: "idx_features_category")
  @@index([priority], map: "idx_features_priority")
  @@index([implementation_status], map: "idx_features_status")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model platform_pages {
  id                   String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  section_key          String                 @db.VarChar(50)
  section_title        String                 @db.VarChar(100)
  section_desc         String?
  section_color        String?                @default("bg-gray-500/10 text-gray-600") @db.VarChar(60)
  section_icon         String?                @default("Compass") @db.VarChar(30)
  section_order        Int                    @default(0)
  path                 String                 @unique @db.VarChar(200)
  name                 String                 @db.VarChar(100)
  description          String?
  tags                 String[]               @default([])
  status               String                 @default("active") @db.VarChar(20)
  redirect_to          String?                @db.VarChar(200)
  page_order           Int                    @default(0)
  is_visible           Boolean                @default(true)
  created_at           DateTime               @default(now()) @db.Timestamptz(6)
  updated_at           DateTime               @default(now()) @db.Timestamptz(6)
  section_title_it     String?                @db.VarChar(100)
  section_title_en     String?                @db.VarChar(100)
  section_desc_it      String?
  section_desc_en      String?
  name_it              String?                @db.VarChar(100)
  name_en              String?                @db.VarChar(100)
  description_it       String?
  description_en       String?
  page_table_relations page_table_relations[]

  @@index([section_key], map: "idx_platform_pages_section")
  @@index([status], map: "idx_platform_pages_status")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model plugin_api_keys {
  id                     String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String                @db.Uuid
  plugin_installation_id String?               @db.Uuid
  name                   String                @db.VarChar(200)
  key_hash               String                @db.VarChar(128)
  key_prefix             String                @db.VarChar(12)
  scopes                 String[]              @default([])
  expires_at             DateTime?             @db.Timestamptz(6)
  last_used_at           DateTime?             @db.Timestamptz(6)
  is_active              Boolean?              @default(true)
  created_by             String?               @db.Uuid
  created_at             DateTime?             @default(now()) @db.Timestamptz(6)
  revoked_at             DateTime?             @db.Timestamptz(6)
  created_by_employee_id String?               @db.Uuid
  deleted_at             DateTime?             @db.Timestamptz(6)
  employees              employees?            @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  plugin_installations   plugin_installations? @relation(fields: [plugin_installation_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                tenants               @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([created_by_employee_id], map: "idx_plugin_api_keys_created_by_employee_id")
  @@index([plugin_installation_id], map: "idx_plugin_api_keys_installation")
  @@index([key_prefix], map: "idx_plugin_api_keys_key_prefix")
  @@index([tenant_id], map: "idx_plugin_api_keys_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model plugin_categories {
  id                      String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                    String              @unique @db.VarChar(100)
  slug                    String              @unique @db.VarChar(100)
  description             String?
  icon                    String?             @db.VarChar(100)
  sort_order              Int?                @default(0)
  parent_id               String?             @db.Uuid
  created_at              DateTime?           @default(now()) @db.Timestamptz(6)
  updated_at              DateTime?           @default(now()) @db.Timestamptz(6)
  plugin_categories       plugin_categories?  @relation("plugin_categoriesToplugin_categories", fields: [parent_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  other_plugin_categories plugin_categories[] @relation("plugin_categoriesToplugin_categories")
  plugins                 plugins[]

  @@index([parent_id], map: "idx_plugin_categories_parent")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model plugin_configurations {
  id                     String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String               @db.Uuid
  plugin_installation_id String               @db.Uuid
  config_data            Json?                @default("{}")
  config_version         Int?                 @default(1)
  updated_by             String?              @db.Uuid
  created_at             DateTime?            @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?            @default(now()) @db.Timestamptz(6)
  updated_by_employee_id String?              @db.Uuid
  plugin_installations   plugin_installations @relation(fields: [plugin_installation_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                tenants              @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees              employees?           @relation(fields: [updated_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([tenant_id, plugin_installation_id])
  @@index([plugin_installation_id], map: "idx_plugin_configurations_installation")
  @@index([tenant_id], map: "idx_plugin_configurations_tenant")
  @@index([updated_by_employee_id], map: "idx_plugin_configurations_updated_by_employee_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model plugin_dependencies {
  id                                                        String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  plugin_id                                                 String    @db.Uuid
  depends_on_plugin_id                                      String    @db.Uuid
  min_version                                               String?   @db.VarChar(50)
  max_version                                               String?   @db.VarChar(50)
  is_optional                                               Boolean?  @default(false)
  created_at                                                DateTime? @default(now()) @db.Timestamptz(6)
  plugins_plugin_dependencies_depends_on_plugin_idToplugins plugins   @relation("plugin_dependencies_depends_on_plugin_idToplugins", fields: [depends_on_plugin_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  plugins_plugin_dependencies_plugin_idToplugins            plugins   @relation("plugin_dependencies_plugin_idToplugins", fields: [plugin_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([plugin_id, depends_on_plugin_id])
  @@index([depends_on_plugin_id], map: "idx_plugin_dependencies_depends_on")
  @@index([plugin_id], map: "idx_plugin_dependencies_plugin")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model plugin_hook_executions {
  id            String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  hook_id       String       @db.Uuid
  tenant_id     String       @db.Uuid
  trigger_event String       @db.VarChar(200)
  input_data    Json?
  output_data   Json?
  status        String?      @default("pending") @db.VarChar(50)
  error_message String?
  duration_ms   Int?
  started_at    DateTime?    @default(now()) @db.Timestamptz(6)
  completed_at  DateTime?    @db.Timestamptz(6)
  created_at    DateTime?    @default(now()) @db.Timestamptz(6)
  plugin_hooks  plugin_hooks @relation(fields: [hook_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants       tenants      @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([hook_id], map: "idx_plugin_hook_executions_hook")
  @@index([started_at], map: "idx_plugin_hook_executions_started")
  @@index([status], map: "idx_plugin_hook_executions_status")
  @@index([tenant_id], map: "idx_plugin_hook_executions_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model plugin_hooks {
  id                     String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  plugin_id              String                   @db.Uuid
  hook_name              String                   @db.VarChar(200)
  handler_path           String                   @db.VarChar(500)
  priority               Int?                     @default(100)
  is_async               Boolean?                 @default(false)
  timeout_ms             Int?                     @default(5000)
  enabled                Boolean?                 @default(true)
  created_at             DateTime?                @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?                @default(now()) @db.Timestamptz(6)
  plugin_hook_executions plugin_hook_executions[]
  plugins                plugins                  @relation(fields: [plugin_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([plugin_id, hook_name])
  @@index([hook_name], map: "idx_plugin_hooks_name")
  @@index([plugin_id], map: "idx_plugin_hooks_plugin")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model plugin_installations {
  id                       String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                String                  @db.Uuid
  plugin_id                String                  @db.Uuid
  plugin_version_id        String                  @db.Uuid
  installed_by             String?                 @db.Uuid
  status                   String?                 @default("active") @db.VarChar(50)
  auto_update              Boolean?                @default(true)
  installed_at             DateTime?               @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?               @default(now()) @db.Timestamptz(6)
  disabled_at              DateTime?               @db.Timestamptz(6)
  disabled_reason          String?
  installed_by_employee_id String?                 @db.Uuid
  created_at               DateTime?               @default(now()) @db.Timestamptz(6)
  plugin_api_keys          plugin_api_keys[]
  plugin_configurations    plugin_configurations[]
  employees                employees?              @relation(fields: [installed_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  plugins                  plugins                 @relation(fields: [plugin_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  plugin_versions          plugin_versions         @relation(fields: [plugin_version_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                  tenants                 @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  plugin_webhooks          plugin_webhooks[]

  @@unique([tenant_id, plugin_id])
  @@index([installed_by_employee_id], map: "idx_plugin_installations_installed_by_employee_id")
  @@index([plugin_id], map: "idx_plugin_installations_plugin")
  @@index([plugin_version_id], map: "idx_plugin_installations_plugin_version_id")
  @@index([status], map: "idx_plugin_installations_status")
  @@index([tenant_id], map: "idx_plugin_installations_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model plugin_reviews {
  id                  String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String     @db.Uuid
  plugin_id           String     @db.Uuid
  user_id             String     @db.Uuid
  rating              Int
  title               String?    @db.VarChar(200)
  review_text         String?
  is_verified_install Boolean?   @default(false)
  helpful_count       Int?       @default(0)
  status              String?    @default("published") @db.VarChar(50)
  created_at          DateTime?  @default(now()) @db.Timestamptz(6)
  updated_at          DateTime?  @default(now()) @db.Timestamptz(6)
  user_id_employee_id String?    @db.Uuid
  users               users      @relation(fields: [user_id], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_pr_user")
  plugins             plugins    @relation(fields: [plugin_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants             tenants    @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees           employees? @relation(fields: [user_id_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([plugin_id, user_id])
  @@index([plugin_id], map: "idx_plugin_reviews_plugin")
  @@index([plugin_id, rating], map: "idx_plugin_reviews_rating")
  @@index([tenant_id], map: "idx_plugin_reviews_tenant")
  @@index([user_id], map: "idx_plugin_reviews_user")
  @@index([user_id_employee_id], map: "idx_plugin_reviews_user_id_employee_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model plugin_ui_slots {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  plugin_id      String    @db.Uuid
  slot_name      String    @db.VarChar(200)
  component_path String    @db.VarChar(500)
  props_schema   Json?     @default("{}")
  priority       Int?      @default(100)
  enabled        Boolean?  @default(true)
  created_at     DateTime? @default(now()) @db.Timestamptz(6)
  updated_at     DateTime? @default(now()) @db.Timestamptz(6)
  plugins        plugins   @relation(fields: [plugin_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([plugin_id, slot_name])
  @@index([plugin_id], map: "idx_plugin_ui_slots_plugin")
  @@index([slot_name], map: "idx_plugin_ui_slots_slot")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model plugin_versions {
  id                   String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  plugin_id            String                 @db.Uuid
  version              String                 @db.VarChar(50)
  release_notes        String?
  changelog            String?
  min_platform_version String?                @db.VarChar(50)
  max_platform_version String?                @db.VarChar(50)
  config_schema        Json?                  @default("{}")
  permissions_required String[]               @default([])
  entry_point          String?                @db.VarChar(500)
  package_url          String?                @db.VarChar(500)
  package_size_bytes   BigInt?
  checksum             String?                @db.VarChar(128)
  status               String?                @default("draft") @db.VarChar(50)
  is_latest            Boolean?               @default(false)
  published_at         DateTime?              @db.Timestamptz(6)
  created_at           DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at           DateTime?              @default(now()) @db.Timestamptz(6)
  plugin_installations plugin_installations[]
  plugins              plugins                @relation(fields: [plugin_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([plugin_id, version])
  @@index([plugin_id], map: "idx_plugin_versions_plugin")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model plugin_webhook_deliveries {
  id               String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  webhook_id       String          @db.Uuid
  event_type       String          @db.VarChar(200)
  payload          Json            @default("{}")
  response_status  Int?
  response_body    String?
  response_headers Json?
  duration_ms      Int?
  status           String?         @default("pending") @db.VarChar(50)
  attempt_number   Int?            @default(1)
  next_retry_at    DateTime?       @db.Timestamptz(6)
  delivered_at     DateTime?       @db.Timestamptz(6)
  created_at       DateTime?       @default(now()) @db.Timestamptz(6)
  plugin_webhooks  plugin_webhooks @relation(fields: [webhook_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_at], map: "idx_plugin_webhook_deliveries_created")
  @@index([status], map: "idx_plugin_webhook_deliveries_status")
  @@index([webhook_id], map: "idx_plugin_webhook_deliveries_webhook")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model plugin_webhooks {
  id                        String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                 String                      @db.Uuid
  plugin_installation_id    String                      @db.Uuid
  url                       String                      @db.VarChar(1000)
  secret_hash               String?                     @db.VarChar(128)
  events                    String[]                    @default([])
  is_active                 Boolean?                    @default(true)
  description               String?                     @db.VarChar(500)
  created_by                String?                     @db.Uuid
  created_at                DateTime?                   @default(now()) @db.Timestamptz(6)
  updated_at                DateTime?                   @default(now()) @db.Timestamptz(6)
  created_by_employee_id    String?                     @db.Uuid
  deleted_at                DateTime?                   @db.Timestamptz(6)
  plugin_webhook_deliveries plugin_webhook_deliveries[]
  employees                 employees?                  @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  plugin_installations      plugin_installations        @relation(fields: [plugin_installation_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                   tenants                     @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([created_by_employee_id], map: "idx_plugin_webhooks_created_by_employee_id")
  @@index([plugin_installation_id], map: "idx_plugin_webhooks_installation")
  @@index([tenant_id], map: "idx_plugin_webhooks_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model plugins {
  id                                                                    String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                                                                  String                 @db.VarChar(200)
  slug                                                                  String                 @unique @db.VarChar(200)
  short_description                                                     String?                @db.VarChar(500)
  description                                                           String?
  category_id                                                           String?                @db.Uuid
  publisher_tenant_id                                                   String?                @db.Uuid
  publisher_name                                                        String?                @db.VarChar(200)
  icon_url                                                              String?                @db.VarChar(500)
  homepage_url                                                          String?                @db.VarChar(500)
  repository_url                                                        String?                @db.VarChar(500)
  license                                                               String?                @default("proprietary") @db.VarChar(100)
  status                                                                String?                @default("draft") @db.VarChar(50)
  visibility                                                            String?                @default("public") @db.VarChar(50)
  pricing_model                                                         String?                @default("free") @db.VarChar(50)
  price_cents                                                           Int?                   @default(0)
  currency                                                              String?                @default("EUR") @db.VarChar(3)
  tags                                                                  String[]               @default([])
  avg_rating                                                            Decimal?               @default(0) @db.Decimal(3, 2)
  total_ratings                                                         Int?                   @default(0)
  total_installations                                                   Int?                   @default(0)
  featured                                                              Boolean?               @default(false)
  created_at                                                            DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at                                                            DateTime?              @default(now()) @db.Timestamptz(6)
  screenshot_urls                                                       String[]               @default([])
  banner_url                                                            String?                @db.VarChar(500)
  plugin_dependencies_plugin_dependencies_depends_on_plugin_idToplugins plugin_dependencies[]  @relation("plugin_dependencies_depends_on_plugin_idToplugins")
  plugin_dependencies_plugin_dependencies_plugin_idToplugins            plugin_dependencies[]  @relation("plugin_dependencies_plugin_idToplugins")
  plugin_hooks                                                          plugin_hooks[]
  plugin_installations                                                  plugin_installations[]
  plugin_reviews                                                        plugin_reviews[]
  plugin_ui_slots                                                       plugin_ui_slots[]
  plugin_versions                                                       plugin_versions[]
  plugin_categories                                                     plugin_categories?     @relation(fields: [category_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                                                               tenants?               @relation(fields: [publisher_tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([category_id], map: "idx_plugins_category")
  @@index([publisher_tenant_id], map: "idx_plugins_publisher_tenant")
  @@index([status], map: "idx_plugins_status")
  @@index([tags], map: "idx_plugins_tags", type: Gin)
  @@index([visibility], map: "idx_plugins_visibility")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model position_skill_requirements {
  id                  String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String        @db.Uuid
  position_id         String        @db.Uuid
  position_name       String        @db.VarChar(255)
  esco_skill_id       String?       @db.Uuid
  custom_skill_name   String?       @db.VarChar(255)
  requirement_type    String?       @default("essential") @db.VarChar(20)
  minimum_proficiency Int?
  weight              Decimal?      @default(1.0) @db.Decimal(3, 2)
  created_at          DateTime?     @default(now()) @db.Timestamptz(6)
  updated_at          DateTime?     @default(now()) @db.Timestamptz(6)
  job_templates       job_templates @relation(fields: [position_id], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_psr_position")
  esco_skills         esco_skills?  @relation(fields: [esco_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants             tenants       @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([esco_skill_id], map: "idx_position_skill_requirements_esco_skill_id")
  @@index([position_id], map: "idx_position_skills_position")
  @@index([tenant_id], map: "idx_position_skills_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model preboarding_equipment {
  id                                                                  String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  session_id                                                          String?               @db.Uuid
  equipment_type                                                      String                @db.VarChar(50)
  specifications                                                      String?
  quantity                                                            Int?                  @default(1)
  status                                                              String?               @default("requested") @db.VarChar(30)
  requested_by                                                        String?               @db.Uuid
  approved_by                                                         String?               @db.Uuid
  approved_at                                                         DateTime?             @db.Timestamp(6)
  delivery_date                                                       DateTime?             @db.Date
  tracking_number                                                     String?               @db.VarChar(100)
  notes                                                               String?
  created_at                                                          DateTime?             @default(now()) @db.Timestamp(6)
  updated_at                                                          DateTime?             @default(now()) @db.Timestamp(6)
  approved_by_employee_id                                             String?               @db.Uuid
  requested_by_employee_id                                            String?               @db.Uuid
  employees_preboarding_equipment_approved_by_employee_idToemployees  employees?            @relation("preboarding_equipment_approved_by_employee_idToemployees", fields: [approved_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "preboarding_equipment_approved_by_fkey_emp")
  employees_preboarding_equipment_requested_by_employee_idToemployees employees?            @relation("preboarding_equipment_requested_by_employee_idToemployees", fields: [requested_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "preboarding_equipment_requested_by_fkey_emp")
  preboarding_sessions                                                preboarding_sessions? @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([approved_by_employee_id], map: "idx_preboarding_equipment_approved_by_employee_id")
  @@index([requested_by_employee_id], map: "idx_preboarding_equipment_requested_by_employee_id")
  @@index([session_id], map: "idx_preboarding_equipment_session")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model preboarding_notifications {
  id                   String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  session_id           String?               @db.Uuid
  notification_type    String                @db.VarChar(50)
  recipient_email      String                @db.VarChar(200)
  recipient_type       String                @db.VarChar(30)
  subject              String?               @db.VarChar(300)
  message              String?
  sent_at              DateTime?             @default(now()) @db.Timestamp(6)
  status               String?               @default("sent") @db.VarChar(20)
  created_at           DateTime?             @default(now()) @db.Timestamptz(6)
  preboarding_sessions preboarding_sessions? @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([session_id], map: "idx_preboarding_notifications_session_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model preboarding_sessions {
  id                                                               String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                                        String                      @db.Uuid
  new_hire_name                                                    String                      @db.VarChar(200)
  new_hire_email                                                   String                      @db.VarChar(200)
  new_hire_phone                                                   String?                     @db.VarChar(50)
  department                                                       String?                     @db.VarChar(100)
  job_title                                                        String?                     @db.VarChar(200)
  manager_id                                                       String?                     @db.Uuid
  start_date                                                       DateTime                    @db.Date
  work_location                                                    String?                     @db.VarChar(200)
  work_type                                                        String?                     @default("hybrid") @db.VarChar(30)
  template_id                                                      String?                     @db.Uuid
  status                                                           String?                     @default("pending") @db.VarChar(30)
  access_token                                                     String?                     @unique @db.VarChar(100)
  token_expires_at                                                 DateTime?                   @db.Timestamp(6)
  total_items                                                      Int?                        @default(0)
  completed_items                                                  Int?                        @default(0)
  welcome_message                                                  String?
  hr_notes                                                         String?
  created_by                                                       String?                     @db.Uuid
  created_at                                                       DateTime?                   @default(now()) @db.Timestamp(6)
  updated_at                                                       DateTime?                   @default(now()) @db.Timestamp(6)
  created_by_employee_id                                           String?                     @db.Uuid
  preboarding_equipment                                            preboarding_equipment[]
  preboarding_notifications                                        preboarding_notifications[]
  employees_preboarding_sessions_manager_idToemployees             employees?                  @relation("preboarding_sessions_manager_idToemployees", fields: [manager_id], references: [id], onUpdate: NoAction, map: "fk_ps_manager")
  employees_preboarding_sessions_created_by_employee_idToemployees employees?                  @relation("preboarding_sessions_created_by_employee_idToemployees", fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "preboarding_sessions_created_by_fkey_emp")
  tenants                                                          tenants                     @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  preboarding_tasks                                                preboarding_tasks[]

  @@index([created_by_employee_id], map: "idx_preboarding_sessions_created_by_employee_id")
  @@index([manager_id], map: "idx_preboarding_sessions_manager_id")
  @@index([start_date], map: "idx_preboarding_sessions_start_date")
  @@index([status], map: "idx_preboarding_sessions_status")
  @@index([tenant_id], map: "idx_preboarding_sessions_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model preboarding_tasks {
  id                   String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  session_id           String?               @db.Uuid
  name                 String                @db.VarChar(200)
  description          String?
  category             String                @db.VarChar(50)
  assigned_to          String?               @default("new_hire") @db.VarChar(50)
  status               String?               @default("pending") @db.VarChar(30)
  is_required          Boolean?              @default(true)
  due_date             DateTime?             @db.Date
  completed_at         DateTime?             @db.Timestamp(6)
  completed_by         String?               @db.VarChar(200)
  requires_upload      Boolean?              @default(false)
  uploaded_file_url    String?
  uploaded_file_name   String?               @db.VarChar(255)
  form_data            Json?
  sort_order           Int?                  @default(0)
  notes                String?
  created_at           DateTime?             @default(now()) @db.Timestamp(6)
  updated_at           DateTime?             @default(now()) @db.Timestamp(6)
  preboarding_sessions preboarding_sessions? @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([session_id], map: "idx_preboarding_tasks_session")
  @@index([status], map: "idx_preboarding_tasks_status")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model preboarding_templates {
  id                     String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String     @db.Uuid
  name                   String     @db.VarChar(200)
  description            String?
  department             String?    @db.VarChar(100)
  role_type              String?    @db.VarChar(100)
  items                  Json?      @default("[]")
  is_active              Boolean?   @default(true)
  created_by             String?    @db.Uuid
  created_at             DateTime?  @default(now()) @db.Timestamp(6)
  updated_at             DateTime?  @default(now()) @db.Timestamp(6)
  created_by_employee_id String?    @db.Uuid
  deleted_at             DateTime?  @db.Timestamptz(6)
  employees              employees? @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "preboarding_templates_created_by_fkey_emp")
  tenants                tenants    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_by_employee_id], map: "idx_preboarding_templates_created_by_employee_id")
  @@index([tenant_id], map: "idx_preboarding_templates_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model preboarding_welcome_content {
  id                     String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String     @db.Uuid
  title                  String     @db.VarChar(200)
  content_type           String     @db.VarChar(50)
  content                String?
  description            String?
  sort_order             Int?       @default(0)
  is_active              Boolean?   @default(true)
  show_to_all            Boolean?   @default(true)
  department_filter      String?    @db.VarChar(100)
  created_by             String?    @db.Uuid
  created_at             DateTime?  @default(now()) @db.Timestamp(6)
  updated_at             DateTime?  @default(now()) @db.Timestamp(6)
  created_by_employee_id String?    @db.Uuid
  deleted_at             DateTime?  @db.Timestamptz(6)
  employees              employees? @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "preboarding_welcome_content_created_by_fkey_emp")
  tenants                tenants    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_by_employee_id], map: "idx_preboarding_welcome_content_created_by_employee_id")
  @@index([tenant_id], map: "idx_preboarding_welcome_tenant")
}

model prediction_actions {
  id                     String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  action_code            String    @unique @db.VarChar(50)
  action_name            String    @db.VarChar(200)
  action_category        String    @db.VarChar(50)
  description            String?
  applicable_risk_levels String[]  @default(["medium", "high"])
  applicable_hipo        Boolean?  @default(false)
  estimated_impact       String?   @default("medium") @db.VarChar(20)
  typical_duration       String?   @db.VarChar(50)
  created_at             DateTime? @default(now()) @db.Timestamptz(6)
}

model prediction_factors {
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  factor_code     String    @unique @db.VarChar(50)
  factor_name     String    @db.VarChar(200)
  factor_category String    @db.VarChar(50)
  description     String?
  weight_default  Decimal?  @default(0.100) @db.Decimal(4, 3)
  data_source     String?   @db.VarChar(100)
  is_positive     Boolean?  @default(true)
  is_active       Boolean?  @default(true)
  created_at      DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at      DateTime? @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model prediction_model_accuracy {
  id                     String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String    @db.Uuid
  model_name             String    @db.VarChar(100)
  model_version          Int
  prediction_period      String    @db.VarChar(20)
  total_predictions      Int?      @default(0)
  validated_predictions  Int?      @default(0)
  mean_absolute_error    Decimal?  @db.Decimal(5, 4)
  root_mean_square_error Decimal?  @db.Decimal(5, 4)
  accuracy_within_05     Decimal?  @db.Decimal(5, 4)
  accuracy_within_10     Decimal?  @db.Decimal(5, 4)
  hipo_precision         Decimal?  @db.Decimal(5, 4)
  hipo_recall            Decimal?  @db.Decimal(5, 4)
  risk_precision         Decimal?  @db.Decimal(5, 4)
  calculated_at          DateTime? @default(now()) @db.Timestamptz(6)
  created_at             DateTime? @default(now()) @db.Timestamptz(6)
  tenants                tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, model_name, model_version, prediction_period])
  @@index([tenant_id], map: "idx_model_accuracy_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model predictive_models {
  id                String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String              @db.Uuid
  name              String              @db.VarChar(200)
  model_type        String              @db.VarChar(100)
  description       String?
  algorithm         String?             @db.VarChar(100)
  features          Json                @default("[]")
  target_variable   String?             @db.VarChar(100)
  hyperparameters   Json?               @default("{}")
  training_config   Json?               @default("{}")
  version           Int?                @default(1)
  status            String?             @default("draft") @db.VarChar(20)
  accuracy_metrics  Json?               @default("{}")
  last_trained_at   DateTime?           @db.Timestamptz(6)
  last_predicted_at DateTime?           @db.Timestamptz(6)
  is_active         Boolean?            @default(true)
  created_at        DateTime?           @default(now()) @db.Timestamptz(6)
  updated_at        DateTime?           @default(now()) @db.Timestamptz(6)
  deleted_at        DateTime?           @db.Timestamptz(6)
  model_predictions model_predictions[]
  tenants           tenants             @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_predictive_models_tenant")
  @@index([model_type], map: "idx_predictive_models_type")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model process_cost_centers {
  id                       String                     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  process_id               String                     @db.Uuid
  cost_center_code         String                     @db.VarChar(20)
  cost_center_name         String                     @db.VarChar(100)
  cost_type                String                     @db.VarChar(50)
  description              String?
  created_at               DateTime?                  @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?                  @default(now()) @db.Timestamptz(6)
  org_unit_process_mapping org_unit_process_mapping[]
  business_processes       business_processes         @relation(fields: [process_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([process_id, cost_center_code])
  @@index([process_id], map: "idx_process_cost_centers_process")
  @@index([cost_type], map: "idx_process_cost_centers_type")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model process_kpis {
  id                 String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  process_id         String             @db.Uuid
  phase_id           String?            @db.Uuid
  kpi_code           String             @db.VarChar(50)
  kpi_name           String             @db.VarChar(255)
  measurement_unit   String?            @db.VarChar(100)
  target_direction   String?            @db.VarChar(20)
  benchmark_value    Decimal?           @db.Decimal
  benchmark_min      Decimal?           @db.Decimal
  benchmark_max      Decimal?           @db.Decimal
  description        String?
  created_at         DateTime?          @default(now()) @db.Timestamptz(6)
  updated_at         DateTime?          @default(now()) @db.Timestamptz(6)
  process_phases     process_phases?    @relation(fields: [phase_id], references: [id], onUpdate: NoAction)
  business_processes business_processes @relation(fields: [process_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([process_id, kpi_code])
  @@index([phase_id], map: "idx_process_kpis_phase_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model process_phases {
  id                         String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  process_id                 String                       @db.Uuid
  phase_code                 String                       @db.VarChar(50)
  phase_name                 String                       @db.VarChar(255)
  phase_order                Int
  description                String?
  estimated_duration_days    Int?
  is_optional                Boolean?                     @default(false)
  created_at                 DateTime?                    @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime?                    @default(now()) @db.Timestamptz(6)
  process_kpis               process_kpis[]
  business_processes         business_processes           @relation(fields: [process_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  process_roles              process_roles[]
  process_skill_requirements process_skill_requirements[]

  @@unique([process_id, phase_code])
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model process_roles {
  id                 String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  process_id         String             @db.Uuid
  phase_id           String?            @db.Uuid
  role_name          String             @db.VarChar(255)
  role_type          String             @db.VarChar(50)
  esco_occupation_id String?            @db.Uuid
  min_headcount      Int?               @default(1)
  max_headcount      Int?
  description        String?
  created_at         DateTime?          @default(now()) @db.Timestamptz(6)
  updated_at         DateTime?          @default(now()) @db.Timestamptz(6)
  esco_occupations   esco_occupations?  @relation(fields: [esco_occupation_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  process_phases     process_phases?    @relation(fields: [phase_id], references: [id], onUpdate: NoAction)
  business_processes business_processes @relation(fields: [process_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([esco_occupation_id], map: "idx_process_roles_esco_occupation_id")
  @@index([phase_id], map: "idx_process_roles_phase_id")
  @@index([process_id], map: "idx_process_roles_process_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model process_skill_requirements {
  id                 String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  process_id         String             @db.Uuid
  phase_id           String?            @db.Uuid
  esco_skill_id      String             @db.Uuid
  proficiency_level  Int
  is_mandatory       Boolean?           @default(true)
  description        String?
  created_at         DateTime?          @default(now()) @db.Timestamptz(6)
  updated_at         DateTime?          @default(now()) @db.Timestamptz(6)
  esco_skills        esco_skills        @relation(fields: [esco_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  process_phases     process_phases?    @relation(fields: [phase_id], references: [id], onUpdate: NoAction)
  business_processes business_processes @relation(fields: [process_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([process_id, phase_id, esco_skill_id])
  @@index([esco_skill_id], map: "idx_process_skill_requirements_esco_skill_id")
  @@index([phase_id], map: "idx_process_skill_requirements_phase_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model pulse_checks {
  id                 String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String     @db.Uuid
  employee_id        String?    @db.Uuid
  mood_score         Int?
  workload_score     Int?
  satisfaction_score Int?
  comment            String?
  check_date         DateTime?  @default(dbgenerated("CURRENT_DATE")) @db.Date
  week_number        Int?
  created_at         DateTime?  @default(now()) @db.Timestamptz(6)
  employees          employees? @relation(fields: [employee_id], references: [id], onUpdate: NoAction, map: "fk_pc_employee")
  tenants            tenants    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([check_date], map: "idx_pulse_checks_date")
  @@index([employee_id], map: "idx_pulse_checks_employee_id")
  @@index([tenant_id], map: "idx_pulse_checks_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model rag_document_chunks {
  id                   String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id            String        @db.Uuid
  document_id          String        @db.Uuid
  chunk_index          Int
  content              String
  content_hash         String?       @db.VarChar(64)
  start_char           Int?
  end_char             Int?
  page_number          Int?
  section_title        String?       @db.VarChar(255)
  embedding_model      String?       @db.VarChar(100)
  embedding_dimensions Int?
  embedding            Json?
  created_at           DateTime?     @default(now()) @db.Timestamp(6)
  rag_documents        rag_documents @relation(fields: [document_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants              tenants       @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([document_id], map: "idx_rag_chunks_document")
  @@index([content_hash], map: "idx_rag_chunks_hash")
  @@index([tenant_id], map: "idx_rag_chunks_tenant")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model rag_documents {
  id                      String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id               String                @db.Uuid
  filename                String                @db.VarChar(255)
  original_name           String                @db.VarChar(255)
  mime_type               String                @db.VarChar(100)
  file_size               Int
  file_path               String?               @db.VarChar(500)
  status                  String?               @default("pending") @db.VarChar(50)
  chunk_count             Int?                  @default(0)
  error_message           String?
  metadata                Json?                 @default("{}")
  created_at              DateTime?             @default(now()) @db.Timestamp(6)
  processed_at            DateTime?             @db.Timestamp(6)
  uploaded_by             String?               @db.Uuid
  source_type             String?               @default("document") @db.VarChar(50)
  version                 Int?                  @default(1)
  is_latest               Boolean?              @default(true)
  parent_document_id      String?               @db.Uuid
  version_notes           String?
  uploaded_by_employee_id String?               @db.Uuid
  knowledge_base_id       String?               @db.Uuid
  document_comments       document_comments[]
  document_locks          document_locks?
  document_versions       document_versions[]
  rag_document_chunks     rag_document_chunks[]
  rag_knowledge_bases     rag_knowledge_bases?  @relation(fields: [knowledge_base_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  rag_documents           rag_documents?        @relation("rag_documentsTorag_documents", fields: [parent_document_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  other_rag_documents     rag_documents[]       @relation("rag_documentsTorag_documents")
  tenants                 tenants               @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees               employees?            @relation(fields: [uploaded_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "rag_documents_uploaded_by_fkey_emp")

  @@index([knowledge_base_id], map: "idx_rag_documents_kb")
  @@index([parent_document_id], map: "idx_rag_documents_parent_document_id")
  @@index([status], map: "idx_rag_documents_status")
  @@index([tenant_id], map: "idx_rag_documents_tenant")
  @@index([uploaded_by_employee_id], map: "idx_rag_documents_uploaded_by_employee_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model rag_knowledge_bases {
  id              String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id       String          @db.Uuid
  code            String          @db.VarChar(50)
  name            String          @db.VarChar(200)
  description     String?
  kb_type         String          @db.VarChar(50)
  is_public       Boolean?        @default(false)
  allowed_roles   String[]
  embedding_model String?         @default("text-embedding-3-small") @db.VarChar(100)
  chunk_size      Int?            @default(1000)
  chunk_overlap   Int?            @default(200)
  is_active       Boolean?        @default(true)
  created_at      DateTime?       @default(now()) @db.Timestamp(6)
  updated_at      DateTime?       @default(now()) @db.Timestamp(6)
  deleted_at      DateTime?       @db.Timestamptz(6)
  rag_documents   rag_documents[]
  tenants         tenants         @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_rag_kb_tenant")
  @@index([kb_type], map: "idx_rag_kb_type")
}

model rag_messages {
  id                  String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  session_id          String?               @db.Uuid
  role                String                @db.VarChar(20)
  content             String
  sources             Json?                 @default("[]")
  generated_sql       String?
  sql_result          Json?
  tokens_input        Int?
  tokens_output       Int?
  created_at          DateTime?             @default(now()) @db.Timestamp(6)
  confidence_score    Decimal?              @db.Decimal(5, 4)
  confidence_factors  Json?
  requires_escalation Boolean?              @default(false)
  escalation_reason   String?               @db.VarChar(255)
  escalated_at        DateTime?             @db.Timestamp(6)
  escalated_to        String?               @db.Uuid
  human_response      String?
  human_responded_at  DateTime?             @db.Timestamp(6)
  feedback_rating     Int?
  feedback_comment    String?
  ai_escalation_queue ai_escalation_queue[]
  ai_query_audit      ai_query_audit[]
  rag_sessions        rag_sessions?         @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([session_id], map: "idx_rag_messages_session")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model rag_provider_keys {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String    @db.Uuid
  provider          String    @db.VarChar(50)
  api_key_encrypted String
  is_valid          Boolean?  @default(false)
  last_validated    DateTime? @db.Timestamp(6)
  requests_count    Int?      @default(0)
  tokens_used       Int?      @default(0)
  created_at        DateTime? @default(now()) @db.Timestamp(6)
  updated_at        DateTime? @default(now()) @db.Timestamp(6)
  tenants           tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, provider])
  @@index([tenant_id], map: "idx_rag_provider_keys_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model rag_sessions {
  id                  String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id             String?               @db.Uuid
  tenant_id           String                @db.Uuid
  provider            String                @db.VarChar(50)
  model               String?               @db.VarChar(100)
  title               String?               @db.VarChar(255)
  system_prompt       String?
  sources_enabled     Json?                 @default("[\"db\", \"documents\"]")
  created_at          DateTime?             @default(now()) @db.Timestamp(6)
  updated_at          DateTime?             @default(now()) @db.Timestamp(6)
  is_archived         Boolean?              @default(false)
  user_id_employee_id String?               @db.Uuid
  ai_escalation_queue ai_escalation_queue[]
  ai_query_audit      ai_query_audit[]
  rag_messages        rag_messages[]
  users               users?                @relation(fields: [user_id], references: [id], onUpdate: NoAction, map: "fk_rs_user")
  tenants             tenants               @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees           employees?            @relation(fields: [user_id_employee_id], references: [id], onUpdate: NoAction, map: "rag_sessions_user_id_fkey_emp")

  @@index([user_id_employee_id], map: "idx_rag_sessions_user_id_employee_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model rag_usage_stats {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id        String    @db.Uuid
  period_date      DateTime  @db.Date
  provider         String    @db.VarChar(50)
  chat_requests    Int?      @default(0)
  search_requests  Int?      @default(0)
  sql_requests     Int?      @default(0)
  document_uploads Int?      @default(0)
  tokens_input     Int?      @default(0)
  tokens_output    Int?      @default(0)
  embedding_tokens Int?      @default(0)
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  tenants          tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, period_date, provider])
  @@index([tenant_id, period_date], map: "idx_rag_usage_stats_tenant_period")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model rating_scales {
  id           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id    String    @db.Uuid
  name         String    @db.VarChar(100)
  description  String?
  scale_type   String?   @default("numeric") @db.VarChar(30)
  min_value    Decimal?  @default(1) @db.Decimal(3, 1)
  max_value    Decimal?  @default(5) @db.Decimal(3, 1)
  scale_points Json      @default("[{\"label\": \"Needs Improvement\", \"value\": 1, \"description\": \"Performance consistently below expectations\"}, {\"label\": \"Developing\", \"value\": 2, \"description\": \"Performance sometimes meets expectations\"}, {\"label\": \"Meets Expectations\", \"value\": 3, \"description\": \"Performance consistently meets expectations\"}, {\"label\": \"Exceeds Expectations\", \"value\": 4, \"description\": \"Performance frequently exceeds expectations\"}, {\"label\": \"Outstanding\", \"value\": 5, \"description\": \"Performance consistently exceeds expectations\"}]")
  is_default   Boolean?  @default(false)
  is_active    Boolean?  @default(true)
  created_at   DateTime? @default(now()) @db.Timestamptz(6)
  updated_at   DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at   DateTime? @db.Timestamptz(6)
  tenants      tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_rating_scales_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model rbp_area_perspectives {
  id                   Int                  @id @default(autoincrement())
  functional_area_id   Int
  perspective_code     String               @db.VarChar(20)
  relevance            String?              @default("PRIMARY") @db.VarChar(10)
  created_at           DateTime?            @default(now()) @db.Timestamptz(6)
  rbp_functional_areas rbp_functional_areas @relation(fields: [functional_area_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  rbp_perspectives     rbp_perspectives     @relation(fields: [perspective_code], references: [code], onDelete: NoAction, onUpdate: NoAction)

  @@unique([functional_area_id, perspective_code])
  @@index([functional_area_id], map: "idx_area_perspectives_area")
  @@index([perspective_code], map: "idx_area_perspectives_code")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model rbp_dashboard_nav_items {
  id                                                                         Int             @id @default(autoincrement())
  dashboard_id                                                               Int
  item_type                                                                  String          @db.VarChar(20)
  target_page_id                                                             Int?
  target_dashboard_id                                                        Int?
  external_url                                                               String?         @db.VarChar(500)
  section                                                                    String          @default("main") @db.VarChar(50)
  label_override                                                             String?         @db.VarChar(150)
  icon_override                                                              String?         @db.VarChar(50)
  sort_order                                                                 Int             @default(0)
  is_visible                                                                 Boolean         @default(true)
  created_at                                                                 DateTime        @default(now()) @db.Timestamptz(6)
  label_override_it                                                          String?         @db.VarChar(150)
  label_override_en                                                          String?         @db.VarChar(150)
  rbp_dashboards_rbp_dashboard_nav_items_dashboard_idTorbp_dashboards        rbp_dashboards  @relation("rbp_dashboard_nav_items_dashboard_idTorbp_dashboards", fields: [dashboard_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  rbp_dashboards_rbp_dashboard_nav_items_target_dashboard_idTorbp_dashboards rbp_dashboards? @relation("rbp_dashboard_nav_items_target_dashboard_idTorbp_dashboards", fields: [target_dashboard_id], references: [id], onUpdate: NoAction)
  rbp_pages                                                                  rbp_pages?      @relation(fields: [target_page_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([dashboard_id, sort_order], map: "idx_rbp_nav_items_dashboard")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model rbp_dashboards {
  id                                                                                  Int                       @id @default(autoincrement())
  code                                                                                String                    @unique @db.VarChar(50)
  name                                                                                String                    @db.VarChar(100)
  description                                                                         String?
  layout_path                                                                         String                    @db.VarChar(100)
  icon                                                                                String?                   @db.VarChar(50)
  sort_order                                                                          Int                       @default(0)
  is_active                                                                           Boolean                   @default(true)
  theme_config                                                                        Json?                     @default("{}")
  created_at                                                                          DateTime                  @default(now()) @db.Timestamptz(6)
  updated_at                                                                          DateTime                  @default(now()) @db.Timestamptz(6)
  name_it                                                                             String?                   @db.VarChar(100)
  name_en                                                                             String?                   @db.VarChar(100)
  description_it                                                                      String?
  description_en                                                                      String?
  rbp_dashboard_nav_items_rbp_dashboard_nav_items_dashboard_idTorbp_dashboards        rbp_dashboard_nav_items[] @relation("rbp_dashboard_nav_items_dashboard_idTorbp_dashboards")
  rbp_dashboard_nav_items_rbp_dashboard_nav_items_target_dashboard_idTorbp_dashboards rbp_dashboard_nav_items[] @relation("rbp_dashboard_nav_items_target_dashboard_idTorbp_dashboards")
  rbp_role_dashboards                                                                 rbp_role_dashboards[]
  rbp_roles                                                                           rbp_roles[]
  user_workspaces                                                                     user_workspaces[]
}

model rbp_data_classifications {
  id                 Int                  @id @default(autoincrement())
  code               String               @unique @db.VarChar(30)
  name               String               @db.VarChar(100)
  description        String?
  sensitivity_level  Int                  @unique
  created_at         DateTime             @default(now()) @db.Timestamptz(6)
  rbp_field_policies rbp_field_policies[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an index with non-default null sort order and requires additional setup for migrations. Visit https://pris.ly/d/default-index-null-ordering for more info.
model rbp_field_classifications {
  id             Int      @id @default(autoincrement())
  tenant_id      String?  @db.Uuid
  table_name     String   @db.VarChar(100)
  column_name    String   @db.VarChar(100)
  classification String   @db.VarChar(20)
  notes          String?
  created_at     DateTime @default(now()) @db.Timestamptz(6)
  updated_at     DateTime @default(now()) @db.Timestamptz(6)
  tenants        tenants? @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, table_name, column_name], map: "rbp_field_classifications_unique")
  @@index([table_name, tenant_id], map: "idx_rbp_field_classifications_lookup")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model rbp_field_policies {
  id                       Int                      @id @default(autoincrement())
  role_id                  Int
  data_classification_id   Int
  action                   String                   @default("MASK") @db.VarChar(20)
  field_list               Json?                    @default("[]")
  created_at               DateTime                 @default(now()) @db.Timestamptz(6)
  updated_at               DateTime                 @default(now()) @db.Timestamptz(6)
  rbp_data_classifications rbp_data_classifications @relation(fields: [data_classification_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  rbp_roles                rbp_roles                @relation(fields: [role_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([role_id, data_classification_id])
  @@index([role_id], map: "idx_rbp_field_policies_role")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model rbp_functional_areas {
  id                       Int                        @id @default(autoincrement())
  code                     String                     @unique @db.VarChar(50)
  name                     String                     @db.VarChar(100)
  description              String?
  category                 String                     @db.VarChar(50)
  sort_order               Int                        @default(0)
  is_active                Boolean                    @default(true)
  created_at               DateTime                   @default(now()) @db.Timestamptz(6)
  updated_at               DateTime                   @default(now()) @db.Timestamptz(6)
  name_it                  String?                    @db.VarChar(100)
  name_en                  String?                    @db.VarChar(100)
  description_it           String?
  description_en           String?
  admin_component_registry admin_component_registry[]
  rbp_area_perspectives    rbp_area_perspectives[]
  rbp_pages                rbp_pages[]
  rbp_role_permissions     rbp_role_permissions[]
  widget_catalog           widget_catalog[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rbp_pages {
  id                      Int                       @id @default(autoincrement())
  code                    String                    @unique @db.VarChar(80)
  name                    String                    @db.VarChar(150)
  description             String?
  route_path              String                    @db.VarChar(200)
  functional_area_code    String                    @db.VarChar(50)
  status                  String                    @default("ACTIVE") @db.VarChar(20)
  redirect_to             String?                   @db.VarChar(200)
  icon                    String?                   @db.VarChar(50)
  component_path          String?                   @db.VarChar(200)
  requires_auth           Boolean                   @default(true)
  metadata                Json?                     @default("{}")
  created_at              DateTime                  @default(now()) @db.Timestamptz(6)
  updated_at              DateTime                  @default(now()) @db.Timestamptz(6)
  suggested_dashboards    String[]                  @default([]) @db.VarChar
  name_it                 String?                   @db.VarChar(150)
  name_en                 String?                   @db.VarChar(150)
  description_it          String?
  description_en          String?
  rbp_dashboard_nav_items rbp_dashboard_nav_items[]
  rbp_functional_areas    rbp_functional_areas      @relation(fields: [functional_area_code], references: [code], onDelete: NoAction, onUpdate: NoAction)

  @@index([functional_area_code], map: "idx_rbp_pages_area")
  @@index([status], map: "idx_rbp_pages_status")
}

model rbp_perspectives {
  code                  String                  @id @db.VarChar(20)
  name                  String                  @db.VarChar(100)
  description           String?
  icon                  String?                 @db.VarChar(50)
  color                 String?                 @db.VarChar(7)
  sort_order            Int?                    @default(0)
  created_at            DateTime?               @default(now()) @db.Timestamptz(6)
  name_it               String?                 @db.VarChar(100)
  name_en               String?                 @db.VarChar(100)
  description_it        String?
  description_en        String?
  rbp_area_perspectives rbp_area_perspectives[]
  widget_catalog        widget_catalog[]
  dashboard_elements    dashboard_elements[]
  dashboard_presets     dashboard_presets[]
}

model rbp_role_dashboards {
  id             Int            @id @default(autoincrement())
  role_id        Int
  dashboard_id   Int
  is_default     Boolean        @default(false)
  created_at     DateTime       @default(now()) @db.Timestamptz(6)
  rbp_dashboards rbp_dashboards @relation(fields: [dashboard_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  rbp_roles      rbp_roles      @relation(fields: [role_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([role_id, dashboard_id])
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model rbp_role_permissions {
  id                   Int                  @id @default(autoincrement())
  role_id              Int
  functional_area_id   Int
  can_view             Boolean              @default(false)
  can_create           Boolean              @default(false)
  can_edit             Boolean              @default(false)
  can_delete           Boolean              @default(false)
  can_approve          Boolean              @default(false)
  can_export           Boolean              @default(false)
  scope_type           String               @default("SELF") @db.VarChar(30)
  conditions           Json?                @default("{}")
  created_at           DateTime             @default(now()) @db.Timestamptz(6)
  updated_at           DateTime             @default(now()) @db.Timestamptz(6)
  rbp_functional_areas rbp_functional_areas @relation(fields: [functional_area_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  rbp_roles            rbp_roles            @relation(fields: [role_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([role_id, functional_area_id])
  @@index([functional_area_id], map: "idx_rbp_role_permissions_area")
  @@index([role_id], map: "idx_rbp_role_permissions_role")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model rbp_roles {
  id                     Int                    @id @default(autoincrement())
  code                   String                 @unique @db.VarChar(50)
  name                   String                 @db.VarChar(100)
  description            String?
  hierarchy_level        Int                    @unique(map: "rbp_roles_hierarchy_level_unique")
  is_system_role         Boolean                @default(true)
  is_assignable          Boolean                @default(true)
  inherits_from          String?                @db.VarChar(50)
  metadata               Json?                  @default("{}")
  created_at             DateTime               @default(now()) @db.Timestamptz(6)
  updated_at             DateTime               @default(now()) @db.Timestamptz(6)
  default_dashboard_code String?                @db.VarChar(50)
  description_it         String?
  description_en         String?
  rbp_field_policies     rbp_field_policies[]
  rbp_role_dashboards    rbp_role_dashboards[]
  rbp_role_permissions   rbp_role_permissions[]
  rbp_dashboards         rbp_dashboards?        @relation(fields: [default_dashboard_code], references: [code], onDelete: NoAction, onUpdate: NoAction)
  rbp_roles              rbp_roles?             @relation("rbp_rolesTorbp_roles", fields: [inherits_from], references: [code], onDelete: NoAction, onUpdate: NoAction)
  other_rbp_roles        rbp_roles[]            @relation("rbp_rolesTorbp_roles")
  rbp_scope_rules        rbp_scope_rules[]
  workspace_templates    workspace_templates[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model rbp_scope_rules {
  id           Int       @id @default(autoincrement())
  role_id      Int
  scope_type   String    @db.VarChar(30)
  description  String?
  sql_template String
  parameters   Json?     @default("{}")
  created_at   DateTime  @default(now()) @db.Timestamptz(6)
  rbp_roles    rbp_roles @relation(fields: [role_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([role_id, scope_type])
  @@index([role_id], map: "idx_rbp_scope_rules_role")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model rbp_section_translations {
  id           String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  section_code String
  locale       String
  label        String
  tenant_id    String?  @db.Uuid
  created_at   DateTime @default(now()) @db.Timestamptz(6)
  updated_at   DateTime @default(now()) @db.Timestamptz(6)
  tenants      tenants? @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([section_code, locale, tenant_id], map: "rbp_section_translations_unique")
  @@index([section_code, locale], map: "rbp_section_translations_lookup_idx")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model rbp_sections {
  id         String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id  String?   @db.Uuid
  code       String    @db.VarChar(50)
  label_it   String    @db.VarChar(100)
  label_en   String?   @db.VarChar(100)
  sort_order Int
  icon       String?   @db.VarChar(50)
  created_at DateTime? @default(now()) @db.Timestamptz(6)
  updated_at DateTime? @default(now()) @db.Timestamptz(6)
  tenants    tenants?  @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, code])
  @@index([tenant_id, sort_order], map: "idx_rbp_sections_tenant")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model rbp_team_leaders {
  id          Int       @id @default(autoincrement())
  team_id     Int
  leader_id   String    @db.Uuid
  assigned_at DateTime  @default(now()) @db.Timestamptz(6)
  revoked_at  DateTime? @db.Timestamptz(6)
  employees   employees @relation(fields: [leader_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  rbp_teams   rbp_teams @relation(fields: [team_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([team_id, leader_id])
  @@index([team_id], map: "idx_rbp_team_leaders_team")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model rbp_team_members {
  id           Int       @id @default(autoincrement())
  team_id      Int
  employee_id  String    @db.Uuid
  role_in_team String    @default("member") @db.VarChar(50)
  joined_at    DateTime  @default(now()) @db.Timestamptz(6)
  left_at      DateTime? @db.Timestamptz(6)
  employees    employees @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  rbp_teams    rbp_teams @relation(fields: [team_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([team_id, employee_id])
  @@index([team_id], map: "idx_rbp_team_members_team")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model rbp_teams {
  id               Int                @id @default(autoincrement())
  tenant_id        String             @db.Uuid
  code             String             @db.VarChar(50)
  name             String             @db.VarChar(150)
  description      String?
  purpose          String?
  created_by       String             @db.Uuid
  is_active        Boolean            @default(true)
  created_at       DateTime           @default(now()) @db.Timestamptz(6)
  updated_at       DateTime           @default(now()) @db.Timestamptz(6)
  rbp_team_leaders rbp_team_leaders[]
  rbp_team_members rbp_team_members[]
  users            users              @relation(fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants          tenants            @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([tenant_id, code])
  @@index([tenant_id], map: "idx_rbp_teams_tenant")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model recognition {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id        String    @db.Uuid
  from_employee_id String?   @db.Uuid
  to_employee_id   String?   @db.Uuid
  message          String
  category         String?   @db.VarChar(50)
  is_public        Boolean?  @default(true)
  created_at       DateTime? @default(now()) @db.Timestamp(6)
  receiver_id      String?   @db.Uuid
  giver_id         String?   @db.Uuid
  badge_type       String?   @db.VarChar(50)
  core_value       String?   @db.VarChar(100)
  points_awarded   Int?      @default(0)
  tenants          tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_recognition_tenant_id")
  @@index([to_employee_id], map: "idx_recognition_to")
}

model recruiting_candidate_history {
  id                    String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  candidate_id          String?                @db.Uuid
  from_stage            String?                @db.VarChar(50)
  to_stage              String                 @db.VarChar(50)
  changed_by            String?                @db.Uuid
  notes                 String?
  changed_at            DateTime?              @default(now()) @db.Timestamp(6)
  created_at            DateTime?              @default(now()) @db.Timestamptz(6)
  recruiting_candidates recruiting_candidates? @relation(fields: [candidate_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([candidate_id], map: "idx_recruiting_candidate_history_candidate_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model recruiting_candidates {
  id                           String                         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                    String                         @db.Uuid
  requisition_id               String?                        @db.Uuid
  first_name                   String                         @db.VarChar(100)
  last_name                    String                         @db.VarChar(100)
  email                        String                         @db.VarChar(255)
  phone                        String?                        @db.VarChar(50)
  current_company              String?                        @db.VarChar(255)
  job_title                    String?                        @db.VarChar(255)
  experience_years             Int?
  stage                        String?                        @default("applied") @db.VarChar(50)
  rating                       Decimal?                       @db.Decimal(3, 2)
  source                       String?                        @db.VarChar(100)
  resume_url                   String?
  linkedin_url                 String?
  portfolio_url                String?
  notes                        String?
  skills                       Json?                          @default("[]")
  applied_at                   DateTime?                      @default(now()) @db.Timestamp(6)
  last_activity                DateTime?                      @default(now()) @db.Timestamp(6)
  created_at                   DateTime?                      @default(now()) @db.Timestamp(6)
  updated_at                   DateTime?                      @default(now()) @db.Timestamp(6)
  profile_embedding            Unsupported("vector")?
  embedding_text_hash          String?                        @db.VarChar(64)
  embedding_model              String?                        @db.VarChar(100)
  embedding_generated_at       DateTime?                      @db.Timestamptz(6)
  recruiting_candidate_history recruiting_candidate_history[]
  recruiting_requisitions      recruiting_requisitions?       @relation(fields: [requisition_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                      tenants                        @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  recruiting_offers            recruiting_offers[]

  @@index([requisition_id], map: "idx_recruiting_cand_req")
  @@index([stage], map: "idx_recruiting_cand_stage")
  @@index([tenant_id], map: "idx_recruiting_cand_tenant")
  @@index([profile_embedding], map: "idx_recruiting_candidates_embedding")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model recruiting_interview_participants {
  id                                                                         String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  interview_id                                                               String?                @db.Uuid
  participant_type                                                           String                 @db.VarChar(30)
  user_id                                                                    String?                @db.Uuid
  employee_id                                                                String?                @db.Uuid
  external_email                                                             String?                @db.VarChar(255)
  external_name                                                              String?                @db.VarChar(200)
  is_required                                                                Boolean?               @default(true)
  is_lead                                                                    Boolean?               @default(false)
  rsvp_status                                                                String?                @default("pending") @db.VarChar(20)
  feedback_submitted                                                         Boolean?               @default(false)
  feedback                                                                   String?
  rating                                                                     Int?
  recommendation                                                             String?                @db.VarChar(30)
  created_at                                                                 DateTime?              @default(now()) @db.Timestamp(6)
  user_id_employee_id                                                        String?                @db.Uuid
  employees_recruiting_interview_participants_employee_idToemployees         employees?             @relation("recruiting_interview_participants_employee_idToemployees", fields: [employee_id], references: [id], onUpdate: NoAction, map: "fk_rip_employee")
  users                                                                      users?                 @relation(fields: [user_id], references: [id], onUpdate: NoAction, map: "fk_rip_user")
  recruiting_interviews                                                      recruiting_interviews? @relation(fields: [interview_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees_recruiting_interview_participants_user_id_employee_idToemployees employees?             @relation("recruiting_interview_participants_user_id_employee_idToemployees", fields: [user_id_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "recruiting_interview_participants_user_id_fkey_emp")

  @@index([interview_id], map: "idx_interview_participants_interview")
  @@index([user_id], map: "idx_interview_participants_user")
  @@index([user_id_employee_id], map: "idx_recruiting_interview_participants_user_id_employee_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model recruiting_interview_templates {
  id                    String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id             String    @db.Uuid
  name                  String    @db.VarChar(200)
  interview_type        String    @db.VarChar(50)
  description           String?
  duration_minutes      Int?      @default(60)
  default_location_type String?   @default("video") @db.VarChar(30)
  default_meeting_link  String?   @db.VarChar(500)
  default_instructions  String?
  questions             String?
  evaluation_criteria   String?
  is_active             Boolean?  @default(true)
  created_at            DateTime? @default(now()) @db.Timestamp(6)
  updated_at            DateTime? @default(now()) @db.Timestamp(6)
  deleted_at            DateTime? @db.Timestamptz(6)
  tenants               tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_recruiting_interview_templates_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model recruiting_interviewer_availability {
  id                        String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                 String     @db.Uuid
  user_id                   String?    @db.Uuid
  day_of_week               Int?
  start_time                DateTime   @db.Time(6)
  end_time                  DateTime   @db.Time(6)
  specific_date             DateTime?  @db.Date
  is_recurring              Boolean?   @default(true)
  max_interviews_per_day    Int?       @default(4)
  preferred_interview_types String[]
  is_active                 Boolean?   @default(true)
  created_at                DateTime?  @default(now()) @db.Timestamp(6)
  user_id_employee_id       String?    @db.Uuid
  deleted_at                DateTime?  @db.Timestamptz(6)
  users                     users?     @relation(fields: [user_id], references: [id], onUpdate: NoAction, map: "fk_ria_user")
  tenants                   tenants    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees                 employees? @relation(fields: [user_id_employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "recruiting_interviewer_availability_user_id_fkey_emp")

  @@index([user_id], map: "idx_interviewer_availability_user")
  @@index([tenant_id], map: "idx_recruiting_interviewer_availability_tenant_id")
  @@index([user_id_employee_id], map: "idx_recruiting_interviewer_availability_user_id_employee_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model recruiting_interviews {
  id                                String                              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                         String                              @db.Uuid
  candidate_id                      String                              @db.Uuid
  job_posting_id                    String?                             @db.Uuid
  interview_type                    String                              @db.VarChar(50)
  title                             String                              @db.VarChar(255)
  description                       String?
  scheduled_at                      DateTime                            @db.Timestamp(6)
  duration_minutes                  Int?                                @default(60)
  timezone                          String?                             @default("Europe/Rome") @db.VarChar(50)
  location_type                     String?                             @default("video") @db.VarChar(30)
  location                          String?                             @db.VarChar(500)
  meeting_link                      String?                             @db.VarChar(500)
  dial_in                           String?                             @db.VarChar(100)
  status                            String?                             @default("scheduled") @db.VarChar(30)
  outcome                           String?                             @db.VarChar(30)
  feedback                          String?
  rating                            Int?
  calendar_event_id                 String?                             @db.VarChar(255)
  ical_uid                          String?                             @db.VarChar(255)
  reminder_sent                     Boolean?                            @default(false)
  reminder_at                       DateTime?                           @db.Timestamp(6)
  notes                             String?
  created_by                        String?                             @db.Uuid
  created_at                        DateTime?                           @default(now()) @db.Timestamp(6)
  updated_at                        DateTime?                           @default(now()) @db.Timestamp(6)
  created_by_employee_id            String?                             @db.Uuid
  recruiting_interview_participants recruiting_interview_participants[]
  internal_job_postings             internal_job_postings?              @relation(fields: [job_posting_id], references: [id], onUpdate: NoAction, map: "fk_ri_posting")
  employees                         employees?                          @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "recruiting_interviews_created_by_fkey_emp")
  tenants                           tenants                             @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([candidate_id], map: "idx_interviews_candidate")
  @@index([status], map: "idx_interviews_status")
  @@index([tenant_id], map: "idx_interviews_tenant")
  @@index([created_by_employee_id], map: "idx_recruiting_interviews_created_by_employee_id")
  @@index([job_posting_id], map: "idx_recruiting_interviews_job_posting_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model recruiting_offers {
  id                      String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id               String                   @db.Uuid
  candidate_id            String?                  @db.Uuid
  requisition_id          String?                  @db.Uuid
  status                  String?                  @default("draft") @db.VarChar(50)
  salary_offered          Decimal?                 @db.Decimal(12, 2)
  currency                String?                  @default("EUR") @db.VarChar(3)
  bonus_offered           Decimal?                 @db.Decimal(12, 2)
  equity_offered          String?                  @db.VarChar(100)
  start_date              DateTime?                @db.Date
  expiry_date             DateTime?                @db.Date
  job_title               String?                  @db.VarChar(255)
  department              String?                  @db.VarChar(100)
  location                String?                  @db.VarChar(255)
  employment_type         String?                  @db.VarChar(50)
  benefits                Json?                    @default("{}")
  notes                   String?
  approved_by             String?                  @db.Uuid
  approved_at             DateTime?                @db.Timestamp(6)
  sent_at                 DateTime?                @db.Timestamp(6)
  responded_at            DateTime?                @db.Timestamp(6)
  created_at              DateTime?                @default(now()) @db.Timestamp(6)
  updated_at              DateTime?                @default(now()) @db.Timestamp(6)
  recruiting_candidates   recruiting_candidates?   @relation(fields: [candidate_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  recruiting_requisitions recruiting_requisitions? @relation(fields: [requisition_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                 tenants                  @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([candidate_id], map: "idx_recruiting_offers_cand")
  @@index([requisition_id], map: "idx_recruiting_offers_requisition_id")
  @@index([tenant_id], map: "idx_recruiting_offers_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model recruiting_requisitions {
  id                    String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id             String                  @db.Uuid
  title                 String                  @db.VarChar(255)
  department            String?                 @db.VarChar(100)
  location              String?                 @db.VarChar(255)
  employment_type       String?                 @default("full_time") @db.VarChar(50)
  status                String?                 @default("draft") @db.VarChar(50)
  priority              String?                 @default("medium") @db.VarChar(20)
  description           String?
  requirements          Json?                   @default("[]")
  salary_min            Decimal?                @db.Decimal(12, 2)
  salary_max            Decimal?                @db.Decimal(12, 2)
  currency              String?                 @default("EUR") @db.VarChar(3)
  hiring_manager_id     String?                 @db.Uuid
  recruiter_id          String?                 @db.Uuid
  target_hire_date      DateTime?               @db.Date
  headcount             Int?                    @default(1)
  created_at            DateTime?               @default(now()) @db.Timestamp(6)
  updated_at            DateTime?               @default(now()) @db.Timestamp(6)
  closed_at             DateTime?               @db.Timestamp(6)
  recruiting_candidates recruiting_candidates[]
  recruiting_offers     recruiting_offers[]
  tenants               tenants                 @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([status], map: "idx_recruiting_req_status")
  @@index([tenant_id], map: "idx_recruiting_req_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model report_definitions {
  id                     String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String                 @db.Uuid
  name                   String                 @db.VarChar(255)
  description            String?
  data_source            String                 @db.VarChar(100)
  columns                Json                   @default("[]")
  filters                Json?                  @default("[]")
  sorting                Json?                  @default("[]")
  grouping               Json?                  @default("[]")
  chart_config           Json?
  is_template            Boolean?               @default(false)
  is_public              Boolean?               @default(false)
  created_by             String?                @db.Uuid
  created_at             DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?              @default(now()) @db.Timestamptz(6)
  created_by_employee_id String?                @db.Uuid
  calculated_fields      Json?                  @default("[]")
  joins                  Json?                  @default("[]")
  parameters             Json?                  @default("[]")
  drill_down_config      Json?
  access_control         Json?                  @default("{\"roles\": [], \"users\": []}")
  employees              employees?             @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "report_definitions_created_by_fkey_emp")
  tenants                tenants                @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  report_executions      report_executions[]
  report_schedules       report_schedules[]
  report_subscriptions   report_subscriptions[]

  @@index([created_by], map: "idx_report_definitions_created_by")
  @@index([created_by_employee_id], map: "idx_report_definitions_created_by_employee_id")
  @@index([tenant_id], map: "idx_report_definitions_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model report_delivery_log {
  id                   String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id            String                @db.Uuid
  subscription_id      String?               @db.Uuid
  execution_id         String?               @db.Uuid
  delivery_method      String?               @db.VarChar(20)
  recipient            String?               @db.VarChar(255)
  status               String?               @default("pending") @db.VarChar(20)
  sent_at              DateTime?             @db.Timestamptz(6)
  error_message        String?
  created_at           DateTime?             @default(now()) @db.Timestamptz(6)
  report_executions    report_executions?    @relation(fields: [execution_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  report_subscriptions report_subscriptions? @relation(fields: [subscription_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants              tenants               @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([status], map: "idx_delivery_log_status")
  @@index([subscription_id], map: "idx_delivery_log_subscription")
  @@index([execution_id], map: "idx_report_delivery_log_execution_id")
  @@index([tenant_id], map: "idx_report_delivery_log_tenant_id")
}

model report_executions {
  id                      String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  report_id               String?               @db.Uuid
  executed_by             String?               @db.Uuid
  execution_type          String?               @default("manual") @db.VarChar(50)
  parameters              Json?
  row_count               Int?
  duration_ms             Int?
  status                  String?               @default("completed") @db.VarChar(50)
  error_message           String?
  file_path               String?
  created_at              DateTime?             @default(now()) @db.Timestamptz(6)
  executed_by_employee_id String?               @db.Uuid
  report_delivery_log     report_delivery_log[]
  employees               employees?            @relation(fields: [executed_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "report_executions_executed_by_fkey_emp")
  report_definitions      report_definitions?   @relation(fields: [report_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([executed_by_employee_id], map: "idx_report_executions_executed_by_employee_id")
  @@index([report_id], map: "idx_report_executions_report")
}

model report_schedules {
  id                 String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  report_id          String?             @db.Uuid
  schedule_type      String              @db.VarChar(50)
  schedule_config    Json
  recipients         Json?               @default("[]")
  format             String?             @default("excel") @db.VarChar(20)
  is_active          Boolean?            @default(true)
  last_run_at        DateTime?           @db.Timestamptz(6)
  next_run_at        DateTime?           @db.Timestamptz(6)
  created_at         DateTime?           @default(now()) @db.Timestamptz(6)
  deleted_at         DateTime?           @db.Timestamptz(6)
  report_definitions report_definitions? @relation(fields: [report_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([report_id], map: "idx_report_schedules_report")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model report_subscriptions {
  id                  String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String                @db.Uuid
  report_id           String?               @db.Uuid
  subscriber_id       String?               @db.Uuid
  delivery_method     String?               @default("email") @db.VarChar(20)
  format              String?               @default("excel") @db.VarChar(20)
  filters             Json?                 @default("{}")
  schedule_type       String                @db.VarChar(50)
  schedule_config     Json                  @default("{}")
  is_active           Boolean?              @default(true)
  last_delivered_at   DateTime?             @db.Timestamptz(6)
  delivery_count      Int?                  @default(0)
  created_at          DateTime?             @default(now()) @db.Timestamptz(6)
  updated_at          DateTime?             @default(now()) @db.Timestamptz(6)
  deleted_at          DateTime?             @db.Timestamptz(6)
  report_delivery_log report_delivery_log[]
  report_definitions  report_definitions?   @relation(fields: [report_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees           employees?            @relation(fields: [subscriber_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants             tenants               @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([report_id], map: "idx_subscriptions_report")
  @@index([subscriber_id], map: "idx_subscriptions_subscriber")
  @@index([tenant_id], map: "idx_subscriptions_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model requisitions {
  id                String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String         @db.Uuid
  title             String         @db.VarChar(255)
  description       String?
  requirements      String?
  department        String?        @db.VarChar(100)
  location          String?        @db.VarChar(255)
  employment_type   String?        @default("full_time") @db.VarChar(50)
  remote_policy     String?        @default("hybrid") @db.VarChar(50)
  salary_min        Decimal?       @db.Decimal(15, 2)
  salary_max        Decimal?       @db.Decimal(15, 2)
  salary_currency   String?        @default("EUR") @db.VarChar(3)
  status            String?        @default("draft") @db.VarChar(50)
  priority          String?        @default("medium") @db.VarChar(20)
  hiring_manager_id String?        @db.Uuid
  recruiter_id      String?        @db.Uuid
  positions_total   Int?           @default(1)
  positions_filled  Int?           @default(0)
  target_start_date DateTime?      @db.Date
  posted_date       DateTime?      @db.Date
  closed_date       DateTime?      @db.Date
  created_at        DateTime?      @default(now()) @db.Timestamptz(6)
  updated_at        DateTime?      @default(now()) @db.Timestamptz(6)
  applications      applications[]
  tenants           tenants        @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([status], map: "idx_requisitions_status")
  @@index([tenant_id], map: "idx_requisitions_tenant")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model review_cycle_notifications {
  id                String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String        @db.Uuid
  review_cycle_id   String        @db.Uuid
  notification_type String        @db.VarChar(50)
  recipient_type    String        @db.VarChar(20)
  recipient_id      String?       @db.Uuid
  subject           String?       @db.VarChar(255)
  body              String?
  scheduled_at      DateTime?     @db.Timestamptz(6)
  sent_at           DateTime?     @db.Timestamptz(6)
  status            String?       @default("pending") @db.VarChar(20)
  error_message     String?
  created_at        DateTime?     @default(now()) @db.Timestamptz(6)
  employees         employees?    @relation(fields: [recipient_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  review_cycles     review_cycles @relation(fields: [review_cycle_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants           tenants       @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([recipient_id], map: "idx_review_cycle_notifications_recipient_id")
  @@index([tenant_id], map: "idx_review_cycle_notifications_tenant_id")
  @@index([review_cycle_id], map: "idx_review_notifications_cycle")
  @@index([scheduled_at], map: "idx_review_notifications_scheduled")
  @@index([status], map: "idx_review_notifications_status")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model review_cycle_participants {
  id                                                         String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                                  String        @db.Uuid
  review_cycle_id                                            String        @db.Uuid
  employee_id                                                String        @db.Uuid
  manager_id                                                 String?       @db.Uuid
  status                                                     String?       @default("pending") @db.VarChar(30)
  self_review_completed                                      Boolean?      @default(false)
  self_review_completed_at                                   DateTime?     @db.Timestamptz(6)
  manager_review_completed                                   Boolean?      @default(false)
  manager_review_completed_at                                DateTime?     @db.Timestamptz(6)
  calibrated                                                 Boolean?      @default(false)
  calibrated_at                                              DateTime?     @db.Timestamptz(6)
  acknowledged                                               Boolean?      @default(false)
  acknowledged_at                                            DateTime?     @db.Timestamptz(6)
  excluded_reason                                            String?
  created_at                                                 DateTime?     @default(now()) @db.Timestamptz(6)
  updated_at                                                 DateTime?     @default(now()) @db.Timestamptz(6)
  employees_review_cycle_participants_employee_idToemployees employees     @relation("review_cycle_participants_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees_review_cycle_participants_manager_idToemployees  employees?    @relation("review_cycle_participants_manager_idToemployees", fields: [manager_id], references: [id], onUpdate: NoAction)
  review_cycles                                              review_cycles @relation(fields: [review_cycle_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                                                    tenants       @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([review_cycle_id, employee_id], map: "unique_cycle_employee")
  @@index([review_cycle_id], map: "idx_review_cycle_participants_cycle")
  @@index([employee_id], map: "idx_review_cycle_participants_employee")
  @@index([manager_id], map: "idx_review_cycle_participants_manager")
  @@index([status], map: "idx_review_cycle_participants_status")
  @@index([tenant_id], map: "idx_review_cycle_participants_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model review_cycle_phases {
  id                    String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id             String        @db.Uuid
  review_cycle_id       String        @db.Uuid
  phase_name            String        @db.VarChar(50)
  phase_order           Int
  start_date            DateTime      @db.Date
  end_date              DateTime      @db.Date
  status                String?       @default("pending") @db.VarChar(20)
  instructions          String?
  reminder_days_before  Int?          @default(3)
  escalation_days_after Int?          @default(2)
  is_required           Boolean?      @default(true)
  created_at            DateTime?     @default(now()) @db.Timestamptz(6)
  updated_at            DateTime?     @default(now()) @db.Timestamptz(6)
  review_cycles         review_cycles @relation(fields: [review_cycle_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants               tenants       @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([review_cycle_id, phase_name])
  @@index([review_cycle_id], map: "idx_review_cycle_phases_cycle")
  @@index([tenant_id], map: "idx_review_cycle_phases_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model review_cycles {
  id                            String                          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                     String                          @db.Uuid
  name                          String                          @db.VarChar(255)
  description                   String?
  cycle_type                    String?                         @default("annual") @db.VarChar(50)
  start_date                    DateTime                        @db.Date
  end_date                      DateTime                        @db.Date
  status                        String?                         @default("draft") @db.VarChar(50)
  created_at                    DateTime?                       @default(now()) @db.Timestamptz(6)
  updated_at                    DateTime?                       @default(now()) @db.Timestamptz(6)
  self_review_deadline          DateTime?                       @db.Date
  manager_review_deadline       DateTime?                       @db.Date
  calibration_deadline          DateTime?                       @db.Date
  feedback_deadline             DateTime?                       @db.Date
  acknowledgment_deadline       DateTime?                       @db.Date
  include_self_review           Boolean?                        @default(true)
  include_peer_review           Boolean?                        @default(false)
  include_upward_review         Boolean?                        @default(false)
  include_360_feedback          Boolean?                        @default(false)
  require_goal_assessment       Boolean?                        @default(true)
  require_competency_rating     Boolean?                        @default(true)
  review_template_id            String?                         @db.Uuid
  competency_framework_id       String?                         @db.Uuid
  rating_scale_id               String?                         @db.Uuid
  eligible_employees_filter     Json?                           @default("{}")
  finalization_deadline         DateTime?                       @db.Date
  rating_scale_type             String?                         @default("1-5") @db.VarChar(20)
  rating_scale_config           Json?                           @default("{\"max\": 5, \"min\": 1, \"labels\": [\"Needs Improvement\", \"Below Expectations\", \"Meets Expectations\", \"Exceeds Expectations\", \"Outstanding\"]}")
  launched_at                   DateTime?                       @db.Timestamptz(6)
  completed_at                  DateTime?                       @db.Timestamptz(6)
  template_id                   String?                         @db.Uuid
  feedback_360_anonymous        Boolean?                        @default(true)
  feedback_360_min_responses    Int?                            @default(3)
  feedback_360_deadline         DateTime?                       @db.Date
  feedback_360_peer_suggestions feedback_360_peer_suggestions[]
  feedback_requests             feedback_requests[]
  performance_reviews           performance_reviews[]
  review_cycle_notifications    review_cycle_notifications[]
  review_cycle_participants     review_cycle_participants[]
  review_cycle_phases           review_cycle_phases[]
  tenants                       tenants                         @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([start_date, end_date], map: "idx_review_cycles_dates")
  @@index([tenant_id, status], map: "idx_review_cycles_status")
  @@index([tenant_id], map: "idx_review_cycles_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model roadmap_phases {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name          String    @db.VarChar(100)
  description   String?
  status        String?   @default("planned") @db.VarChar(20)
  start_date    DateTime? @db.Date
  end_date      DateTime? @db.Date
  display_order Int?      @default(0)
  created_at    DateTime? @default(now()) @db.Timestamp(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model role_permissions {
  id            String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  role          rbac_role
  permission_id String           @db.Uuid
  scope         permission_scope
  conditions    Json?            @default("{}")
  granted_at    DateTime?        @default(now()) @db.Timestamptz(6)
  granted_by    String?          @db.Uuid
  notes         String?
  tenant_id     String           @db.Uuid
  created_at    DateTime?        @default(now()) @db.Timestamptz(6)
  updated_at    DateTime?        @default(now()) @db.Timestamptz(6)
  permissions   permissions      @relation(fields: [permission_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants       tenants          @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([role, permission_id], map: "uq_role_permission")
  @@index([permission_id], map: "idx_role_permissions_permission")
  @@index([role], map: "idx_role_permissions_role")
  @@index([scope], map: "idx_role_permissions_scope")
  @@index([tenant_id], map: "idx_role_permissions_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model role_skill_requirements {
  id                                                      String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                               String                 @db.Uuid
  role_id                                                 String                 @db.Uuid
  skill_id                                                String                 @db.Uuid
  required_knowledge_level                                Int?                   @db.SmallInt
  required_skill_level                                    Int?                   @db.SmallInt
  required_ability_level                                  Int?                   @db.SmallInt
  required_behavior_level                                 Int?                   @db.SmallInt
  required_attitude_level                                 Int?                   @db.SmallInt
  min_composite_score                                     Decimal?               @db.Decimal(4, 2)
  importance                                              requirement_importance @default(important)
  weight                                                  Decimal?               @default(1.00) @db.Decimal(3, 2)
  is_primary                                              Boolean?               @default(false)
  notes                                                   String?
  source                                                  String?                @default("manual") @db.VarChar(50)
  created_at                                              DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at                                              DateTime?              @default(now()) @db.Timestamptz(6)
  created_by                                              String?                @db.Uuid
  updated_by                                              String?                @db.Uuid
  employees_role_skill_requirements_created_byToemployees employees?             @relation("role_skill_requirements_created_byToemployees", fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  job_templates                                           job_templates          @relation(fields: [role_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  esco_skills                                             esco_skills            @relation(fields: [skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                                                 tenants                @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees_role_skill_requirements_updated_byToemployees employees?             @relation("role_skill_requirements_updated_byToemployees", fields: [updated_by], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([tenant_id, role_id, skill_id], map: "unique_role_skill")
  @@index([created_by], map: "idx_role_skill_requirements_created_by")
  @@index([updated_by], map: "idx_role_skill_requirements_updated_by")
  @@index([importance], map: "idx_rsr_importance")
  @@index([role_id], map: "idx_rsr_role")
  @@index([skill_id], map: "idx_rsr_skill")
  @@index([tenant_id, role_id], map: "idx_rsr_tenant_role")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model salary_band_assignments {
  id                                                                   String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id                                                          String       @db.Uuid
  band_id                                                              String       @db.Uuid
  current_salary                                                       Decimal?     @db.Decimal(12, 2)
  compa_ratio                                                          Decimal?     @db.Decimal(5, 2)
  range_penetration                                                    Decimal?     @db.Decimal(5, 2)
  assigned_at                                                          DateTime?    @default(now()) @db.Timestamp(6)
  assigned_by                                                          String?      @db.Uuid
  assigned_by_employee_id                                              String?      @db.Uuid
  created_at                                                           DateTime?    @default(now()) @db.Timestamptz(6)
  updated_at                                                           DateTime?    @default(now()) @db.Timestamptz(6)
  employees_salary_band_assignments_employee_idToemployees             employees    @relation("salary_band_assignments_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_sba_employee")
  employees_salary_band_assignments_assigned_by_employee_idToemployees employees?   @relation("salary_band_assignments_assigned_by_employee_idToemployees", fields: [assigned_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "salary_band_assignments_assigned_by_fkey_emp")
  salary_bands                                                         salary_bands @relation(fields: [band_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([employee_id, band_id])
  @@index([assigned_by_employee_id], map: "idx_salary_band_assignments_assigned_by_employee_id")
  @@index([employee_id], map: "idx_salary_band_assignments_employee")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model salary_bands {
  id                      String                    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id               String                    @db.Uuid
  band_code               String                    @db.VarChar(20)
  band_name               String                    @db.VarChar(100)
  description             String?
  job_level               String?                   @db.VarChar(50)
  job_family              String?                   @db.VarChar(100)
  currency                String?                   @default("EUR") @db.VarChar(3)
  min_salary              Decimal                   @db.Decimal(12, 2)
  mid_salary              Decimal?                  @db.Decimal(12, 2)
  max_salary              Decimal                   @db.Decimal(12, 2)
  range_spread_percent    Decimal?                  @db.Decimal(5, 2)
  geo_region              String?                   @db.VarChar(100)
  geo_adjustment_percent  Decimal?                  @default(0) @db.Decimal(5, 2)
  effective_from          DateTime                  @default(dbgenerated("CURRENT_DATE")) @db.Date
  effective_to            DateTime?                 @db.Date
  is_active               Boolean?                  @default(true)
  created_at              DateTime?                 @default(now()) @db.Timestamp(6)
  updated_at              DateTime?                 @default(now()) @db.Timestamp(6)
  created_by              String?                   @db.Uuid
  created_by_employee_id  String?                   @db.Uuid
  deleted_at              DateTime?                 @db.Timestamptz(6)
  salary_band_assignments salary_band_assignments[]
  employees               employees?                @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "salary_bands_created_by_fkey_emp")
  tenants                 tenants                   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([tenant_id, band_code, effective_from, geo_region])
  @@index([created_by_employee_id], map: "idx_salary_bands_created_by_employee_id")
  @@index([job_level, job_family], map: "idx_salary_bands_job_level")
  @@index([tenant_id], map: "idx_salary_bands_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model salary_history {
  id                                              String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                       String     @db.Uuid
  employee_id                                     String     @db.Uuid
  effective_date                                  DateTime   @db.Date
  salary                                          Decimal    @db.Decimal(12, 2)
  currency                                        String?    @default("EUR") @db.VarChar(3)
  salary_type                                     String?    @default("annual") @db.VarChar(20)
  change_reason                                   String?    @db.VarChar(100)
  previous_salary                                 Decimal?   @db.Decimal(12, 2)
  change_percentage                               Decimal?   @db.Decimal(5, 2)
  contract_id                                     String?    @db.Uuid
  approved_by                                     String?    @db.Uuid
  notes                                           String?
  created_at                                      DateTime?  @default(now()) @db.Timestamptz(6)
  updated_at                                      DateTime?  @default(now()) @db.Timestamptz(6)
  employees_salary_history_approved_byToemployees employees? @relation("salary_history_approved_byToemployees", fields: [approved_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  contracts                                       contracts? @relation(fields: [contract_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees_salary_history_employee_idToemployees employees  @relation("salary_history_employee_idToemployees", fields: [employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                                         tenants    @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([approved_by], map: "idx_salary_history_approved_by")
  @@index([contract_id], map: "idx_salary_history_contract_id")
  @@index([effective_date], map: "idx_salary_history_date")
  @@index([employee_id], map: "idx_salary_history_employee")
  @@index([tenant_id], map: "idx_salary_history_tenant")
}

model sap_config {
  id           Int       @id @default(autoincrement())
  config_key   String    @unique @db.VarChar(50)
  config_value String    @db.VarChar(100)
  description  String?   @db.VarChar(255)
  module       String?   @db.VarChar(10)
  created_at   DateTime? @default(now()) @db.Timestamp(6)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sap_delta_sync_log {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String    @db.Uuid
  sync_type         String    @db.VarChar(30)
  source_timestamp  DateTime? @db.Timestamp(6)
  local_timestamp   DateTime? @default(now()) @db.Timestamp(6)
  records_checked   Int?      @default(0)
  records_created   Int?      @default(0)
  records_updated   Int?      @default(0)
  records_deleted   Int?      @default(0)
  records_unchanged Int?      @default(0)
  status            String?   @default("completed") @db.VarChar(30)
  error_message     String?
  details           Json?
  created_at        DateTime? @default(now()) @db.Timestamp(6)
  tenants           tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_at], map: "idx_sap_delta_date")
  @@index([tenant_id], map: "idx_sap_delta_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sap_employee_mapping {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String    @db.Uuid
  pernr             String    @db.VarChar(20)
  employee_id       String    @db.Uuid
  last_synced_at    DateTime? @db.Timestamp(6)
  sync_status       String?   @default("active") @db.VarChar(30)
  sap_data_snapshot Json?
  notes             String?
  created_at        DateTime? @default(now()) @db.Timestamp(6)
  updated_at        DateTime? @default(now()) @db.Timestamp(6)
  employees         employees @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants           tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, pernr])
  @@index([employee_id], map: "idx_sap_emp_mapping_employee")
  @@index([pernr], map: "idx_sap_emp_mapping_pernr")
  @@index([tenant_id], map: "idx_sap_emp_mapping_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sap_infotype_mappings {
  id                    String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id             String    @db.Uuid
  infotype              String    @db.VarChar(10)
  infotype_name         String?   @db.VarChar(200)
  sap_field             String    @db.VarChar(100)
  sap_field_description String?   @db.VarChar(255)
  target_table          String    @db.VarChar(100)
  target_field          String    @db.VarChar(100)
  transform_type        String?   @default("direct") @db.VarChar(50)
  transform_config      Json?
  validation_rules      Json?
  required              Boolean?  @default(false)
  default_value         String?
  is_active             Boolean?  @default(true)
  priority              Int?      @default(100)
  notes                 String?
  created_at            DateTime? @default(now()) @db.Timestamp(6)
  updated_at            DateTime? @default(now()) @db.Timestamp(6)
  deleted_at            DateTime? @db.Timestamptz(6)
  tenants               tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, infotype, sap_field])
  @@index([infotype], map: "idx_sap_mappings_infotype")
  @@index([tenant_id], map: "idx_sap_mappings_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sap_migration_jobs {
  id                         String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                  String                       @db.Uuid
  job_name                   String                       @db.VarChar(200)
  job_type                   String                       @db.VarChar(50)
  source_system              String?                      @db.VarChar(100)
  source_file_path           String?
  source_file_hash           String?                      @db.VarChar(64)
  status                     String?                      @default("pending") @db.VarChar(30)
  progress_percent           Int?                         @default(0)
  current_phase              String?                      @db.VarChar(50)
  total_records              Int?                         @default(0)
  processed_records          Int?                         @default(0)
  success_count              Int?                         @default(0)
  error_count                Int?                         @default(0)
  warning_count              Int?                         @default(0)
  skipped_count              Int?                         @default(0)
  started_at                 DateTime?                    @db.Timestamp(6)
  completed_at               DateTime?                    @db.Timestamp(6)
  estimated_completion       DateTime?                    @db.Timestamp(6)
  config                     Json?                        @default("{}")
  infotype_selection         String[]
  summary                    Json?
  error_log                  Json?
  created_by                 String?                      @db.Uuid
  created_at                 DateTime?                    @default(now()) @db.Timestamp(6)
  updated_at                 DateTime?                    @default(now()) @db.Timestamp(6)
  tenants                    tenants                      @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sap_migration_rollback_log sap_migration_rollback_log[]
  sap_staged_data            sap_staged_data[]

  @@index([status], map: "idx_sap_jobs_status")
  @@index([tenant_id], map: "idx_sap_jobs_tenant")
  @@index([job_type], map: "idx_sap_jobs_type")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sap_migration_rollback_log {
  id                 String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String             @db.Uuid
  job_id             String             @db.Uuid
  operation          String             @db.VarChar(20)
  target_table       String             @db.VarChar(100)
  target_id          String             @db.Uuid
  old_data           Json?
  new_data           Json?
  rolled_back        Boolean?           @default(false)
  rolled_back_at     DateTime?          @db.Timestamp(6)
  created_at         DateTime?          @default(now()) @db.Timestamp(6)
  sap_migration_jobs sap_migration_jobs @relation(fields: [job_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants            tenants            @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([job_id], map: "idx_sap_rollback_job")
  @@index([tenant_id], map: "idx_sap_rollback_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sap_staged_data {
  id                  String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String             @db.Uuid
  job_id              String             @db.Uuid
  infotype            String             @db.VarChar(10)
  pernr               String             @db.VarChar(20)
  subtype             String?            @db.VarChar(10)
  object_id           String?            @db.VarChar(50)
  begda               DateTime?          @db.Date
  endda               DateTime?          @db.Date
  seqnr               Int?
  raw_data            Json
  status              String?            @default("pending") @db.VarChar(30)
  validation_errors   Json?
  validation_warnings Json?
  mapped_data         Json?
  target_table        String?            @db.VarChar(100)
  target_id           String?            @db.Uuid
  processed_at        DateTime?          @db.Timestamp(6)
  created_at          DateTime?          @default(now()) @db.Timestamp(6)
  sap_migration_jobs  sap_migration_jobs @relation(fields: [job_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants             tenants            @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([infotype], map: "idx_sap_staged_infotype")
  @@index([job_id], map: "idx_sap_staged_job")
  @@index([pernr], map: "idx_sap_staged_pernr")
  @@index([status], map: "idx_sap_staged_status")
  @@index([tenant_id], map: "idx_sap_staged_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model saved_jobs {
  id                    String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id             String                @db.Uuid
  employee_id           String                @db.Uuid
  job_posting_id        String                @db.Uuid
  saved_at              DateTime?             @default(now()) @db.Timestamp(6)
  notes                 String?
  created_at            DateTime?             @default(now()) @db.Timestamptz(6)
  updated_at            DateTime?             @default(now()) @db.Timestamptz(6)
  employees             employees             @relation(fields: [employee_id], references: [id], onDelete: SetNull, onUpdate: NoAction, map: "fk_sj_employee")
  internal_job_postings internal_job_postings @relation(fields: [job_posting_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_sj_posting")
  tenants               tenants               @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([employee_id, job_posting_id])
  @@index([tenant_id], map: "idx_saved_jobs_tenant_id")
}

model schema_migrations {
  version    String    @id @db.VarChar(255)
  applied_at DateTime? @default(now()) @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model self_assessment_evidence {
  id                                                        String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                                 String              @db.Uuid
  performance_review_id                                     String              @db.Uuid
  employee_id                                               String              @db.Uuid
  evidence_type                                             String              @db.VarChar(50)
  title                                                     String              @db.VarChar(255)
  description                                               String?
  file_url                                                  String?             @db.VarChar(500)
  external_link                                             String?             @db.VarChar(500)
  related_goal_id                                           String?             @db.Uuid
  related_competency                                        String?             @db.VarChar(100)
  date_achieved                                             DateTime?           @db.Date
  verified                                                  Boolean?            @default(false)
  verified_by                                               String?             @db.Uuid
  verified_at                                               DateTime?           @db.Timestamptz(6)
  created_at                                                DateTime?           @default(now()) @db.Timestamptz(6)
  updated_at                                                DateTime?           @default(now()) @db.Timestamptz(6)
  employees_self_assessment_evidence_employee_idToemployees employees           @relation("self_assessment_evidence_employee_idToemployees", fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  performance_reviews                                       performance_reviews @relation(fields: [performance_review_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  goals                                                     goals?              @relation(fields: [related_goal_id], references: [id], onUpdate: NoAction)
  tenants                                                   tenants             @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees_self_assessment_evidence_verified_byToemployees employees?          @relation("self_assessment_evidence_verified_byToemployees", fields: [verified_by], references: [id], onUpdate: NoAction)

  @@index([employee_id], map: "idx_self_assessment_evidence_employee")
  @@index([related_goal_id], map: "idx_self_assessment_evidence_goal")
  @@index([performance_review_id], map: "idx_self_assessment_evidence_review")
  @@index([tenant_id], map: "idx_self_assessment_evidence_tenant")
  @@index([verified_by], map: "idx_self_assessment_evidence_verified_by")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model self_reviews {
  id                      String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id               String              @db.Uuid
  performance_review_id   String              @unique(map: "unique_self_review") @db.Uuid
  employee_id             String              @db.Uuid
  self_overall_rating     Decimal?            @db.Decimal(3, 1)
  self_goal_rating        Decimal?            @db.Decimal(3, 1)
  self_competency_rating  Decimal?            @db.Decimal(3, 1)
  achievements            String?
  challenges              String?
  learnings               String?
  goals_for_next_period   String?
  feedback_for_manager    String?
  competency_self_ratings Json?               @default("[]")
  goal_self_assessments   Json?               @default("[]")
  status                  String?             @default("draft") @db.VarChar(20)
  submitted_at            DateTime?           @db.Timestamptz(6)
  created_at              DateTime?           @default(now()) @db.Timestamptz(6)
  updated_at              DateTime?           @default(now()) @db.Timestamptz(6)
  goal_ratings            Json?
  ksaba_ratings           Json?
  evidence_count          Int?                @default(0)
  last_saved_at           DateTime?           @db.Timestamptz(6)
  employees               employees           @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  performance_reviews     performance_reviews @relation(fields: [performance_review_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                 tenants             @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([employee_id], map: "idx_self_reviews_employee")
  @@index([tenant_id], map: "idx_self_reviews_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model semantic_entity_index {
  id                     String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String                @db.Uuid
  entity_type            String                @db.VarChar(50)
  entity_id              String                @db.Uuid
  entity_name            String?               @db.VarChar(500)
  entity_context         String?
  embedding              Unsupported("vector")
  embedding_model        String?               @db.VarChar(100)
  embedding_generated_at DateTime?             @default(now()) @db.Timestamptz(6)
  metadata               Json?
  is_active              Boolean?              @default(true)
  created_at             DateTime?             @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?             @default(now()) @db.Timestamptz(6)
  deleted_at             DateTime?             @db.Timestamptz(6)
  tenants                tenants               @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, entity_type, entity_id], map: "uk_semantic_entity")
  @@index([embedding], map: "idx_semantic_entity_embedding")
  @@index([tenant_id, entity_type], map: "idx_semantic_entity_type")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model semantic_entity_relations {
  id                 String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String    @db.Uuid
  source_entity_type String    @db.VarChar(50)
  source_entity_id   String    @db.Uuid
  target_entity_type String    @db.VarChar(50)
  target_entity_id   String    @db.Uuid
  relation_type      String    @db.VarChar(50)
  relation_strength  Decimal?  @default(1.0) @db.Decimal(3, 2)
  metadata           Json?
  created_at         DateTime? @default(now()) @db.Timestamptz(6)
  tenants            tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, source_entity_type, source_entity_id, target_entity_type, target_entity_id, relation_type], map: "uk_semantic_relation")
  @@index([tenant_id, source_entity_type, source_entity_id], map: "idx_semantic_relations_source")
  @@index([tenant_id, target_entity_type, target_entity_id], map: "idx_semantic_relations_target")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model semantic_search_log {
  id                 String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String                 @db.Uuid
  user_id            String?                @db.Uuid
  query_text         String
  query_embedding    Unsupported("vector")?
  entity_types       String[]
  filters            Json?
  results_count      Int?
  top_results        Json?
  search_duration_ms Int?
  feedback_score     Int?
  created_at         DateTime?              @default(now()) @db.Timestamptz(6)
  users              users?                 @relation(fields: [user_id], references: [id], onUpdate: NoAction, map: "fk_ssl_user")
  tenants            tenants                @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([tenant_id, created_at(sort: Desc)], map: "idx_semantic_search_log_tenant")
  @@index([user_id], map: "idx_semantic_search_log_user_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model service_config {
  id           Int       @id @default(autoincrement())
  config_key   String    @unique @db.VarChar(100)
  config_value String
  config_type  String?   @default("string") @db.VarChar(50)
  description  String?
  created_at   DateTime? @default(now()) @db.Timestamp(6)
  updated_at   DateTime? @default(now()) @db.Timestamp(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model signature_recipients {
  id                 String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  request_id         String?             @db.Uuid
  employee_id        String?             @db.Uuid
  email              String              @db.VarChar(200)
  name               String?             @db.VarChar(200)
  sign_order         Int?                @default(1)
  status             String?             @default("pending") @db.VarChar(30)
  sent_at            DateTime?           @db.Timestamp(6)
  viewed_at          DateTime?           @db.Timestamp(6)
  signed_at          DateTime?           @db.Timestamp(6)
  declined_at        DateTime?           @db.Timestamp(6)
  decline_reason     String?
  signature_data     String?
  ip_address         String?             @db.VarChar(50)
  created_at         DateTime?           @default(now()) @db.Timestamp(6)
  employees          employees?          @relation(fields: [employee_id], references: [id], onUpdate: NoAction, map: "fk_sr_employee")
  signature_requests signature_requests? @relation(fields: [request_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([employee_id], map: "idx_signature_recipients_emp")
  @@index([request_id], map: "idx_signature_recipients_request")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model signature_requests {
  id                     String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String                 @db.Uuid
  document_name          String                 @db.VarChar(300)
  document_type          String?                @db.VarChar(100)
  document_url           String
  status                 String?                @default("pending") @db.VarChar(30)
  external_id            String?                @db.VarChar(200)
  provider               String?                @default("internal") @db.VarChar(50)
  created_by             String?                @db.Uuid
  expires_at             DateTime?              @db.Timestamp(6)
  completed_at           DateTime?              @db.Timestamp(6)
  created_at             DateTime?              @default(now()) @db.Timestamp(6)
  updated_at             DateTime?              @default(now()) @db.Timestamp(6)
  created_by_employee_id String?                @db.Uuid
  signature_recipients   signature_recipients[]
  employees              employees?             @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "signature_requests_created_by_fkey_emp")
  tenants                tenants                @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_by_employee_id], map: "idx_signature_requests_created_by_employee_id")
  @@index([status], map: "idx_signature_requests_status")
  @@index([tenant_id], map: "idx_signature_requests_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model skill_adjacencies {
  id                                                           String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  skill_id                                                     String      @db.Uuid
  adjacent_skill_id                                            String      @db.Uuid
  adjacency_score                                              Decimal     @db.Decimal(4, 3)
  adjacency_type                                               String?     @db.VarChar(30)
  job_posting_cooccurrence                                     Int?        @default(0)
  employee_cooccurrence                                        Int?        @default(0)
  calculated_at                                                DateTime?   @default(now()) @db.Timestamptz(6)
  created_at                                                   DateTime?   @default(now()) @db.Timestamptz(6)
  esco_skills_skill_adjacencies_adjacent_skill_idToesco_skills esco_skills @relation("skill_adjacencies_adjacent_skill_idToesco_skills", fields: [adjacent_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  esco_skills_skill_adjacencies_skill_idToesco_skills          esco_skills @relation("skill_adjacencies_skill_idToesco_skills", fields: [skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([skill_id, adjacent_skill_id], map: "uq_skill_adjacency")
  @@index([adjacent_skill_id], map: "idx_skill_adj_adjacent")
  @@index([job_posting_cooccurrence(sort: Desc)], map: "idx_skill_adj_cooccur")
  @@index([adjacency_score(sort: Desc)], map: "idx_skill_adj_score")
  @@index([skill_id], map: "idx_skill_adj_skill")
  @@index([adjacency_type], map: "idx_skill_adj_type")
}

/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model skill_aliases {
  id               String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  esco_skill_id    String?      @db.Uuid
  alias_text       String       @db.VarChar(255)
  alias_type       String?      @default("synonym") @db.VarChar(50)
  language_code    String?      @default("en") @db.VarChar(5)
  confidence_score Decimal?     @default(1.0) @db.Decimal(3, 2)
  source           String?      @default("manual") @db.VarChar(50)
  is_verified      Boolean?     @default(false)
  created_at       DateTime?    @default(now()) @db.Timestamptz(6)
  esco_skills      esco_skills? @relation(fields: [esco_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([esco_skill_id], map: "idx_skill_aliases_esco")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model skill_classifications {
  id                          String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  esco_skill_id               String          @unique(map: "uq_skill_classifications_esco") @db.Uuid
  primary_category            String          @db.VarChar(20)
  primary_category_confidence Decimal?        @db.Decimal(3, 2)
  cognitive_level             Int?
  cognitive_level_label       String?         @db.VarChar(30)
  social_dimension            String?         @db.VarChar(30)
  transferability             String          @default("transferable") @db.VarChar(20)
  transferability_score       Decimal?        @db.Decimal(3, 2)
  skill_cluster_id            String?         @db.Uuid
  classification_source       String?         @default("ai_assisted") @db.VarChar(50)
  classified_by               String?         @db.Uuid
  classified_at               DateTime?       @db.Timestamptz(6)
  needs_review                Boolean?        @default(false)
  review_notes                String?
  created_at                  DateTime?       @default(now()) @db.Timestamptz(6)
  updated_at                  DateTime?       @default(now()) @db.Timestamptz(6)
  confidence_score            Decimal?        @default(0.0) @db.Decimal(5, 4)
  users                       users?          @relation(fields: [classified_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  esco_skills                 esco_skills     @relation(fields: [esco_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  skill_clusters              skill_clusters? @relation(fields: [skill_cluster_id], references: [id], onUpdate: NoAction)

  @@index([primary_category], map: "idx_skill_class_category")
  @@index([skill_cluster_id], map: "idx_skill_class_cluster")
  @@index([cognitive_level], map: "idx_skill_class_cognitive")
  @@index([social_dimension], map: "idx_skill_class_social")
  @@index([classification_source], map: "idx_skill_class_source")
  @@index([transferability], map: "idx_skill_class_transfer")
  @@index([classified_by], map: "idx_skill_classifications_classified_by")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model skill_clusters {
  id                    String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  code                  String                  @unique @db.VarChar(50)
  name_en               String                  @db.VarChar(200)
  name_it               String?                 @db.VarChar(200)
  description           String?
  parent_cluster_id     String?                 @db.Uuid
  cluster_level         Int?                    @default(1)
  career_path_codes     String[]
  industry_codes        String[]
  is_active             Boolean?                @default(true)
  sort_order            Int?                    @default(0)
  created_at            DateTime?               @default(now()) @db.Timestamptz(6)
  updated_at            DateTime?               @default(now()) @db.Timestamptz(6)
  deleted_at            DateTime?               @db.Timestamptz(6)
  skill_classifications skill_classifications[]
  skill_clusters        skill_clusters?         @relation("skill_clustersToskill_clusters", fields: [parent_cluster_id], references: [id], onUpdate: NoAction)
  other_skill_clusters  skill_clusters[]        @relation("skill_clustersToskill_clusters")

  @@index([career_path_codes], map: "idx_skill_clusters_career", type: Gin)
  @@index([industry_codes], map: "idx_skill_clusters_industry", type: Gin)
  @@index([cluster_level], map: "idx_skill_clusters_level")
  @@index([parent_cluster_id], map: "idx_skill_clusters_parent")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model skill_demand_metrics {
  id                  String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String      @db.Uuid
  esco_skill_id       String      @db.Uuid
  period_start        DateTime    @db.Date
  period_end          DateTime    @db.Date
  total_postings      Int?        @default(0)
  postings_required   Int?        @default(0)
  postings_preferred  Int?        @default(0)
  avg_salary_min      Decimal?    @db.Decimal(12, 2)
  avg_salary_max      Decimal?    @db.Decimal(12, 2)
  by_experience_level Json?       @default("{}")
  by_location         Json?       @default("{}")
  by_industry         Json?       @default("{}")
  trend_vs_previous   Decimal?    @db.Decimal(5, 2)
  created_at          DateTime?   @default(now()) @db.Timestamptz(6)
  esco_skills         esco_skills @relation(fields: [esco_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants             tenants     @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, esco_skill_id, period_start, period_end])
  @@index([esco_skill_id], map: "idx_skill_demand_esco")
  @@index([period_start, period_end], map: "idx_skill_demand_period")
  @@index([tenant_id], map: "idx_skill_demand_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model skill_development_paths {
  id                  String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id         String?    @db.Uuid
  target_job_family   String?    @db.VarChar(100)
  target_job_level    String?    @db.VarChar(50)
  current_skills      String[]
  missing_skills      String[]
  recommended_actions String?
  progress_percent    Int?       @default(0)
  estimated_months    Int?
  is_active           Boolean?   @default(true)
  created_at          DateTime?  @default(now()) @db.Timestamp(6)
  updated_at          DateTime?  @default(now()) @db.Timestamp(6)
  deleted_at          DateTime?  @db.Timestamptz(6)
  employees           employees? @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_sdp_employee")

  @@index([employee_id], map: "idx_skill_development_paths_employee_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model skill_extraction_jobs {
  id                 String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String    @db.Uuid
  job_type           String    @db.VarChar(50)
  source_type        String    @db.VarChar(50)
  source_reference   String?   @db.VarChar(500)
  source_text        String?
  status             String?   @default("pending") @db.VarChar(20)
  extracted_skills   Json?     @default("[]")
  mapped_skills      Json?     @default("[]")
  unmapped_skills    Json?     @default("[]")
  extraction_model   String?   @db.VarChar(100)
  processing_time_ms Int?
  error_message      String?
  created_at         DateTime? @default(now()) @db.Timestamptz(6)
  completed_at       DateTime? @db.Timestamptz(6)
  tenants            tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([status], map: "idx_skill_extraction_status")
  @@index([tenant_id], map: "idx_skill_extraction_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model skill_gap_analyses {
  id                      String                    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id               String                    @db.Uuid
  analysis_name           String                    @db.VarChar(255)
  analysis_type           String                    @db.VarChar(50)
  target_entity_type      String?                   @db.VarChar(50)
  target_entity_id        String?                   @db.Uuid
  target_position_id      String?                   @db.Uuid
  target_position_name    String?                   @db.VarChar(255)
  comparison_type         String?                   @db.VarChar(50)
  analysis_date           DateTime?                 @default(dbgenerated("CURRENT_DATE")) @db.Date
  overall_match_score     Decimal?                  @db.Decimal(5, 2)
  coverage_score          Decimal?                  @db.Decimal(5, 2)
  proficiency_score       Decimal?                  @db.Decimal(5, 2)
  skill_matches           Json?                     @default("[]")
  skill_gaps              Json?                     @default("[]")
  skill_surplus           Json?                     @default("[]")
  recommendations         Json?                     @default("[]")
  priority_skills         Json?                     @default("[]")
  market_comparison       Json?
  internal_comparison     Json?
  created_by              String?                   @db.Uuid
  created_at              DateTime?                 @default(now()) @db.Timestamptz(6)
  updated_at              DateTime?                 @default(now()) @db.Timestamptz(6)
  created_by_employee_id  String?                   @db.Uuid
  analysis_embedding      Unsupported("vector")?
  embedding_text_hash     String?                   @db.VarChar(64)
  embedding_model         String?                   @db.VarChar(100)
  embedding_generated_at  DateTime?                 @db.Timestamptz(6)
  performance_skill_links performance_skill_links[]
  employees               employees?                @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                 tenants                   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_by_employee_id], map: "idx_skill_gap_analyses_created_by_employee_id")
  @@index([analysis_embedding], map: "idx_skill_gap_analyses_embedding")
  @@index([tenant_id], map: "idx_skill_gap_analyses_tenant_id")
  @@index([target_entity_type, target_entity_id], map: "idx_skill_gap_entity")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model skill_gap_snapshots {
  id                     String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String     @db.Uuid
  name                   String     @db.VarChar(255)
  analysis_date          DateTime   @default(dbgenerated("CURRENT_DATE")) @db.Date
  scope                  Json?      @default("{}")
  gap_metrics            Json       @default("{}")
  recommendations        Json?      @default("[]")
  created_by             String?    @db.Uuid
  created_at             DateTime?  @default(now()) @db.Timestamptz(6)
  created_by_employee_id String?    @db.Uuid
  employees              employees? @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                tenants    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([analysis_date], map: "idx_skill_gap_date")
  @@index([created_by_employee_id], map: "idx_skill_gap_snapshots_created_by_employee_id")
  @@index([tenant_id], map: "idx_skill_gap_tenant")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model skill_matrices {
  id                 String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String    @db.Uuid
  matrix_name        String    @db.VarChar(255)
  matrix_type        String    @db.VarChar(50)
  entity_type        String?   @db.VarChar(50)
  entity_id          String?   @db.Uuid
  generated_at       DateTime? @default(now()) @db.Timestamptz(6)
  total_employees    Int?
  total_skills       Int?
  skill_coverage     Json?     @default("{}")
  skill_distribution Json?     @default("{}")
  skill_trends       Json?     @default("[]")
  top_skills         Json?     @default("[]")
  rare_skills        Json?     @default("[]")
  critical_gaps      Json?     @default("[]")
  created_at         DateTime? @default(now()) @db.Timestamptz(6)
  tenants            tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([entity_type, entity_id], map: "idx_skill_matrix_entity")
  @@index([tenant_id], map: "idx_skill_matrix_tenant")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model skill_migration_jobs {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String    @db.Uuid
  job_type          String    @db.VarChar(50)
  status            String    @default("pending") @db.VarChar(20)
  total_records     Int       @default(0)
  processed_records Int       @default(0)
  matched_records   Int       @default(0)
  failed_records    Int       @default(0)
  error_message     String?
  started_at        DateTime? @db.Timestamptz(6)
  completed_at      DateTime? @db.Timestamptz(6)
  created_at        DateTime  @default(now()) @db.Timestamptz(6)
  tenants           tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([status], map: "idx_skill_migration_jobs_status")
  @@index([tenant_id], map: "idx_skill_migration_jobs_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model skill_pair_usage {
  id                                                   String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  skill_id_1                                           String      @db.Uuid
  skill_id_2                                           String      @db.Uuid
  tenant_id                                            String      @db.Uuid
  co_occurrence_count                                  Int         @default(1)
  last_seen_at                                         DateTime?   @default(now()) @db.Timestamptz(6)
  context_type                                         String?     @db.VarChar(50)
  usage_strength                                       Decimal?    @default(0.0) @db.Decimal(5, 4)
  created_at                                           DateTime?   @default(now()) @db.Timestamptz(6)
  updated_at                                           DateTime?   @default(now()) @db.Timestamptz(6)
  esco_skills_skill_pair_usage_skill_id_1Toesco_skills esco_skills @relation("skill_pair_usage_skill_id_1Toesco_skills", fields: [skill_id_1], references: [id], onDelete: NoAction, onUpdate: NoAction)
  esco_skills_skill_pair_usage_skill_id_2Toesco_skills esco_skills @relation("skill_pair_usage_skill_id_2Toesco_skills", fields: [skill_id_2], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                                              tenants     @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([skill_id_1, skill_id_2, tenant_id, context_type], map: "skill_pair_unique")
  @@index([context_type], map: "idx_skill_pair_usage_context")
  @@index([skill_id_1], map: "idx_skill_pair_usage_skill1")
  @@index([skill_id_2], map: "idx_skill_pair_usage_skill2")
  @@index([usage_strength(sort: Desc)], map: "idx_skill_pair_usage_strength")
  @@index([tenant_id], map: "idx_skill_pair_usage_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model skill_relationships {
  id                                                           String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  source_skill_id                                              String      @db.Uuid
  target_skill_id                                              String      @db.Uuid
  relationship_type                                            String      @db.VarChar(30)
  relationship_strength                                        Decimal?    @default(0.50) @db.Decimal(3, 2)
  is_bidirectional                                             Boolean?    @default(false)
  substitution_context                                         String?
  prerequisite_level                                           Int?
  relationship_source                                          String?     @default("ai_inferred") @db.VarChar(50)
  validated_by                                                 String?     @db.Uuid
  validated_at                                                 DateTime?   @db.Timestamptz(6)
  created_at                                                   DateTime?   @default(now()) @db.Timestamptz(6)
  esco_skills_skill_relationships_source_skill_idToesco_skills esco_skills @relation("skill_relationships_source_skill_idToesco_skills", fields: [source_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  esco_skills_skill_relationships_target_skill_idToesco_skills esco_skills @relation("skill_relationships_target_skill_idToesco_skills", fields: [target_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users                                                        users?      @relation(fields: [validated_by], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([source_skill_id, target_skill_id, relationship_type], map: "uq_skill_relationship")
  @@index([source_skill_id], map: "idx_skill_rel_source")
  @@index([relationship_strength(sort: Desc)], map: "idx_skill_rel_strength")
  @@index([target_skill_id], map: "idx_skill_rel_target")
  @@index([relationship_type], map: "idx_skill_rel_type")
  @@index([validated_by], map: "idx_skill_relationships_validated_by")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model skill_requirements_templates {
  id                   String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id            String       @db.Uuid
  name                 String       @db.VarChar(255)
  description          String?
  is_default           Boolean?     @default(false)
  skill_id             String?      @db.Uuid
  skill_name           String       @db.VarChar(255)
  required_count       Int          @default(1)
  required_proficiency Decimal      @default(3.0) @db.Decimal(3, 1)
  priority             String       @default("medium") @db.VarChar(20)
  notes                String?
  created_at           DateTime     @default(now()) @db.Timestamptz(6)
  updated_at           DateTime     @default(now()) @db.Timestamptz(6)
  org_unit_id          String?      @db.Uuid
  org_units            org_units?   @relation(fields: [org_unit_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  esco_skills          esco_skills? @relation(fields: [skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants              tenants      @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, name, skill_name])
  @@index([skill_id], map: "idx_skill_req_templates_skill")
  @@index([tenant_id], map: "idx_skill_req_templates_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model skill_supply_metrics {
  id                       String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                String      @db.Uuid
  esco_skill_id            String      @db.Uuid
  calculated_date          DateTime?   @default(dbgenerated("CURRENT_DATE")) @db.Date
  employee_count           Int?        @default(0)
  avg_proficiency          Decimal?    @db.Decimal(3, 2)
  proficiency_distribution Json?       @default("{}")
  by_department            Json?       @default("{}")
  by_tenure                Json?       @default("{}")
  trend_vs_previous        Decimal?    @db.Decimal(5, 2)
  created_at               DateTime?   @default(now()) @db.Timestamptz(6)
  esco_skills              esco_skills @relation(fields: [esco_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                  tenants     @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, esco_skill_id, calculated_date])
  @@index([esco_skill_id], map: "idx_skill_supply_esco")
  @@index([tenant_id], map: "idx_skill_supply_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model skill_synonyms {
  id            String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  esco_skill_id String      @db.Uuid
  synonym       String
  language      String?     @default("en") @db.VarChar(10)
  source        String?     @db.VarChar(50)
  usage_count   Int?        @default(0)
  created_at    DateTime?   @default(now()) @db.Timestamptz(6)
  esco_skills   esco_skills @relation(fields: [esco_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([esco_skill_id, synonym, language])
  @@index([esco_skill_id], map: "idx_skill_synonyms_esco")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model skill_taxonomy_extensions {
  id              String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id       String      @db.Uuid
  skill_id        String      @db.Uuid
  extension_type  String      @db.VarChar(50)
  extension_key   String?     @db.VarChar(100)
  extension_value String?
  language        String?     @default("it") @db.VarChar(10)
  source          String?     @default("manual") @db.VarChar(50)
  is_approved     Boolean?    @default(false)
  approved_by     String?     @db.Uuid
  created_at      DateTime?   @default(now()) @db.Timestamptz(6)
  updated_at      DateTime?   @default(now()) @db.Timestamptz(6)
  employees       employees?  @relation(fields: [approved_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  esco_skills     esco_skills @relation(fields: [skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants         tenants     @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([approved_by], map: "idx_skill_taxonomy_extensions_approved_by")
  @@index([skill_id], map: "idx_skill_taxonomy_extensions_skill")
  @@index([tenant_id], map: "idx_skill_taxonomy_extensions_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model social_comments {
  id                    String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  post_id               String?           @db.Uuid
  author_id             String?           @db.Uuid
  parent_comment_id     String?           @db.Uuid
  content               String
  likes_count           Int?              @default(0)
  created_at            DateTime?         @default(now()) @db.Timestamp(6)
  updated_at            DateTime?         @default(now()) @db.Timestamp(6)
  social_comments       social_comments?  @relation("social_commentsTosocial_comments", fields: [parent_comment_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  other_social_comments social_comments[] @relation("social_commentsTosocial_comments")
  social_posts          social_posts?     @relation(fields: [post_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  social_likes          social_likes[]

  @@index([parent_comment_id], map: "idx_social_comments_parent_comment_id")
  @@index([post_id], map: "idx_social_comments_post")
}

model social_likes {
  id              String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  post_id         String?          @db.Uuid
  comment_id      String?          @db.Uuid
  employee_id     String?          @db.Uuid
  created_at      DateTime?        @default(now()) @db.Timestamp(6)
  employees       employees?       @relation(fields: [employee_id], references: [id], onUpdate: NoAction, map: "fk_sl_employee")
  social_comments social_comments? @relation(fields: [comment_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  social_posts    social_posts?    @relation(fields: [post_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([post_id, employee_id])
  @@index([comment_id], map: "idx_social_likes_comment_id")
  @@index([post_id], map: "idx_social_likes_post")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_posts {
  id                 String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String            @db.Uuid
  author_id          String?           @db.Uuid
  content            String
  post_type          String?           @default("update") @db.VarChar(50)
  attachments        Json?
  poll_options       Json?
  visibility         String?           @default("company") @db.VarChar(30)
  target_departments String[]
  likes_count        Int?              @default(0)
  comments_count     Int?              @default(0)
  is_pinned          Boolean?          @default(false)
  pinned_until       DateTime?         @db.Timestamp(6)
  published_at       DateTime?         @default(now()) @db.Timestamp(6)
  created_at         DateTime?         @default(now()) @db.Timestamp(6)
  updated_at         DateTime?         @default(now()) @db.Timestamp(6)
  social_comments    social_comments[]
  social_likes       social_likes[]
  tenants            tenants           @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([author_id], map: "idx_social_posts_author")
  @@index([published_at(sort: Desc)], map: "idx_social_posts_published")
  @@index([tenant_id], map: "idx_social_posts_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sso_configurations {
  id                     String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String               @db.Uuid
  name                   String               @db.VarChar(200)
  provider_type          String               @db.VarChar(30)
  saml_entity_id         String?              @db.VarChar(500)
  saml_sso_url           String?              @db.VarChar(500)
  saml_slo_url           String?              @db.VarChar(500)
  saml_certificate       String?
  saml_name_id_format    String?              @db.VarChar(100)
  oidc_issuer            String?              @db.VarChar(500)
  oidc_client_id         String?              @db.VarChar(200)
  oidc_client_secret     String?              @db.VarChar(500)
  oidc_authorization_url String?              @db.VarChar(500)
  oidc_token_url         String?              @db.VarChar(500)
  oidc_userinfo_url      String?              @db.VarChar(500)
  oidc_scopes            String?              @default("openid profile email")
  attribute_mapping      Json?                @default("{\"email\": \"email\", \"lastName\": \"family_name\", \"firstName\": \"given_name\", \"employeeId\": \"employee_id\"}")
  is_active              Boolean?             @default(false)
  allow_password_login   Boolean?             @default(true)
  auto_provision_users   Boolean?             @default(false)
  default_role           String?              @default("employee") @db.VarChar(50)
  allowed_domains        String[]
  created_by             String?              @db.Uuid
  created_at             DateTime?            @default(now()) @db.Timestamp(6)
  updated_at             DateTime?            @default(now()) @db.Timestamp(6)
  created_by_employee_id String?              @db.Uuid
  deleted_at             DateTime?            @db.Timestamptz(6)
  employees              employees?           @relation(fields: [created_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "sso_configurations_created_by_fkey_emp")
  tenants                tenants              @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sso_login_attempts     sso_login_attempts[]

  @@unique([tenant_id, provider_type])
  @@index([created_by_employee_id], map: "idx_sso_configurations_created_by_employee_id")
  @@index([tenant_id], map: "idx_sso_configurations_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model sso_login_attempts {
  id                 String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  config_id          String?             @db.Uuid
  email              String?             @db.VarChar(200)
  status             String?             @db.VarChar(30)
  error_message      String?
  ip_address         String?             @db.VarChar(50)
  user_agent         String?
  created_at         DateTime?           @default(now()) @db.Timestamp(6)
  sso_configurations sso_configurations? @relation(fields: [config_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([config_id], map: "idx_sso_login_attempts_config")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model succession_candidates {
  id                    String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  critical_role_id      String?   @db.Uuid
  candidate_employee_id String?   @db.Uuid
  readiness_level       String?   @default("ready_2_years") @db.VarChar(50)
  strengths             String?
  development_needs     String?
  development_plan      String?
  rank_order            Int?      @default(1)
  created_at            DateTime? @default(now()) @db.Timestamp(6)
  updated_at            DateTime? @default(now()) @db.Timestamp(6)

  @@index([critical_role_id], map: "idx_succession_candidates_role")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model succession_plans {
  id                     String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String                 @db.Uuid
  position_name          String                 @db.VarChar(200)
  position_id            String?                @db.Uuid
  incumbent_employee_id  String?                @db.Uuid
  criticality_level      String?                @default("medium") @db.VarChar(20)
  risk_level             String?                @default("medium") @db.VarChar(20)
  notes                  String?
  target_date            DateTime?              @db.Date
  status                 String?                @default("active") @db.VarChar(50)
  created_by             String?                @db.Uuid
  created_at             DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?              @default(now()) @db.Timestamptz(6)
  embedding              Unsupported("vector")?
  embedding_text_hash    String?                @db.VarChar(64)
  embedding_model        String?                @db.VarChar(100)
  embedding_generated_at DateTime?              @db.Timestamptz(6)
  tenants                tenants                @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([embedding], map: "idx_succession_plans_embedding")
  @@index([tenant_id, status], map: "idx_succession_plans_tenant")
}

model survey_questions {
  id               String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  survey_id        String?            @db.Uuid
  question_text    String
  question_type    String?            @default("rating") @db.VarChar(50)
  options          Json?
  category         String?            @db.VarChar(100)
  display_order    Int?               @default(0)
  is_required      Boolean?           @default(true)
  created_at       DateTime?          @default(now()) @db.Timestamptz(6)
  surveys          surveys?           @relation(fields: [survey_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  survey_responses survey_responses[]

  @@index([survey_id], map: "idx_survey_questions_survey_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model survey_responses {
  id               String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  survey_id        String?           @db.Uuid
  question_id      String?           @db.Uuid
  employee_id      String?           @db.Uuid
  rating_value     Int?
  text_value       String?
  choice_value     String?           @db.VarChar(255)
  created_at       DateTime?         @default(now()) @db.Timestamp(6)
  employees        employees?        @relation(fields: [employee_id], references: [id], onUpdate: NoAction, map: "fk_svr_employee")
  survey_questions survey_questions? @relation(fields: [question_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  surveys          surveys?          @relation(fields: [survey_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([employee_id], map: "idx_survey_responses_employee_id")
  @@index([question_id], map: "idx_survey_responses_question_id")
  @@index([survey_id], map: "idx_survey_responses_survey")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model survey_templates {
  id                     String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String    @db.Uuid
  name                   String    @db.VarChar(255)
  description            String?
  survey_type            String?   @default("engagement") @db.VarChar(50)
  questions              Json
  is_anonymous           Boolean?  @default(true)
  estimated_time_minutes Int?      @default(10)
  is_active              Boolean?  @default(true)
  is_system_template     Boolean?  @default(false)
  created_at             DateTime? @default(now()) @db.Timestamptz(6)
  updated_at             DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at             DateTime? @db.Timestamptz(6)
  tenants                tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_survey_templates_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model surveys {
  id                String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String             @db.Uuid
  title             String             @db.VarChar(255)
  description       String?
  survey_type       String?            @default("engagement") @db.VarChar(50)
  start_date        DateTime?          @db.Date
  end_date          DateTime?          @db.Date
  is_anonymous      Boolean?           @default(true)
  is_active         Boolean?           @default(true)
  created_at        DateTime?          @default(now()) @db.Timestamp(6)
  updated_at        DateTime?          @default(now()) @db.Timestamp(6)
  status            String?            @default("draft") @db.VarChar(50)
  questions         Json?
  total_invitations Int?               @default(0)
  deleted_at        DateTime?          @db.Timestamptz(6)
  survey_questions  survey_questions[]
  survey_responses  survey_responses[]
  tenants           tenants            @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_surveys_tenant")
}

model sync_field_mapping {
  id                 String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  source_table       String    @db.VarChar(100)
  source_field       String    @db.VarChar(100)
  target_table       String    @db.VarChar(100)
  target_field       String    @db.VarChar(100)
  transform_function String?   @db.VarChar(255)
  is_active          Boolean?  @default(true)
  notes              String?
  created_at         DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at         DateTime? @db.Timestamptz(6)

  @@unique([source_table, source_field, target_table, target_field])
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sync_log {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  sync_type         String    @db.VarChar(50)
  tenant_id         String    @db.Uuid
  source_table      String    @db.VarChar(100)
  target_table      String    @db.VarChar(100)
  records_processed Int?      @default(0)
  records_success   Int?      @default(0)
  records_failed    Int?      @default(0)
  status            String?   @default("pending") @db.VarChar(20)
  started_at        DateTime? @default(now()) @db.Timestamp(6)
  completed_at      DateTime? @db.Timestamp(6)
  error_message     String?
  details           Json?
  created_by        String?   @db.VarChar(100)
  created_at        DateTime? @default(now()) @db.Timestamptz(6)
  tenants           tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([started_at(sort: Desc)], map: "idx_sync_log_started")
  @@index([status], map: "idx_sync_log_status")
  @@index([tenant_id], map: "idx_sync_log_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sync_queue {
  id           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  entity_type  String    @db.VarChar(50)
  entity_id    String    @db.Uuid
  tenant_id    String    @db.Uuid
  operation    String    @db.VarChar(20)
  priority     Int?      @default(5)
  status       String?   @default("pending") @db.VarChar(20)
  attempts     Int?      @default(0)
  max_attempts Int?      @default(3)
  last_error   String?
  created_at   DateTime? @default(now()) @db.Timestamp(6)
  processed_at DateTime? @db.Timestamp(6)
  tenants      tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([status, priority], map: "idx_sync_queue_status")
  @@index([tenant_id], map: "idx_sync_queue_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model t001p {
  id         Int       @id @default(autoincrement())
  werks      String    @db.VarChar(4)
  btrtl      String    @db.VarChar(4)
  btext      String    @db.VarChar(40)
  molga      String?   @default("IT") @db.VarChar(2)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@unique([werks, btrtl])
}

model t005 {
  id         Int       @id @default(autoincrement())
  land1      String    @unique(map: "t005_unique") @db.VarChar(3)
  landk      String?   @db.VarChar(3)
  lnplz      String?   @db.VarChar(2)
  pression   String?   @db.VarChar(4)
  xession    String?   @db.VarChar(3)
  spession   String?   @db.VarChar(2)
  created_at DateTime? @default(now()) @db.Timestamp(6)
}

model t005s {
  id         Int       @id @default(autoincrement())
  land1      String    @db.VarChar(3)
  bland      String    @db.VarChar(3)
  fession    String?   @db.VarChar(25)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@unique([land1, bland], map: "t005s_unique")
}

model t005t {
  id         Int       @id @default(autoincrement())
  sprsl      String    @db.VarChar(2)
  land1      String    @db.VarChar(3)
  landx      String?   @db.VarChar(50)
  nession    String?   @db.VarChar(50)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@unique([sprsl, land1], map: "t005t_unique")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model t500c {
  id                 Int                  @id @default(autoincrement())
  bukrs              String               @unique @db.VarChar(4)
  butxt              String               @db.VarChar(40)
  ort01              String?              @db.VarChar(40)
  land1              String?              @db.VarChar(3)
  waession           String?              @default("EUR") @db.VarChar(5)
  created_at         DateTime?            @default(now()) @db.Timestamp(6)
  user_pernr_mapping user_pernr_mapping[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model t500p {
  id         Int       @id @default(autoincrement())
  persa      String    @db.VarChar(4)
  bukrs      String    @db.VarChar(4)
  name1      String    @db.VarChar(40)
  molga      String?   @default("IT") @db.VarChar(2)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@unique([persa, bukrs])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model t503 {
  id         Int       @id @default(autoincrement())
  persg      String    @unique @db.VarChar(1)
  pgtxt      String    @db.VarChar(40)
  created_at DateTime? @default(now()) @db.Timestamp(6)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model t503k {
  id         Int       @id @default(autoincrement())
  persg      String    @db.VarChar(1)
  persk      String    @db.VarChar(2)
  pktxt      String    @db.VarChar(40)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@unique([persg, persk])
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model t510 {
  id         Int       @id @default(autoincrement())
  molga      String?   @default("US") @db.VarChar(2)
  trfar      String    @db.VarChar(2)
  trfgb      String    @db.VarChar(2)
  trfgr      String    @db.VarChar(8)
  trfst      String    @db.VarChar(2)
  endda      DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda      DateTime  @default(dbgenerated("'1900-01-01'::date")) @db.Date
  betrg      Decimal   @db.Decimal(13, 2)
  waession   String?   @default("USD") @db.VarChar(5)
  divgv      Decimal?  @default(1.00) @db.Decimal(5, 2)
  zession    String?   @default("M") @db.VarChar(2)
  created_at DateTime? @default(now()) @db.Timestamp(6)
  updated_at DateTime? @default(now()) @db.Timestamp(6)

  @@unique([molga, trfar, trfgb, trfgr, trfst, begda], map: "t510_unique")
  @@index([trfar, trfgb, trfgr], map: "idx_t510_scale")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model t510a {
  id           Int       @id @default(autoincrement())
  molga        String?   @default("US") @db.VarChar(2)
  trfar        String    @db.VarChar(2)
  endda        DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda        DateTime  @default(dbgenerated("'1900-01-01'::date")) @db.Date
  aession_text String    @db.VarChar(40)
  waession     String?   @default("USD") @db.VarChar(5)
  created_at   DateTime? @default(now()) @db.Timestamp(6)
  updated_at   DateTime? @default(now()) @db.Timestamp(6)

  @@unique([molga, trfar, begda], map: "t510a_unique")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model t510g {
  id            Int       @id @default(autoincrement())
  molga         String?   @default("US") @db.VarChar(2)
  trfar         String    @db.VarChar(2)
  trfgb         String    @db.VarChar(2)
  trfgr         String    @db.VarChar(8)
  endda         DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda         DateTime  @default(dbgenerated("'1900-01-01'::date")) @db.Date
  sprsl         String?   @default("EN") @db.VarChar(2)
  gression_text String    @db.VarChar(40)
  created_at    DateTime? @default(now()) @db.Timestamp(6)
  updated_at    DateTime? @default(now()) @db.Timestamp(6)

  @@unique([molga, trfar, trfgb, trfgr, sprsl, begda], map: "t510g_unique")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model t512w {
  id                 Int       @id @default(autoincrement())
  molga              String?   @default("US") @db.VarChar(2)
  lgart              String    @db.VarChar(4)
  endda              DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda              DateTime  @default(dbgenerated("'1900-01-01'::date")) @db.Date
  lgtxt              String    @db.VarChar(25)
  kurzt              String?   @db.VarChar(12)
  lgession           String?   @db.VarChar(2)
  opession           String?   @default("+") @db.VarChar(2)
  kumession          String?   @db.VarChar(2)
  beession_type      String?   @db.VarChar(2)
  indirect           Boolean?  @default(false)
  esco_wage_category String?   @db.VarChar(100)
  created_at         DateTime? @default(now()) @db.Timestamp(6)
  updated_at         DateTime? @default(now()) @db.Timestamp(6)

  @@unique([molga, lgart, begda], map: "t512w_unique")
  @@index([lgart], map: "idx_t512w_lgart")
  @@index([molga], map: "idx_t512w_molga")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model t513 {
  id                  Int       @id @default(autoincrement())
  stession            String    @unique @db.VarChar(8)
  stltx               String    @db.VarChar(40)
  job_family          String?   @db.VarChar(40)
  esco_occupation_uri String?   @db.VarChar(255)
  created_at          DateTime? @default(now()) @db.Timestamp(6)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model t527x {
  id         Int       @id @default(autoincrement())
  orgeh      String    @unique @db.VarChar(8)
  orgtx      String    @db.VarChar(40)
  ersda      DateTime? @db.Date
  created_at DateTime? @default(now()) @db.Timestamp(6)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model t528b {
  id         Int       @id @default(autoincrement())
  plans      String    @unique @db.VarChar(8)
  plstx      String    @db.VarChar(40)
  orgeh      String?   @db.VarChar(8)
  stell      String?   @db.VarChar(8)
  created_at DateTime? @default(now()) @db.Timestamp(6)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model t529a {
  id         Int       @id @default(autoincrement())
  massn      String    @unique @db.VarChar(2)
  mession    String    @db.VarChar(40)
  created_at DateTime? @default(now()) @db.Timestamp(6)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model t529g {
  id         Int       @id @default(autoincrement())
  massn      String    @db.VarChar(2)
  massg      String    @db.VarChar(2)
  mession    String    @db.VarChar(40)
  created_at DateTime? @default(now()) @db.Timestamp(6)

  @@unique([massn, massg])
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model t549a {
  id           Int       @id @default(autoincrement())
  abkrs        String    @db.VarChar(4)
  endda        DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda        DateTime  @default(dbgenerated("'1900-01-01'::date")) @db.Date
  aession_text String    @db.VarChar(40)
  molga        String?   @default("US") @db.VarChar(2)
  pession      String?   @db.VarChar(2)
  datession    String?   @db.VarChar(2)
  abession     Boolean?  @default(true)
  created_at   DateTime? @default(now()) @db.Timestamp(6)
  updated_at   DateTime? @default(now()) @db.Timestamp(6)

  @@unique([abkrs, begda], map: "t549a_unique")
  @@index([abkrs], map: "idx_t549a_abkrs")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model t549q {
  id            Int       @id @default(autoincrement())
  pession       String    @db.VarChar(2)
  paession      Int
  pession_no    Int
  begda         DateTime  @db.Date
  endda         DateTime  @db.Date
  paession_date DateTime  @db.Date
  bonession     String?   @db.VarChar(2)
  status        String?   @default("0") @db.VarChar(2)
  created_at    DateTime? @default(now()) @db.Timestamp(6)
  updated_at    DateTime? @default(now()) @db.Timestamp(6)

  @@unique([pession, paession, pession_no], map: "t549q_unique")
  @@index([begda, endda], map: "idx_t549q_dates")
  @@index([pession, paession], map: "idx_t549q_period")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model t5ubp {
  id                   Int       @id @default(autoincrement())
  bession              String    @db.VarChar(4)
  endda                DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda                DateTime  @default(dbgenerated("'1900-01-01'::date")) @db.Date
  plan_name            String    @db.VarChar(80)
  plan_type            String    @db.VarChar(4)
  plan_category        String?   @db.VarChar(4)
  provider             String?   @db.VarChar(80)
  eligibility_rule     String?   @db.VarChar(4)
  waiting_period       Int?      @default(0)
  ee_cost_monthly      Decimal?  @db.Decimal(13, 2)
  er_cost_monthly      Decimal?  @db.Decimal(13, 2)
  family_surcharge     Decimal?  @db.Decimal(13, 2)
  coverage_summary     String?
  is_active            Boolean?  @default(true)
  open_enrollment_only Boolean?  @default(true)
  created_at           DateTime? @default(now()) @db.Timestamp(6)
  updated_at           DateTime? @default(now()) @db.Timestamp(6)
  deleted_at           DateTime? @db.Timestamptz(6)

  @@unique([bession, begda], map: "t5ubp_unique")
  @@index([plan_category], map: "idx_t5ubp_category")
  @@index([plan_type], map: "idx_t5ubp_type")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model t771q {
  id                Int       @id @default(autoincrement())
  qualifi           String    @db.VarChar(8)
  endda             DateTime  @default(dbgenerated("'9999-12-31'::date")) @db.Date
  begda             DateTime  @default(dbgenerated("'1900-01-01'::date")) @db.Date
  qualifitext       String    @db.VarChar(80)
  quession_group    String?   @db.VarChar(8)
  scale_id          String?   @db.VarChar(4)
  max_level         Int?      @default(5)
  esco_skill_uri    String?   @db.VarChar(255)
  esco_concept_type String?   @db.VarChar(50)
  is_active         Boolean?  @default(true)
  created_at        DateTime? @default(now()) @db.Timestamp(6)
  deleted_at        DateTime? @db.Timestamptz(6)

  @@unique([qualifi, begda], map: "t771q_unique")
  @@index([esco_skill_uri], map: "idx_t771q_esco")
  @@index([qualifi], map: "idx_t771q_qualifi")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model table_usage_rules {
  id             Int       @id @default(autoincrement())
  table_name     String    @db.VarChar(100)
  schema_name    String?   @default("public") @db.VarChar(50)
  table_category String    @db.VarChar(30)
  usage_allowed  String    @db.VarChar(20)
  description    String?
  example_use    String?
  forbidden_use  String?
  created_at     DateTime? @default(now()) @db.Timestamp(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model talent_pool_members {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id      String       @db.Uuid
  talent_pool_id String       @db.Uuid
  employee_id    String       @db.Uuid
  added_reason   String?
  added_by       String?      @db.Uuid
  added_at       DateTime?    @default(now()) @db.Timestamptz(6)
  removed_at     DateTime?    @db.Timestamptz(6)
  removed_reason String?
  created_at     DateTime?    @default(now()) @db.Timestamptz(6)
  updated_at     DateTime?    @default(now()) @db.Timestamptz(6)
  employees      employees    @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_tpm_employee")
  talent_pools   talent_pools @relation(fields: [talent_pool_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants        tenants      @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([talent_pool_id, employee_id], map: "uk_talent_pool_member")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model talent_pools {
  id                  String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String                 @db.Uuid
  name                String                 @db.VarChar(200)
  description         String?
  pool_type           String                 @db.VarChar(50)
  criteria            Json?
  is_active           Boolean?               @default(true)
  created_by          String?                @db.Uuid
  created_at          DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at          DateTime?              @default(now()) @db.Timestamptz(6)
  embedding           Unsupported("vector")?
  embedding_model     String?                @db.VarChar(100)
  deleted_at          DateTime?              @db.Timestamptz(6)
  talent_pool_members talent_pool_members[]
  tenants             tenants                @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, name], map: "uk_talent_pool_name")
  @@index([embedding], map: "idx_talent_pools_embedding")
  @@index([tenant_id, is_active], map: "idx_talent_pools_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model tenant_custom_skills {
  id                         String                    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                  String                    @db.Uuid
  code                       String                    @db.VarChar(100)
  name_en                    String                    @db.VarChar(255)
  name_it                    String?                   @db.VarChar(255)
  skill_type                 String                    @db.VarChar(50)
  description_en             String?
  description_it             String?
  base_esco_skill_id         String?                   @db.Uuid
  category_id                String?                   @db.Uuid
  embedding_en               Unsupported("vector")?
  embedding_it               Unsupported("vector")?
  embedding_model            String?                   @db.VarChar(100)
  embedding_generated_at     DateTime?                 @db.Timestamptz(6)
  version                    Int?                      @default(1)
  is_active                  Boolean?                  @default(true)
  superseded_by              String?                   @db.Uuid
  created_at                 DateTime?                 @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime?                 @default(now()) @db.Timestamptz(6)
  created_by                 String?                   @db.Uuid
  updated_by                 String?                   @db.Uuid
  deleted_at                 DateTime?                 @db.Timestamptz(6)
  job_template_skills        job_template_skills[]
  esco_skills                esco_skills?              @relation(fields: [base_esco_skill_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  ontology_categories        ontology_categories?      @relation(fields: [category_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenant_custom_skills       tenant_custom_skills?     @relation("tenant_custom_skillsTotenant_custom_skills", fields: [superseded_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  other_tenant_custom_skills tenant_custom_skills[]    @relation("tenant_custom_skillsTotenant_custom_skills")
  tenants                    tenants                   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenant_skill_dimensions    tenant_skill_dimensions[]

  @@unique([tenant_id, code], map: "tenant_skills_code_unique")
  @@index([superseded_by], map: "idx_tenant_custom_skills_superseded_by")
  @@index([base_esco_skill_id], map: "idx_tenant_skills_base")
  @@index([category_id], map: "idx_tenant_skills_category")
  @@index([tenant_id], map: "idx_tenant_skills_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model tenant_industry_classifications {
  id                       String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                String                   @db.Uuid
  classification_code      String                   @db.VarChar(10)
  classification_role      String                   @default("PRIMARY") @db.VarChar(20)
  is_active                Boolean                  @default(true)
  created_at               DateTime                 @default(now()) @db.Timestamptz(6)
  industry_classifications industry_classifications @relation(fields: [classification_code], references: [code], onDelete: NoAction, onUpdate: NoAction)
  tenants                  tenants                  @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([classification_code], map: "idx_tic_classification")
  @@index([tenant_id], map: "idx_tic_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tenant_job_kpis {
  id                   String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_job_id        String                 @db.Uuid
  source_kpi_id        String?                @db.Uuid
  kpi_code             String                 @db.VarChar(20)
  name_it              String                 @db.VarChar(255)
  name_en              String?                @db.VarChar(255)
  description          String?
  measurement_type     String?                @db.VarChar(20)
  unit                 String?                @db.VarChar(20)
  target_value         Decimal?               @db.Decimal(12, 2)
  min_acceptable       Decimal?               @db.Decimal(12, 2)
  stretch_target       Decimal?               @db.Decimal(12, 2)
  kpi_category         String?                @db.VarChar(50)
  frequency            String?                @db.VarChar(20)
  weight               Decimal?               @db.Decimal(5, 2)
  is_custom            Boolean?               @default(false)
  created_at           DateTime?              @default(now()) @db.Timestamptz(6)
  employee_kpi_targets employee_kpi_targets[]
  tenant_jobs          tenant_jobs            @relation(fields: [tenant_job_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_job_id, kpi_code], map: "uq_tenant_kpi_per_job")
  @@index([kpi_category], map: "idx_tenant_kpis_category")
  @@index([tenant_job_id], map: "idx_tenant_kpis_job")
  @@index([source_kpi_id], map: "idx_tenant_kpis_source")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tenant_job_skills {
  id                         String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_job_id              String                       @db.Uuid
  source_skill_id            String?                      @db.Uuid
  skill_name_it              String                       @db.VarChar(255)
  skill_name_en              String?                      @db.VarChar(255)
  esco_skill_uri             String?                      @db.VarChar(255)
  esco_skill_code            String?                      @db.VarChar(50)
  required_level             Int?
  is_required                Boolean?                     @default(true)
  importance                 String?                      @db.VarChar(20)
  skill_category             String?                      @db.VarChar(50)
  is_custom                  Boolean?                     @default(false)
  created_at                 DateTime?                    @default(now()) @db.Timestamptz(6)
  employee_skill_assessments employee_skill_assessments[]
  tenant_jobs                tenant_jobs                  @relation(fields: [tenant_job_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([esco_skill_uri], map: "idx_tenant_skills_esco")
  @@index([tenant_job_id], map: "idx_tenant_skills_job")
  @@index([source_skill_id], map: "idx_tenant_skills_source")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tenant_job_tasks {
  id                      String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_job_id           String      @db.Uuid
  source_task_id          String?     @db.Uuid
  task_order              Int
  description_it          String
  description_en          String?
  frequency               String?     @db.VarChar(20)
  importance              String?     @db.VarChar(20)
  time_allocation_percent Int?
  is_custom               Boolean?    @default(false)
  created_at              DateTime?   @default(now()) @db.Timestamptz(6)
  tenant_jobs             tenant_jobs @relation(fields: [tenant_job_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([tenant_job_id], map: "idx_tenant_tasks_job")
  @@index([source_task_id], map: "idx_tenant_tasks_source")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model tenant_jobs {
  id                       String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                String                     @db.Uuid
  source_template_id       String?                    @db.Uuid
  tenant_org_unit_id       String?                    @db.Uuid
  job_code                 String                     @db.VarChar(20)
  title_it                 String                     @db.VarChar(255)
  title_en                 String?                    @db.VarChar(255)
  description              String?
  summary                  String?
  org_level                Int?
  is_management            Boolean?                   @default(false)
  employment_type          String?                    @default("full_time") @db.VarChar(20)
  esco_occupation_uri      String?                    @db.VarChar(255)
  esco_occupation_code     String?                    @db.VarChar(20)
  salary_band_code         String?                    @db.VarChar(20)
  salary_min               Decimal?                   @db.Decimal(12, 2)
  salary_max               Decimal?                   @db.Decimal(12, 2)
  currency                 String?                    @default("EUR") @db.VarChar(3)
  budgeted_positions       Int?                       @default(1)
  filled_positions         Int?                       @default(0)
  custom_fields            Json?
  is_active                Boolean?                   @default(true)
  created_at               DateTime?                  @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?                  @default(now()) @db.Timestamptz(6)
  deleted_at               DateTime?                  @db.Timestamptz(6)
  career_path_levels       career_path_levels[]
  career_simulations       career_simulations[]
  employee_job_assignments employee_job_assignments[]
  tenant_job_kpis          tenant_job_kpis[]
  tenant_job_skills        tenant_job_skills[]
  tenant_job_tasks         tenant_job_tasks[]
  tenants                  tenants                    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenant_org_units         tenant_org_units?          @relation(fields: [tenant_org_unit_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([tenant_id, job_code], map: "uq_job_code_per_tenant")
  @@index([esco_occupation_code], map: "idx_tenant_jobs_esco")
  @@index([org_level], map: "idx_tenant_jobs_level")
  @@index([source_template_id], map: "idx_tenant_jobs_source")
  @@index([tenant_id], map: "idx_tenant_jobs_tenant")
  @@index([tenant_org_unit_id], map: "idx_tenant_jobs_unit")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model tenant_onboarding_profiles {
  id                           String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                    String    @db.Uuid
  nace_occupations_snapshot    Json?
  size_modifiers               Json?
  prototype_config             Json?
  departments_generated        Int?      @default(0)
  positions_generated          Int?      @default(0)
  skill_requirements_generated Int?      @default(0)
  generated_at                 DateTime? @default(now()) @db.Timestamptz(6)
  generator_version            String?   @default("1.0") @db.VarChar(20)
  tenants                      tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([tenant_id], map: "idx_tenant_onboarding_profiles_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model tenant_org_charts {
  id                 String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String             @db.Uuid
  source_template_id String?            @db.Uuid
  name               String             @db.VarChar(255)
  description        String?
  version            String?            @default("1.0.0") @db.VarChar(20)
  status             String?            @default("draft") @db.VarChar(20)
  effective_date     DateTime?          @db.Date
  is_active          Boolean?           @default(true)
  created_at         DateTime?          @default(now()) @db.Timestamptz(6)
  updated_at         DateTime?          @default(now()) @db.Timestamptz(6)
  deleted_at         DateTime?          @db.Timestamptz(6)
  tenants            tenants            @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenant_org_units   tenant_org_units[]

  @@index([source_template_id], map: "idx_tenant_charts_source")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tenant_org_units {
  id                     String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  chart_id               String                @db.Uuid
  source_unit_id         String?               @db.Uuid
  parent_id              String?               @db.Uuid
  path                   Unsupported("ltree")?
  depth                  Int                   @default(0)
  sort_order             Int?                  @default(0)
  code                   String                @db.VarChar(20)
  cost_center            String?               @db.VarChar(10)
  name_it                String                @db.VarChar(255)
  name_en                String?               @db.VarChar(255)
  short_name             String?               @db.VarChar(50)
  area_code              String?               @db.VarChar(10)
  level                  Int?
  is_line                Boolean?              @default(true)
  is_management          Boolean?              @default(false)
  headcount_budget       Int?
  headcount_actual       Int?                  @default(0)
  custom_fields          Json?
  manager_employee_id    String?               @db.Uuid
  is_active              Boolean?              @default(true)
  created_at             DateTime?             @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?             @default(now()) @db.Timestamptz(6)
  deleted_at             DateTime?             @db.Timestamptz(6)
  tenant_jobs            tenant_jobs[]
  tenant_org_charts      tenant_org_charts     @relation(fields: [chart_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenant_org_units       tenant_org_units?     @relation("tenant_org_unitsTotenant_org_units", fields: [parent_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  other_tenant_org_units tenant_org_units[]    @relation("tenant_org_unitsTotenant_org_units")

  @@unique([chart_id, code], map: "uq_unit_code_per_chart")
  @@index([area_code], map: "idx_tenant_units_area")
  @@index([chart_id], map: "idx_tenant_units_chart")
  @@index([level], map: "idx_tenant_units_level")
  @@index([manager_employee_id], map: "idx_tenant_units_manager")
  @@index([parent_id], map: "idx_tenant_units_parent")
  @@index([path], map: "idx_tenant_units_path", type: Gist)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model tenant_retirement_rules {
  id                                  String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                           String    @db.Uuid
  country_code                        String    @default("IT") @db.VarChar(3)
  old_age_retirement_years            Int       @default(67)
  old_age_min_contribution_years      Int       @default(20)
  early_retirement_contribution_years Decimal   @default(42.83) @db.Decimal(5, 2)
  effective_from                      DateTime  @default(dbgenerated("CURRENT_DATE")) @db.Date
  notes                               String?
  created_at                          DateTime? @default(now()) @db.Timestamptz(6)
  updated_at                          DateTime? @default(now()) @db.Timestamptz(6)
  tenants                             tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, country_code, effective_from])
  @@index([tenant_id, effective_from(sort: Desc)], map: "idx_retirement_rules_tenant")
}

model tenant_sap_mapping {
  id          Int       @id @default(autoincrement())
  tenant_uuid String    @unique @db.Uuid
  tenant_name String    @db.VarChar(50)
  sap_bukrs   String    @unique @db.VarChar(4)
  emp_prefix  String    @db.VarChar(20)
  is_active   Boolean?  @default(true)
  created_at  DateTime? @default(now()) @db.Timestamp(6)
  deleted_at  DateTime? @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model tenant_skill_dimensions {
  id                   String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_skill_id      String                 @db.Uuid
  dimension_type       String                 @db.VarChar(20)
  description_en       String?
  description_it       String?
  level_scale          String?                @default("basic_to_expert") @db.VarChar(50)
  min_level            Int?                   @default(1)
  max_level            Int?                   @default(5)
  embedding            Unsupported("vector")?
  is_primary           Boolean?               @default(false)
  created_at           DateTime?              @default(now()) @db.Timestamptz(6)
  tenant_custom_skills tenant_custom_skills   @relation(fields: [tenant_skill_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_skill_id, dimension_type], map: "tenant_dimensions_unique")
  @@index([tenant_skill_id], map: "idx_tenant_dimensions_skill")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model tenants_books {
  id                      String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id               String     @unique(map: "uq_tenant_book") @db.Uuid
  company_legal_name      String     @db.VarChar(255)
  incorporation_date      DateTime?  @db.Date
  legal_address           String?
  corporate_purpose       String?
  business_description    String?    @db.VarChar(2000)
  nace_section_code       String?    @db.VarChar(10)
  nace_division_code      String?    @db.VarChar(10)
  nace_group_code         String?    @db.VarChar(10)
  size_code               String?    @db.VarChar(10)
  branches_info           Json?      @default("[]")
  org_structure_prototype Json?
  org_structure_actual    Json?
  historical_profile      String?
  version                 Int?       @default(1)
  last_updated_at         DateTime?  @default(now()) @db.Timestamptz(6)
  approved                Boolean?   @default(false)
  approved_by             String?    @db.Uuid
  approved_at             DateTime?  @db.Timestamptz(6)
  created_at              DateTime?  @default(now()) @db.Timestamptz(6)
  updated_at              DateTime?  @default(now()) @db.Timestamptz(6)
  approved_by_employee_id String?    @db.Uuid
  employees               employees? @relation(fields: [approved_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "tenants_books_approved_by_fkey_emp")
  tenants                 tenants    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([approved_by_employee_id], map: "idx_tenants_books_approved_by_employee_id")
  @@index([nace_section_code, nace_division_code], map: "idx_tenants_books_nace")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model turnover_risk_scores {
  id                  String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String     @db.Uuid
  employee_id         String?    @db.Uuid
  risk_score          Decimal    @db.Decimal(5, 4)
  risk_level          String     @db.VarChar(20)
  risk_factors        Json?      @default("[]")
  recommended_actions Json?      @default("[]")
  model_version       Int?
  valid_from          DateTime?  @default(now()) @db.Timestamptz(6)
  valid_until         DateTime?  @db.Timestamptz(6)
  is_current          Boolean?   @default(true)
  created_at          DateTime?  @default(now()) @db.Timestamptz(6)
  employees           employees? @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants             tenants    @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([employee_id], map: "idx_turnover_risk_employee")
  @@index([tenant_id], map: "idx_turnover_risk_tenant")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model unknown_skills {
  id                                                        String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                                 String       @db.Uuid
  raw_text                                                  String
  occurrence_count                                          Int?         @default(1)
  first_seen_at                                             DateTime?    @default(now()) @db.Timestamptz(6)
  last_seen_at                                              DateTime?    @default(now()) @db.Timestamptz(6)
  suggested_esco_id                                         String?      @db.Uuid
  suggested_confidence                                      Decimal?     @db.Decimal(3, 2)
  review_status                                             String?      @default("pending") @db.VarChar(50)
  reviewed_by                                               String?      @db.Uuid
  reviewed_at                                               DateTime?    @db.Timestamptz(6)
  mapped_to_esco_id                                         String?      @db.Uuid
  reviewed_by_employee_id                                   String?      @db.Uuid
  created_at                                                DateTime?    @default(now()) @db.Timestamptz(6)
  esco_skills_unknown_skills_mapped_to_esco_idToesco_skills esco_skills? @relation("unknown_skills_mapped_to_esco_idToesco_skills", fields: [mapped_to_esco_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employees                                                 employees?   @relation(fields: [reviewed_by_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  esco_skills_unknown_skills_suggested_esco_idToesco_skills esco_skills? @relation("unknown_skills_suggested_esco_idToesco_skills", fields: [suggested_esco_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                                                   tenants      @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, raw_text])
  @@index([mapped_to_esco_id], map: "idx_unknown_skills_mapped_to_esco_id")
  @@index([reviewed_by_employee_id], map: "idx_unknown_skills_reviewed_by_employee_id")
  @@index([review_status], map: "idx_unknown_skills_status")
  @@index([suggested_esco_id], map: "idx_unknown_skills_suggested_esco_id")
  @@index([tenant_id], map: "idx_unknown_skills_tenant")
}

model user_pernr_mapping {
  id           Int       @id @default(autoincrement())
  user_uuid    String    @unique @db.Uuid
  pernr        String    @unique @db.VarChar(8)
  bukrs        String    @db.VarChar(4)
  last_sync_at DateTime? @default(now()) @db.Timestamp(6)
  sync_status  String?   @default("synced") @db.VarChar(20)
  created_at   DateTime? @default(now()) @db.Timestamp(6)
  t500c        t500c     @relation(fields: [bukrs], references: [bukrs], onDelete: NoAction, onUpdate: NoAction, map: "fk_bukrs")

  @@index([bukrs], map: "idx_user_pernr_mapping_bukrs")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_workspaces {
  id                       Int                 @id @default(autoincrement())
  user_id                  String              @db.Uuid
  tenant_id                String              @db.Uuid
  name                     String              @default("La mia Scrivania") @db.VarChar(150)
  description              String?
  layout_config            Json?               @default("{\"gap\": 16, \"columns\": 12, \"row_height\": 80}")
  is_default               Boolean?            @default(false)
  is_active                Boolean?            @default(true)
  created_from_template_id Int?
  sort_order               Int?                @default(0)
  created_at               DateTime?           @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?           @default(now()) @db.Timestamptz(6)
  dashboard_id             Int?
  rbp_dashboards           rbp_dashboards?     @relation(fields: [dashboard_id], references: [id], onUpdate: NoAction)
  tenants                  tenants             @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users                    users               @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  workspace_widgets        workspace_widgets[]

  @@unique([user_id, tenant_id, dashboard_id], map: "user_workspaces_user_tenant_dashboard_uniq")
  @@index([tenant_id], map: "idx_user_workspaces_tenant")
  @@index([tenant_id], map: "idx_user_workspaces_tenant_id")
  @@index([user_id], map: "idx_user_workspaces_user")
  @@index([user_id], map: "idx_user_workspaces_user_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model webhook_deliveries {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  webhook_id       String    @db.Uuid
  tenant_id        String    @db.Uuid
  event_type       String    @db.VarChar(100)
  payload          Json?
  status           String?   @default("pending") @db.VarChar(50)
  response_code    Int?
  response_time_ms Int?
  error_message    String?
  retry_count      Int?      @default(0)
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  tenants          tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  webhooks         webhooks  @relation(fields: [webhook_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([webhook_id, status], map: "idx_webhook_deliveries_status")
  @@index([webhook_id], map: "idx_webhook_deliveries_webhook")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model webhooks {
  id                 String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String               @db.Uuid
  name               String               @db.VarChar(255)
  url                String
  event_types        String[]
  secret_key         String
  secret_key_hint    String?              @db.VarChar(20)
  headers            Json?
  retry_count        Int?                 @default(3)
  timeout_ms         Int?                 @default(30000)
  is_active          Boolean?             @default(true)
  last_triggered_at  DateTime?            @db.Timestamptz(6)
  last_status        String?              @db.VarChar(50)
  created_at         DateTime?            @default(now()) @db.Timestamptz(6)
  updated_at         DateTime?            @default(now()) @db.Timestamptz(6)
  deleted_at         DateTime?            @db.Timestamptz(6)
  webhook_deliveries webhook_deliveries[]
  tenants            tenants              @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([tenant_id, is_active], map: "idx_webhooks_active")
  @@index([tenant_id], map: "idx_webhooks_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model wellbeing_checkins {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String    @db.Uuid
  employee_id       String    @db.Uuid
  checkin_date      DateTime  @default(dbgenerated("CURRENT_DATE")) @db.Date
  mood_score        Int?
  energy_level      Int?
  stress_level      Int?
  work_life_balance Int?
  sleep_quality     Int?
  notes             String?
  is_anonymous      Boolean?  @default(false)
  created_at        DateTime? @default(now()) @db.Timestamptz(6)
  employees         employees @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_wc_employee")
  tenants           tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([employee_id, checkin_date], map: "unique_daily_checkin")
  @@index([employee_id], map: "idx_wellbeing_checkins_employee")
  @@index([tenant_id, checkin_date], map: "idx_wellbeing_checkins_tenant_date")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model wellbeing_goals {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id     String    @db.Uuid
  employee_id   String    @db.Uuid
  goal_type     String    @db.VarChar(50)
  title         String    @db.VarChar(200)
  description   String?
  target_value  Int?
  current_value Int?      @default(0)
  unit          String?   @db.VarChar(50)
  start_date    DateTime  @db.Date
  target_date   DateTime  @db.Date
  status        String?   @default("active") @db.VarChar(20)
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  updated_at    DateTime? @default(now()) @db.Timestamptz(6)
  employees     employees @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_wg_employee")
  tenants       tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([employee_id], map: "idx_wellbeing_goals_employee")
  @@index([tenant_id], map: "idx_wellbeing_goals_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model wellbeing_program_enrollments {
  id                  String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String              @db.Uuid
  employee_id         String              @db.Uuid
  resource_id         String              @db.Uuid
  enrolled_at         DateTime?           @default(now()) @db.Timestamptz(6)
  completed_at        DateTime?           @db.Timestamptz(6)
  status              String?             @default("enrolled") @db.VarChar(20)
  rating              Int?
  feedback            String?
  created_at          DateTime?           @default(now()) @db.Timestamptz(6)
  updated_at          DateTime?           @default(now()) @db.Timestamptz(6)
  employees           employees           @relation(fields: [employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_wpe_employee")
  wellbeing_resources wellbeing_resources @relation(fields: [resource_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants             tenants             @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([employee_id], map: "idx_wellbeing_program_enrollments_employee_id")
  @@index([resource_id], map: "idx_wellbeing_program_enrollments_resource_id")
  @@index([tenant_id], map: "idx_wellbeing_program_enrollments_tenant_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model wellbeing_resources {
  id                            String                          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                     String                          @db.Uuid
  title                         String                          @db.VarChar(200)
  description                   String?
  category                      String                          @db.VarChar(50)
  resource_type                 String                          @db.VarChar(50)
  url                           String?
  provider                      String?                         @db.VarChar(100)
  is_active                     Boolean?                        @default(true)
  created_at                    DateTime?                       @default(now()) @db.Timestamptz(6)
  deleted_at                    DateTime?                       @db.Timestamptz(6)
  wellbeing_program_enrollments wellbeing_program_enrollments[]
  tenants                       tenants                         @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([category], map: "idx_wellbeing_resources_category")
  @@index([tenant_id], map: "idx_wellbeing_resources_tenant_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model whistleblowing_attachments {
  id                      String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  report_id               String?                  @db.Uuid
  message_id              String?                  @db.Uuid
  file_name               String                   @db.VarChar(500)
  file_path               String                   @db.VarChar(1000)
  file_size               Int?
  mime_type               String?                  @db.VarChar(100)
  file_hash               String?                  @db.VarChar(128)
  uploaded_by_type        String?                  @db.VarChar(20)
  uploaded_by_id          String?                  @db.Uuid
  encrypted               Boolean?                 @default(true)
  scanned_for_malware     Boolean?                 @default(false)
  scan_result             String?                  @db.VarChar(50)
  created_at              DateTime?                @default(now()) @db.Timestamp(6)
  whistleblowing_messages whistleblowing_messages? @relation(fields: [message_id], references: [id], onUpdate: NoAction)
  whistleblowing_reports  whistleblowing_reports?  @relation(fields: [report_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([message_id], map: "idx_whistleblowing_attachments_message_id")
  @@index([report_id], map: "idx_whistleblowing_attachments_report_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model whistleblowing_audit_log {
  id                     String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  report_id              String?                 @db.Uuid
  action                 String                  @db.VarChar(50)
  old_value              String?
  new_value              String?
  user_id                String?                 @db.Uuid
  user_role              String?                 @db.VarChar(50)
  ip_address_hash        String?                 @db.VarChar(64)
  created_at             DateTime?               @default(now()) @db.Timestamp(6)
  user_id_employee_id    String?                 @db.Uuid
  users                  users?                  @relation(fields: [user_id], references: [id], onUpdate: NoAction, map: "fk_wal_user")
  whistleblowing_reports whistleblowing_reports? @relation(fields: [report_id], references: [id], onUpdate: NoAction)
  employees              employees?              @relation(fields: [user_id_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "whistleblowing_audit_log_user_id_fkey_emp")

  @@index([report_id], map: "idx_wb_audit_report")
  @@index([user_id_employee_id], map: "idx_whistleblowing_audit_log_user_id_employee_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model whistleblowing_handlers {
  id                               String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                        String     @db.Uuid
  user_id                          String?    @db.Uuid
  role                             String?    @default("handler") @db.VarChar(50)
  categories                       String[]
  is_active                        Boolean?   @default(true)
  conflict_of_interest_declaration Boolean?   @default(false)
  training_completed               Boolean?   @default(false)
  training_date                    DateTime?  @db.Date
  created_at                       DateTime?  @default(now()) @db.Timestamp(6)
  updated_at                       DateTime?  @default(now()) @db.Timestamp(6)
  user_id_employee_id              String?    @db.Uuid
  deleted_at                       DateTime?  @db.Timestamptz(6)
  users                            users?     @relation(fields: [user_id], references: [id], onUpdate: NoAction, map: "fk_wh_user")
  tenants                          tenants    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  employees                        employees? @relation(fields: [user_id_employee_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "whistleblowing_handlers_user_id_fkey_emp")

  @@index([tenant_id], map: "idx_wb_handlers_tenant")
  @@index([user_id_employee_id], map: "idx_whistleblowing_handlers_user_id_employee_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model whistleblowing_messages {
  id                         String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  report_id                  String?                      @db.Uuid
  sender_type                String                       @db.VarChar(20)
  sender_id                  String?                      @db.Uuid
  sender_name                String?                      @db.VarChar(200)
  message_type               String?                      @default("message") @db.VarChar(20)
  content                    String
  has_attachments            Boolean?                     @default(false)
  read_by_reporter           Boolean?                     @default(false)
  read_by_handler            Boolean?                     @default(false)
  created_at                 DateTime?                    @default(now()) @db.Timestamp(6)
  whistleblowing_attachments whistleblowing_attachments[]
  whistleblowing_reports     whistleblowing_reports?      @relation(fields: [report_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([report_id], map: "idx_wb_messages_report")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model whistleblowing_reports {
  id                         String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                  String                       @db.Uuid
  case_number                String                       @db.VarChar(50)
  anonymous_token            String?                      @unique @db.VarChar(255)
  reporter_type              String?                      @default("anonymous") @db.VarChar(20)
  reporter_name              String?                      @db.VarChar(200)
  reporter_email             String?                      @db.VarChar(255)
  reporter_phone             String?                      @db.VarChar(50)
  reporter_employee_id       String?                      @db.Uuid
  reporter_role              String?                      @db.VarChar(100)
  category                   String                       @db.VarChar(50)
  severity                   String?                      @default("medium") @db.VarChar(20)
  title                      String                       @db.VarChar(300)
  description                String
  incident_date              DateTime?                    @db.Date
  incident_location          String?                      @db.VarChar(300)
  involved_persons           String?
  witnesses                  String?
  status                     String?                      @default("submitted") @db.VarChar(30)
  priority                   String?                      @default("normal") @db.VarChar(20)
  assigned_to                String?                      @db.Uuid
  assigned_team              String?                      @db.VarChar(100)
  acknowledgement_sent       Boolean?                     @default(false)
  acknowledgement_date       DateTime?                    @db.Timestamp(6)
  feedback_provided          Boolean?                     @default(false)
  feedback_date              DateTime?                    @db.Timestamp(6)
  resolution_type            String?                      @db.VarChar(50)
  resolution_summary         String?
  consent_data_processing    Boolean?                     @default(true)
  retention_end_date         DateTime?                    @db.Date
  anonymized                 Boolean?                     @default(false)
  source                     String?                      @default("web_form") @db.VarChar(50)
  ip_hash                    String?                      @db.VarChar(64)
  user_agent_hash            String?                      @db.VarChar(64)
  created_at                 DateTime?                    @default(now()) @db.Timestamp(6)
  updated_at                 DateTime?                    @default(now()) @db.Timestamp(6)
  assigned_to_employee_id    String?                      @db.Uuid
  whistleblowing_attachments whistleblowing_attachments[]
  whistleblowing_audit_log   whistleblowing_audit_log[]
  whistleblowing_messages    whistleblowing_messages[]
  employees                  employees?                   @relation(fields: [assigned_to_employee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "whistleblowing_reports_assigned_to_fkey_emp")
  tenants                    tenants                      @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([assigned_to], map: "idx_wb_reports_assigned")
  @@index([case_number], map: "idx_wb_reports_case")
  @@index([status], map: "idx_wb_reports_status")
  @@index([tenant_id], map: "idx_wb_reports_tenant")
  @@index([assigned_to_employee_id], map: "idx_whistleblowing_reports_assigned_to_employee_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model whistleblowing_settings {
  id                            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                     String    @unique @db.Uuid
  enabled                       Boolean?  @default(true)
  allow_anonymous               Boolean?  @default(true)
  require_consent               Boolean?  @default(true)
  acknowledgement_deadline_days Int?      @default(7)
  feedback_deadline_days        Int?      @default(90)
  retention_period_months       Int?      @default(24)
  auto_anonymize                Boolean?  @default(true)
  notify_handlers_immediately   Boolean?  @default(true)
  escalation_after_days         Int?      @default(14)
  intro_text                    String?
  categories_enabled            String[]
  custom_fields                 String?
  created_at                    DateTime? @default(now()) @db.Timestamp(6)
  updated_at                    DateTime? @default(now()) @db.Timestamp(6)
  tenants                       tenants   @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model widget_catalog {
  id                   Int                   @id @default(autoincrement())
  code                 String                @unique @db.VarChar(60)
  name                 String                @db.VarChar(150)
  description          String?
  widget_type          String                @db.VarChar(30)
  data_source_type     String                @db.VarChar(20)
  data_source_config   Json?                 @default("{}")
  default_size         Json?                 @default("{\"h\": 3, \"w\": 4}")
  min_size             Json?                 @default("{\"h\": 2, \"w\": 2}")
  max_size             Json?                 @default("{\"h\": 8, \"w\": 12}")
  icon                 String?               @db.VarChar(50)
  functional_area_code String?               @db.VarChar(60)
  perspective_code     String?               @db.VarChar(20)
  is_active            Boolean?              @default(true)
  requires_min_role    Int?                  @default(8)
  metadata             Json?                 @default("{}")
  created_at           DateTime?             @default(now()) @db.Timestamptz(6)
  updated_at           DateTime?             @default(now()) @db.Timestamptz(6)
  frontend_module      String?
  cache_ttl_seconds    Int                   @default(0)
  swr_seconds          Int                   @default(0)
  name_it              String?               @db.VarChar(150)
  name_en              String?               @db.VarChar(150)
  description_it       String?
  description_en       String?
  rbp_functional_areas rbp_functional_areas? @relation(fields: [functional_area_code], references: [code], onDelete: NoAction, onUpdate: NoAction)
  rbp_perspectives     rbp_perspectives?     @relation(fields: [perspective_code], references: [code], onDelete: NoAction, onUpdate: NoAction)
  workspace_widgets    workspace_widgets[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model widget_templates {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id      String    @db.Uuid
  code           String    @db.VarChar(100)
  name           String    @db.VarChar(200)
  description    String?
  widget_type    String    @db.VarChar(50)
  data_source    String    @db.VarChar(100)
  query_config   Json      @default("{}")
  display_config Json      @default("{}")
  default_width  Int?      @default(4)
  default_height Int?      @default(3)
  category       String?   @db.VarChar(50)
  is_system      Boolean?  @default(false)
  is_active      Boolean?  @default(true)
  created_at     DateTime? @default(now()) @db.Timestamptz(6)
  deleted_at     DateTime? @db.Timestamptz(6)
  tenants        tenants   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([category], map: "idx_widget_templates_category")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model workforce_plan_actions {
  id                       String                    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                String                    @db.Uuid
  workforce_plan_id        String                    @db.Uuid
  scenario_id              String?                   @db.Uuid
  action_type              String                    @db.VarChar(50)
  priority                 String?                   @default("medium") @db.VarChar(20)
  title                    String                    @db.VarChar(255)
  description              String?
  target_role              String?                   @db.VarChar(200)
  headcount                Int?                      @default(1)
  target_date              DateTime?                 @db.Date
  status                   String?                   @default("pending") @db.VarChar(20)
  estimated_cost           Decimal?                  @db.Decimal(12, 2)
  notes                    String?
  created_at               DateTime?                 @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?                 @default(now()) @db.Timestamptz(6)
  target_org_unit_id       String?                   @db.Uuid
  workforce_plan_scenarios workforce_plan_scenarios? @relation(fields: [scenario_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  org_units                org_units?                @relation(fields: [target_org_unit_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                  tenants                   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  workforce_plans          workforce_plans           @relation(fields: [workforce_plan_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([workforce_plan_id], map: "idx_workforce_plan_actions_plan")
  @@index([scenario_id], map: "idx_workforce_plan_actions_scenario_id")
  @@index([tenant_id], map: "idx_workforce_plan_actions_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model workforce_plan_scenarios {
  id                     String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String                   @db.Uuid
  workforce_plan_id      String                   @db.Uuid
  name                   String                   @db.VarChar(255)
  description            String?
  scenario_type          String?                  @default("base") @db.VarChar(50)
  assumptions            Json?                    @default("{}")
  target_date            DateTime?                @db.Date
  status                 String?                  @default("draft") @db.VarChar(20)
  created_by             String?                  @db.Uuid
  created_at             DateTime?                @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?                @default(now()) @db.Timestamptz(6)
  workforce_plan_actions workforce_plan_actions[]
  employees              employees?               @relation(fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                tenants                  @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  workforce_plans        workforce_plans          @relation(fields: [workforce_plan_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([created_by], map: "idx_workforce_plan_scenarios_created_by")
  @@index([workforce_plan_id], map: "idx_workforce_plan_scenarios_plan")
  @@index([tenant_id], map: "idx_workforce_plan_scenarios_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model workforce_plans {
  id                                      String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                               String                     @db.Uuid
  name                                    String                     @db.VarChar(255)
  description                             String?
  target_date                             DateTime                   @db.Date
  status                                  String                     @default("draft") @db.VarChar(20)
  requirements                            Json                       @default("[]")
  gap_analysis                            Json                       @default("[]")
  hiring_recommendations                  Json                       @default("[]")
  training_investments                    Json                       @default("[]")
  summary                                 Json                       @default("{}")
  created_by                              String?                    @db.Uuid
  updated_by                              String?                    @db.Uuid
  created_at                              DateTime                   @default(now()) @db.Timestamptz(6)
  updated_at                              DateTime                   @default(now()) @db.Timestamptz(6)
  workforce_plan_actions                  workforce_plan_actions[]
  workforce_plan_scenarios                workforce_plan_scenarios[]
  users_workforce_plans_created_byTousers users?                     @relation("workforce_plans_created_byTousers", fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants                                 tenants                    @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users_workforce_plans_updated_byTousers users?                     @relation("workforce_plans_updated_byTousers", fields: [updated_by], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([tenant_id, created_at(sort: Desc)], map: "idx_workforce_plans_created")
  @@index([created_by], map: "idx_workforce_plans_created_by")
  @@index([requirements], map: "idx_workforce_plans_requirements", type: Gin)
  @@index([tenant_id, status], map: "idx_workforce_plans_status")
  @@index([summary], map: "idx_workforce_plans_summary", type: Gin)
  @@index([tenant_id, target_date], map: "idx_workforce_plans_target_date")
  @@index([tenant_id], map: "idx_workforce_plans_tenant")
  @@index([updated_by], map: "idx_workforce_plans_updated_by")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model workspace_templates {
  id             Int        @id @default(autoincrement())
  code           String     @unique @db.VarChar(60)
  name           String     @db.VarChar(150)
  description    String?
  target_role_id Int?
  tenant_id      String?    @db.Uuid
  layout_config  Json?      @default("{\"gap\": 16, \"columns\": 12, \"row_height\": 80}")
  widget_config  Json       @default("[]")
  icon           String?    @db.VarChar(50)
  is_active      Boolean?   @default(true)
  is_system      Boolean?   @default(true)
  sort_order     Int?       @default(0)
  created_at     DateTime?  @default(now()) @db.Timestamptz(6)
  updated_at     DateTime?  @default(now()) @db.Timestamptz(6)
  name_it        String?    @db.VarChar(150)
  name_en        String?    @db.VarChar(150)
  description_it String?
  description_en String?
  rbp_roles      rbp_roles? @relation(fields: [target_role_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tenants        tenants?   @relation(fields: [tenant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([target_role_id], map: "idx_workspace_templates_role")
  @@index([tenant_id], map: "idx_workspace_templates_tenant")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model workspace_widgets {
  id                Int             @id @default(autoincrement())
  workspace_id      Int
  widget_catalog_id Int
  position_x        Int             @default(0)
  position_y        Int             @default(0)
  width             Int             @default(4)
  height            Int             @default(3)
  config_override   Json?           @default("{}")
  title_override    String?         @db.VarChar(150)
  is_visible        Boolean?        @default(true)
  sort_order        Int?            @default(0)
  created_at        DateTime?       @default(now()) @db.Timestamptz(6)
  updated_at        DateTime?       @default(now()) @db.Timestamptz(6)
  widget_catalog    widget_catalog  @relation(fields: [widget_catalog_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  user_workspaces   user_workspaces @relation(fields: [workspace_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([workspace_id], map: "idx_workspace_widgets_workspace")
  @@index([workspace_id], map: "idx_workspace_widgets_workspace_id")
}

/// Phase 13.B — dashboard_presets (registry of mockup-derived templates) + dashboard_elements (widget binding). RLS on dashboard_elements (tenant_id NULL = platform default · non-NULL = tenant override P10).
model dashboard_presets {
  id                 BigInt               @id @default(autoincrement())
  code               String               @unique @db.VarChar(64)
  name_it            String               @db.VarChar(255)
  name_en            String               @db.VarChar(255)
  description_it     String?
  description_en     String?
  perspective_code   String               @db.VarChar(32)
  source_mockup_path String?
  rbp_dashboard_code String?              @db.VarChar(64)
  persona_label      String?              @db.VarChar(255)
  is_published       Boolean              @default(false)
  sort_order         Int                  @default(0)
  theme_config       Json?
  created_at         DateTime             @default(now()) @db.Timestamptz(6)
  updated_at         DateTime             @default(now()) @db.Timestamptz(6)
  rbp_perspectives   rbp_perspectives     @relation(fields: [perspective_code], references: [code])
  dashboard_elements dashboard_elements[]

  @@index([perspective_code], map: "idx_dashboard_presets_perspective")
}

/// This model contains row level security: tenant_id IS NULL OR tenant_id = current_setting('app.current_tenant_id')
model dashboard_elements {
  id                  BigInt               @id @default(autoincrement())
  dashboard_preset_id BigInt
  widget_catalog_id   Int?
  widget_code         String               @db.VarChar(64)
  position            Int
  grid_col_start      Int                  @default(1)
  grid_col_span       Int                  @default(12)
  grid_row_start      Int                  @default(1)
  grid_row_span       Int                  @default(1)
  perspective_code    String?              @db.VarChar(32)
  visibility_min_role Int                  @default(6)
  config_overrides    Json?
  tenant_id           String?              @db.Uuid
  /// G4: parent slot for hierarchy (NULL = top-level, non-NULL = child of layout container)
  parent_element_id   BigInt?
  /// G4: catalog livello 2 modifier (BEM-like, e.g. 'pill-warn', 'fill-info')
  variant             String?              @db.VarChar(64)
  created_at          DateTime             @default(now()) @db.Timestamptz(6)
  updated_at          DateTime             @default(now()) @db.Timestamptz(6)
  dashboard_presets   dashboard_presets    @relation(fields: [dashboard_preset_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  rbp_perspectives    rbp_perspectives?    @relation(fields: [perspective_code], references: [code], onDelete: Restrict)
  tenants             tenants?             @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  // L54 (S24): widget_catalog FK decommissioned — widget_catalog_id resta Int? unconstrained.
  // Naming convention diversa da widget_code (0/17 backfill match). Vedi DECISIONS-LOG L54.
  parent              dashboard_elements?  @relation("dashboard_elements_hierarchy", fields: [parent_element_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  children            dashboard_elements[] @relation("dashboard_elements_hierarchy")

  @@index([dashboard_preset_id], map: "idx_dashboard_elements_preset")
  @@index([parent_element_id], map: "idx_dashboard_elements_parent")
}

enum career_skill_importance {
  critical
  important
  nice_to_have
}

enum company_size {
  MICRO
  SMALL
  MEDIUM
  LARGE
  ENTERPRISE
}

enum crud_operation {
  create
  read
  update
  delete
  configure
  approve
  export
}

enum permission_scope {
  own
  team
  department
  tenant
  platform
}

enum rbac_role {
  SUPERUSER
  TENANT_OWNER
  IT_ADMIN
  HR_DIRECTOR
  HR_MANAGER
  DEPT_HEAD
  LINE_MANAGER
  EMPLOYEE
}

enum requirement_importance {
  essential
  important
  nice_to_have
  developmental
}

enum skill_source_type {
  self_declaration
  assessment
  certification
  training
  inferred
  manager_override
  imported
}

enum skill_verification_status {
  pending
  verified
  rejected
  expired
}
