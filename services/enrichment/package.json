{
  "name": "@heuresys/enrichment",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Heuresys AI enrichment worker â€” BullMQ + Anthropic + ESCO match (scaffold per RTG Phase 3 task 3.9-3.10)",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "echo '@heuresys/enrichment: lint via tsc only' && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "clean": "rimraf dist node_modules/.cache",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.96.0",
    "@heuresys/shared": "*",
    "bullmq": "^5.13.0",
    "ioredis": "^5.4.1",
    "pino": "^10.3.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "25.8.0",
    "@vitest/coverage-v8": "^4.1.0",
    "tsx": "4.22.1",
    "typescript": "6.0.3",
    "vitest": "^4.1.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
