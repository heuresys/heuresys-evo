# Handoff — heuresys.com.evo (greenfield `heuresys-evo`)

> Updated: 2026-05-03 (S7 close — Brand Studio + Auth unblock + Domain HTTPS + Dependency hardening) · See `PROJECT-STATE.md` for full state · See `PROJECT-LOG.md` for history

## Recap last session

S7 (~6h): la sessione è partita da una domanda esplorativa ("c'è un theme builder?") ed è esplosa in un hardening end-to-end. Tre tronconi: **Feature** — `/brand-studio` shipped come dev tool gated by `SUPERUSER` (Server Component + 3 Server Actions con defense-in-depth + cookie preview + file Apply); **Infra** — login NextAuth sbloccato (nginx routing fix `/api/auth/` → `:3200` invece che `:8200`), cross-service JWT allineato (gateway env `AUTH_SECRET`+`NEXTAUTH_SECRET`), dominio legacy `www.heuresys.com`+apex con HTTPS Let's Encrypt (DNS Porkbun spostato a VM, vhost nginx + cert ECDSA), drop-in systemd `NODE_OPTIONS=4096` per evitare OOM su tsc; **Supply chain** — 12 → 5 vulns (-58%, 0 high): override `postcss^8.5.10`+`uuid^14`, `xlsx` → `exceljs`, `vitest` 2→4, gitleaks-action → CLI self-host, npm@11 pinato in CI per lockfile v3 compat. 11 commit pushati, 250 test verdi, CI/Build/Security tutti verdi.

## Priorities for next session

1. **Rimuovere hint dev `evo.dev / admin123` dalla `/login` UI** (S, ~5min). Esposto in chiaro nella pagina di sign-in: ok per dev interno, va rimosso/gating prima di esporre il dominio. Soluzione minima: gating su env var `NEXT_PUBLIC_SHOW_DEV_HINT=1`.
   - **Files**: `services/app/src/app/login/page.tsx` (o login-form.tsx — verificare in quale è il messaggio)
   - **Done when**: anonymous load di `/login` su prod non mostra "Use your Heuresys credentials. Dev seed: evo.dev / admin123."

2. **Migrazione `next-auth` v4 → v5** (M, ~3h). Risolve le 3 low cookie vulns residue e allinea l'app al gateway (Auth.js v5 family). Significativa: cambio cookie format, JWT shape, callback signatures, possibile breaking dei test e del proxy.ts.
   - **Files**: `services/app/package.json`, `services/app/src/lib/auth.ts`, `services/app/src/lib/auth.config.ts`, `services/app/src/proxy.ts`, `services/app/src/app/api/auth/[...nextauth]/route.ts`, tutti i call site `getServerSession`/`useSession`
   - **Done when**: `npm test` verde + login flow E2E manuale OK su evo.heuresys.com + cross-service JWT verifica continua a funzionare con gateway invariato

3. **Bump Prisma 5.22 → 7** (M, ~3h). Major migration con migrate command rinaming, generator path changes (`prisma/generated` → potenzialmente diverso), binaryTargets riconfig. Notice ricorrente nei log build.
   - **Files**: `services/app/package.json`, `services/api-gateway/package.json`, `services/app/prisma/schema.prisma`, `services/api-gateway/prisma/schema.prisma`, eventuali `db/scripts/*` che invocano prisma CLI
   - **Done when**: `npx prisma migrate status` clean, build green, 250 test green, smoke query Postgres funzionante

4. **Bump `exceljs` per chiudere `uuid` nested** (S, ~30min). exceljs 4.4.0 ha `uuid<14` interno; bump a versione che usa uuid 14+ chiude le 2 moderate residue. Verificare changelog per breaking.
   - **Files**: `packages/ui/package.json`
   - **Done when**: `npm audit` mostra 3 vulns residue (solo cookie next-auth), 0 high, 0 from exceljs

5. **Cleanup `.env.bak` + `nginx*.conf.bak-*`** (XS, ~5min). Backup di safety creati durante S7, ora che le modifiche sono in prod stabili da 1+ giorno è safe rimuoverli.
   - **Files**: `services/app/.env.bak-pre-authfix-*`, `services/api-gateway/.env.bak-pre-authfix-*`, `/etc/nginx/sites-available/evo.heuresys.com.conf.bak-pre-authfix-*`
   - **Done when**: niente `.bak-pre-authfix-*` rimasti, `git status` solo file tracked

## Open questions

- **next-auth v5 migration: in sessione dedicata o accoppiata a Prisma 7 bump?** Entrambe sono major migrations, separarle facilita debug se qualcosa rompe.
- **Brand Studio: DB-backed evolution** è ancora **fuori scope** (utente ha esplicitamente rifiutato in S7). Riaprire SOLO se utente lo richiede. Per ora è dev-tool single-user, persistenza solo localStorage + file.
- **`.env.bak` retention policy**: tenerli N giorni? Subito? In assenza di policy esplicita, mia raccomandazione: rimuovere dopo 1 giornata di prod stabile (oggi rimuovibili).
- **Phase 5 cutover go/no-go**: rimasto pendente da S6 (BLOCK 14 cutover-prep). Tag `rtg/evo/phase5/ready-for-go-no-go` da emettere quando utente decide.

## Carry-forward (still open, exploratory)

- **TOTP step in NextAuth** (Phase 3 task 3.1-3.3 deferred owner-side, owner-deferred): users con `totp_enabled=true` → secondo fattore via `users.totp_secret_encrypted` decrypt + otplib verify. Lavoro non urgente finché non si abilita TOTP per tenant clienti.
- **OAuth providers (Google / Microsoft)**: deferred a NextAuth v4 (richiede PrismaAdapter v1.x, l'attuale `@auth/prisma-adapter` v2.11 è v5-family). Si sblocca con la migrazione next-auth v5.
- **services/enrichment AI handler reali** (S5+ target): smoke handler `esco-match` ha hardcoded map; la versione vera userà pgvector + Anthropic SDK. Phase 6 work.
- **Storybook publish CI** (deploy preview): 84 stories pronte ma non c'è ancora un job che pubblica chromatic / storybook-static su preview URL.

## Known issues

### Webapp / app **P3**

- **`Failed to find Server Action 'x'` nei log app post-deploy**: clienti con build cache vecchia inviano action ID mismatch dopo nuovo deploy. Self-resolve con hard refresh utente (Ctrl+Shift+R). Non urgente.
- **Hint dev `evo.dev / admin123` esposto in `/login`** **P2 NEW S7**: vedi priority #1 sopra.

### Engine / api-gateway **P3**

- (nessun issue noto attivo)

### Operational **P3**

- **Backups `.env.bak-pre-authfix-*`** orfani in working tree (gitignore-coperti via `*.bak`, ma visibili in `git status`). Vedi priority #5.
- **Auto-handoff breadcrumbs accumulati** in `.handoff/auto/` (10+ in 24h durante S7). Non urgente, ma valutare retention rotation.

### Supply chain **P3**

- **5 vulns residue**: 3 low cookie via next-auth (chiusura con priority #2), 2 moderate uuid nested via exceljs (chiusura con priority #4). 0 high.

### Phase 5 carry-forward **P1**

- **STOP-AUTONOMO-FINAL pre-5.12 go/no-go decision** (BLOCK 14 cutover-prep): vedi PROJECT-STATE backlog. Sblocca cutover legacy → greenfield real-traffic.

## Verification commands

### Login + auth (greenfield)

```bash
curl -sI https://evo.heuresys.com/api/auth/csrf | head -5
# expected: HTTP/1.1 200 OK + set-cookie __Host-next-auth.csrf-token

curl -s https://evo.heuresys.com/api/auth/providers
# expected: JSON con credentials provider

curl -s -o /dev/null -w "%{http_code}\n" https://evo.heuresys.com/brand-studio
# expected: 307 (redirect to /login?callbackUrl=...) per anonymous
```

### Domains HTTPS

```bash
for d in evo.heuresys.com www.heuresys.com heuresys.com; do
  echo -n "$d: "; curl -sI -m 5 https://$d/ | head -1
done
# expected: tutti HTTP/1.1 200 OK (or 307 for evo /brand-studio)
```

### CI status

```bash
gh run list --branch main --limit 3 --json name,conclusion
# expected: CI=success, Build=success, Security=success
```

### Local dev/test

```bash
NODE_OPTIONS=--max-old-space-size=4096 npm run typecheck
# expected: exit 0, no errors

NODE_OPTIONS=--max-old-space-size=4096 npm test
# expected: 250 test passed (api-gateway 93, app 16, enrichment 7, shared 70, ui 64)

npm audit
# expected: 5 vulns (3 low + 2 moderate, 0 high)
```

### Prisma drift

```bash
cd services/app && npx prisma migrate status
# expected: "Database schema is up to date!"
```

## How to start the next session

1. **Read `.handoff/HANDOFF.md`** (this file) — get plan + open questions
2. **Read `.handoff/PROJECT-STATE.md`** — get current architecture/components/metrics
3. **Scan `.handoff/auto/`** for breadcrumbs newer than this HANDOFF mtime — surface any post-handoff state
4. **Verify CI status**: `gh run list --branch main --limit 3` — confirm all green
5. **Verify local sanity**: `git status -sb` (should be clean, in sync con origin/main)
6. **Surface to user**: 1-line state recap + top 3 todos + any open questions
7. **Ask**: "Continuiamo dalla todo #1 (rimozione hint dev login), scegli un'altra priorità, o qualcosa di nuovo?"
8. **Wait for user direction** before doing anything else

### Quick context for fresh agents

- Repo: `/home/ubuntu/heuresys-evo` (greenfield, NON `/home/ubuntu/heuresys.com.evo` legacy Docker)
- VM: `oracle-vm-default` (80.225.82.207), Ubuntu 24.04 ARM64, systemd-managed
- Domini in produzione: `https://evo.heuresys.com` (greenfield), `https://www.heuresys.com` + `https://heuresys.com` (legacy)
- DB: PostgreSQL 16 bare-metal su `:5432` (NON containerizzato, ADR-0001)
- Tools dev: vitest 4, npm 11, Node 20, Prisma 5.22 (pending bump 7)
- Login dev: `evo.dev / admin123` (hint visibile in UI, da rimuovere)
- Auth model: NextAuth v4 (pending migrazione v5), JWT cross-service, cookie `authjs.session-token`
- CI: 3 workflow (CI/Build/Security), tutti verdi su `main`
- Brand Studio: `/brand-studio` accessibile a `SUPERUSER` autenticati, scrive `services/app/src/styles/active-theme.css`
