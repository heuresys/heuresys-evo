# === OS ===
.DS_Store
Thumbs.db
desktop.ini

# === Editor / IDE ===
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# === Logs and temp ===
*.log
*.tmp
.cache/
tmp/

# === Playwright MCP / browser automation artifacts ===
.playwright-mcp/
/*.png
/*.jpg
/*.jpeg

# === Environment ===
.env
.env.local
.env.*.local

# === Node / npm workspaces ===
node_modules/
services/*/node_modules/
packages/*/node_modules/

# === Build output ===
dist/
build/
coverage/
.turbo/
*.tsbuildinfo

# === Next.js (services/marketing, services/app, services/playground) ===
services/*/.next/
services/*/out/
services/*/next-env.d.ts

# === Storybook (packages/ui) ===
packages/*/storybook-static/

# === Handoff auto-breadcrumbs (Stop hook output, kept locally only) ===
.handoff/auto/*.md
!.handoff/auto/.gitkeep

# === Database backups (binary dumps, kept locally only) ===
backups/**/*.dump
backups/**/*.sql.gz
backups/**/*.tar

# === Claude Code ===
CLAUDE.local.md
.claude/settings.local.json
.claude/agent-memory-local/

# RTG bridge plan automation (symlinked from legacy .com.evo)
scripts/rtg/

# Local-only dev helpers (machine-specific, e.g. SSH tunnel scripts)
scripts/dev-local/

# === RTGB hardening state + cantiere coordination ===
.rtg-state-evo/
.cantiere-lock-*

# === Misc backup files ===
.gitignore.bak
*.bak
*.bak-*
*.orig
*.rej

# === Test outputs / coverage ===
test-results/
playwright-report/
playwright/.cache/

# === Secrets ===
.env.production
.env.staging
*.pem
*.key
!*.pub.pem

# studio skill — usage logs (append-only, local only)
.claude/skills/studio/.logs/

# === Asset showcase webapp (local management tool, gitignored entirely) ===
# Tutto lo stack della webapp di gestione catalogo brand è solo locale.
# Eccezione: _legacy/ contiene snapshot storici committabili.
.ux-design/09-asset-showcase/*
!.ux-design/09-asset-showcase/_legacy/
!.ux-design/09-asset-showcase/_legacy/**
